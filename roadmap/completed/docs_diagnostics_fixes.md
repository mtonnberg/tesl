# D9 + D12 — machine-applicable fixes & proof-free bindings-hash

> **DONE 2026-07-07.** Both items implemented as class fixes, not per-message tweaks.
>
> **D9** — the blocker this note recorded ("`add_error` is message-only; `diagnostic_fix`
> lives in the compile.ml layer") had already been dissolved by E1 import ergonomics
> (`type_error.fix`, `add_error_fix`). This wave removed the remaining structural gaps so
> that ANY diagnostic that knows its exact edit can ship it:
> - `Diag_fix` (new dependency-leaf `compiler/lib/diag_fix.ml`) now owns the fix type —
>   re-exported unchanged through `Type_system`/`Compile` type equations — plus two new
>   kinds: column-precise `Replace_range` (token-level edits) and `Multi` (several edits
>   in one action; the LSP flattens it into one TextEdit list).
> - The **parser** gained a fix channel (`parse_error.fix`, `err_fix`) — parse errors
>   could never carry a fix before.
> - The **checker** gained a source snapshot (`ctx.source_lines`) and source-VERIFIED
>   builders (`Diag_fix.verified_delete` / `verified_token_replace`): a fix ships only
>   when the source confirms the edit target; no snapshot → error without fix, never a
>   wrong fix. `EBinop` now records `op_loc` (the operator token), because expression
>   locs cannot place the operator.
> - Fixes shipped: `return x` → delete `return` (checker.ml, EVar arm); `+` on String →
>   `++` at the operator token (infer_binop); single-line `if … then a else b` → the
>   indented form via a `Multi` fix that splits at `then` AND before `else`
>   (parser.ml, parse_body_require_indent — applying the fix chain converges to the
>   canonical form; a naive single edit provably got stuck on an unfixable
>   intermediate error).
> - **Class backstop:** `compiler/test/test_fix_apply.ml` — the apply-and-recompile
>   seam. Every fix-carrying diagnostic is machine-applied (`Diag_fix.apply`, the
>   reference semantics the LSP TextEdits mirror) and the file recompiled until clean
>   within a fuel bound; byte-exact golden for the `if` chain (structural fixes +
>   W010/W050 linter fixes compose to a fully clean file). A wrong, stale, or
>   non-convergent fix now fails the suite instead of shipping.
> - LSP (`editor/tesl-lsp/tesl-lsp.rkt`): `replace_range`/`multi` TextEdit construction
>   (`diag->fix-edits`), self-tests included. JSON wire: `fix_to_json` gained
>   `replace_range` and `multi` kinds; existing kinds byte-identical.
>
> **D12** — the roadmap note pointed at `emit_racket.ml`, but the hash is generated by
> the Racket macro layer: `build-executable-expansion` in `dsl/web.rkt` emitted the
> `~a-erased-all-arg-bindings` `(hash …)` for every function, while its only reader is
> `tesl-establish-param-proof` on proof-annotated params — a dead allocation per call
> for proof-free functions, contradicting the erased-proof design's "proof-free params
> are zero-alloc" rule (§2a). The clause is now gated on "any param carries a runtime
> proof datum". Guard tests in `tests/web-test.rkt`: proof-free expansion contains no
> such identifier, proof-carrying expansion keeps it (cross-param proofs depend on it),
> suppressed-hash functions still run.

Original deferral note (2026-07-02, `stability_wave`) follows.

---

**Status:** DEFERRED to `later` 2026-07-02 (`stability_wave`). The high-value, low-risk parts of
`docs_and_small_features_backlog` (D6 dead-keyword removal, D8 idiom-transfer diagnostics) landed;
D11 was discarded. D9 and D12 are lower-value and each needs more than a message tweak, so they are
deferred rather than rushed at the end of a soundness-focused wave.

## D9 — structured machine-applicable fixes beyond Boolean→Bool
Only the `Boolean→Bool` migration emits a structured `Replace_line` `diagnostic_fix`
(`compile.ml`); ~170 diagnostics carry `fix = None`. Extend structured fixes to the highest-
frequency AI-hit diagnostics — the new D8 hints are the obvious first targets: `+`→`++` (replace the
operator), `return x` removal, single-line-`if` → indented form.
**Why deferred:** the checker's `add_error` is message-only; `diagnostic_fix` lives in the
`compile.ml` diagnostic layer. Emitting a structured fix from a checker/parser diagnostic requires
threading a `fix` field through the checker's error representation into the compile diagnostics — a
plumbing change across `checker.ml`/`compile.ml`, not a local edit. The prose hints (D8) already
give humans/AI the guidance; the machine-applicable *auto-fix* is the incremental win.
