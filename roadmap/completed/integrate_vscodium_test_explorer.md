We would like to integrate the tests explorer in vscode/vscodium    with Tesl, showing all tests in the project and their status (passing/failing etc).

Goal

Make the developer experience in the test explorer A-tier