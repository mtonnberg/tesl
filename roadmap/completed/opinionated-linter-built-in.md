Tesl should have a very opinionated linter that is built in, to enforce that all Tesl code looks the same. The goal is to remove the number of things developer needs to decide/think about.

perhaps built into the compiler? follow the best practices from rust/elm/golang and other very opinioated languages

Also add a formatter/format checker to the linter/compiler/vscodium-extension