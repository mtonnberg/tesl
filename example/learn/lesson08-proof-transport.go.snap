// Generated by scripts/regen-go-snapshots.sh — DO NOT EDIT.
//
// A byte-exact snapshot of the Go emitted for example/learn/lesson08-proof-transport.tesl.
// The vendored runtime (internal/teslrt/**) is omitted: it is identical for every
// program and gated on its own by ci.sh phase 2a.

//tesl-snapshot: go.mod
module tesl.generated/teslmodlesson08prooftransport

go 1.26

//tesl-snapshot: internal/teslmodlesson08prooftransport/module.go
package teslmodlesson08prooftransport

import "tesl.generated/teslmodlesson08prooftransport/internal/teslrt"

//line lesson08-proof-transport.tesl:28
func CheckAge(n teslrt.Int) teslrt.Check[teslrt.Int] {
//line lesson08-proof-transport.tesl:29
	if (teslrt.Compare(n, teslrt.FromInt64(0)) >= 0) && (teslrt.Compare(n, teslrt.FromInt64(150)) <= 0) {
//line lesson08-proof-transport.tesl:30
		return teslrt.Accept(n)
	} else {
//line lesson08-proof-transport.tesl:32
		return teslrt.Reject[teslrt.Int](400, "invalid age")
	}
}

//line lesson08-proof-transport.tesl:39
func Birthday(age teslrt.Int) teslrt.Int {
	{
//line lesson08-proof-transport.tesl:40
		withoutProof := age
		_ = withoutProof
//line lesson08-proof-transport.tesl:41
		return teslrt.Add(withoutProof, teslrt.FromInt64(1))
	}
}

//line lesson08-proof-transport.tesl:49
func ShowAge(age teslrt.Int) string {
	{
//line lesson08-proof-transport.tesl:50
		rawAge := age
		_ = rawAge
		ageProof := struct{}{}
		_ = ageProof
//line lesson08-proof-transport.tesl:53
		return ("age is " + rawAge.String())
	}
}

//tesl-snapshot: internal/teslmodlesson08prooftransport/module_test.go
package teslmodlesson08prooftransport

import (
	"fmt"
	"os"
	"tesl.generated/teslmodlesson08prooftransport/internal/teslrt"
	"testing"
)

func TestMain(teslM *testing.M) {
	_, _ = fmt.Fprintln(os.Stderr, "TESL_GO_TESTS_STARTED")
	testExitCode := teslM.Run()
	os.Exit(testExitCode)
}

func TestTesl0(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkAge valid" && teslWanted != "TestTesl0" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson08-proof-transport.tesl:163
		n1 := teslrt.FromInt64(0)
		_ = n1
		{
//line lesson08-proof-transport.tesl:164
			r1 := teslrt.MustCheck(CheckAge(n1))
			_ = r1
			{
//line lesson08-proof-transport.tesl:165
				n2 := teslrt.FromInt64(25)
				_ = n2
				{
//line lesson08-proof-transport.tesl:166
					r2 := teslrt.MustCheck(CheckAge(n2))
					_ = r2
					{
//line lesson08-proof-transport.tesl:167
						n3 := teslrt.FromInt64(150)
						_ = n3
						{
//line lesson08-proof-transport.tesl:168
							r3 := teslrt.MustCheck(CheckAge(n3))
							_ = r3
//line lesson08-proof-transport.tesl:169
							if !teslrt.Equal(teslrt.FromInt64(1), teslrt.FromInt64(1)) {
//line lesson08-proof-transport.tesl:169
								teslT.Fatal("Tesl expectation failed")
							}
						}
					}
				}
			}
		}
	}
}

func TestTesl1(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkAge rejects" && teslWanted != "TestTesl1" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
//line lesson08-proof-transport.tesl:173
	if (CheckAge(teslrt.FromInt64(-1))).OK() {
		teslT.Fatal("expected Tesl check failure")
	}
//line lesson08-proof-transport.tesl:174
	if (CheckAge(teslrt.FromInt64(151))).OK() {
		teslT.Fatal("expected Tesl check failure")
	}
}

func TestTesl2(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "birthday increments" && teslWanted != "TestTesl2" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson08-proof-transport.tesl:178
		n := teslrt.FromInt64(30)
		_ = n
		{
//line lesson08-proof-transport.tesl:179
			age := teslrt.MustCheck(CheckAge(n))
			_ = age
			teslLeft2 := Birthday(age)
//line lesson08-proof-transport.tesl:180
			if !teslrt.Equal(teslLeft2, teslrt.FromInt64(31)) {
//line lesson08-proof-transport.tesl:180
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

func TestTesl3(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "showAge produces string" && teslWanted != "TestTesl3" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson08-proof-transport.tesl:184
		n := teslrt.FromInt64(25)
		_ = n
		{
//line lesson08-proof-transport.tesl:185
			age := teslrt.MustCheck(CheckAge(n))
			_ = age
			teslLeft3 := ShowAge(age)
//line lesson08-proof-transport.tesl:186
			if teslLeft3 != "age is 25" {
//line lesson08-proof-transport.tesl:186
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}
