// Generated by scripts/regen-go-snapshots.sh — DO NOT EDIT.
//
// A byte-exact snapshot of the Go emitted for example/learn/lesson71-money.tesl.
// The vendored runtime (internal/teslrt/**) is omitted: it is identical for every
// program and gated on its own by ci.sh phase 2a.

//tesl-snapshot: go.mod
module tesl.generated/teslmodlesson71money

go 1.26

//tesl-snapshot: internal/teslmodlesson71money/module.go
package teslmodlesson71money

import "tesl.generated/teslmodlesson71money/internal/teslrt"

//line lesson71-money.tesl:192
type Consultant struct {
	Id     string
	Hourly teslrt.MoneyRate
}

//line lesson71-money.tesl:109
type OrderLine struct {
	Id       string
	Price    teslrt.Money
	Quantity teslrt.Int
}

//line lesson71-money.tesl:192
var ConsultantTable = teslrt.NewTable[Consultant]()

//line lesson71-money.tesl:109
var OrderLineTable = teslrt.NewTable[OrderLine]()

//line lesson71-money.tesl:73
func ShowPrice() string {
	{
//line lesson71-money.tesl:74
		price := teslrt.MoneyOf(teslrt.FromInt64(1050), "USD", 2)
		_ = price
//line lesson71-money.tesl:75
		return teslrt.MoneyDisplay(price)
	}
}

//line lesson71-money.tesl:80
func LineTotal(unitPrice teslrt.Money, quantity teslrt.Int) teslrt.Money {
//line lesson71-money.tesl:81
	return teslrt.MoneyScale(unitPrice, quantity)
}

//line lesson71-money.tesl:87
func AddSameCurrency(a teslrt.Money, b teslrt.Money) teslrt.Money {
	{
//line lesson71-money.tesl:88
		proven := teslrt.MustCheck(teslrt.MoneyRequireSameCurrency(a, b))
		_ = proven
//line lesson71-money.tesl:89
		return teslrt.MoneyAdd(a, proven)
	}
}

//line lesson71-money.tesl:95
func ConvertToDisplay(rate teslrt.ExchangeRate, amount teslrt.Money) string {
//line lesson71-money.tesl:96
	{
		teslScrut1 := teslrt.MoneyConvert(rate, amount)
		switch teslScrut1.Tag {
		case teslrt.ResultOk:
			converted := teslScrut1.OkValue
			_ = converted
//line lesson71-money.tesl:97
			return teslrt.MoneyDisplay(converted)
		case teslrt.ResultErr:
			message := teslScrut1.ErrValue
			_ = message
//line lesson71-money.tesl:98
			return message
		default:
			panic("unreachable: checker guarantees case exhaustiveness")
		}
	}
}

//line lesson71-money.tesl:103
func ConvertChecked(rate teslrt.ExchangeRate, amount teslrt.Money) teslrt.Money {
	{
//line lesson71-money.tesl:104
		proven := teslrt.MustCheck(teslrt.MoneyRequireRateFor(rate, amount))
		_ = proven
//line lesson71-money.tesl:105
		return teslrt.MoneyConvertChecked(rate, proven)
	}
}

//line lesson71-money.tesl:123
func TotalRevenue() teslrt.Money {
//line lesson71-money.tesl:124
	return teslrt.TableSumMoney(OrderLineTable, func(_ OrderLine) bool {
		return true
	}, func(teslRow OrderLine) teslrt.Money { return teslRow.Price }, "OrderLine", "price")
}

//line lesson71-money.tesl:176
func consultantInvoice(hourly teslrt.MoneyRate, worked float64) teslrt.Money {
//line lesson71-money.tesl:177
	return teslrt.MoneyRateMul(hourly, worked)
}

//tesl-snapshot: internal/teslmodlesson71money/module_test.go
package teslmodlesson71money

import (
	"fmt"
	"os"
	"tesl.generated/teslmodlesson71money/internal/teslrt"
	"testing"
)

func TestMain(teslM *testing.M) {
	_, _ = fmt.Fprintln(os.Stderr, "TESL_GO_TESTS_STARTED")
	testExitCode := teslM.Run()
	os.Exit(testExitCode)
}

func TestTesl0(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "money is integer minor units with a currency-aware display" && teslWanted != "TestTesl0" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:129
		price := teslrt.MoneyOf(teslrt.FromInt64(1050), "USD", 2)
		_ = price
		teslLeft1 := teslrt.MoneyMinorUnits(price)
//line lesson71-money.tesl:130
		if !teslrt.Equal(teslLeft1, teslrt.FromInt64(1050)) {
			teslT.Fatal("Tesl expectation failed")
		}
		teslLeft2 := teslrt.MoneyDisplay(price)
//line lesson71-money.tesl:131
		if teslLeft2 != "$10.50" {
			teslT.Fatal("Tesl expectation failed")
		}
		teslLeft3 := teslrt.MoneyDisplay(teslrt.MoneyFromMinorUnits(teslrt.CurrencyOf("JPY", 0), teslrt.FromInt64(1000)))
//line lesson71-money.tesl:132
		if teslLeft3 != "\xc2\xa51000" {
			teslT.Fatal("Tesl expectation failed")
		}
		teslLeft4 := teslrt.MoneyDisplay(teslrt.MoneyOf(teslrt.FromInt64(1050), "SEK", 2))
//line lesson71-money.tesl:133
		if teslLeft4 != "10.50 SEK" {
			teslT.Fatal("Tesl expectation failed")
		}
		teslLeft5 := ShowPrice()
//line lesson71-money.tesl:134
		if teslLeft5 != "$10.50" {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

func TestTesl1(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "scale multiplies by an exact integer" && teslWanted != "TestTesl1" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:138
		unitPrice := teslrt.MoneyOf(teslrt.FromInt64(199), "USD", 2)
		_ = unitPrice
		teslLeft6 := teslrt.MoneyMinorUnits(LineTotal(unitPrice, teslrt.FromInt64(3)))
//line lesson71-money.tesl:139
		if !teslrt.Equal(teslLeft6, teslrt.FromInt64(597)) {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

func TestTesl2(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "add works once the SameCurrency proof is minted" && teslWanted != "TestTesl2" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:143
		a := teslrt.MoneyOf(teslrt.FromInt64(1000), "USD", 2)
		_ = a
		{
//line lesson71-money.tesl:144
			b := teslrt.MoneyOf(teslrt.FromInt64(250), "USD", 2)
			_ = b
			teslLeft7 := teslrt.MoneyDisplay(AddSameCurrency(a, b))
//line lesson71-money.tesl:145
			if teslLeft7 != "$12.50" {
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

func TestTesl3(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "convert applies a runtime rate with banker's rounding" && teslWanted != "TestTesl3" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:150
		rate := teslrt.ExchangeRateMake(teslrt.CurrencyOf("USD", 2), teslrt.CurrencyOf("EUR", 2), float64(0.9155), teslrt.SecondsToPosix(teslrt.FromInt64(1751900000)))
		_ = rate
		teslLeft8 := ConvertToDisplay(rate, teslrt.MoneyOf(teslrt.FromInt64(1000), "USD", 2))
//line lesson71-money.tesl:151
		if teslLeft8 != "\xe2\x82\xac9.16" {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

func TestTesl4(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "convert is an Err when the rate does not match the amount" && teslWanted != "TestTesl4" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:155
		eurRate := teslrt.ExchangeRateMake(teslrt.CurrencyOf("EUR", 2), teslrt.CurrencyOf("USD", 2), float64(1.0922), teslrt.SecondsToPosix(teslrt.FromInt64(1751900000)))
		_ = eurRate
		teslLeft9 := ConvertToDisplay(eurRate, teslrt.MoneyOf(teslrt.FromInt64(1000), "USD", 2))
//line lesson71-money.tesl:156
		if teslLeft9 != "exchange rate is FROM EUR but amount is in USD" {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

func TestTesl5(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "convertChecked is total behind a RateFor proof" && teslWanted != "TestTesl5" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:160
		rate := teslrt.ExchangeRateMake(teslrt.CurrencyOf("USD", 2), teslrt.CurrencyOf("EUR", 2), float64(0.9155), teslrt.SecondsToPosix(teslrt.FromInt64(1751900000)))
		_ = rate
		teslLeft10 := teslrt.MoneyMinorUnits(ConvertChecked(rate, teslrt.MoneyOf(teslrt.FromInt64(1000), "USD", 2)))
//line lesson71-money.tesl:161
		if !teslrt.Equal(teslLeft10, teslrt.FromInt64(916)) {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

func TestTesl6(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "a Money column round-trips and selectSum sums one currency" && teslWanted != "TestTesl6" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:165
		_ = teslrt.TableInsert(OrderLineTable, "OrderLine", OrderLine{Id: "l1", Price: teslrt.MoneyOf(teslrt.FromInt64(1050), "USD", 2), Quantity: teslrt.FromInt64(1)}, func(teslRow, teslNew OrderLine) bool { return (teslRow.Id == teslNew.Id) })
		{
//line lesson71-money.tesl:166
			_ = teslrt.TableInsert(OrderLineTable, "OrderLine", OrderLine{Id: "l2", Price: teslrt.MoneyOf(teslrt.FromInt64(500), "USD", 2), Quantity: teslrt.FromInt64(2)}, func(teslRow, teslNew OrderLine) bool { return (teslRow.Id == teslNew.Id) })
			teslLeft11 := teslrt.MoneyDisplay(TotalRevenue())
//line lesson71-money.tesl:167
			if teslLeft11 != "$15.50" {
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

func TestTesl7(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "950 SEK/h for 1.5 h bills 1425 SEK" && teslWanted != "TestTesl7" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:180
		hourly := teslrt.MoneyRateOfLabel(teslrt.MoneyOf(teslrt.FromInt64(95000), "SEK", 2), 3600, 1, "h")
		_ = hourly
		teslLeft12 := teslrt.MoneyRateDisplay(hourly)
//line lesson71-money.tesl:181
		if teslLeft12 != "950.00 SEK/h" {
			teslT.Fatal("Tesl expectation failed")
		}
		teslLeft13 := teslrt.MoneyDisplay(consultantInvoice(hourly, teslrt.DurationHours(float64(1.5))))
//line lesson71-money.tesl:182
		if teslLeft13 != "1425.00 SEK" {
			teslT.Fatal("Tesl expectation failed")
		}
		teslLeft14 := teslrt.MoneyDisplay(consultantInvoice(hourly, teslrt.DurationMinutes(float64(3e+01))))
//line lesson71-money.tesl:183
		if teslLeft14 != "475.00 SEK" {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

func TestTesl8(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "a stored hourly rate round-trips and bills" && teslWanted != "TestTesl8" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	teslResetTestState()
	{
//line lesson71-money.tesl:203
		_ = teslrt.TableInsert(ConsultantTable, "Consultant", Consultant{Id: "c1", Hourly: teslrt.MoneyRateOfLabel(teslrt.MoneyOf(teslrt.FromInt64(95000), "SEK", 2), 3600, 1, "h")}, func(teslRow, teslNew Consultant) bool { return (teslRow.Id == teslNew.Id) })
		{
//line lesson71-money.tesl:204
			found := teslrt.TableSelectOne(ConsultantTable, func(c Consultant) bool {
				return (c.Id == "c1")
			})
			_ = found
//line lesson71-money.tesl:205
			if !(func() bool {
				{
					teslScrut4 := found
					switch teslScrut4.Tag {
					case teslrt.MaybeSomething:
						c2 := teslScrut4.SomethingValue
						_ = c2
						return (teslrt.MoneyDisplay(consultantInvoice(c2.Hourly, teslrt.DurationHours(float64(2)))) == "1900.00 SEK")
					case teslrt.MaybeNothing:
						return false
					default:
						panic("unreachable: checker guarantees case exhaustiveness")
					}
				}
			}()) {
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

// teslResetTestState empties the stores every test block starts from, so no block
// inherits another's rows, jobs, or outbound-HTTP stubs.
func teslResetTestState() {
	teslrt.TableTruncate(ConsultantTable)
	teslrt.TableTruncate(OrderLineTable)
}
