// Generated by scripts/regen-go-snapshots.sh — DO NOT EDIT.
//
// A byte-exact snapshot of the Go emitted for example/learn/lesson61-step-debugging.tesl.
// The vendored runtime (internal/teslrt/**) is omitted: it is identical for every
// program and gated on its own by ci.sh phase 2a.

//tesl-snapshot: go.mod
module tesl.generated/teslmodlesson61stepdebugging

go 1.26

//tesl-snapshot: internal/teslmodlesson61stepdebugging/module.go
package teslmodlesson61stepdebugging

import "tesl.generated/teslmodlesson61stepdebugging/internal/teslrt"

//line lesson61-step-debugging.tesl:91
func checkScore(n teslrt.Int) teslrt.Check[teslrt.Int] {
//line lesson61-step-debugging.tesl:92
	if (teslrt.Compare(n, teslrt.FromInt64(0)) >= 0) && (teslrt.Compare(n, teslrt.FromInt64(100)) <= 0) {
//line lesson61-step-debugging.tesl:93
		return teslrt.Accept(n)
	} else {
//line lesson61-step-debugging.tesl:95
		return teslrt.Reject[teslrt.Int](400, "score must be 0-100")
	}
}

//line lesson61-step-debugging.tesl:100
func DescribeScore(score teslrt.Int) string {
//line lesson61-step-debugging.tesl:101
	if teslrt.Compare(score, teslrt.FromInt64(90)) >= 0 {
//line lesson61-step-debugging.tesl:102
		return "A"
	} else {
//line lesson61-step-debugging.tesl:103
		if teslrt.Compare(score, teslrt.FromInt64(80)) >= 0 {
//line lesson61-step-debugging.tesl:104
			return "B"
		} else {
//line lesson61-step-debugging.tesl:105
			if teslrt.Compare(score, teslrt.FromInt64(70)) >= 0 {
//line lesson61-step-debugging.tesl:106
				return "C"
			} else {
//line lesson61-step-debugging.tesl:107
				if teslrt.Compare(score, teslrt.FromInt64(60)) >= 0 {
//line lesson61-step-debugging.tesl:108
					return "D"
				} else {
//line lesson61-step-debugging.tesl:110
					return "F"
				}
			}
		}
	}
}

//line lesson61-step-debugging.tesl:116
func ComputeGrade(rawScore teslrt.Int) string {
	{
//line lesson61-step-debugging.tesl:117
		validated := teslrt.MustCheck(checkScore(rawScore))
		_ = validated
//line lesson61-step-debugging.tesl:118
		return DescribeScore(validated)
	}
}

//tesl-snapshot: internal/teslmodlesson61stepdebugging/module_test.go
package teslmodlesson61stepdebugging

import (
	"fmt"
	"os"
	"tesl.generated/teslmodlesson61stepdebugging/internal/teslrt"
	"testing"
)

func TestMain(teslM *testing.M) {
	_, _ = fmt.Fprintln(os.Stderr, "TESL_GO_TESTS_STARTED")
	testExitCode := teslM.Run()
	os.Exit(testExitCode)
}

func teslExpectFailure(teslT *testing.T, teslThunk func()) {
	teslT.Helper()
	defer func() {
		if recover() == nil {
			teslT.Fatal("expected Tesl failure")
		}
	}()
	teslThunk()
}

func TestTesl0(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkScore accepts valid score 0" && teslWanted != "TestTesl0" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson61-step-debugging.tesl:173
		n := teslrt.FromInt64(0)
		_ = n
		{
//line lesson61-step-debugging.tesl:174
			result := teslrt.MustCheck(checkScore(n))
			_ = result
//line lesson61-step-debugging.tesl:175
			if !teslrt.Equal(result, teslrt.FromInt64(0)) {
//line lesson61-step-debugging.tesl:175
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

func TestTesl1(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkScore accepts valid score 100" && teslWanted != "TestTesl1" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson61-step-debugging.tesl:179
		n := teslrt.FromInt64(100)
		_ = n
		{
//line lesson61-step-debugging.tesl:180
			result := teslrt.MustCheck(checkScore(n))
			_ = result
//line lesson61-step-debugging.tesl:181
			if !teslrt.Equal(result, teslrt.FromInt64(100)) {
//line lesson61-step-debugging.tesl:181
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

func TestTesl2(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkScore accepts mid-range score" && teslWanted != "TestTesl2" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson61-step-debugging.tesl:185
		n := teslrt.FromInt64(75)
		_ = n
		{
//line lesson61-step-debugging.tesl:186
			result := teslrt.MustCheck(checkScore(n))
			_ = result
//line lesson61-step-debugging.tesl:187
			if !teslrt.Equal(result, teslrt.FromInt64(75)) {
//line lesson61-step-debugging.tesl:187
				teslT.Fatal("Tesl expectation failed")
			}
		}
	}
}

func TestTesl3(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkScore rejects negative score" && teslWanted != "TestTesl3" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson61-step-debugging.tesl:191
		n := teslrt.Neg(teslrt.FromInt64(10))
		_ = n
		{
//line lesson61-step-debugging.tesl:192
			y := "doo"
			_ = y
			{
//line lesson61-step-debugging.tesl:193
				z := float64(2.32)
				_ = z
//line lesson61-step-debugging.tesl:194
				teslExpectFailure(teslT, func() {
					_ = teslrt.MustCheck(checkScore(n))
				})
			}
		}
	}
}

func TestTesl4(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "checkScore rejects score over 100" && teslWanted != "TestTesl4" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	{
//line lesson61-step-debugging.tesl:198
		n := teslrt.FromInt64(150)
		_ = n
//line lesson61-step-debugging.tesl:199
		teslExpectFailure(teslT, func() {
			_ = teslrt.MustCheck(checkScore(n))
		})
	}
}

func TestTesl5(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "computeGrade returns A for 95" && teslWanted != "TestTesl5" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	teslLeft4 := ComputeGrade(teslrt.FromInt64(95))
//line lesson61-step-debugging.tesl:203
	if teslLeft4 != "A" {
//line lesson61-step-debugging.tesl:203
		teslT.Fatal("Tesl expectation failed")
	}
}

func TestTesl6(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "computeGrade returns B for 85" && teslWanted != "TestTesl6" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	teslLeft5 := ComputeGrade(teslrt.FromInt64(85))
//line lesson61-step-debugging.tesl:207
	if teslLeft5 != "B" {
//line lesson61-step-debugging.tesl:207
		teslT.Fatal("Tesl expectation failed")
	}
}

func TestTesl7(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "computeGrade returns C for 75" && teslWanted != "TestTesl7" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	teslLeft6 := ComputeGrade(teslrt.FromInt64(75))
//line lesson61-step-debugging.tesl:211
	if teslLeft6 != "C" {
//line lesson61-step-debugging.tesl:211
		teslT.Fatal("Tesl expectation failed")
	}
}

func TestTesl8(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "computeGrade returns F for 55" && teslWanted != "TestTesl8" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "test" {
		teslT.Skip("test kind filter")
	}
	defer func() {
		if teslTrap := recover(); teslTrap != nil {
			teslT.Fatalf("Tesl test trapped: %v", teslTrap)
		}
	}()
	teslLeft7 := ComputeGrade(teslrt.FromInt64(55))
//line lesson61-step-debugging.tesl:215
	if teslLeft7 != "F" {
//line lesson61-step-debugging.tesl:215
		teslT.Fatal("Tesl expectation failed")
	}
}
