// Generated by scripts/regen-go-snapshots.sh — DO NOT EDIT.
//
// A byte-exact snapshot of the Go emitted for tests/secret-field-proof-tests.tesl.
// The vendored runtime (internal/teslrt/**) is omitted: it is identical for every
// program and gated on its own by ci.sh phase 2a.

//tesl-snapshot: go.mod
module tesl.generated/teslmodsecretfieldproof

go 1.26

//tesl-snapshot: internal/teslmodsecretfieldproof/module.go
package teslmodsecretfieldproof

import (
	"net/http"
	"tesl.generated/teslmodsecretfieldproof/internal/teslrt"
)

//line secret-field-proof-tests.tesl:43
type Password struct {
	Value teslrt.SecretString
}

func (teslSecret Password) String() string { return teslrt.SecretRedaction }

//line secret-field-proof-tests.tesl:53
type RegisterBody struct {
	Email    string
	Password Password
}

//line secret-field-proof-tests.tesl:57
type RegisterOut struct {
	Registered bool
}

//line secret-field-proof-tests.tesl:47
func isLongEnough(text string) teslrt.Check[string] {
//line secret-field-proof-tests.tesl:48
	if teslrt.Compare(teslrt.StringLength(text), teslrt.FromInt64(8)) >= 0 {
//line secret-field-proof-tests.tesl:49
		return teslrt.Accept(text)
	} else {
//line secret-field-proof-tests.tesl:51
		return teslrt.Reject[string](400, "Password too short")
	}
}

//line secret-field-proof-tests.tesl:71
func Register(body RegisterBody) RegisterOut {
	{
//line secret-field-proof-tests.tesl:72
		out := RegisterOut{Registered: teslrt.SecretEqual(body.Password.Value, (Password{Value: teslrt.MakeSecret("unused-comparison")}).Value)}
		_ = out
//line secret-field-proof-tests.tesl:73
		return out
	}
}

func teslDecodeRegisterBodyAlt0(teslJSON any) teslrt.Check[RegisterBody] {
	teslFieldEmail, teslErrEmail := teslrt.DecodeStringField(teslJSON, "email")
	if teslErrEmail != nil {
		return teslrt.RejectShape[RegisterBody](teslErrEmail.Error())
	}
	teslFieldPassword, teslErrPassword := teslrt.DecodeStringField(teslJSON, "password")
	if teslErrPassword != nil {
		return teslrt.RejectShape[RegisterBody](teslErrPassword.Error())
	}
	teslCheckedPassword := isLongEnough(teslFieldPassword)
	if !teslCheckedPassword.OK() {
		return teslrt.Reject[RegisterBody](teslCheckedPassword.Status(), teslCheckedPassword.Message())
	}
	teslFieldPassword, _ = teslCheckedPassword.Value()
	teslDecoded := RegisterBody{Email: teslFieldEmail, Password: Password{Value: teslrt.MakeSecret(teslFieldPassword)}}
	return teslrt.Accept(teslDecoded)
}

func DecodeRegisterBodyJSON(teslJSON any) teslrt.Check[RegisterBody] {
	var teslFirstFailure teslrt.Check[RegisterBody]
	teslHaveFailure := false
	teslResult0 := teslDecodeRegisterBodyAlt0(teslJSON)
	if teslResult0.OK() {
		return teslResult0
	}
	if !teslResult0.IsShapeMismatch() && !teslHaveFailure {
		teslFirstFailure = teslResult0
		teslHaveFailure = true
	}
	if teslHaveFailure {
		return teslFirstFailure
	}
	return teslrt.Reject[RegisterBody](400, "no decode alternative matched")
}

var RegisterServer = teslrt.Server{
	Routes: []teslrt.Route{
		{Method: "POST", Path: "/register", Endpoint: "register"},
	},
	Handlers: map[string]teslrt.HandlerFunc{
		"register": func(teslScope *teslrt.RequestScope, teslRequest *http.Request) teslrt.Response {
			_ = teslScope
			teslBodyBytes, teslBodyStatus, teslBodyMessage := teslrt.ReadRequestBody(teslRequest)
			if teslBodyStatus != 0 {
				return teslrt.Fail(teslBodyStatus, teslBodyMessage)
			}
			if teslStatus, teslMessage := teslrt.CheckJSONPayload(teslRequest, teslBodyBytes); teslStatus != 0 {
				return teslrt.Fail(teslStatus, teslMessage)
			}
			teslParsed, teslParseErr := teslrt.ParseJSON(teslBodyBytes)
			if teslParseErr != nil {
				return teslrt.Fail(400, "Malformed JSON payload")
			}
			teslDecoded := DecodeRegisterBodyJSON(teslParsed)
			if !teslDecoded.OK() {
				return teslrt.Fail(teslDecoded.Status(), teslDecoded.Message())
			}
			teslBody, _ := teslDecoded.Value()
			return teslrt.Response{Status: 200, Body: teslEncode2(Register(teslBody))}
		},
	},
}

func teslEncode1(teslValue bool) any {
	return teslValue
}

func teslEncode2(teslValue RegisterOut) any {
	return map[string]any{
		"registered": teslEncode1(teslValue.Registered),
	}
}

// Declared in Tesl but not called within this module.  Tesl permits an unused
// private declaration; Go's linters do not, so these references keep them.
var _ = isLongEnough

//tesl-snapshot: internal/teslmodsecretfieldproof/module_test.go
package teslmodsecretfieldproof

import (
	"fmt"
	"os"
	"tesl.generated/teslmodsecretfieldproof/internal/teslrt"
	"testing"
)

func TestMain(teslM *testing.M) {
	_, _ = fmt.Fprintln(os.Stderr, "TESL_GO_TESTS_STARTED")
	testExitCode := teslM.Run()
	os.Exit(testExitCode)
}

// api-test a too-short password is rejected before it ever becomes a secret
//
//line secret-field-proof-tests.tesl:85
func TestTeslApi0(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "a too-short password is rejected before it ever becomes a secret" && teslWanted != "TestTeslApi0" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "api-test" {
		teslT.Skip("test kind filter")
	}
	{
//line secret-field-proof-tests.tesl:86
		r := teslrt.ApiRequest(RegisterServer, "POST", "/register", "{\"email\":\"a@example.com\",\"password\":\"short\"}", nil, nil)
		_ = r
		teslLeft1 := r.Status
//line secret-field-proof-tests.tesl:87
		if !teslrt.Equal(teslLeft1, teslrt.FromInt64(400)) {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}

// api-test a long-enough password validates directly into a redacted field
//
//line secret-field-proof-tests.tesl:90
func TestTeslApi1(teslT *testing.T) {
	if teslWanted := os.Getenv("TESL_TEST_NAME"); teslWanted != "" && teslWanted != "a long-enough password validates directly into a redacted field" && teslWanted != "TestTeslApi1" {
		teslT.Skip("named test filter")
	}
	if teslKind := os.Getenv("TESL_TEST_KIND"); teslKind != "" && teslKind != "api-test" {
		teslT.Skip("test kind filter")
	}
	{
//line secret-field-proof-tests.tesl:91
		r := teslrt.ApiRequest(RegisterServer, "POST", "/register", "{\"email\":\"a@example.com\",\"password\":\"hunter2222\"}", nil, nil)
		_ = r
//line secret-field-proof-tests.tesl:92
		if !(teslrt.StatusOk(r.Status)) {
			teslT.Fatal("Tesl expectation failed")
		}
	}
}
