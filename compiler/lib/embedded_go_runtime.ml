(** Generated by compiler/gen/gen_go_runtime.ml. *)
let files = [
  ("agent.go", "package teslrt\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"regexp\"\n\t\"strings\"\n\t\"sync\"\n)\n\n// The `Tesl.Agent` surface: an LLM provider, an agent spec, and the tool-calling loop\n// that runs between them.\n//\n// The `aiProvider` capability that gates every inference entry point is a COMPILE-TIME\n// grant, so nothing about it survives here \226\128\148 the same erasure every other capability\n// gets. What does survive is the loop's structure, which is where the guarantees live:\n//\n//\tbounded iteration    a provider that keeps asking for tools cannot spin forever;\n//\tcontained tools      a tool whose arguments do not validate, or whose body traps,\n//\t                     becomes an is_error tool_result the model SEES, never an error\n//\t                     the caller has to catch \226\128\148 the model gets to correct itself;\n//\tno held connections  a tool runs, returns, and releases before the next provider\n//\t                     call, so a slow model never pins a database connection.\n//\n// A `Conversation` is an agent plus the transcript so far. Persistence is the PROGRAM's\n// job: ConversationJSON and ConversationFrom round-trip the history through a string it\n// stores in its own entity, so the runtime is not coupled to any schema. The JSON shape\n// is this runtime's own and is not a wire format \226\128\148 it is what ConversationFrom reads\n// back, nothing else.\n\n// maxAgentIterations bounds the tool-calling loop. A provider that answers every\n// tool_result with another tool_use would otherwise never return.\nconst maxAgentIterations = 16\n\n// \226\148\128\226\148\128 Provider protocol \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// LlmToolDecl is one tool as the provider sees it: a name, a description, and the\n// JSON-Schema object describing its arguments. Schema is raw JSON so a provider forwards\n// it verbatim rather than reparsing a shape it does not interpret.\ntype LlmToolDecl struct {\n\tName        string\n\tDescription string\n\tSchema      json.RawMessage\n}\n\n// LlmRequest is one provider round-trip's input.\n//\n// OnDelta is non-nil only when the caller opted into token streaming. A provider that\n// cannot stream simply never calls it; a caller that did not opt in never sees partial\n// text.\ntype LlmRequest struct {\n\tSystem    string\n\tMaxTokens Int\n\tMessages  []AgentMessage\n\tTools     []LlmToolDecl\n\tOnDelta   func(string)\n}\n\n// LlmToolCall is one tool the model asked for. Args is the model's raw JSON \226\128\148 it is\n// UNVALIDATED input that reaches a tool's validator, never a Tesl value directly.\ntype LlmToolCall struct {\n\tID   string\n\tName string\n\tArgs json.RawMessage\n}\n\n// LlmUsage is the token accounting a provider reports, summed across the loop.\ntype LlmUsage struct {\n\tInput      Int\n\tOutput     Int\n\tCacheRead  Int\n\tCacheWrite Int\n}\n\n// LlmResponse is one provider round-trip's output, normalised across providers. It is\n// also the Tesl `ToolStep` type: a mock provider's script is a list of these.\ntype LlmResponse struct {\n\tText       string\n\tUsage      LlmUsage\n\tToolCalls  []LlmToolCall\n\tStopReason string\n}\n\n// LlmProvider wraps the call itself. It is a struct rather than a bare func so it is a\n// named Tesl type with a zero value that fails loudly instead of a nil func that panics\n// with Go's own message.\ntype LlmProvider struct {\n\tinvoke func(LlmRequest) LlmResponse\n}\n\n// ProviderOf builds an LlmProvider from a round-trip function. Real providers and the\n// test doubles both go through it.\nfunc ProviderOf(invoke func(LlmRequest) LlmResponse) LlmProvider {\n\treturn LlmProvider{invoke: invoke}\n}\n\n// DeferredProvider builds the provider on the first CALL rather than where it was written,\n// once, and reuses it after.\n//\n// It exists for the top-level `agent X = Agent { \226\128\166 }` declaration, whose provider is\n// typically `anthropic (requireEnv \"\226\128\166\") \"model\"`: that reads configuration, and a package\n// variable's initialiser runs when the program loads \226\128\148 so a test suite that overrides the\n// provider on every call (`askWith agent prompt mock`) would still need the key to start.\n// Deferring makes the read happen when the provider is actually used, which is also where\n// a missing key is worth reporting. The Racket backend reaches the same place from the\n// other side: `requireEnv` there answers false rather than failing, so its module-load read\n// is harmless \226\128\148 and an empty key is then sent to the API. Failing at the first call is the\n// better half of that trade.\n//\n// Only the top-level declaration defers. An `Agent { \226\128\166 }` written inside a function body is\n// ordinary code and evaluates where it stands.\nfunc DeferredProvider(build func() LlmProvider) LlmProvider {\n\tonce := sync.OnceValue(build)\n\treturn ProviderOf(func(request LlmRequest) LlmResponse { return once().call(request) })\n}\n\nfunc (provider LlmProvider) call(request LlmRequest) LlmResponse {\n\tif provider.invoke == nil {\n\t\tpanic(\"Tesl.Agent: the agent has no provider\")\n\t}\n\treturn provider.invoke(request)\n}\n\n// \226\148\128\226\148\128 Mock providers \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// deltaChunks splits reply text the way a streaming provider would emit it: alternating\n// runs of whitespace and non-whitespace, so concatenating the chunks reproduces the text\n// exactly.\nvar deltaChunks = regexp.MustCompile(`\\s+|\\S+`)\n\n// MockProvider scripts one text reply per call, in order. Exhausting the script is a\n// trap, not an empty reply: a test that made more calls than it scripted has a bug the\n// runtime must not paper over.\nfunc MockProvider(replies []string) LlmProvider {\n\tsteps := make([]LlmResponse, 0, len(replies))\n\tfor _, reply := range replies {\n\t\tsteps = append(steps, TextStep(reply))\n\t}\n\treturn MockToolProvider(steps)\n}\n\n// MockToolProvider scripts a multi-round tool-calling sequence \226\128\148 one LlmResponse per\n// provider round-trip.\nfunc MockToolProvider(steps []LlmResponse) LlmProvider {\n\tscript := make([]LlmResponse, len(steps))\n\tcopy(script, steps)\n\tcalls := 0\n\treturn ProviderOf(func(request LlmRequest) LlmResponse {\n\t\tif calls >= len(script) {\n\t\t\tpanic(fmt.Sprintf(\n\t\t\t\t\"mockProvider: exhausted \226\128\148 %d call(s) made but only %d scripted response(s)\",\n\t\t\t\tcalls+1, len(script)))\n\t\t}\n\t\tresponse := script[calls]\n\t\tcalls++\n\t\t// Synthesize deltas for a text turn so a streaming consumer exercises the same\n\t\t// path a real provider drives. A tool-use step streams no user-facing text, as\n\t\t// a real provider does not either.\n\t\tif request.OnDelta != nil && len(response.ToolCalls) == 0 {\n\t\t\tfor _, chunk := range deltaChunks.FindAllString(response.Text, -1) {\n\t\t\t\trequest.OnDelta(chunk)\n\t\t\t}\n\t\t}\n\t\treturn response\n\t})\n}\n\n// ToolUseStep scripts one tool_use turn. argsJSON is parsed here so a malformed script\n// fails at construction, where the test author can see it, rather than inside the loop.\nfunc ToolUseStep(toolName, callID, argsJSON string) LlmResponse {\n\tvar args json.RawMessage\n\tif err := json.Unmarshal([]byte(argsJSON), &args); err != nil {\n\t\tpanic(\"toolUseStep: arguments are not valid JSON: \" + err.Error())\n\t}\n\treturn LlmResponse{\n\t\tUsage:      unitUsage(),\n\t\tToolCalls:  []LlmToolCall{{ID: callID, Name: toolName, Args: args}},\n\t\tStopReason: \"tool-use\",\n\t}\n}\n\n// TextStep scripts a final assistant text turn.\nfunc TextStep(text string) LlmResponse {\n\treturn LlmResponse{Text: text, Usage: unitUsage(), StopReason: \"end-turn\"}\n}\n\nfunc unitUsage() LlmUsage {\n\tone := FromInt64(1)\n\treturn LlmUsage{Input: one, Output: one}\n}\n\n// \226\148\128\226\148\128 Tools \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// Tool is a registered tool with its argument type ERASED. `validate` turns the model's\n// raw argument JSON into that type and `dispatch` turns it back into result text; both\n// are closures built by ToolOf, which is the only place the type is known.\n//\n// Both may panic \226\128\148 that is the contract, not a leak. Bad arguments and a failing body\n// are ordinary outcomes of handing a tool to a model, and runToolCall turns each into an\n// is_error tool_result the model reads and reacts to.\ntype Tool struct {\n\tName        string\n\tDescription string\n\tSchema      string\n\tvalidate    func(argsJSON string) any\n\tdispatch    func(validated any) string\n}\n\n// ToolOf builds a Tool from a typed validator/dispatch pair, erasing the argument type\n// into the closures. A is the tool's validated argument type \226\128\148 the value that crosses\n// from validate to dispatch, and the only place it is ever named.\nfunc ToolOf[A any](name, description, schema string,\n\tvalidate func(string) A, dispatch func(A) string) Tool {\n\treturn Tool{\n\t\tName:        name,\n\t\tDescription: description,\n\t\tSchema:      schema,\n\t\tvalidate:    func(argsJSON string) any { return validate(argsJSON) },\n\t\tdispatch: func(validated any) string {\n\t\t\ttyped, ok := validated.(A)\n\t\t\tif !ok {\n\t\t\t\tpanic(\"Tesl.Agent: tool argument type mismatch\")\n\t\t\t}\n\t\t\treturn dispatch(typed)\n\t\t},\n\t}\n}\n\n// toolResult is one entry of the tool turn fed back to the model.\ntype toolResult struct {\n\tID      string `json:\"id\"`\n\tContent string `json:\"content\"`\n\tIsError bool   `json:\"isError,omitempty\"`\n}\n\n// runToolCall validates and dispatches one tool call, containing every failure as an\n// is_error result. A trap inside a tool body is the model's problem to route around, not\n// the caller's to catch \226\128\148 killing the whole loop over one bad tool would lose the reply\n// the model could still have produced.\nfunc runToolCall(agent Agent, call LlmToolCall) (result toolResult) {\n\tfor _, tool := range agent.Tools {\n\t\tif tool.Name == call.Name {\n\t\t\treturn dispatchTool(tool, call)\n\t\t}\n\t}\n\treturn toolResult{ID: call.ID, Content: \"unknown tool: \" + call.Name, IsError: true}\n}\n\nfunc dispatchTool(tool Tool, call LlmToolCall) (result toolResult) {\n\targsJSON := string(call.Args)\n\tif len(argsJSON) == 0 {\n\t\targsJSON = \"{}\"\n\t}\n\tvalidated, failure := recovered(func() any { return tool.validate(argsJSON) })\n\tif failure != \"\" {\n\t\treturn toolResult{ID: call.ID, Content: \"invalid arguments: \" + failure, IsError: true}\n\t}\n\ttext, failure := recovered(func() any { return tool.dispatch(validated) })\n\tif failure != \"\" {\n\t\treturn toolResult{ID: call.ID, Content: \"tool failed: \" + failure, IsError: true}\n\t}\n\tanswer, ok := text.(string)\n\tif !ok {\n\t\treturn toolResult{ID: call.ID, Content: \"tool failed: dispatch returned no text\", IsError: true}\n\t}\n\treturn toolResult{ID: call.ID, Content: answer}\n}\n\n// recovered runs body and reports a panic as a message rather than unwinding. The empty\n// string means \"no failure\" \226\128\148 a tool that fails with an empty message still reports the\n// panic value's rendering, which is never empty for the values the runtime raises.\nfunc recovered(body func() any) (value any, failure string) {\n\tdefer func() {\n\t\tif raised := recover(); raised != nil {\n\t\t\tvalue = nil\n\t\t\tfailure = panicMessage(raised)\n\t\t}\n\t}()\n\treturn body(), \"\"\n}\n\nfunc panicMessage(raised any) string {\n\tswitch typed := raised.(type) {\n\tcase error:\n\t\treturn typed.Error()\n\tcase string:\n\t\tif typed == \"\" {\n\t\t\treturn \"tool raised\"\n\t\t}\n\t\treturn typed\n\tdefault:\n\t\treturn fmt.Sprintf(\"%v\", typed)\n\t}\n}\n\n// \226\148\128\226\148\128 Agent \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// Agent is the spec the loop runs against: which provider to call, what to tell it, how\n// much to let it say, and which tools it may reach for.\n// The field names are the Tesl ones: `Agent { provider, systemPrompt, maxTokens, tools }`\n// is emitted as a keyed struct literal, so a rename here is a rename of the surface.\ntype Agent struct {\n\tProvider     LlmProvider\n\tSystemPrompt string\n\tMaxTokens    Int\n\tTools        []Tool\n}\n\n// NewAgent builds an Agent positionally. The emitted code uses the keyed literal; this is\n// for runtime-internal construction and for tests.\nfunc NewAgent(provider LlmProvider, systemPrompt string, maxTokens Int, tools []Tool) Agent {\n\treturn Agent{Provider: provider, SystemPrompt: systemPrompt, MaxTokens: maxTokens, Tools: tools}\n}\n\n// AgentReply is one completed turn: the final assistant text, what it cost, and how many\n// tools fired getting there.\ntype AgentReply struct {\n\tText       string\n\tusage      LlmUsage\n\ttoolCalls  Int\n\tstopReason string\n}\n\nfunc ReplyText(reply AgentReply) string { return reply.Text }\n\n// ReplyTokens is input plus output. Cache reads and writes are accounted separately by\n// the providers that report them and are not billed as either.\nfunc ReplyTokens(reply AgentReply) Int {\n\treturn Add(reply.usage.Input, reply.usage.Output)\n}\n\nfunc ReplyToolCalls(reply AgentReply) Int { return reply.toolCalls }\n\n// \226\148\128\226\148\128 Transcript \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// AgentBlock is one piece of an assistant or tool turn. The variants are distinguished\n// by Kind rather than by a sum type because this shape is also the persisted JSON, and a\n// tagged struct round-trips through encoding/json without a custom unmarshaller.\ntype AgentBlock struct {\n\tKind    string          `json:\"kind\"`\n\tText    string          `json:\"text,omitempty\"`\n\tID      string          `json:\"id,omitempty\"`\n\tName    string          `json:\"name,omitempty\"`\n\tArgs    json.RawMessage `json:\"args,omitempty\"`\n\tResults []toolResult    `json:\"results,omitempty\"`\n}\n\n// AgentMessage is one turn of the transcript. A user turn carries Text; assistant and\n// tool turns carry Content blocks.\ntype AgentMessage struct {\n\tRole    string       `json:\"role\"`\n\tText    string       `json:\"text,omitempty\"`\n\tContent []AgentBlock `json:\"content,omitempty\"`\n}\n\nfunc userMessage(prompt string) AgentMessage {\n\treturn AgentMessage{Role: \"user\", Text: prompt}\n}\n\n// assistantMessage reconstructs the model's turn for the transcript: its text, then one\n// block per tool it asked for. An empty text with no tool calls produces no blocks, and\n// the caller drops the turn rather than recording an empty one.\nfunc assistantMessage(response LlmResponse) AgentMessage {\n\tblocks := make([]AgentBlock, 0, 1+len(response.ToolCalls))\n\tif response.Text != \"\" {\n\t\tblocks = append(blocks, AgentBlock{Kind: \"text\", Text: response.Text})\n\t}\n\tfor _, call := range response.ToolCalls {\n\t\tblocks = append(blocks, AgentBlock{\n\t\t\tKind: \"tool-use\", ID: call.ID, Name: call.Name, Args: call.Args,\n\t\t})\n\t}\n\treturn AgentMessage{Role: \"assistant\", Content: blocks}\n}\n\nfunc toolMessage(results []toolResult) AgentMessage {\n\treturn AgentMessage{\n\t\tRole:    \"tool\",\n\t\tContent: []AgentBlock{{Kind: \"tool-result\", Results: results}},\n\t}\n}\n\nfunc mergeUsage(into, from LlmUsage) LlmUsage {\n\treturn LlmUsage{\n\t\tInput:      Add(into.Input, from.Input),\n\t\tOutput:     Add(into.Output, from.Output),\n\t\tCacheRead:  Add(into.CacheRead, from.CacheRead),\n\t\tCacheWrite: Add(into.CacheWrite, from.CacheWrite),\n\t}\n}\n\n// \226\148\128\226\148\128 The loop \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// runLoop drives provider round-trips until the model stops asking for tools, and\n// answers the final reply plus the FULL transcript \226\128\148 the transcript is what a\n// conversation threads into its next turn.\n//\n// onStep, when non-nil, is called once per loop step: as each tool is dispatched, and\n// once with the final text. It never runs while a database connection is held.\nfunc runLoop(agent Agent, provider LlmProvider, messages []AgentMessage,\n\tonStep func(string), streamTokens bool) (AgentReply, []AgentMessage) {\n\tusage := LlmUsage{}\n\ttoolCalls := 0\n\tstep := func(event string) {\n\t\tif onStep != nil {\n\t\t\tonStep(event)\n\t\t}\n\t}\n\tfor iteration := 0; ; iteration++ {\n\t\tif iteration >= maxAgentIterations {\n\t\t\tpanic(fmt.Sprintf(\"askReply: tool-calling loop exceeded %d iterations\",\n\t\t\t\tmaxAgentIterations))\n\t\t}\n\t\trequest := LlmRequest{\n\t\t\tSystem:    agent.SystemPrompt,\n\t\t\tMaxTokens: agent.MaxTokens,\n\t\t\tMessages:  messages,\n\t\t\tTools:     toolDecls(agent),\n\t\t}\n\t\tif onStep != nil && streamTokens {\n\t\t\trequest.OnDelta = func(partial string) { step(\"text-delta: \" + partial) }\n\t\t}\n\t\tresponse := provider.call(request)\n\t\tusage = mergeUsage(usage, response.Usage)\n\t\tif len(response.ToolCalls) == 0 {\n\t\t\tif response.Text != \"\" {\n\t\t\t\tmessages = append(messages, assistantMessage(response))\n\t\t\t}\n\t\t\tstep(\"text: \" + response.Text)\n\t\t\treturn AgentReply{\n\t\t\t\tText:       response.Text,\n\t\t\t\tusage:      usage,\n\t\t\t\ttoolCalls:  FromInt64(int64(toolCalls)),\n\t\t\t\tstopReason: response.StopReason,\n\t\t\t}, messages\n\t\t}\n\t\tfor _, call := range response.ToolCalls {\n\t\t\tstep(\"tool: \" + call.Name)\n\t\t}\n\t\tresults := make([]toolResult, 0, len(response.ToolCalls))\n\t\tfor _, call := range response.ToolCalls {\n\t\t\tresults = append(results, runToolCall(agent, call))\n\t\t}\n\t\tmessages = append(messages, assistantMessage(response), toolMessage(results))\n\t\ttoolCalls += len(response.ToolCalls)\n\t}\n}\n\n// toolDecls renders the agent's tools as the provider sees them. A tool whose schema is\n// not parseable JSON is still offered, described as an open object \226\128\148 the schema is model\n// GUIDANCE, and the validator remains authoritative either way.\nfunc toolDecls(agent Agent) []LlmToolDecl {\n\tif len(agent.Tools) == 0 {\n\t\treturn nil\n\t}\n\tdecls := make([]LlmToolDecl, 0, len(agent.Tools))\n\tfor _, tool := range agent.Tools {\n\t\tschema := json.RawMessage(tool.Schema)\n\t\tif !json.Valid(schema) {\n\t\t\tschema = json.RawMessage(`{\"type\":\"object\"}`)\n\t\t}\n\t\tdecls = append(decls, LlmToolDecl{\n\t\t\tName: tool.Name, Description: tool.Description, Schema: schema,\n\t\t})\n\t}\n\treturn decls\n}\n\n// \226\148\128\226\148\128 Inference entry points \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// Ask runs the full loop and answers the final text. A tool-augmented agent still works\n// through it; the tools just do not show up in the answer.\nfunc Ask(agent Agent, prompt string) string {\n\treturn AskReply(agent, prompt).Text\n}\n\n// AskReply runs the full loop and answers text, usage, and tool count.\nfunc AskReply(agent Agent, prompt string) AgentReply {\n\treply, _ := runLoop(agent, agent.Provider, []AgentMessage{userMessage(prompt)}, nil, false)\n\treturn reply\n}\n\n// AskWith overrides the provider for THIS call only \226\128\148 the BYOK path. The agent's own\n// binding, its system prompt, and its tools are untouched.\nfunc AskWith(agent Agent, prompt string, provider LlmProvider) AgentReply {\n\treply, _ := runLoop(agent, provider, []AgentMessage{userMessage(prompt)}, nil, false)\n\treturn reply\n}\n\n// AgentPublisher is a Tesl `String -> Unit`. The empty struct is what Unit erases to, so\n// this is the type an emitted lambda and an emitted `fn \226\128\166 -> Unit` both already have \226\128\148\n// naming it here is what lets either be passed straight through with no adapter.\ntype AgentPublisher = func(string) struct{}\n\n// stepCallback adapts a Tesl publisher to the loop's own callback, discarding the Unit.\nfunc stepCallback(publisher AgentPublisher) func(string) {\n\treturn func(event string) { _ = publisher(event) }\n}\n\n// AgentRun runs the loop to completion, calling publisher once per step. It is what a\n// worker job body calls: the publisher closes over a channel publish, so the SAME\n// channel/SSE path streams the agent's progress.\nfunc AgentRun(agent Agent, prompt string, publisher AgentPublisher) AgentReply {\n\tif publisher == nil {\n\t\tpanic(\"agentRun: publisher must be a function String -> Unit\")\n\t}\n\treply, _ := runLoop(agent, agent.Provider,\n\t\t[]AgentMessage{userMessage(prompt)}, stepCallback(publisher), false)\n\treturn reply\n}\n\n// AskFor asks for a typed value: run the loop, hand the final text to decode, and on\n// failure append the error to the prompt and re-ask, up to maxRetries extra times.\n//\n// decode reports failure by trapping \226\128\148 which is what a codec decode does \226\128\148 so the retry\n// is driven by recovering it here rather than by a second error channel the emitted\n// decoder would have to thread.\nfunc AskFor[A any](agent Agent, prompt string, decode func(string) A, maxRetries Int) A {\n\tretries, exact := maxRetries.Int64()\n\tif !exact || retries < 0 {\n\t\tretries = 0\n\t}\n\tattempt := prompt\n\tfor left := retries; ; left-- {\n\t\treply := AskReply(agent, attempt)\n\t\tdecoded, failure := recovered(func() any { return decode(reply.Text) })\n\t\tif failure == \"\" {\n\t\t\ttyped, ok := decoded.(A)\n\t\t\tif !ok {\n\t\t\t\tpanic(\"askFor: decoder returned an unexpected type\")\n\t\t\t}\n\t\t\treturn typed\n\t\t}\n\t\tif left <= 0 {\n\t\t\tplural := \"ies\"\n\t\t\tif retries == 1 {\n\t\t\t\tplural = \"y\"\n\t\t\t}\n\t\t\tpanic(fmt.Sprintf(\"askFor: structured output did not decode after %d retr%s: %s\",\n\t\t\t\tretries, plural, failure))\n\t\t}\n\t\tattempt = prompt +\n\t\t\t\"\\n\\nYour previous reply could not be parsed: \" + failure +\n\t\t\t\"\\nReturn ONLY valid JSON matching the requested shape.\"\n\t}\n}\n\n// \226\148\128\226\148\128 Conversation \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// Conversation is an agent bound to the transcript so far. Turn N sees turns 1..N-1\n// because Converse threads the whole transcript into the loop.\ntype Conversation struct {\n\tagent    Agent\n\tmessages []AgentMessage\n}\n\n// ConversationTurn is one turn's outcome: the reply, and the conversation advanced by\n// this turn for the caller to thread into the next one.\ntype ConversationTurn struct {\n\treply        AgentReply\n\tconversation Conversation\n}\n\nfunc NewConversation(agent Agent) Conversation {\n\treturn Conversation{agent: agent}\n}\n\nfunc Converse(conversation Conversation, prompt string) ConversationTurn {\n\treturn converseWith(conversation, prompt, nil, false)\n}\n\n// ConverseStreaming is Converse with progress: publish is called with \"tool: <name>\" as\n// each tool fires, \"text-delta: <part>\" as the model generates, and \"text: <reply>\" once\n// the turn completes. A consumer that only reads \"text:\" keeps working \226\128\148 it ignores the\n// prefixes it does not know.\nfunc ConverseStreaming(conversation Conversation, prompt string,\n\tpublish AgentPublisher) ConversationTurn {\n\tif publish == nil {\n\t\tpanic(\"converseStreaming: third argument must be a function String -> Unit\")\n\t}\n\treturn converseWith(conversation, prompt, stepCallback(publish), true)\n}\n\nfunc converseWith(conversation Conversation, prompt string,\n\tonStep func(string), streamTokens bool) ConversationTurn {\n\thistory := make([]AgentMessage, 0, len(conversation.messages)+2)\n\thistory = append(history, conversation.messages...)\n\thistory = append(history, userMessage(prompt))\n\treply, transcript := runLoop(conversation.agent, conversation.agent.Provider,\n\t\thistory, onStep, streamTokens)\n\treturn ConversationTurn{\n\t\treply:        reply,\n\t\tconversation: Conversation{agent: conversation.agent, messages: transcript},\n\t}\n}\n\nfunc TurnReply(turn ConversationTurn) AgentReply { return turn.reply }\n\nfunc TurnConversation(turn ConversationTurn) Conversation { return turn.conversation }\n\nfunc ConversationLength(conversation Conversation) Int {\n\treturn FromInt64(int64(len(conversation.messages)))\n}\n\n// ConversationJSON serialises the transcript for the program to persist. The shape is\n// this runtime's own \226\128\148 ConversationFrom is its only reader \226\128\148 but it is deliberately\n// legible: the prompts and replies appear as themselves, so a stored row can be read.\nfunc ConversationJSON(conversation Conversation) string {\n\tmessages := conversation.messages\n\tif messages == nil {\n\t\tmessages = []AgentMessage{}\n\t}\n\tencoded, err := json.Marshal(messages)\n\tif err != nil {\n\t\tpanic(\"conversationJson: history could not be serialized: \" + err.Error())\n\t}\n\treturn string(encoded)\n}\n\n// ConversationFrom restores a transcript previously produced by ConversationJSON and\n// binds it to agent.\nfunc ConversationFrom(agent Agent, history string) Conversation {\n\tif strings.TrimSpace(history) == \"\" {\n\t\treturn Conversation{agent: agent}\n\t}\n\tvar messages []AgentMessage\n\tif err := json.Unmarshal([]byte(history), &messages); err != nil {\n\t\tpanic(\"conversationFrom: history is not valid JSON: \" + err.Error())\n\t}\n\treturn Conversation{agent: agent, messages: messages}\n}\n\n// \226\148\128\226\148\128 Typed decoding at the model boundary \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Everything a model produces is UNTRUSTED text. The two entry points below are where it\n// becomes a typed value, and both go through the program's own codec \226\128\148 the same decode an\n// HTTP request body gets \226\128\148 so a proof a type carries is established here or not at all.\n// Neither returns an error: a failure TRAPS, which is what lets AskFor retry and what\n// turns a bad tool argument into an is_error result the model reads.\n\n// DecodeAs decodes a JSON string as the named type. typeName appears only in the failure\n// message; which decoder runs was decided at compile time, from the literal type name at\n// the call site.\nfunc DecodeAs[T any](typeName, jsonText string, decode func(any) Check[T]) T {\n\t// ParseJSON, not json.Unmarshal: it reads numbers as json.Number, which is what an\n\t// arbitrary-precision Int decodes from. Unmarshalling into `any` would hand the codec a\n\t// float64 and quietly round anything above 2^53 \226\128\148 the same reason the HTTP body path\n\t// goes through it.\n\traw, err := ParseJSON([]byte(jsonText))\n\tif err != nil {\n\t\tpanic(\"decodeAs: not valid JSON: \" + err.Error())\n\t}\n\tresult := decode(raw)\n\tvalue, ok := result.Value()\n\tif !ok {\n\t\tpanic(\"decodeAs: decoded value failed validation: \" + result.Message())\n\t}\n\t_ = typeName\n\treturn value\n}\n\n// ToolArguments parses a tool call's argument JSON into its fields. A payload that is not\n// a JSON object is a trap: the schema asked for an object, and reading a scalar as one\n// would silently give every argument its zero value.\nfunc ToolArguments(argsJSON string) map[string]any {\n\t// Through ParseJSON for the reason DecodeAs is: an Int-typed tool parameter reads its\n\t// digits, and a float64 would round the large ones.\n\tparsed, err := ParseJSON([]byte(argsJSON))\n\tif err != nil {\n\t\tpanic(\"tool: arguments were not valid JSON: \" + err.Error())\n\t}\n\tfields, ok := parsed.(map[string]any)\n\tif !ok {\n\t\tpanic(\"tool: expected a JSON object of arguments\")\n\t}\n\treturn fields\n}\n\nfunc toolArgValue(fields map[string]any, name string) any {\n\tvalue, present := fields[name]\n\tif !present {\n\t\tpanic(\"tool: missing required argument: \" + name)\n\t}\n\treturn value\n}\n\nfunc ToolArgString(fields map[string]any, name string) string {\n\ttext, err := DecodeStringValue(toolArgValue(fields, name))\n\tif err != nil {\n\t\tpanic(\"tool: argument \" + name + \" must be a string\")\n\t}\n\treturn text\n}\n\n// ToolArgInt refuses a fractional number rather than truncating it. A model that answered\n// 2.5 for a count did not mean 2, and silently agreeing with it is how a wrong quantity\n// reaches a tool body. DecodeIntValue is the same reader a codec uses, so the digits \226\128\148 not\n// a float64 \226\128\148 are what the value comes from.\nfunc ToolArgInt(fields map[string]any, name string) Int {\n\tvalue, err := DecodeIntValue(toolArgValue(fields, name))\n\tif err != nil {\n\t\tpanic(\"tool: argument \" + name + \" must be an integer\")\n\t}\n\treturn value\n}\n\nfunc ToolArgFloat(fields map[string]any, name string) float64 {\n\tnumber, err := DecodeFloatValue(toolArgValue(fields, name))\n\tif err != nil {\n\t\tpanic(\"tool: argument \" + name + \" must be a number\")\n\t}\n\treturn number\n}\n\nfunc ToolArgBool(fields map[string]any, name string) bool {\n\tvalue, err := DecodeBoolValue(toolArgValue(fields, name))\n\tif err != nil {\n\t\tpanic(\"tool: argument \" + name + \" must be a boolean\")\n\t}\n\treturn value\n}\n\n// ToolArgPosixMillis reads the epoch-millisecond integer the schema describes. The\n// description is what keeps a model from sending seconds or a formatted date; this is\n// what refuses one that did anyway.\nfunc ToolArgPosixMillis(fields map[string]any, name string) PosixMillis {\n\treturn PosixMillis{Value: ToolArgInt(fields, name)}\n}\n\n// ToolArgMoney reads `{minorUnits, currency}`. Extra keys are tolerated \226\128\148 a model that\n// echoes back the enriched `display` field a Money tool RESULT carries is being helpful,\n// not wrong \226\128\148 but minor units are never inferred from a major-unit amount.\nfunc ToolArgMoney(fields map[string]any, name string) Money {\n\tobject, ok := toolArgValue(fields, name).(map[string]any)\n\tif !ok {\n\t\tpanic(\"tool: argument \" + name + \" must be an object {minorUnits, currency}\")\n\t}\n\tminorUnits, present := object[\"minorUnits\"]\n\tif !present {\n\t\tpanic(\"tool: argument \" + name + \" is missing minorUnits\")\n\t}\n\tunits, err := DecodeIntValue(minorUnits)\n\tif err != nil {\n\t\tpanic(\"tool: argument \" + name + \" must have integer minorUnits\")\n\t}\n\tcode, present := object[\"currency\"]\n\tif !present {\n\t\tpanic(\"tool: argument \" + name + \" is missing currency\")\n\t}\n\ttext, isText := code.(string)\n\tif !isText {\n\t\tpanic(\"tool: argument \" + name + \" has unknown currency code\")\n\t}\n\tcurrency, known := CurrencyFromCode(text).Value()\n\tif !known {\n\t\tpanic(\"tool: argument \" + name + \" has unknown currency code: \" + text)\n\t}\n\treturn Money{MinorUnits: units, Currency: currency}\n}\n\n// ToolDispatchWith captures a tool dispatch's leading argument, leaving the model to supply\n// only the validated one. A partial application in the surface \226\128\148 `tool \226\128\166 validateSpec\n// (dispatchReport conversationId)` \226\128\148 is what makes a tool PER-TURN: the captured value is\n// the program's, so the model chooses what the report is about and cannot choose whose\n// conversation it lands in.\nfunc ToolDispatchWith[Captured, Argument any](\n\tdispatch func(Captured, Argument) string, captured Captured) func(Argument) string {\n\treturn func(argument Argument) string { return dispatch(captured, argument) }\n}\n");
  ("agent_endpoint.go", "package teslrt\n\nimport (\n\t\"crypto/rand\"\n\t\"encoding/hex\"\n\t\"fmt\"\n)\n\n// Server endpoints as agent tools \226\128\148 the runtime half of `serverTools` and `humanActions`.\n//\n// `serverTools MyServer user` produces one tool per endpoint the user's proof covers, and\n// each of those tools IS the endpoint: the same handler, called with the same authenticated\n// user, so every ownership and authorization check in the handler body runs unchanged. What\n// the emitter generates per endpoint is the argument decode and the call; what lives here is\n// what both need to behave like the HTTP boundary \226\128\148 a rejection reaching the model as an\n// is_error result rather than as a crash.\n//\n// `humanActions MyServer user` is the complement: one INERT tool per endpoint the user's\n// proof does NOT cover. It is inert by construction \226\128\148 HumanActions takes the server's NAME,\n// never the server, so there is no path from a request descriptor back to a handler.\n\n// ToolArgDecoded reads one argument through a type's own codec \226\128\148 the same decode an HTTP\n// request body goes through, so a tool argument can never be validated more weakly than the\n// endpoint's boundary.\nfunc ToolArgDecoded[T any](fields map[string]any, name string, decode func(any) Check[T]) T {\n\treturn ToolChecked(name, decode(toolArgValue(fields, name)))\n}\n\n// ToolChecked unwraps a check on a tool argument, reporting a rejection with the check's own\n// message. The message reaches the MODEL as the is_error result, which is what lets it\n// correct the argument and try again.\nfunc ToolChecked[T any](name string, result Check[T]) T {\n\tvalue, ok := result.Value()\n\tif !ok {\n\t\tpanic(fmt.Sprintf(\"argument %s: %s\", name, result.Message()))\n\t}\n\treturn value\n}\n\n// ToolRejection is deferred by an emitted endpoint dispatch. A handler that rejects answers\n// the HTTP client with a status; called as a tool it has no response to write, so the\n// rejection becomes the tool_result text \226\128\148 carrying the status, because \"not found (HTTP\n// 404)\" tells the model something \"not found\" alone does not.\n//\n// Anything else re-panics unchanged: the agent loop already contains a trapping tool body as\n// an is_error result, and rewriting the message here would only hide where it came from.\nfunc ToolRejection() {\n\traised := recover()\n\tif raised == nil {\n\t\treturn\n\t}\n\tif rejection, ok := raised.(RequestRejection); ok {\n\t\tpanic(fmt.Sprintf(\"%s (HTTP %d)\", rejection.Message, rejection.Status))\n\t}\n\tpanic(raised)\n}\n\n// HumanActionSpec is one endpoint the agent may NOT perform, as the human sees it.\ntype HumanActionSpec struct {\n\tName        string\n\tDescription string\n\tSchema      string\n}\n\nfunc HumanActionOf(name, description, schema string) HumanActionSpec {\n\treturn HumanActionSpec{Name: name, Description: description, Schema: schema}\n}\n\n// humanActionSuffix is appended to every human action's description. The model has to be\n// told what calling the tool does, or it reports the action as done.\nconst humanActionSuffix = \"  (You cannot perform this action yourself. Calling this tool\" +\n\t\" asks the human to perform it via a button in their app; you will be told the result\" +\n\t\" in a later turn.)\"\n\n// HumanActions builds one inert tool per excluded endpoint.\n//\n// The server NAME is what it takes, and all it takes: with no route table and no handler in\n// reach, there is no in-process path from a request descriptor back to a call. The agent\n// chooses WHICH excluded action to ask for and prefills its arguments; the human's browser\n// resolves the action tag to a real URL from generated client code and performs the call\n// under their own session, where the endpoint re-checks auth. So the agent can neither\n// fabricate an action, relabel it, redirect it, nor perform it.\nfunc HumanActions(serverName string, actions []HumanActionSpec) []Tool {\n\ttools := make([]Tool, 0, len(actions))\n\tfor _, action := range actions {\n\t\tname := action.Name\n\t\ttools = append(tools, ToolOf(name, action.Description+humanActionSuffix, action.Schema,\n\t\t\t// The prefill is advisory: the human confirms the arguments and the real\n\t\t\t// endpoint re-validates them. Well-formedness is all that is checked, so the\n\t\t\t// request can be displayed.\n\t\t\tToolArguments,\n\t\t\tfunc(prefill map[string]any) string {\n\t\t\t\treturn humanActionRequest(serverName, name, prefill)\n\t\t\t}))\n\t}\n\treturn tools\n}\n\nfunc humanActionRequest(serverName, action string, prefill map[string]any) string {\n\treturn EncodeJSON(map[string]any{\n\t\t\"kind\":   \"human-action-request\",\n\t\t\"server\": serverName,\n\t\t\"action\": action,\n\t\t\"args\":   prefill,\n\t\t// An unguessable handle correlates the request with the result the human's\n\t\t// completed action produces, so it can re-enter the loop as a later turn.\n\t\t\"handle\": freshActionHandle(),\n\t})\n}\n\nfunc freshActionHandle() string {\n\traw := make([]byte, 16)\n\tif _, err := rand.Read(raw); err != nil {\n\t\tpanic(\"humanAction: could not generate a correlation handle: \" + err.Error())\n\t}\n\treturn hex.EncodeToString(raw)\n}\n");
  ("agent_provider.go", "package teslrt\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"strconv\"\n)\n\n// The real LLM providers.\n//\n// Each one is a translation layer and nothing more: it renders the normalised request\n// (agent.go) into a vendor's wire format, posts it through HttpPost \226\128\148 so the network is\n// gated by `httpClient`, judged by the SSRF containment, and interceptable by the same\n// `stubHttp` double every other outbound call is \226\128\148 and normalises the answer back. The\n// agent loop never learns which vendor it is talking to.\n//\n// TOKEN STREAMING is not implemented against a real provider yet. `converseStreaming` and\n// `agentRun` still publish every event they promise, in order, and a mock provider still\n// synthesizes per-word deltas; what a real provider does not do here is emit `text-delta:`\n// AS the model generates. The reply arrives complete and is published as one delta, so a UI\n// renders the answer in one step instead of typing it out. Nothing is lost but the\n// incremental rendering, and closing it needs a streaming HTTP body in the runtime, which\n// httpclient.go does not have yet.\n\nconst anthropicVersion = \"2023-06-01\"\n\nconst (\n\tanthropicEndpoint = \"https://api.anthropic.com/v1/messages\"\n\topenaiEndpoint    = \"https://api.openai.com/v1/chat/completions\"\n\tmistralEndpoint   = \"https://api.mistral.ai/v1/chat/completions\"\n)\n\n// AnthropicProvider talks to the Messages API.\nfunc AnthropicProvider(apiKey, model string) LlmProvider {\n\treturn ProviderOf(func(request LlmRequest) LlmResponse {\n\t\theaders := []Tuple2[string, string]{\n\t\t\t{Tuple2First: \"x-api-key\", Tuple2Second: apiKey},\n\t\t\t{Tuple2First: \"anthropic-version\", Tuple2Second: anthropicVersion},\n\t\t\t{Tuple2First: \"content-type\", Tuple2Second: \"application/json\"},\n\t\t}\n\t\tbody := anthropicRequest{\n\t\t\tModel:     model,\n\t\t\tMaxTokens: providerMaxTokens(request),\n\t\t\tSystem:    request.System,\n\t\t\tMessages:  anthropicMessages(request.Messages),\n\t\t\tTools:     anthropicTools(request.Tools),\n\t\t}\n\t\tanswer := providerPost(\"anthropic\", anthropicEndpoint, headers, body)\n\t\tvar decoded anthropicResponse\n\t\tproviderDecode(\"anthropic\", answer, &decoded)\n\t\treturn anthropicNormalize(decoded, request)\n\t})\n}\n\n// OpenAIProvider talks to the Chat Completions API.\nfunc OpenAIProvider(apiKey, model string) LlmProvider {\n\treturn openAIWireProvider(apiKey, model, openaiEndpoint)\n}\n\n// MistralProvider speaks the OpenAI chat-completions format at Mistral's endpoint, which is\n// what Mistral serves \226\128\148 the only difference is where it is posted.\nfunc MistralProvider(apiKey, model string) LlmProvider {\n\treturn openAIWireProvider(apiKey, model, mistralEndpoint)\n}\n\n// LocalProvider is the escape hatch for a self-hosted OpenAI-compatible server \226\128\148 llama.cpp,\n// vLLM, Ollama's /v1. The endpoint is the program's to name, so no default applies.\nfunc LocalProvider(endpoint, model string) LlmProvider {\n\treturn openAIWireProvider(\"\", model, endpoint)\n}\n\nfunc openAIWireProvider(apiKey, model, endpoint string) LlmProvider {\n\treturn ProviderOf(func(request LlmRequest) LlmResponse {\n\t\theaders := []Tuple2[string, string]{\n\t\t\t{Tuple2First: \"content-type\", Tuple2Second: \"application/json\"},\n\t\t}\n\t\t// A local server usually wants no credential at all; sending an empty Bearer\n\t\t// would be a header it has to ignore.\n\t\tif apiKey != \"\" {\n\t\t\theaders = append(headers,\n\t\t\t\tTuple2[string, string]{Tuple2First: \"authorization\", Tuple2Second: \"Bearer \" + apiKey})\n\t\t}\n\t\tbody := openaiRequest{\n\t\t\tModel:     model,\n\t\t\tMaxTokens: providerMaxTokens(request),\n\t\t\tMessages:  openaiMessages(request.System, request.Messages),\n\t\t\tTools:     openaiTools(request.Tools),\n\t\t}\n\t\tanswer := providerPost(\"openai\", endpoint, headers, body)\n\t\tvar decoded openaiResponse\n\t\tproviderDecode(\"openai\", answer, &decoded)\n\t\treturn openaiNormalize(decoded, request)\n\t})\n}\n\n// \226\148\128\226\148\128 Shared plumbing \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// providerMaxTokens defaults the budget the way the Racket runtime does, so an agent built\n// without one is not a request with `max_tokens: 0`.\nfunc providerMaxTokens(request LlmRequest) int64 {\n\tvalue, exact := request.MaxTokens.Int64()\n\tif !exact || value <= 0 {\n\t\treturn 1024\n\t}\n\treturn value\n}\n\nfunc providerPost(who, endpoint string, headers []Tuple2[string, string], body any) string {\n\tencoded, err := json.Marshal(body)\n\tif err != nil {\n\t\tpanic(who + \": request could not be encoded: \" + err.Error())\n\t}\n\tresponse := HttpPost(endpoint, headers, string(encoded))\n\tstatus, exact := response.Status.Int64()\n\tif !exact || status >= 400 {\n\t\tpanic(fmt.Sprintf(\"%s: API error (HTTP %s): %s\", who, response.Status.String(), response.Body))\n\t}\n\treturn response.Body\n}\n\nfunc providerDecode(who, body string, into any) {\n\tif err := json.Unmarshal([]byte(body), into); err != nil {\n\t\tpanic(who + \": provider returned non-JSON body: \" + err.Error())\n\t}\n}\n\n// deliverWholeReply publishes a completed reply as a single delta. It is what stands in for\n// per-token streaming against a real provider; see the note at the top of this file.\nfunc deliverWholeReply(request LlmRequest, text string) {\n\tif request.OnDelta != nil && text != \"\" {\n\t\trequest.OnDelta(text)\n\t}\n}\n\nfunc tokenCount(value int64) Int { return FromInt64(value) }\n\n// \226\148\128\226\148\128 Anthropic \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\ntype anthropicRequest struct {\n\tModel     string             `json:\"model\"`\n\tMaxTokens int64              `json:\"max_tokens\"`\n\tSystem    string             `json:\"system\"`\n\tMessages  []anthropicMessage `json:\"messages\"`\n\tTools     []anthropicTool    `json:\"tools\"`\n}\n\ntype anthropicTool struct {\n\tName        string          `json:\"name\"`\n\tDescription string          `json:\"description\"`\n\tInputSchema json.RawMessage `json:\"input_schema\"`\n}\n\n// Content is either a string (a plain user turn) or a block list, which is why it is `any`\n// rather than a slice: Anthropic accepts both and the transcript carries both.\ntype anthropicMessage struct {\n\tRole    string `json:\"role\"`\n\tContent any    `json:\"content\"`\n}\n\ntype anthropicBlock struct {\n\tType      string          `json:\"type\"`\n\tText      string          `json:\"text,omitempty\"`\n\tID        string          `json:\"id,omitempty\"`\n\tName      string          `json:\"name,omitempty\"`\n\tInput     json.RawMessage `json:\"input,omitempty\"`\n\tToolUseID string          `json:\"tool_use_id,omitempty\"`\n\tContent   string          `json:\"content,omitempty\"`\n\tIsError   bool            `json:\"is_error,omitempty\"`\n}\n\nfunc anthropicTools(tools []LlmToolDecl) []anthropicTool {\n\tdecls := make([]anthropicTool, 0, len(tools))\n\tfor _, tool := range tools {\n\t\tdecls = append(decls, anthropicTool{\n\t\t\tName: tool.Name, Description: tool.Description, InputSchema: tool.Schema,\n\t\t})\n\t}\n\treturn decls\n}\n\nfunc anthropicMessages(messages []AgentMessage) []anthropicMessage {\n\trendered := make([]anthropicMessage, 0, len(messages))\n\tfor _, message := range messages {\n\t\t// Anthropic has only \"user\" and \"assistant\": a tool RESULT is a content block\n\t\t// sent back as the user. Passing \"tool\" through is an HTTP 400 that makes every\n\t\t// tool-using agent unusable on this provider.\n\t\trole := message.Role\n\t\tif role == \"tool\" {\n\t\t\trole = \"user\"\n\t\t}\n\t\tif len(message.Content) == 0 {\n\t\t\trendered = append(rendered, anthropicMessage{Role: role, Content: message.Text})\n\t\t\tcontinue\n\t\t}\n\t\tblocks := []anthropicBlock{}\n\t\tfor _, block := range message.Content {\n\t\t\tswitch block.Kind {\n\t\t\tcase \"text\":\n\t\t\t\tblocks = append(blocks, anthropicBlock{Type: \"text\", Text: block.Text})\n\t\t\tcase \"tool-use\":\n\t\t\t\tblocks = append(blocks, anthropicBlock{\n\t\t\t\t\tType: \"tool_use\", ID: block.ID, Name: block.Name, Input: block.Args,\n\t\t\t\t})\n\t\t\tcase \"tool-result\":\n\t\t\t\tfor _, result := range block.Results {\n\t\t\t\t\tblocks = append(blocks, anthropicBlock{\n\t\t\t\t\t\tType: \"tool_result\", ToolUseID: result.ID,\n\t\t\t\t\t\tContent: result.Content, IsError: result.IsError,\n\t\t\t\t\t})\n\t\t\t\t}\n\t\t\tdefault:\n\t\t\t\tpanic(\"anthropic: unknown content block kind \" + block.Kind)\n\t\t\t}\n\t\t}\n\t\trendered = append(rendered, anthropicMessage{Role: role, Content: blocks})\n\t}\n\treturn rendered\n}\n\ntype anthropicResponse struct {\n\tContent []struct {\n\t\tType  string          `json:\"type\"`\n\t\tText  string          `json:\"text\"`\n\t\tID    string          `json:\"id\"`\n\t\tName  string          `json:\"name\"`\n\t\tInput json.RawMessage `json:\"input\"`\n\t} `json:\"content\"`\n\tStopReason string `json:\"stop_reason\"`\n\tUsage      struct {\n\t\tInput      int64 `json:\"input_tokens\"`\n\t\tOutput     int64 `json:\"output_tokens\"`\n\t\tCacheRead  int64 `json:\"cache_read_input_tokens\"`\n\t\tCacheWrite int64 `json:\"cache_creation_input_tokens\"`\n\t} `json:\"usage\"`\n}\n\nfunc anthropicNormalize(decoded anthropicResponse, request LlmRequest) LlmResponse {\n\ttext := \"\"\n\tcalls := []LlmToolCall{}\n\tfor _, block := range decoded.Content {\n\t\tswitch block.Type {\n\t\tcase \"text\":\n\t\t\ttext += block.Text\n\t\tcase \"tool_use\":\n\t\t\targs := block.Input\n\t\t\tif len(args) == 0 {\n\t\t\t\targs = json.RawMessage(\"{}\")\n\t\t\t}\n\t\t\tcalls = append(calls, LlmToolCall{ID: block.ID, Name: block.Name, Args: args})\n\t\t}\n\t}\n\tdeliverWholeReply(request, text)\n\treturn LlmResponse{\n\t\tText: text,\n\t\tUsage: LlmUsage{\n\t\t\tInput:      tokenCount(decoded.Usage.Input),\n\t\t\tOutput:     tokenCount(decoded.Usage.Output),\n\t\t\tCacheRead:  tokenCount(decoded.Usage.CacheRead),\n\t\t\tCacheWrite: tokenCount(decoded.Usage.CacheWrite),\n\t\t},\n\t\tToolCalls:  calls,\n\t\tStopReason: anthropicStopReason(decoded.StopReason),\n\t}\n}\n\nfunc anthropicStopReason(reason string) string {\n\tswitch reason {\n\tcase \"end_turn\":\n\t\treturn \"end-turn\"\n\tcase \"tool_use\":\n\t\treturn \"tool-use\"\n\tcase \"max_tokens\":\n\t\treturn \"max-tokens\"\n\tcase \"refusal\":\n\t\treturn \"refusal\"\n\tdefault:\n\t\treturn \"other\"\n\t}\n}\n\n// \226\148\128\226\148\128 OpenAI wire format \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\ntype openaiRequest struct {\n\tModel     string          `json:\"model\"`\n\tMaxTokens int64           `json:\"max_tokens\"`\n\tMessages  []openaiMessage `json:\"messages\"`\n\tTools     []openaiTool    `json:\"tools\"`\n}\n\ntype openaiTool struct {\n\tType     string           `json:\"type\"`\n\tFunction openaiToolSchema `json:\"function\"`\n}\n\ntype openaiToolSchema struct {\n\tName        string          `json:\"name\"`\n\tDescription string          `json:\"description\"`\n\tStrict      bool            `json:\"strict\"`\n\tParameters  json.RawMessage `json:\"parameters\"`\n}\n\ntype openaiMessage struct {\n\tRole       string           `json:\"role\"`\n\tContent    string           `json:\"content\"`\n\tToolCallID string           `json:\"tool_call_id,omitempty\"`\n\tToolCalls  []openaiToolCall `json:\"tool_calls,omitempty\"`\n}\n\ntype openaiToolCall struct {\n\tID       string             `json:\"id\"`\n\tType     string             `json:\"type\"`\n\tFunction openaiToolCallBody `json:\"function\"`\n}\n\ntype openaiToolCallBody struct {\n\tName string `json:\"name\"`\n\t// OpenAI carries the arguments as a JSON STRING, not as an object.\n\tArguments string `json:\"arguments\"`\n}\n\nfunc openaiTools(tools []LlmToolDecl) []openaiTool {\n\tdecls := make([]openaiTool, 0, len(tools))\n\tfor _, tool := range tools {\n\t\tdecls = append(decls, openaiTool{\n\t\t\tType: \"function\",\n\t\t\tFunction: openaiToolSchema{\n\t\t\t\tName: tool.Name, Description: tool.Description,\n\t\t\t\tStrict: true, Parameters: tool.Schema,\n\t\t\t},\n\t\t})\n\t}\n\treturn decls\n}\n\nfunc openaiMessages(system string, messages []AgentMessage) []openaiMessage {\n\trendered := []openaiMessage{}\n\tif system != \"\" {\n\t\trendered = append(rendered, openaiMessage{Role: \"system\", Content: system})\n\t}\n\tfor _, message := range messages {\n\t\tif len(message.Content) == 0 {\n\t\t\trendered = append(rendered, openaiMessage{Role: message.Role, Content: message.Text})\n\t\t\tcontinue\n\t\t}\n\t\tif message.Role == \"tool\" {\n\t\t\t// One message per result. A turn where the model called several tools has\n\t\t\t// several results, and every one has to come back under its own call id.\n\t\t\tfor _, block := range message.Content {\n\t\t\t\tfor _, result := range block.Results {\n\t\t\t\t\trendered = append(rendered, openaiMessage{\n\t\t\t\t\t\tRole: \"tool\", ToolCallID: result.ID, Content: result.Content,\n\t\t\t\t\t})\n\t\t\t\t}\n\t\t\t}\n\t\t\tcontinue\n\t\t}\n\t\ttext := \"\"\n\t\tcalls := []openaiToolCall{}\n\t\tfor _, block := range message.Content {\n\t\t\tswitch block.Kind {\n\t\t\tcase \"text\":\n\t\t\t\ttext += block.Text\n\t\t\tcase \"tool-use\":\n\t\t\t\targuments := string(block.Args)\n\t\t\t\tif arguments == \"\" {\n\t\t\t\t\targuments = \"{}\"\n\t\t\t\t}\n\t\t\t\tcalls = append(calls, openaiToolCall{\n\t\t\t\t\tID: block.ID, Type: \"function\",\n\t\t\t\t\tFunction: openaiToolCallBody{Name: block.Name, Arguments: arguments},\n\t\t\t\t})\n\t\t\t}\n\t\t}\n\t\trendered = append(rendered, openaiMessage{\n\t\t\tRole: message.Role, Content: text, ToolCalls: calls,\n\t\t})\n\t}\n\treturn rendered\n}\n\ntype openaiResponse struct {\n\tChoices []struct {\n\t\tMessage struct {\n\t\t\tContent   string           `json:\"content\"`\n\t\t\tToolCalls []openaiToolCall `json:\"tool_calls\"`\n\t\t} `json:\"message\"`\n\t\tFinishReason string `json:\"finish_reason\"`\n\t} `json:\"choices\"`\n\tUsage struct {\n\t\tPrompt       int64 `json:\"prompt_tokens\"`\n\t\tCompletion   int64 `json:\"completion_tokens\"`\n\t\tPromptDetail struct {\n\t\t\tCached int64 `json:\"cached_tokens\"`\n\t\t} `json:\"prompt_tokens_details\"`\n\t} `json:\"usage\"`\n}\n\nfunc openaiNormalize(decoded openaiResponse, request LlmRequest) LlmResponse {\n\ttext := \"\"\n\tfinish := \"\"\n\tcalls := []LlmToolCall{}\n\tif len(decoded.Choices) > 0 {\n\t\tchoice := decoded.Choices[0]\n\t\ttext = choice.Message.Content\n\t\tfinish = choice.FinishReason\n\t\tfor _, call := range choice.Message.ToolCalls {\n\t\t\targuments := call.Function.Arguments\n\t\t\tif arguments == \"\" {\n\t\t\t\targuments = \"{}\"\n\t\t\t}\n\t\t\tif !json.Valid([]byte(arguments)) {\n\t\t\t\tpanic(\"openai: tool arguments were not valid JSON: \" + strconv.Quote(arguments))\n\t\t\t}\n\t\t\tcalls = append(calls, LlmToolCall{\n\t\t\t\tID: call.ID, Name: call.Function.Name, Args: json.RawMessage(arguments),\n\t\t\t})\n\t\t}\n\t}\n\tdeliverWholeReply(request, text)\n\treturn LlmResponse{\n\t\tText: text,\n\t\tUsage: LlmUsage{\n\t\t\tInput:     tokenCount(decoded.Usage.Prompt),\n\t\t\tOutput:    tokenCount(decoded.Usage.Completion),\n\t\t\tCacheRead: tokenCount(decoded.Usage.PromptDetail.Cached),\n\t\t},\n\t\tToolCalls:  calls,\n\t\tStopReason: openaiStopReason(finish, len(calls) > 0),\n\t}\n}\n\nfunc openaiStopReason(finish string, hasTools bool) string {\n\tif hasTools {\n\t\treturn \"tool-use\"\n\t}\n\tswitch finish {\n\tcase \"stop\":\n\t\treturn \"end-turn\"\n\tcase \"tool_calls\":\n\t\treturn \"tool-use\"\n\tcase \"length\":\n\t\treturn \"max-tokens\"\n\tcase \"content_filter\":\n\t\treturn \"refusal\"\n\tdefault:\n\t\treturn \"other\"\n\t}\n}\n");
  ("apitest.go", "package teslrt\n\nimport (\n\t\"io\"\n\t\"net/http/httptest\"\n\t\"strings\"\n)\n\n// In-process driving of an emitted server, for Tesl's `api-test` blocks.\n//\n// The request never leaves the process \226\128\148 no socket, no port to collide with \226\128\148 which is\n// what makes these usable as ordinary `go test` cases. Racket's api-tests dispatch the\n// same way (`dispatch-api-test-request` calls the server's own router rather than opening\n// a connection), so the two backends exercise the same layer.\ntype ApiResponse struct {\n\tStatus Int\n\t// The body as a PARSED JSON value, not as text.  An api-test inspects it without types\n\t// (`resp.body.userId`), which is the ergonomics the Racket surface has:\n\t// `api-test-field-access-ref` normalises the response and hands back the parsed body, so a\n\t// raw string here would have made every assertion a string-compare against serialised\n\t// JSON \226\128\148 a different language for the same test.\n\tBody    JsonValue\n\tHeaders Dict[string, string]\n}\n\n// ApiRequest dispatches one request and captures the response. `body` is sent as-is;\n// `cookies` are `name=value` pairs; `headers` are sent as written, which is what the\n// `headers { \226\128\166 }` modifier means \226\128\148 a test signing its own payload puts the tag in a header, and\n// a header value is transport text, not JSON.\nfunc ApiRequest(server Server, method, path, body string, cookies []string,\n\theaders []Tuple2[string, string]) ApiResponse {\n\trequest := httptest.NewRequest(method, path, strings.NewReader(body))\n\t// A REQUEST cookie is just `name=value` on the wire \226\128\148 it carries none of the\n\t// Secure/HttpOnly/SameSite attributes a response cookie does, so the header is built\n\t// directly rather than through http.Cookie (which would also make gosec flag a\n\t// missing-attributes issue that cannot apply here).\n\tif len(cookies) > 0 {\n\t\trequest.Header.Set(\"Cookie\", strings.Join(cookies, \"; \"))\n\t}\n\tfor _, header := range headers {\n\t\t// Added rather than set: a test may send the same header twice deliberately, and the\n\t\t// server side is what decides which value wins.\n\t\trequest.Header.Add(header.Tuple2First, header.Tuple2Second)\n\t}\n\trecorder := httptest.NewRecorder()\n\tserver.ServeHTTP(recorder, request)\n\tresult := recorder.Result()\n\traw, _ := io.ReadAll(result.Body)\n\t// The RESPONSE headers, as the Dict an api-test reads: last-wins on a repeated name, the\n\t// same rule NewHttpRequest applies inbound.\n\tresponseHeaders := DictEmpty[string, string]()\n\tfor name, values := range result.Header {\n\t\tif len(values) > 0 {\n\t\t\tresponseHeaders = DictInsert(responseHeaders, strings.ToLower(name),\n\t\t\t\tvalues[len(values)-1], stringKeyLess)\n\t\t}\n\t}\n\treturn ApiResponse{\n\t\tStatus:  FromInt64(int64(result.StatusCode)),\n\t\tBody:    JsonParseBody(string(raw)),\n\t\tHeaders: responseHeaders,\n\t}\n}\n\n// The status predicates Tesl.ApiTest exposes.\nfunc StatusOk(status Int) bool          { return statusInRange(status, 200, 300) }\nfunc StatusClientError(status Int) bool { return statusInRange(status, 400, 500) }\nfunc StatusServerError(status Int) bool { return statusInRange(status, 500, 600) }\n\nfunc statusInRange(status Int, low, high int64) bool {\n\tvalue, ok := status.Int64()\n\treturn ok && value >= low && value < high\n}\n");
  ("apitest_json.go", "package teslrt\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"math/big\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// The api-test view of a JSON value.\n//\n// Inside an `api-test` block a response body is inspected WITHOUT types \226\128\148 `resp.body.userId`\n// is deliberately untyped, and the checker types it as a fresh variable so the assertion\n// reads like the JSON it is checking. That ergonomics is the point, so this type mirrors what\n// the Racket api-test surface does rather than inventing a typed view: a field that is not\n// there is NULL rather than an error (`api-test-field-access-ref` returns `'null`), and the\n// predicates below are the same ones `tesl/api-test.rkt` exports, with the same argument\n// order.\n//\n// `raw` holds the parsed shape: nil for JSON null or a missing field, `json.Number` for a\n// number (so an exact integer stays exact), string, bool, `[]any`, `map[string]any`.\ntype JsonValue struct {\n\traw any\n}\n\nfunc JsonNull() JsonValue { return JsonValue{} }\n\n// JsonOf wraps an already-parsed shape.\nfunc JsonOf(raw any) JsonValue { return JsonValue{raw: raw} }\n\n// JsonValueOf wraps an ORDINARY Tesl value for the api-test JSON surface. Racket's predicates\n// normalise whatever they are given (`api-test-normalize-json` runs the value\226\134\146jsexpr walk\n// first), so `hasField \"k\" job` on a plain record and `isNotNull err` on a String are\n// legitimate assertions there. This is that normalisation, applied once at the boundary\n// instead of inside each predicate.\nfunc JsonValueOf(value any) JsonValue { return JsonValue{raw: jsonNormalize(value)} }\n\n// JsonRaw is for the runtime's own use (comparing, printing); emitted code goes through the\n// helpers below.\nfunc (value JsonValue) JsonRaw() any { return value.raw }\n\n// JsonParseBody parses a response body for inspection. An empty body is JSON null \226\128\148 a\n// handler that answered `204` has nothing to inspect, and that is not a test failure by\n// itself. A malformed body traps: the test asked to look inside a body that is not JSON.\nfunc JsonParseBody(body string) JsonValue {\n\ttrimmed := strings.TrimSpace(body)\n\tif trimmed == \"\" {\n\t\treturn JsonNull()\n\t}\n\tparsed, err := ParseJSON([]byte(trimmed))\n\tif err != nil {\n\t\tpanic(\"api-test: response body is not JSON: \" + err.Error())\n\t}\n\treturn JsonOf(parsed)\n}\n\n// JsonFieldOf is `value.field` inside an api-test: a missing key \226\128\148 or a field read on\n// anything that is not an object \226\128\148 is NULL, matching `api-test-field-access-ref`. That is\n// what lets `expect isNull resp.body.missing` be written at all.\nfunc JsonFieldOf(value JsonValue, key string) JsonValue {\n\tfields, ok := value.raw.(map[string]any)\n\tif !ok {\n\t\treturn JsonNull()\n\t}\n\tfound, present := fields[key]\n\tif !present {\n\t\treturn JsonNull()\n\t}\n\treturn JsonOf(found)\n}\n\n// \226\148\128\226\148\128 The Tesl.ApiTest predicates, in Tesl's argument order \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc JsonIsNull(value JsonValue) bool {\n\tif value.raw == nil {\n\t\treturn true\n\t}\n\t// Racket also treats the STRING \"null\" as null here, because a body that spelled null as\n\t// text would otherwise read as present.\n\ttext, ok := value.raw.(string)\n\treturn ok && text == \"null\"\n}\n\nfunc JsonIsNotNull(value JsonValue) bool { return !JsonIsNull(value) }\n\n// JsonLength counts an array, an object's keys, or a string's characters; anything else is a\n// trap naming what it got, as Racket's `jsonLength` does.\nfunc JsonLength(value JsonValue) Int {\n\tswitch typed := value.raw.(type) {\n\tcase []any:\n\t\treturn FromInt64(int64(len(typed)))\n\tcase map[string]any:\n\t\treturn FromInt64(int64(len(typed)))\n\tcase string:\n\t\treturn FromInt64(int64(len([]rune(typed))))\n\tdefault:\n\t\tpanic(fmt.Sprintf(\"jsonLength: expected an array, object, or string, got %s\",\n\t\t\tjsonTypeName(value.raw)))\n\t}\n}\n\nfunc JsonHasLength(expected Int, value JsonValue) bool {\n\treturn Compare(expected, JsonLength(value)) == 0\n}\n\nfunc JsonIsEmpty(value JsonValue) bool {\n\treturn Compare(JsonLength(value), FromInt64(0)) == 0\n}\n\nfunc JsonIsNotEmpty(value JsonValue) bool { return !JsonIsEmpty(value) }\n\nfunc JsonHasField(key string, value JsonValue) bool {\n\tfields, ok := value.raw.(map[string]any)\n\tif !ok {\n\t\tpanic(\"hasField: expected a JSON object, got \" + jsonTypeName(value.raw))\n\t}\n\t_, present := fields[key]\n\treturn present\n}\n\n// JsonFieldAt is `fieldAt`: like a field read, but it insists the value IS an object.\nfunc JsonFieldAt(key string, value JsonValue) JsonValue {\n\tfields, ok := value.raw.(map[string]any)\n\tif !ok {\n\t\tpanic(\"fieldAt: expected a JSON object, got \" + jsonTypeName(value.raw))\n\t}\n\tfound, present := fields[key]\n\tif !present {\n\t\treturn JsonNull()\n\t}\n\treturn JsonOf(found)\n}\n\nfunc JsonArrayAt(index Int, value JsonValue) JsonValue {\n\telements, ok := value.raw.([]any)\n\tif !ok {\n\t\tpanic(\"arrayAt: expected a JSON array, got \" + jsonTypeName(value.raw))\n\t}\n\tat, exact := index.Int64()\n\tif !exact || at < 0 || at >= int64(len(elements)) {\n\t\tpanic(fmt.Sprintf(\"arrayAt: index %s is out of range for array of length %d\",\n\t\t\tindex.String(), len(elements)))\n\t}\n\treturn JsonOf(elements[at])\n}\n\n// \226\148\128\226\148\128 The coercing accessors \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc JsonAsInt(value JsonValue) Int {\n\tnumber, ok := value.raw.(json.Number)\n\tif !ok {\n\t\tpanic(\"jsonInt: expected a JSON number, got \" + jsonTypeName(value.raw))\n\t}\n\tparsed, valid := new(big.Int).SetString(number.String(), 10)\n\tif !valid {\n\t\tpanic(\"jsonInt: expected an integer, got \" + number.String())\n\t}\n\treturn fromBig(parsed)\n}\n\nfunc JsonAsString(value JsonValue) string {\n\ttext, ok := value.raw.(string)\n\tif !ok {\n\t\tpanic(\"jsonString: expected a JSON string, got \" + jsonTypeName(value.raw))\n\t}\n\treturn text\n}\n\nfunc JsonAsBool(value JsonValue) bool {\n\tflag, ok := value.raw.(bool)\n\tif !ok {\n\t\tpanic(\"jsonBool: expected a JSON boolean, got \" + jsonTypeName(value.raw))\n\t}\n\treturn flag\n}\n\n// \226\148\128\226\148\128 Comparison \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// JsonEqual compares an untyped JSON value against an ENCODED Tesl value \226\128\148 the same `any`\n// shape the response encoder builds, so the two directions cannot disagree about what a\n// value looks like as JSON.\n//\n// This is what makes `expect resp.body.age == 7` work: on Racket both sides are ordinary\n// values by then and `equal?` decides, so the comparison has to be structural here too.\nfunc JsonEqual(value JsonValue, other any) bool {\n\treturn jsonSame(value.raw, jsonNormalize(other))\n}\n\n// JsonContains is `jsonContains needle value`: substring for two strings, and otherwise\n// \"needle is contained in value\" \226\128\148 every field the needle names matches, recursively.\nfunc JsonContains(needle any, value JsonValue) bool {\n\twanted := jsonNormalize(needle)\n\tif needleText, ok := wanted.(string); ok {\n\t\tif valueText, ok := value.raw.(string); ok {\n\t\t\treturn strings.Contains(valueText, needleText)\n\t\t}\n\t}\n\treturn jsonMatch(wanted, value.raw)\n}\n\n// jsonNormalize maps an encoded Tesl value onto the shapes a parsed body uses, so that an\n// `Int` compares against a `json.Number` and a nested record against an object.\nfunc jsonNormalize(value any) any {\n\tswitch typed := value.(type) {\n\tcase nil:\n\t\treturn nil\n\tcase JsonValue:\n\t\treturn typed.raw\n\tcase Int:\n\t\treturn json.Number(typed.String())\n\tcase json.Number, string, bool:\n\t\treturn typed\n\tcase float64:\n\t\treturn json.Number(FormatFloat(typed))\n\tcase []any:\n\t\tout := make([]any, len(typed))\n\t\tfor index, element := range typed {\n\t\t\tout[index] = jsonNormalize(element)\n\t\t}\n\t\treturn out\n\tcase map[string]any:\n\t\tout := make(map[string]any, len(typed))\n\t\tfor key, element := range typed {\n\t\t\tout[key] = jsonNormalize(element)\n\t\t}\n\t\treturn out\n\tdefault:\n\t\treturn typed\n\t}\n}\n\nfunc jsonSame(left, right any) bool {\n\tswitch typedLeft := left.(type) {\n\tcase nil:\n\t\treturn right == nil\n\tcase json.Number:\n\t\treturn jsonNumbersEqual(typedLeft, right)\n\tcase string:\n\t\ttext, ok := right.(string)\n\t\treturn ok && text == typedLeft\n\tcase bool:\n\t\tflag, ok := right.(bool)\n\t\treturn ok && flag == typedLeft\n\tcase []any:\n\t\tother, ok := right.([]any)\n\t\tif !ok || len(other) != len(typedLeft) {\n\t\t\treturn false\n\t\t}\n\t\tfor index, element := range typedLeft {\n\t\t\tif !jsonSame(element, other[index]) {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t\treturn true\n\tcase map[string]any:\n\t\tother, ok := right.(map[string]any)\n\t\tif !ok || len(other) != len(typedLeft) {\n\t\t\treturn false\n\t\t}\n\t\tfor key, element := range typedLeft {\n\t\t\tcounterpart, present := other[key]\n\t\t\tif !present || !jsonSame(element, counterpart) {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t\treturn true\n\tdefault:\n\t\treturn false\n\t}\n}\n\n// Numbers compare by VALUE, not by text: `1` and `1.0` are the same JSON number, and an\n// exact integer beyond int64 still compares exactly.\nfunc jsonNumbersEqual(left json.Number, right any) bool {\n\tother, ok := right.(json.Number)\n\tif !ok {\n\t\treturn false\n\t}\n\tleftInt, leftExact := new(big.Int).SetString(left.String(), 10)\n\trightInt, rightExact := new(big.Int).SetString(other.String(), 10)\n\tif leftExact && rightExact {\n\t\treturn leftInt.Cmp(rightInt) == 0\n\t}\n\tleftFloat, leftErr := left.Float64()\n\trightFloat, rightErr := other.Float64()\n\treturn leftErr == nil && rightErr == nil && leftFloat == rightFloat\n}\n\n// jsonMatch is containment rather than equality: every key the needle names must match, and\n// extra keys in the value are fine.\nfunc jsonMatch(needle, value any) bool {\n\tswitch typedNeedle := needle.(type) {\n\tcase map[string]any:\n\t\tother, ok := value.(map[string]any)\n\t\tif !ok {\n\t\t\treturn false\n\t\t}\n\t\tfor key, element := range typedNeedle {\n\t\t\tcounterpart, present := other[key]\n\t\t\tif !present || !jsonMatch(element, counterpart) {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t\treturn true\n\tcase []any:\n\t\tother, ok := value.([]any)\n\t\tif !ok {\n\t\t\treturn false\n\t\t}\n\t\t// Every element of the needle must appear somewhere in the value, in any position:\n\t\t// an array assertion is about membership, not about the whole array.\n\t\tfor _, element := range typedNeedle {\n\t\t\tfound := false\n\t\t\tfor _, candidate := range other {\n\t\t\t\tif jsonMatch(element, candidate) {\n\t\t\t\t\tfound = true\n\t\t\t\t\tbreak\n\t\t\t\t}\n\t\t\t}\n\t\t\tif !found {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t\treturn true\n\tdefault:\n\t\treturn jsonSame(needle, value)\n\t}\n}\n\n// JsonListOf lifts a typed slice into the `any` shape `JsonEqual` compares against, so a\n// list can be compared with a JSON array without the emitter building the conversion inline.\nfunc JsonListOf[Element any](elements []Element) []any {\n\tout := make([]any, len(elements))\n\tfor index, element := range elements {\n\t\tout[index] = element\n\t}\n\treturn out\n}\n\n// JsonIncludesWhere is `includesWhere { \"field\": value, \226\128\166 } array`: does SOME element of the\n// array match every field the pattern names? `excludesWhere` is its negation.\n//\n// The pattern is a containment test per element, not equality, so an element with extra fields\n// still matches \226\128\148 an event stream carries ids and timestamps a test has no business pinning.\n// What is NOT tolerated is a pattern field the element does not have at all: that is a typo in\n// the test rather than a non-match, and reporting it as \"no element matched\" would send the\n// author looking at the program instead of at the assertion.\nfunc JsonIncludesWhere(pattern any, value JsonValue) bool {\n\treturn jsonArrayMatches(\"includesWhere\", pattern, value)\n}\n\nfunc JsonExcludesWhere(pattern any, value JsonValue) bool {\n\treturn !jsonArrayMatches(\"excludesWhere\", pattern, value)\n}\n\nfunc jsonArrayMatches(who string, pattern any, value JsonValue) bool {\n\tfields, ok := jsonNormalize(pattern).(map[string]any)\n\tif !ok {\n\t\tpanic(fmt.Sprintf(\"%s: expected a `{ \\\"field\\\": value }` pattern, got %s\",\n\t\t\twho, jsonTypeName(jsonNormalize(pattern))))\n\t}\n\telements, ok := value.raw.([]any)\n\tif !ok {\n\t\tpanic(fmt.Sprintf(\"%s: expected a JSON array, got %s\", who, jsonTypeName(value.raw)))\n\t}\n\tmatched := false\n\tfor _, element := range elements {\n\t\tobject, ok := element.(map[string]any)\n\t\tif !ok {\n\t\t\tpanic(fmt.Sprintf(\"%s: expected every array element to be a JSON object, got %s\",\n\t\t\t\twho, jsonTypeName(element)))\n\t\t}\n\t\telementMatches := true\n\t\tfor key, expected := range fields {\n\t\t\tfound, present := object[key]\n\t\t\tif !present {\n\t\t\t\tnames := make([]string, 0, len(object))\n\t\t\t\tfor name := range object {\n\t\t\t\t\tnames = append(names, name)\n\t\t\t\t}\n\t\t\t\tsort.Strings(names)\n\t\t\t\tpanic(fmt.Sprintf(\n\t\t\t\t\t\"%s: looking for field %q but element does not have it\\n  element has: %s\",\n\t\t\t\t\twho, key, strings.Join(names, \", \")))\n\t\t\t}\n\t\t\tif !jsonMatch(expected, found) {\n\t\t\t\telementMatches = false\n\t\t\t}\n\t\t}\n\t\tif elementMatches {\n\t\t\tmatched = true\n\t\t}\n\t}\n\treturn matched\n}\n\n// ApiTestFragment renders one `{\226\128\166}` slot of an api-test string template.\n//\n// The value goes through the same value\226\134\146JSON walk `api-test-string-fragment` performs in\n// dsl/test-support.rkt, and what comes out is rendered as TEXT: a string as itself, anything\n// else the way Racket's `~a` writes that jsexpr. That includes the two odd ones \226\128\148 a boolean\n// reads `#t`/`#f` and a JSON null reads `null` \226\128\148 because a template that interpolated one has\n// to produce the same request on both backends, and picking Go's spelling instead would be a\n// difference only a failing test would reveal.\nfunc ApiTestFragment(value any) string {\n\tswitch typed := value.(type) {\n\tcase string:\n\t\treturn typed\n\tcase bool:\n\t\tif typed {\n\t\t\treturn \"#t\"\n\t\t}\n\t\treturn \"#f\"\n\tcase Int:\n\t\treturn typed.String()\n\tcase float64:\n\t\treturn FormatFloat(typed)\n\tcase nil:\n\t\treturn \"null\"\n\tdefault:\n\t\treturn EncodeJSONValue(typed)\n\t}\n}\n");
  ("cache.go", "package teslrt\n\nimport (\n\t\"strings\"\n\t\"sync\"\n\t\"time\"\n)\n\n// `cache C = Cache { \226\128\166 }`: a keyed store with per-entry expiry.\n//\n// WHAT THIS IS AND IS NOT. On Racket a cache reads and writes a `tesl_cache` TABLE whenever a\n// PostgreSQL runtime is bound, and its own in-memory hash otherwise \226\128\148 and nothing binds one\n// until `call-with-database`, so the in-memory store is what a program actually uses unless it\n// opens a connection. This is that store, with the same rules:\n//\n//\tan entry with no TTL never expires;\n//\ta TTL is seconds from the moment of the SET, not from the declaration;\n//\ta READ is what notices expiry \226\128\148 the entry is removed and the read answers Nothing, so a\n//\tstale value can never be returned, and no sweeper goroutine is needed;\n//\t`invalidate` matches a LITERAL prefix, not a LIKE pattern. Racket's PostgreSQL path spells\n//\tthat `left(key, length($1)) = $1` precisely because `key LIKE $1 || '%'` treats `%` in the\n//\tprefix as a wildcard \226\128\148 a prefix containing one would wipe far more than it names.\n//\n// The value is typed: `valueType: String` makes it a `Cache[string]`, so a hit needs no decode\n// and cannot answer the wrong shape. That is a departure from Racket, which stores JSON text in\n// the table and decodes on read \226\128\148 the JSON round trip exists there because a SQL column has to\n// hold something, and it is precisely what this store does not need.\ntype cacheEntry[V any] struct {\n\tvalue V\n\t// expiresAt is Unix seconds, or 0 for \"never\" \226\128\148 the same distinction Racket makes with\n\t// a #f expiry.\n\texpiresAt int64\n}\n\ntype Cache[V any] struct {\n\tmutex sync.Mutex\n\t// entries is allocated with the cache and never becomes nil again \226\128\148 a nil map here would\n\t// be a nil flow into every operation that reads or writes it.\n\tentries map[string]cacheEntry[V]\n\t// defaultTTL is `defaultTtl:` in seconds, or 0 when the declaration omits it.\n\tdefaultTTL int64\n}\n\n// NewCache is what a `cache` declaration becomes: one package-level value per declaration.\nfunc NewCache[V any](defaultTTLSeconds int64) *Cache[V] {\n\treturn &Cache[V]{entries: map[string]cacheEntry[V]{}, defaultTTL: defaultTTLSeconds}\n}\n\n// CacheGet answers the live value, or Nothing on a miss OR an expired entry \226\128\148 and removes the\n// expired one on the way out, so the next read does no work either.\nfunc CacheGet[V any](cache *Cache[V], key string) Maybe[V] {\n\tcache.mutex.Lock()\n\tdefer cache.mutex.Unlock()\n\tentry, found := cache.entries[key]\n\tif !found {\n\t\treturn Nothing[V]()\n\t}\n\tif entry.expiresAt != 0 && time.Now().Unix() > entry.expiresAt {\n\t\tdelete(cache.entries, key)\n\t\treturn Nothing[V]()\n\t}\n\treturn Something(entry.value)\n}\n\n// CacheSet stores under the declaration's default TTL.\nfunc CacheSet[V any](cache *Cache[V], key string, value V) struct{} {\n\treturn cacheStore(cache, key, value, cache.defaultTTL)\n}\n\n// CacheSetTTL stores under an explicit TTL in seconds, which overrides the declaration's.\nfunc CacheSetTTL[V any](cache *Cache[V], key string, value V, ttlSeconds Int) struct{} {\n\tseconds, exact := ttlSeconds.Int64()\n\tif !exact || seconds < 0 {\n\t\tpanic(\"Cache.set: \" + ttlSeconds.String() + \" is not a TTL in seconds\")\n\t}\n\treturn cacheStore(cache, key, value, seconds)\n}\n\nfunc cacheStore[V any](cache *Cache[V], key string, value V, ttlSeconds int64) struct{} {\n\tcache.mutex.Lock()\n\tdefer cache.mutex.Unlock()\n\texpiresAt := int64(0)\n\tif ttlSeconds > 0 {\n\t\texpiresAt = time.Now().Unix() + ttlSeconds\n\t}\n\tcache.entries[key] = cacheEntry[V]{value: value, expiresAt: expiresAt}\n\treturn struct{}{}\n}\n\n// CacheDelete evicts one key. Deleting a key that is not there is not an error \226\128\148 the point of\n// the call is the state afterwards.\nfunc CacheDelete[V any](cache *Cache[V], key string) struct{} {\n\tcache.mutex.Lock()\n\tdefer cache.mutex.Unlock()\n\tdelete(cache.entries, key)\n\treturn struct{}{}\n}\n\n// CacheInvalidatePrefix evicts every key that BEGINS WITH prefix, literally.\nfunc CacheInvalidatePrefix[V any](cache *Cache[V], prefix string) struct{} {\n\tcache.mutex.Lock()\n\tdefer cache.mutex.Unlock()\n\tfor key := range cache.entries {\n\t\tif strings.HasPrefix(key, prefix) {\n\t\t\tdelete(cache.entries, key)\n\t\t}\n\t}\n\treturn struct{}{}\n}\n\n// CacheReset empties the store, for a test block that starts from an empty one. It is the\n// cache's counterpart of `TableTruncate`, and exists for the same reason: one block's entries\n// must not be another's.\nfunc CacheReset[V any](cache *Cache[V]) {\n\tcache.mutex.Lock()\n\tdefer cache.mutex.Unlock()\n\tcache.entries = map[string]cacheEntry[V]{}\n}\n");
  ("check.go", "package teslrt\n\nimport \"fmt\"\n\n// Check is the erased runtime result of a Tesl check. Proof identities remain a\n// compiler concern; runtime retains only success/failure and the checked value.\ntype Check[T any] struct {\n\tvalue   T\n\tstatus  int\n\tmessage string\n\tok      bool\n\t// shape marks \"the JSON is not this shape\" as opposed to \"this value is invalid\"; see\n\t// RejectShape. It is a FLAG rather than a reserved status, because the status is also what\n\t// a leaked rejection writes on the wire, and a sentinel status of 0 there is not a\n\t// response \226\128\148 `WriteHeader(0)` panics.\n\tshape bool\n}\n\nfunc Accept[T any](value T) Check[T] {\n\treturn Check[T]{value: value, ok: true}\n}\n\nfunc Reject[T any](status int, message string) Check[T] {\n\treturn Check[T]{status: status, message: message}\n}\n\nfunc (result Check[T]) OK() bool {\n\treturn result.ok\n}\n\nfunc (result Check[T]) Value() (T, bool) {\n\treturn result.value, result.ok\n}\n\nfunc (result Check[T]) Status() int {\n\treturn result.status\n}\n\nfunc (result Check[T]) Message() string {\n\treturn result.message\n}\n\n// MustCheck unwraps a statically expected successful check and fails closed if\n// a caller ignored rejection propagation.\nfunc MustCheck[T any](result Check[T]) T {\n\tif !result.ok {\n\t\tpanic(fmt.Sprintf(\"Tesl check rejected with status %d: %s\", result.status, result.message))\n\t}\n\treturn result.value\n}\n\n// RequestRejection is a check rejection consumed inside a HANDLER body. It travels as a\n// panic because a handler's Go signature returns its value type, with nowhere to put a\n// failure \226\128\148 and the router turns it back into a response carrying the check's own status.\n//\n// That is the Racket behaviour, not an invention: `dsl/web.rkt` installs the\n// raise-on-escaping-failure wrapper for every function kind EXCEPT `handler`, so a\n// rejection reached in a handler body flows out as the response's status (422, 404, \226\128\166)\n// rather than as a server error. Reporting 500 there would turn \"you sent an invalid age\"\n// into \"this server is broken\".\ntype RequestRejection struct {\n\tStatus  int\n\tMessage string\n}\n\n// Error makes a rejection print as its MESSAGE wherever a panic value is formatted \226\128\148 which is\n// what a failed job records (`runJob` keeps `fmt.Sprint(recovered)` as the attempt's error).\n// Racket records `check-fail-message` there, the bare text, so a struct dump would be a\n// different string on the two backends for the same program.\nfunc (rejection RequestRejection) Error() string {\n\treturn rejection.Message\n}\n\n// MustCheckRequest is `MustCheck` at the request boundary: same unwrapping, but a rejection\n// answers the client instead of crashing the request.\nfunc MustCheckRequest[T any](result Check[T]) T {\n\tif !result.ok {\n\t\tpanic(RequestRejection{Status: result.status, Message: result.message})\n\t}\n\treturn result.value\n}\n\n// RejectShape reports that the JSON simply is not this shape \226\128\148 a missing or mistyped field \226\128\148\n// as opposed to a VALIDATION failure from a `via` or cross-check, which carries the status the\n// check itself chose.\n//\n// The distinction is load-bearing when a codec has several alternatives. Racket's\n// registry loop treats a raised decode exception as \"try the next decoder\" while a\n// check failure is remembered (the FIRST one wins) and still lets a later alternative\n// succeed. Collapsing the two would report \"required field \\\"userName\\\" not found\" \226\128\148\n// the last alternative's shape complaint \226\128\148 in place of the real 400 the first\n// alternative's check produced.\n// A shape rejection still carries 400, so a decoder with no alternatives behind it (a DERIVED\n// record decoder) can answer the client with it directly.\nfunc RejectShape[T any](message string) Check[T] {\n\treturn Check[T]{status: 400, message: message, shape: true}\n}\n\n// IsShapeMismatch reports whether a failure means \"not this shape\" rather than \"this\n// value is invalid\".\nfunc (result Check[T]) IsShapeMismatch() bool {\n\treturn !result.ok && result.shape\n}\n");
  ("control.go", "package teslrt\n\n// If evaluates and returns only the branch selected by condition.\nfunc If[T any](condition bool, whenTrue, whenFalse func() T) T {\n\tif condition {\n\t\treturn whenTrue()\n\t}\n\treturn whenFalse()\n}\n\n// Boxed holds a value behind a pointer, and Unboxed reads it back.\n//\n// A RECURSIVE ADT is why these exist: `type Expr = Lit Int | Add Expr Expr` cannot hold an\n// Expr by value in Go (a struct containing itself has no finite size), so a self-referential\n// payload field is a pointer and the emitter goes through these two functions at every\n// construction and every read. Keeping the indirection in one pair of named functions is\n// what makes it invisible in the emitted code's SHAPE: a pattern binding still reads as one\n// call, not as a nil test the reader has to trust.\n//\n// Unboxed traps on a nil pointer rather than answering a zero value. Nothing the emitter\n// produces can construct one \226\128\148 every boxed field is filled by Boxed at construction \226\128\148 so a\n// nil here is a broken invariant, and a fabricated zero Expr would turn it into a silently\n// wrong answer somewhere further along.\nfunc Boxed[T any](value T) *T {\n\treturn &value\n}\n\nfunc Unboxed[T any](boxed *T) T {\n\tif boxed == nil {\n\t\tpanic(\"recursive value: a payload field was never filled\")\n\t}\n\treturn *boxed\n}\n\n// BoolRank orders a Bool: false before true, which is how Racket sorts `#f` and `#t`. It\n// exists because Go has no `<` on bools and an `order p.done asc` column needs one.\nfunc BoolRank(value bool) int {\n\tif value {\n\t\treturn 1\n\t}\n\treturn 0\n}\n\n// \226\148\128\226\148\128 Partial application \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// `add 1` in Tesl is a function of the remaining argument. Go has no partial application,\n// so the emitter goes through these: each supplies the leading arguments and hands back a\n// closure over the rest.\n//\n// They are named functions rather than inline literals for the reason the comparators are:\n// go/printer decides whether a function literal fits on one line by a threshold the emitter\n// cannot predict, so an inline closure reformats under gofmt at some sizes and not others \226\128\148\n// and a gofmt diff on emitted code is an emitter bug. A call to one of these is one\n// expression at every size.\n//\n// The family stops at three parameters because that is what the surface reaches for; a\n// wider one is refused at emission rather than answered by a guess.\n\nfunc Apply1Of2[A, B, R any](fn func(A, B) R, first A) func(B) R {\n\treturn func(second B) R { return fn(first, second) }\n}\n\n// Apply1Of3 answers a CHAIN of one-argument functions, not a two-argument one: Tesl's\n// partial application is curried, so `blend 1` is a function of `b` that answers a function\n// of `c`. The Racket runtime is curried the same way, and a flat `withA 2 3` is an arity\n// error there \226\128\148 so the shape here is the surface's, not Go's convenience.\nfunc Apply1Of3[A, B, C, R any](fn func(A, B, C) R, first A) func(B) func(C) R {\n\treturn func(second B) func(C) R {\n\t\treturn func(third C) R { return fn(first, second, third) }\n\t}\n}\n\nfunc Apply2Of3[A, B, C, R any](fn func(A, B, C) R, first A, second B) func(C) R {\n\treturn func(third C) R { return fn(first, second, third) }\n}\n");
  ("crypto.go", "package teslrt\n\nimport (\n\t\"crypto/hmac\"\n\t\"crypto/rand\"\n\t\"crypto/sha256\"\n\t\"crypto/sha512\"\n\t\"crypto/subtle\"\n\t\"encoding/base64\"\n\t\"encoding/hex\"\n\t\"fmt\"\n\t\"os\"\n\t\"strings\"\n)\n\n// `Tesl.Crypto`: message authentication, digests and tokens.\n//\n// Every primitive here is Go's standard library, and each one is the same primitive the Racket\n// runtime calls into libsodium for \226\128\148 HMAC-SHA256, SHA-256, SHA-512, the OS CSPRNG \226\128\148 so a value\n// produced by one backend verifies on the other. What is NOT here is password storage\n// (`hashPassword`/`checkPassword`/`needsRehash`): Racket uses libsodium's Argon2id, Go's\n// standard library has no Argon2, and matching it means taking on\n// `golang.org/x/crypto/argon2` \226\128\148 a dependency decision, not an implementation detail, since a\n// PBKDF2 substitute would produce hashes the Racket side cannot verify. The emitter refuses\n// those three by name until that is decided.\n//\n// The SHAPE of the surface is the security argument, and it is preserved exactly:\n//\n//\ta Signature cannot be compared          \226\128\148 only `CheckSignature` consumes one, and it\n//\t                                          compares in constant time, so the classic\n//\t                                          timing-unsafe `==` on a MAC tag is unwritable;\n//\ta Secret cannot be printed              \226\128\148 it holds a SecretString, which redacts;\n//\tthe transport forms are one-way pairs   \226\128\148 hex/base64 in and out, because a tag is public\n//\t                                          data that travels in a header.\n\n// Secret is the stdlib's own secret: what `requireSecret` answers. It is a secret NEWTYPE\n// like any a program declares, so it prints as \"[redacted]\", compares in constant time, and\n// reaches an outbound header only through `HttpClient.bearer`/`secretHeader`.\ntype Secret struct {\n\tValue SecretString\n}\n\n// Signature is an opaque MAC tag, held as lowercase hex \226\128\148 the same representation the Racket\n// runtime keeps, so a tag crossing between them is the same string.\ntype Signature struct {\n\tValue string\n}\n\n// RequireSecret is `requireSecret`: the environment value, or a trap. An unset variable is a\n// configuration error, so it fails at the read rather than flowing on as an empty secret.\nfunc RequireSecret(name string) Secret {\n\tvalue, present := os.LookupEnv(name)\n\tif !present || value == \"\" {\n\t\tpanic(fmt.Sprintf(\"requireSecret: environment variable %s is not set (or is empty)\", name))\n\t}\n\treturn Secret{Value: MakeSecret(value)}\n}\n\nfunc hmacSha256Bytes(key, data string) []byte {\n\tmac := hmac.New(sha256.New, []byte(key))\n\t_, _ = mac.Write([]byte(data))\n\treturn mac.Sum(nil)\n}\n\n// SignWith is `Crypto.signWith key payload`: HMAC-SHA256, configuration first and subject\n// last, so `payload |> signWith key` reads correctly.\nfunc SignWith(key Secret, payload string) Signature {\n\treturn Signature{Value: hex.EncodeToString(hmacSha256Bytes(key.Value.Reveal(), payload))}\n}\n\n// CheckSignature is `Crypto.checkSignature key sig payload`. The constant-time compare lives\n// in here, where it cannot be got wrong \226\128\148 which is why there is no `constantTimeEquals` on the\n// surface at all. A malformed tag fails the verification cleanly rather than trapping: it\n// arrived from a header, so it is untrusted input, not a program error.\nfunc CheckSignature(key Secret, sig Signature, payload string) Check[string] {\n\texpected := hmacSha256Bytes(key.Value.Reveal(), payload)\n\tactual, err := hex.DecodeString(sig.Value)\n\tif err != nil || !hmac.Equal(expected, actual) {\n\t\treturn Reject[string](401, \"signature does not match\")\n\t}\n\treturn Accept(payload)\n}\n\n// SignatureHex is the transport form: a tag you produced, for a header or a body. A MAC tag is\n// public data, so this is not an unwrap of a secret \226\128\148 but it DOES make\n// `signatureHex a == signatureHex b` expressible, which is the timing-unsafe comparison, and\n// that is what the SEC002 diagnostic is for.\nfunc SignatureHex(sig Signature) string { return sig.Value }\n\n// SignatureFromHex is the inbound half: a webhook's tag arrives as hex in a header. Parsing\n// untrusted input is safe here \226\128\148 the result can still only be consumed by a verification.\nfunc SignatureFromHex(text string) Signature { return Signature{Value: text} }\n\n// SignatureBase64 is the base64 transport form (Standard Webhooks puts the tag in\n// `webhook-signature: v1,<base64>`).\nfunc SignatureBase64(sig Signature) string {\n\traw, err := hex.DecodeString(sig.Value)\n\tif err != nil {\n\t\t// The tag came from `signatureFromHex` on untrusted input; re-encoding it is not the\n\t\t// place to fail, and an unparseable tag has no base64 form.\n\t\treturn \"\"\n\t}\n\treturn base64.StdEncoding.EncodeToString(raw)\n}\n\n// SignatureFromBase64 is the inbound half for a base64 tag. Malformed input yields a tag that\n// cannot match any MAC, so the verification fails cleanly \226\128\148 the Racket side's decoder is\n// lenient in the same direction (it never turns bad input into an error the program sees).\nfunc SignatureFromBase64(text string) Signature {\n\traw, err := base64.StdEncoding.DecodeString(strings.TrimSpace(text))\n\tif err != nil {\n\t\treturn Signature{}\n\t}\n\treturn Signature{Value: hex.EncodeToString(raw)}\n}\n\n// Fingerprint is `Crypto.fingerprint`: a stable content digest for ETags, cache keys, dedup\n// and idempotency keys. NOT for passwords \226\128\148 a fast digest of a password is exactly the mistake\n// `hashPassword` exists to prevent.\nfunc Fingerprint(content string) string {\n\tsum := sha256.Sum256([]byte(content))\n\treturn hex.EncodeToString(sum[:])\n}\n\n// keyFingerprintLabel is hashed in so a key fingerprint can never coincide with\n// `Fingerprint` of the same string. The trailing NUL is part of the label, as on the Racket\n// side, so the two backends produce the same fingerprint for the same key.\nvar keyFingerprintLabel = []byte(\"tesl-key-fingerprint-v1\\x00\")\n\n// KeyFingerprint answers \"did I load the right key?\" \226\128\148 a short identifier that is safe to log.\n// Truncated to 8 bytes so it reads as an identifier rather than as something to compare\n// cryptographically; it is NOT proof of key possession.\nfunc KeyFingerprint(key Secret) string {\n\tsum := sha256.Sum256(append(append([]byte{}, keyFingerprintLabel...),\n\t\t[]byte(key.Value.Reveal())...))\n\treturn hex.EncodeToString(sum[:8])\n}\n\n// RandomToken is 256 bits from the OS CSPRNG, base64url with no padding (43 characters).\n// There is deliberately no length parameter: a caller who can pass 4 will.\nfunc RandomToken() string {\n\tbuffer := make([]byte, 32)\n\tif _, err := rand.Read(buffer); err != nil {\n\t\tpanic(\"Crypto.randomToken: no source of randomness: \" + err.Error())\n\t}\n\treturn base64.RawURLEncoding.EncodeToString(buffer)\n}\n\n// The expert aliases: the friendly names above are the ones a newcomer can pick correctly, and\n// these exist so someone who already knows what they want can say it.\n\nfunc Sha256Hex(content string) string { return Fingerprint(content) }\n\nfunc Sha512Hex(content string) string {\n\tsum := sha512.Sum512([]byte(content))\n\treturn hex.EncodeToString(sum[:])\n}\n\n// \226\148\128\226\148\128 The authenticating-proxy edge binding (`Tesl.Proxy`) \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// ProxyVerifyBinding compares a request-supplied proxy-binding header against the app's\n// configured shared secret, in constant time, and accepts only on a match.\n//\n// The proof it mints on the Tesl side (`ProxyBound presented`) erases here like every\n// other, but what the proof is worth does not: the fact can be obtained ONLY through this\n// verification, so a function that demands it is reachable only by a value that was\n// actually checked against stored material. That is the whole difference from trusting an\n// `X-Auth-User`-style header, which asserts a network topology nothing verifies.\n//\n// Constant time for the reason Crypto.checkSignature is: a comparison that returns early\n// tells a caller how much of the secret it guessed.\nfunc ProxyVerifyBinding(configured Secret, presented string) Check[string] {\n\tif subtle.ConstantTimeCompare([]byte(configured.Value.Reveal()), []byte(presented)) == 1 {\n\t\treturn Accept(presented)\n\t}\n\t// A 401, not a 403: the request has not proved who it is, which is what an unmatched\n\t// binding means \226\128\148 and it is the status the Racket runtime answers with.\n\treturn Reject[string](401, \"proxy binding does not match\")\n}\n");
  ("dict.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"slices\"\n)\n\n// Dict is Tesl's `Dict k v`, held as entries kept SORTED BY KEY.\n//\n// Two forces pick this representation over a Go map. First, a Go map cannot be keyed\n// by teslrt.Int at all \226\128\148 it is deliberately non-comparable \226\128\148 so a map would need an\n// emitter-supplied key encoding and would still store the original key alongside it.\n// Second, and more important: Go randomises map iteration order per run, while the\n// Racket backend iterates a hash whose order is unspecified but stable within a run\n// (tesl/dict.rkt uses `hash-keys` and `in-hash`, and says so: \"hash has no order\").\n// Emitting raw map order would therefore make a Tesl program's output differ between\n// runs of the SAME binary \226\128\148 strictly worse than the Racket behavior. Sorted order is\n// deterministic, reproducible, and a defensible reading of an operation the language\n// leaves unspecified.\n//\n// Costs, accepted: lookup is O(log n) rather than O(1), and insert is O(n) because a\n// Tesl Dict is immutable and every write copies (the same rule as List \226\128\148 see list.go).\n// Racket's persistent hash is asymptotically better; Tesl dicts are typically small\n// (headers, cookies, config) and this keeps the runtime auditable.\n//\n// Ordering is supplied by the emitter, which knows the concrete key type at every call\n// site \226\128\148 the same device used for list equality and sorting.\ntype Dict[K any, V any] struct {\n\tEntries []DictEntry[K, V]\n}\n\ntype DictEntry[K any, V any] struct {\n\tKey   K\n\tValue V\n}\n\nfunc DictEmpty[K any, V any]() Dict[K, V] {\n\treturn Dict[K, V]{}\n}\n\nfunc DictSize[K any, V any](d Dict[K, V]) Int {\n\treturn FromInt64(int64(len(d.Entries)))\n}\n\nfunc DictIsEmpty[K any, V any](d Dict[K, V]) bool {\n\treturn len(d.Entries) == 0\n}\n\n// dictSearch returns the entry index for key, and whether it was present.\nfunc dictSearch[K any, V any](d Dict[K, V], key K, less func(K, K) bool) (int, bool) {\n\treturn slices.BinarySearchFunc(d.Entries, key, func(entry DictEntry[K, V], key K) int {\n\t\tswitch {\n\t\tcase less(entry.Key, key):\n\t\t\treturn -1\n\t\tcase less(key, entry.Key):\n\t\t\treturn 1\n\t\tdefault:\n\t\t\treturn 0\n\t\t}\n\t})\n}\n\nfunc DictLookup[K any, V any](d Dict[K, V], key K, less func(K, K) bool) Maybe[V] {\n\tif index, found := dictSearch(d, key, less); found {\n\t\treturn Something(d.Entries[index].Value)\n\t}\n\treturn Nothing[V]()\n}\n\nfunc DictMember[K any, V any](d Dict[K, V], key K, less func(K, K) bool) bool {\n\t_, found := dictSearch(d, key, less)\n\treturn found\n}\n\n// DictInsert replaces any existing entry for key. It is a WRITER, so it allocates\n// rather than touching the caller's entries.\nfunc DictInsert[K any, V any](d Dict[K, V], key K, value V, less func(K, K) bool) Dict[K, V] {\n\tindex, found := dictSearch(d, key, less)\n\tif found {\n\t\tentries := make([]DictEntry[K, V], len(d.Entries))\n\t\tcopy(entries, d.Entries)\n\t\tentries[index] = DictEntry[K, V]{Key: key, Value: value}\n\t\treturn Dict[K, V]{Entries: entries}\n\t}\n\tentries := make([]DictEntry[K, V], 0, len(d.Entries)+1)\n\tentries = append(entries, d.Entries[:index]...)\n\tentries = append(entries, DictEntry[K, V]{Key: key, Value: value})\n\tentries = append(entries, d.Entries[index:]...)\n\treturn Dict[K, V]{Entries: entries}\n}\n\nfunc DictRemove[K any, V any](d Dict[K, V], key K, less func(K, K) bool) Dict[K, V] {\n\tindex, found := dictSearch(d, key, less)\n\tif !found {\n\t\treturn d\n\t}\n\tentries := make([]DictEntry[K, V], 0, len(d.Entries)-1)\n\tentries = append(entries, d.Entries[:index]...)\n\tentries = append(entries, d.Entries[index+1:]...)\n\treturn Dict[K, V]{Entries: entries}\n}\n\n// DictUnion is LEFT-BIASED: a key in both dicts keeps the left dict's value, which is what\n// `hash-union #:combine (lambda (_v2 v1) v1)` does in tesl/dict.rkt. Both operands are already\n// in key order, so this is a merge rather than a series of inserts \226\128\148 O(n+m) against O(n log m).\nfunc DictUnion[K any, V any](left, right Dict[K, V], less func(K, K) bool) Dict[K, V] {\n\tentries := make([]DictEntry[K, V], 0, len(left.Entries)+len(right.Entries))\n\tleftAt, rightAt := 0, 0\n\tfor leftAt < len(left.Entries) && rightAt < len(right.Entries) {\n\t\tleftKey := left.Entries[leftAt].Key\n\t\trightKey := right.Entries[rightAt].Key\n\t\tswitch {\n\t\tcase less(leftKey, rightKey):\n\t\t\tentries = append(entries, left.Entries[leftAt])\n\t\t\tleftAt++\n\t\tcase less(rightKey, leftKey):\n\t\t\tentries = append(entries, right.Entries[rightAt])\n\t\t\trightAt++\n\t\tdefault:\n\t\t\t// The same key on both sides: the left value wins, and the right one is dropped.\n\t\t\tentries = append(entries, left.Entries[leftAt])\n\t\t\tleftAt++\n\t\t\trightAt++\n\t\t}\n\t}\n\tentries = append(entries, left.Entries[leftAt:]...)\n\tentries = append(entries, right.Entries[rightAt:]...)\n\treturn Dict[K, V]{Entries: entries}\n}\n\n// sameKey is equality derived from the ordering the emitter supplies: neither key\n// orders before the other.\nfunc sameKey[K any](left, right K, less func(K, K) bool) bool {\n\treturn !less(left, right) && !less(right, left)\n}\n\n// DictKeys, DictValues, and DictToList all iterate in key order, so a Tesl program's\n// output does not depend on which backend ran it or on Go's map seed.\nfunc DictKeys[K any, V any](d Dict[K, V]) []K {\n\tkeys := make([]K, len(d.Entries))\n\tfor index, entry := range d.Entries {\n\t\tkeys[index] = entry.Key\n\t}\n\treturn keys\n}\n\nfunc DictValues[K any, V any](d Dict[K, V]) []V {\n\tvalues := make([]V, len(d.Entries))\n\tfor index, entry := range d.Entries {\n\t\tvalues[index] = entry.Value\n\t}\n\treturn values\n}\n\nfunc DictToList[K any, V any](d Dict[K, V]) []Tuple2[K, V] {\n\tpairs := make([]Tuple2[K, V], len(d.Entries))\n\tfor index, entry := range d.Entries {\n\t\tpairs[index] = Tuple2[K, V]{Tuple2First: entry.Key, Tuple2Second: entry.Value}\n\t}\n\treturn pairs\n}\n\n// DictFromList lets LATER duplicates win, matching tesl/dict.rkt's `for/hash`.\n//\n// Sorted once rather than inserted one at a time: n inserts would be O(n\194\178), where a\n// stable sort plus a dedup pass is O(n log n). Stability is what makes \"later wins\"\n// well defined \226\128\148 equal keys keep their input order, so the last of each run is the\n// one to keep.\nfunc DictFromList[K any, V any](pairs []Tuple2[K, V], less func(K, K) bool) Dict[K, V] {\n\tentries := make([]DictEntry[K, V], len(pairs))\n\tfor index, pair := range pairs {\n\t\tentries[index] = DictEntry[K, V]{Key: pair.Tuple2First, Value: pair.Tuple2Second}\n\t}\n\tslices.SortStableFunc(entries, func(left, right DictEntry[K, V]) int {\n\t\tswitch {\n\t\tcase less(left.Key, right.Key):\n\t\t\treturn -1\n\t\tcase less(right.Key, left.Key):\n\t\t\treturn 1\n\t\tdefault:\n\t\t\treturn 0\n\t\t}\n\t})\n\t// Compacts in place: `entries` was just allocated here, so nothing else can see it,\n\t// and each write lands at an index at or below the one being read.\n\tkept := entries[:0]\n\tfor index := 0; index < len(entries); index++ {\n\t\tlast := index\n\t\tfor last+1 < len(entries) && sameKey(entries[last].Key, entries[last+1].Key, less) {\n\t\t\tlast++\n\t\t}\n\t\tkept = append(kept, entries[last])\n\t\tindex = last\n\t}\n\treturn Dict[K, V]{Entries: kept}\n}\n\n// DictEqualBy compares two dicts entry by entry. Since both are kept in key order, a\n// single pass suffices \226\128\148 no lookup per key. Key and value comparisons are supplied by\n// the emitter, which knows both concrete types at the comparison site.\nfunc DictEqualBy[K any, V any](\n\tleft, right Dict[K, V], keyEqual func(K, K) bool, valueEqual func(V, V) bool,\n) bool {\n\tif len(left.Entries) != len(right.Entries) {\n\t\treturn false\n\t}\n\tfor index := range left.Entries {\n\t\tif !keyEqual(left.Entries[index].Key, right.Entries[index].Key) {\n\t\t\treturn false\n\t\t}\n\t\tif !valueEqual(left.Entries[index].Value, right.Entries[index].Value) {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\n// stringKeyLess is the ordering a String-keyed Dict is built with \226\128\148 the comparator the emitter\n// passes at every `Dict.lookup` on String keys. It lives here rather than with the HTTP request\n// snapshot that first needed it: `Tesl.JWT`'s claims are a String-keyed Dict too, and a program\n// that signs a token need not serve HTTP.\nfunc stringKeyLess(left, right string) bool { return left < right }\n\n// DictSingleton is `Dict.singleton`: the one-entry dict, which is how a claims dict or a\n// single-key lookup table is usually written.\nfunc DictSingleton[K any, V any](key K, value V) Dict[K, V] {\n\treturn Dict[K, V]{Entries: []DictEntry[K, V]{{Key: key, Value: value}}}\n}\n\n// DictRequireKey is `check Dict.requireKey key d`: it answers the SAME dict, and what a caller\n// gains is the `HasKey` proof that makes `Dict.get` reachable. The proof erases here, so what\n// survives is the rejection \226\128\148 a 400, because a missing key in a request-shaped dict is bad input\n// rather than a broken program.\nfunc DictRequireKey[K any, V any](d Dict[K, V], key K, less func(K, K) bool) Check[Dict[K, V]] {\n\tif _, found := dictSearch(d, key, less); found {\n\t\treturn Accept(d)\n\t}\n\treturn Reject[Dict[K, V]](400, fmt.Sprintf(\"expected key %v to be present in Dict\", key))\n}\n\n// DictGet is `Dict.get`: the value for a key the caller has PROVEN present. The proof is what\n// makes it total, and it erases \226\128\148 so this traps rather than answering a zero value, which is the\n// only honest thing left if a caller ever reached it without the proof.\nfunc DictGet[K any, V any](d Dict[K, V], key K, less func(K, K) bool) V {\n\tif index, found := dictSearch(d, key, less); found {\n\t\treturn d.Entries[index].Value\n\t}\n\tpanic(fmt.Sprintf(\"Dict.get: key %v is not present \226\128\148 a `check Dict.requireKey` must prove it \"+\n\t\t\"first\", key))\n}\n");
  ("env.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"math/big\"\n\t\"os\"\n)\n\n// Reading the environment is an effect Tesl gates behind the `envRead` capability, which\n// the checker enforces; nothing about that survives to run time.\n//\n// An EMPTY variable counts as unset everywhere here, which is what `tesl/private/runtime.rkt`\n// does (`empty-string->false`): `FOO=` and an absent `FOO` are the same answer, so a blank\n// value in a deployment cannot look like a configured one.\nfunc envRaw(name string) (string, bool) {\n\tvalue := os.Getenv(name)\n\tif value == \"\" {\n\t\treturn \"\", false\n\t}\n\treturn value, true\n}\n\n// EnvMaybe is `env`: the value, or Nothing when unset.\nfunc EnvMaybe(name string) Maybe[string] {\n\tif value, ok := envRaw(name); ok {\n\t\treturn Something(value)\n\t}\n\treturn Nothing[string]()\n}\n\n// EnvString is `envString`: the value, or the fallback when unset.\nfunc EnvString(name, fallback string) string {\n\tif value, ok := envRaw(name); ok {\n\t\treturn value\n\t}\n\treturn fallback\n}\n\n// EnvInt is `envInt`: the value parsed as an exact integer, or the fallback when unset. A\n// SET-but-unparseable value is a configuration error and traps \226\128\148 it must not quietly become\n// the fallback, or a typo'd port would look like a deliberate default.\nfunc EnvInt(name string, fallback Int) Int {\n\tvalue, ok := envRaw(name)\n\tif !ok {\n\t\treturn fallback\n\t}\n\tparsed, valid := new(big.Int).SetString(value, 10)\n\tif !valid {\n\t\tpanic(fmt.Sprintf(\"envInt: invalid integer environment value %s=%s\", name, value))\n\t}\n\treturn fromBig(parsed)\n}\n\n// RequireEnv is `requireEnv`: the value, or a trap. An unset variable is a configuration\n// error, so it fails at the read rather than flowing on as an empty string.\nfunc RequireEnv(name string) string {\n\tvalue, ok := envRaw(name)\n\tif !ok {\n\t\tpanic(fmt.Sprintf(\"requireEnv: environment variable %s is not set (or is empty)\", name))\n\t}\n\treturn value\n}\n");
  ("either.go", "package teslrt\n\n// Either is Tesl's `Either a b`: Left carries the error-ish side, Right the value.\n// Like Maybe it lives in the runtime because an Either crosses module boundaries, and\n// it uses the same flat tag+payload shape the emitter generates for a user ADT.\n//\n// The zero value is a Left holding the zero value of A, matching the tag order below.\n// That is deliberate rather than incidental: a zero Either must not read as a Right\n// and hand out a fabricated value.\ntype Either[A any, B any] struct {\n\tTag        EitherTag\n\tLeftValue  A\n\tRightValue B\n}\n\n// EitherTag identifies which side an Either holds. The constants are an enum-like set\n// so the `exhaustive` linter can verify a switch over them.\ntype EitherTag int\n\nconst (\n\tEitherLeft EitherTag = iota\n\tEitherRight\n)\n\n// Left and Right exist for hand-written runtime code and readable tests; emitted code\n// writes the composite literal directly.\nfunc Left[A any, B any](value A) Either[A, B] {\n\treturn Either[A, B]{Tag: EitherLeft, LeftValue: value}\n}\n\nfunc Right[A any, B any](value B) Either[A, B] {\n\treturn Either[A, B]{Tag: EitherRight, RightValue: value}\n}\n\n// IsRight reports which side is held.\nfunc (e Either[A, B]) IsRight() bool {\n\treturn e.Tag == EitherRight\n}\n\n// EitherPartition splits a list of Either into its Left payloads and its Right payloads, in\n// input order, as `Either.partition` does in tesl/either.rkt. It answers the two lists as a\n// Tuple2 rather than two values, because Tesl has no multiple return.\nfunc EitherPartition[A any, B any](values []Either[A, B]) Tuple2[[]A, []B] {\n\tlefts := make([]A, 0, len(values))\n\trights := make([]B, 0, len(values))\n\tfor _, value := range values {\n\t\tif value.Tag == EitherRight {\n\t\t\trights = append(rights, value.RightValue)\n\t\t} else {\n\t\t\tlefts = append(lefts, value.LeftValue)\n\t\t}\n\t}\n\treturn Tuple2[[]A, []B]{Tuple2First: lefts, Tuple2Second: rights}\n}\n\n// The Either combinators, as tesl/either.tesl writes them. The ones taking a FUNCTION\n// (`map`, `mapLeft`, `andThen`) are emitted inline instead \226\128\148 the emitter inlines a callback\n// rather than passing a Go func value, which is the same rule the list leaves follow.\n\nfunc EitherIsLeft[A any, B any](value Either[A, B]) bool {\n\treturn value.Tag == EitherLeft\n}\n\nfunc EitherIsRight[A any, B any](value Either[A, B]) bool {\n\treturn value.Tag == EitherRight\n}\n\nfunc EitherFromLeft[A any, B any](value Either[A, B]) Maybe[A] {\n\tif value.Tag == EitherLeft {\n\t\treturn Something(value.LeftValue)\n\t}\n\treturn Nothing[A]()\n}\n\nfunc EitherFromRight[A any, B any](value Either[A, B]) Maybe[B] {\n\tif value.Tag == EitherRight {\n\t\treturn Something(value.RightValue)\n\t}\n\treturn Nothing[B]()\n}\n\n// EitherWithDefault takes the default FIRST, as the Tesl signature does.\nfunc EitherWithDefault[A any, B any](fallback B, value Either[A, B]) B {\n\tif value.Tag == EitherRight {\n\t\treturn value.RightValue\n\t}\n\treturn fallback\n}\n\n// EitherToMaybe forgets WHICH left it was: a Left becomes Nothing.\nfunc EitherToMaybe[A any, B any](value Either[A, B]) Maybe[B] {\n\tif value.Tag == EitherRight {\n\t\treturn Something(value.RightValue)\n\t}\n\treturn Nothing[B]()\n}\n\n// EitherFromMaybe supplies the left a Nothing has no value for.\nfunc EitherFromMaybe[A any, B any](leftValue A, value Maybe[B]) Either[A, B] {\n\tif value.Tag == MaybeSomething {\n\t\treturn Right[A, B](value.SomethingValue)\n\t}\n\treturn Left[A, B](leftValue)\n}\n");
  ("email.go", "package teslrt\n\nimport (\n\t\"crypto/tls\"\n\t\"fmt\"\n\t\"net/smtp\"\n\t\"os\"\n\t\"strings\"\n\t\"sync\"\n\t\"time\"\n)\n\n// `email E = Email { \226\128\166 }` and the two operations on it: `Email.send` and `startEmailWorker`.\n//\n// SENDING IS ENQUEUEING, not delivering. `Email.send` appends to an OUTBOX and returns; the\n// worker delivers. That is Racket's shape (its send writes a `tesl_email_outbox` row, or\n// appends to an in-memory list when no PostgreSQL runtime is bound) and it is the shape that\n// makes a handler's latency independent of the mail server's.\n//\n// The retry rule is Racket's too: at most 5 attempts, the next one no sooner than\n// 5 * 2^attempts minutes after the last \226\128\148 5, 10, 20, 40, 80. A message that exhausts them is\n// DEAD, not silently dropped: it stays in the outbox with that status, so it can be counted.\ntype EmailBodyTag int\n\nconst (\n\tEmailBodyText EmailBodyTag = iota\n\tEmailBodyHTML\n\tEmailBodyRich\n)\n\n// EmailBody is the `EmailBody(..)` ADT. It lives in the runtime for the reason Maybe does: it\n// crosses module boundaries, and two packages declaring their own would be different Go types.\n// The ADT is what makes a body-less email unconstructible \226\128\148 there is no variant for one.\ntype EmailBody struct {\n\tTag  EmailBodyTag\n\tText string\n\tHTML string\n}\n\nfunc TextBody(content string) EmailBody {\n\treturn EmailBody{Tag: EmailBodyText, Text: content}\n}\n\nfunc HTMLBody(content string) EmailBody {\n\treturn EmailBody{Tag: EmailBodyHTML, HTML: content}\n}\n\nfunc RichBody(text, html string) EmailBody {\n\treturn EmailBody{Tag: EmailBodyRich, Text: text, HTML: html}\n}\n\n// EmailStatus is where a message is in its life. A DEAD message is kept rather than dropped:\n// \"we gave up on this one\" is information the sender needs.\ntype EmailStatus int\n\nconst (\n\tEmailPending EmailStatus = iota\n\tEmailSent\n\tEmailDead\n)\n\ntype EmailMessage struct {\n\tTo       string\n\tSubject  string\n\tBody     EmailBody\n\tStatus   EmailStatus\n\tAttempts int\n\t// NextAttemptAt is when the worker may try again; zero means \"now\".\n\tNextAttemptAt time.Time\n\tSentAt        time.Time\n}\n\n// SmtpSettings is what the declaration's `smtp: SmtpConfig { \226\128\166 }` says.\ntype SmtpSettings struct {\n\tHost     string\n\tPort     int\n\tUsername string\n\tPassword string\n\tTLS      bool\n}\n\n// Outbox is one email declaration: its settings plus the messages waiting on them.\ntype Outbox struct {\n\tmutex    sync.Mutex\n\tsettings SmtpSettings\n\tmessages []EmailMessage\n\t// started guards the worker, so a `startEmailWorker` called twice runs one.\n\tstarted bool\n}\n\nfunc NewOutbox(settings SmtpSettings) *Outbox {\n\treturn &Outbox{settings: settings}\n}\n\n// emailMaxAttempts and the backoff below are tesl/email.rkt's, so a message that fails on one\n// backend is retried on the same schedule by the other.\nconst emailMaxAttempts = 5\n\nfunc emailRetryDelay(attempts int) time.Duration {\n\treturn time.Duration(5*(1<<attempts)) * time.Minute\n}\n\n// HeaderFieldSafe reports whether a header-bound field may be used as it stands. A CR or LF in\n// a recipient or a subject would inject arbitrary RFC 2822 headers \226\128\148 Bcc exfiltration,\n// spoofing, a forged body \226\128\148 and both fields are user-influenced.\n//\n// Rejecting beats sanitising: a newline in either field is always a bug or an attack, and\n// stripping it would deliver a message the author did not write.\nfunc HeaderFieldSafe(value string) bool {\n\treturn !strings.ContainsAny(value, \"\\r\\n\")\n}\n\n// SendEmail enqueues one message. The CRLF guard runs HERE, at the boundary where the value\n// enters the system, rather than at delivery: a rejected message must never reach the outbox,\n// or it would sit there being retried.\nfunc SendEmail(outbox *Outbox, to, subject string, body EmailBody) struct{} {\n\tfor _, field := range []struct{ name, value string }{\n\t\t{\"recipient\", to}, {\"subject\", subject},\n\t} {\n\t\tif !HeaderFieldSafe(field.value) {\n\t\t\tpanic(\"email \" + field.name +\n\t\t\t\t\" contains a CR/LF newline \226\128\148 header injection rejected\")\n\t\t}\n\t}\n\toutbox.mutex.Lock()\n\tdefer outbox.mutex.Unlock()\n\toutbox.messages = append(outbox.messages, EmailMessage{\n\t\tTo: to, Subject: subject, Body: body, Status: EmailPending,\n\t})\n\treturn struct{}{}\n}\n\n// StartEmailWorker starts delivery in the background: one goroutine polling every 5 seconds,\n// exactly as the Racket worker does, and a second one dropping sent messages older than 24\n// hours so a long-lived process does not accumulate them.\n//\n// Answers Tesl's Unit so the emitted `main` can return it like any other statement.\nfunc StartEmailWorker(outbox *Outbox) struct{} {\n\toutbox.mutex.Lock()\n\talready := outbox.started\n\toutbox.started = true\n\toutbox.mutex.Unlock()\n\tif already {\n\t\treturn struct{}{}\n\t}\n\tgo func() {\n\t\tfor {\n\t\t\ttime.Sleep(5 * time.Second)\n\t\t\tdeliverPending(outbox)\n\t\t}\n\t}()\n\tgo func() {\n\t\tfor {\n\t\t\ttime.Sleep(time.Hour)\n\t\t\tPruneSentEmail(outbox, 24*time.Hour)\n\t\t}\n\t}()\n\treturn struct{}{}\n}\n\n// deliverPending takes every message whose next attempt is due and tries it once.\nfunc deliverPending(outbox *Outbox) {\n\toutbox.mutex.Lock()\n\tif outbox.settings.Host == \"\" {\n\t\t// No server configured \226\128\148 which is what an unset `SMTP_HOST` means. Messages STAY\n\t\t// pending rather than burning their five attempts against a host that does not\n\t\t// exist: the configuration is what is missing, not the mail.\n\t\toutbox.mutex.Unlock()\n\t\treturn\n\t}\n\tdue := []int{}\n\tfor index, message := range outbox.messages {\n\t\tif message.Status == EmailPending && !time.Now().Before(message.NextAttemptAt) {\n\t\t\tdue = append(due, index)\n\t\t}\n\t}\n\tsettings := outbox.settings\n\tpending := make([]EmailMessage, len(due))\n\tfor slot, index := range due {\n\t\tpending[slot] = outbox.messages[index]\n\t}\n\toutbox.mutex.Unlock()\n\n\tfor slot, message := range pending {\n\t\terr := DeliverEmail(settings, message)\n\t\toutbox.mutex.Lock()\n\t\tindex := due[slot]\n\t\trecord := &outbox.messages[index]\n\t\tif err == nil {\n\t\t\trecord.Status = EmailSent\n\t\t\trecord.SentAt = time.Now()\n\t\t} else {\n\t\t\trecord.Attempts++\n\t\t\tif record.Attempts >= emailMaxAttempts {\n\t\t\t\trecord.Status = EmailDead\n\t\t\t} else {\n\t\t\t\trecord.NextAttemptAt = time.Now().Add(emailRetryDelay(record.Attempts))\n\t\t\t}\n\t\t\t// The failure is reported rather than swallowed: a worker that silently drops\n\t\t\t// every message looks identical to one with nothing to do.\n\t\t\tfmt.Fprintf(os.Stderr, \"tesl: email delivery to %s failed (attempt %d): %v\\n\",\n\t\t\t\trecord.To, record.Attempts, err)\n\t\t}\n\t\toutbox.mutex.Unlock()\n\t}\n}\n\n// DeliverEmail sends ONE message over SMTP, building the same minimal RFC 2822 header the\n// Racket runtime builds \226\128\148 From/To/Subject/MIME-Version plus a content type chosen by which\n// half of the body is present, so an HTML body arrives as HTML.\nfunc DeliverEmail(settings SmtpSettings, message EmailMessage) error {\n\tif !HeaderFieldSafe(message.To) || !HeaderFieldSafe(message.Subject) {\n\t\treturn fmt.Errorf(\"email header contains a CR/LF newline\")\n\t}\n\t// Which half is sent is decided by the VARIANT, not by which string happens to be\n\t// non-empty: an `HtmlBody \"\"` is still an HTML message, and asking the payload would\n\t// silently retype it as text.\n\tcontentType := \"text/plain; charset=utf-8\"\n\tpayload := message.Body.Text\n\tif message.Body.Tag == EmailBodyHTML || message.Body.Tag == EmailBodyRich {\n\t\tcontentType = \"text/html; charset=utf-8\"\n\t\tpayload = message.Body.HTML\n\t}\n\theader := \"From: \" + settings.Username + \"\\r\\n\" +\n\t\t\"To: \" + message.To + \"\\r\\n\" +\n\t\t\"Subject: \" + message.Subject + \"\\r\\n\" +\n\t\t\"MIME-Version: 1.0\\r\\n\" +\n\t\t\"Content-Type: \" + contentType + \"\\r\\n\"\n\taddress := fmt.Sprintf(\"%s:%d\", settings.Host, settings.Port)\n\n\tclient, err := smtp.Dial(address)\n\tif err != nil {\n\t\treturn err\n\t}\n\tdefer func() { _ = client.Close() }()\n\tif settings.TLS {\n\t\t// STARTTLS with the server's own name verified: skipping verification here would\n\t\t// make the encryption decorative, since anything on the path could terminate it.\n\t\tif err := client.StartTLS(&tls.Config{\n\t\t\tServerName: settings.Host,\n\t\t\tMinVersion: tls.VersionTLS12,\n\t\t}); err != nil {\n\t\t\treturn err\n\t\t}\n\t}\n\t// Authenticate only when the server ASKS for it. A server that advertises no AUTH would\n\t// reject the command, and failing there would mean a relay that needs no credentials\n\t// could never be used.\n\t//\n\t// `smtp.PlainAuth` refuses to hand over the password on an unencrypted connection\n\t// (localhost aside). That is a deliberate floor and not worked around here: a credential\n\t// sent in the clear is readable by everything on the path.\n\tif supported, _ := client.Extension(\"AUTH\"); supported && settings.Username != \"\" {\n\t\tauth := smtp.PlainAuth(\"\", settings.Username, settings.Password, settings.Host)\n\t\tif err := client.Auth(auth); err != nil {\n\t\t\treturn err\n\t\t}\n\t}\n\tif err := client.Mail(settings.Username); err != nil {\n\t\treturn err\n\t}\n\tif err := client.Rcpt(message.To); err != nil {\n\t\treturn err\n\t}\n\twriter, err := client.Data()\n\tif err != nil {\n\t\treturn err\n\t}\n\tif _, err := writer.Write([]byte(header + \"\\r\\n\" + payload)); err != nil {\n\t\t_ = writer.Close()\n\t\treturn err\n\t}\n\tif err := writer.Close(); err != nil {\n\t\treturn err\n\t}\n\treturn client.Quit()\n}\n\n// PruneSentEmail drops delivered messages older than `keep`.\nfunc PruneSentEmail(outbox *Outbox, keep time.Duration) {\n\toutbox.mutex.Lock()\n\tdefer outbox.mutex.Unlock()\n\t// A FRESH slice rather than `outbox.messages[:0]`: reusing the backing array aliases the\n\t// store with a value that has already been read out of it, and nilaway reads the reuse as\n\t// a nil flow into the field.\n\tkept := make([]EmailMessage, 0, len(outbox.messages))\n\tfor _, message := range outbox.messages {\n\t\tif message.Status == EmailSent && time.Since(message.SentAt) > keep {\n\t\t\tcontinue\n\t\t}\n\t\tkept = append(kept, message)\n\t}\n\toutbox.messages = kept\n}\n\n// \226\148\128\226\148\128 Inspection seams \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Not Tesl-surface names: they exist so the runtime's own tests can assert what was enqueued,\n// and so the per-test reset can empty an outbox the way it empties a table.\n\nfunc OutboxMessages(outbox *Outbox) []EmailMessage {\n\toutbox.mutex.Lock()\n\tdefer outbox.mutex.Unlock()\n\tout := make([]EmailMessage, len(outbox.messages))\n\tcopy(out, outbox.messages)\n\treturn out\n}\n\nfunc ResetOutbox(outbox *Outbox) {\n\toutbox.mutex.Lock()\n\tdefer outbox.mutex.Unlock()\n\t// An EMPTY slice, not nil: the field is read by every other operation, and a nil there is\n\t// a nil flow into all of them as far as nilaway is concerned.\n\toutbox.messages = []EmailMessage{}\n}\n");
  ("float.go", "package teslrt\n\nimport (\n\t\"math\"\n\t\"strconv\"\n)\n\n// Tesl's Float is Go's float64. Two things about it are NOT the Go default, and both\n// are decided in roadmap/next/migrate_to_golang.md (\"Float decisions\"):\n//\n//   - Rendering adopts Go's shortest-round-trip digits and layout, plus one deviation:\n//     a `.0` is appended when the result carries neither `.` nor `e`. Tesl has both\n//     arbitrary-precision Int and Float, so a bare `1` would make the two\n//     indistinguishable in logs, interpolation, and test expectations.\n//   - Equality is STRUCTURAL, not IEEE: NaN equals NaN and 0.0 does not equal -0.0,\n//     matching Racket's `equal?`. IEEE `==` is not an equivalence relation, which\n//     would cost a record or Dict key holding NaN its own reflexivity. Postgres\n//     independently agrees that NaN = NaN.\n//\n// Ordering, by contrast, IS plain IEEE in both backends, so it needs no helper here.\nfunc FormatFloat(value float64) string {\n\tswitch {\n\tcase math.IsNaN(value):\n\t\treturn \"NaN\"\n\tcase math.IsInf(value, 1):\n\t\treturn \"+Inf\"\n\tcase math.IsInf(value, -1):\n\t\treturn \"-Inf\"\n\t}\n\trendered := strconv.FormatFloat(value, 'g', -1, 64)\n\tfor index := 0; index < len(rendered); index++ {\n\t\tif rendered[index] == '.' || rendered[index] == 'e' {\n\t\t\treturn rendered\n\t\t}\n\t}\n\treturn rendered + \".0\"\n}\n\n// FloatEqual is Racket `equal?` on flonums: reflexive for NaN, and distinguishing the\n// two zeros. Both differ from Go's `==`.\nfunc FloatEqual(left, right float64) bool {\n\tif math.IsNaN(left) || math.IsNaN(right) {\n\t\treturn math.IsNaN(left) && math.IsNaN(right)\n\t}\n\tif left == 0 && right == 0 {\n\t\treturn math.Signbit(left) == math.Signbit(right)\n\t}\n\treturn left == right\n}\n\n// FloatKeyLess is the ordering Dict and Set use for Float KEYS, and it is deliberately\n// NOT the IEEE ordering that user-visible comparisons and List.sort use.\n//\n// Dict and Set are sorted, so their binary search derives key equivalence from the\n// comparator: \"neither side is less\" means \"same key\". Native `<` breaks that in two\n// ways, and both are real lookup bugs rather than output-order quirks:\n//\n//   - Every comparison involving NaN is false, so a NaN key looks equivalent to whatever\n//     value the search happens to probe. `SetMember(NaN, {1,2,3})` returned TRUE with the\n//     native comparator, and inserting NaN was a no-op.\n//   - Native ordering treats -0.0 and +0.0 as equal, while FloatEqual deliberately\n//     distinguishes them, so the two collapsed into one key.\n//\n// The law this function must satisfy, and which float_test.go checks exhaustively:\n//\n//\tFloatEqual(a, b)  ==  !FloatKeyLess(a, b) && !FloatKeyLess(b, a)\n//\n// All NaNs form ONE key-equivalence class (matching FloatEqual) and sort before every\n// number; -0.0 sorts immediately before +0.0. Where the NaN class sits is internal and is\n// not Tesl language semantics \226\128\148 only the equivalence classes are.\nfunc FloatKeyLess(left, right float64) bool {\n\tleftIsNaN, rightIsNaN := math.IsNaN(left), math.IsNaN(right)\n\tif leftIsNaN || rightIsNaN {\n\t\treturn leftIsNaN && !rightIsNaN\n\t}\n\tif left == right {\n\t\t// Only -0.0 and +0.0 reach here as distinct values.\n\t\treturn math.Signbit(left) && !math.Signbit(right)\n\t}\n\treturn left < right\n}\n\n// ParseFloat accepts what Tesl's `Float.parse` accepts: a decimal float, with no\n// leading or trailing space and no Racket reader syntax.\nfunc ParseFloat(text string) Maybe[float64] {\n\tvalue, err := strconv.ParseFloat(text, 64)\n\tif err != nil {\n\t\treturn Nothing[float64]()\n\t}\n\treturn Something(value)\n}\n\nfunc FloatAbs(value float64) float64 {\n\treturn math.Abs(value)\n}\n\n// FloatMin and FloatMax propagate NaN and distinguish the zeros, matching Racket's\n// `min`/`max` on flonums rather than Go's built-in min/max.\nfunc FloatMin(left, right float64) float64 {\n\treturn math.Min(left, right)\n}\n\nfunc FloatMax(left, right float64) float64 {\n\treturn math.Max(left, right)\n}\n\n// FloatFloor, FloatCeil, and FloatRound return an INT, not a Float: tesl/float.rkt\n// wraps each in `inexact->exact`, and their declared Tesl signatures are\n// `Float -> Int`. FloatRound rounds HALF TO EVEN, because that is what Racket's\n// `round` does on a flonum \226\128\148 `math.Round` would round half away from zero and disagree\n// on every exact .5.\n//\n// NaN and infinity have no integer value. Racket RAISES there (`inexact->exact` has no\n// exact representation for them) and the declared Tesl signature is `Float -> Int`\n// rather than `-> Maybe Int`, so these panic rather than inventing a value or widening\n// the type.\nfunc FloatFloor(value float64) Int {\n\treturn mustExactInt(\"Float.floor\", math.Floor(value))\n}\n\nfunc FloatCeil(value float64) Int {\n\treturn mustExactInt(\"Float.ceil\", math.Ceil(value))\n}\n\nfunc FloatRound(value float64) Int {\n\treturn mustExactInt(\"Float.round\", math.RoundToEven(value))\n}\n\nfunc mustExactInt(who string, value float64) Int {\n\tconverted, err := FromFloat64(value)\n\tif err != nil {\n\t\tpanic(who + \": no exact integer for NaN or infinity\")\n\t}\n\treturn converted\n}\n\nfunc FloatClamp(value, low, high float64) float64 {\n\treturn math.Max(low, math.Min(high, value))\n}\n\n// FloatSqrt is bit-identical to Racket's, since both defer to the IEEE-exact\n// operation. The other transcendentals are NOT: they are rejected in the emitter\n// rather than emitted divergent.\nfunc FloatSqrt(value float64) float64 {\n\treturn math.Sqrt(value)\n}\n\n// FloatPow answers NaN wherever the result has no REAL value, which is every negative base with\n// a non-integer exponent. Racket's `expt` returns a COMPLEX number there \226\128\148 a value no Tesl Float\n// can hold, and a bug this port found in `tesl/float.rkt` (fixed to answer NaN as well).\n//\n// The guard is not decoration: C's `pow`, which `math.Pow` follows, makes two deliberate\n// exceptions \226\128\148 `pow(-1, \194\177Inf)` is 1 and `pow(-4, +Inf)` is +Inf \226\128\148 on the grounds that \194\177Inf is\n// \"even\". A Tesl Float is a real number and (-1)^\226\136\158 has no real value, so those answer NaN here\n// too, and the two backends agree at every input rather than at most of them.\nfunc FloatPow(base, exponent float64) float64 {\n\tnonInteger := math.IsInf(exponent, 0) || math.IsNaN(exponent) ||\n\t\texponent != math.Trunc(exponent)\n\tif base < 0 && nonInteger {\n\t\treturn math.NaN()\n\t}\n\treturn math.Pow(base, exponent)\n}\n\n// FloatFromInt is exact up to 2^53 and rounds beyond it, like Racket's exact->inexact.\nfunc FloatFromInt(value Int) float64 {\n\treturn value.Float64()\n}\n\n// FloatToInt truncates toward zero. NaN and infinity are not integers, so this\n// mirrors Int.fromFloat by refusing them.\nfunc FloatToInt(value float64) Maybe[Int] {\n\tconverted, err := FromFloat64(value)\n\tif err != nil {\n\t\treturn Nothing[Int]()\n\t}\n\treturn Something(converted)\n}\n\n// FloatRequireNonZero is Tesl's `Float.requireNonZero` check: the proof erases, the\n// rejection does not. Negative zero is zero.\nfunc FloatRequireNonZero(value float64) Check[float64] {\n\tif value == 0 {\n\t\treturn Reject[float64](400, \"expected a non-zero float\")\n\t}\n\treturn Accept(value)\n}\n\n// FloatToIntTruncating is Tesl's `Float.toInt`, whose declared signature is\n// `Float -> Int`: it truncates toward zero and panics on NaN or infinity, exactly where\n// Racket's `inexact->exact` raises. FloatToInt above is the Maybe-returning form used\n// by hand-written runtime code.\nfunc FloatToIntTruncating(value float64) Int {\n\treturn mustExactInt(\"Float.toInt\", math.Trunc(value))\n}\n\n// FloatRequireNonNegative is Tesl's `Float.requireNonNegative`, the check that\n// discharges `Float.sqrt`'s FloatNonNegative obligation. Zero is accepted: sqrt(0) is\n// 0, so requiring strict positivity would reject a well-defined call.\nfunc FloatRequireNonNegative(value float64) Check[float64] {\n\tif math.IsNaN(value) || value < 0 {\n\t\treturn Reject[float64](422, \"expected a non-negative float\")\n\t}\n\treturn Accept(value)\n}\n\n// The Float ARITHMETIC surface (`Float.add` and friends), for code that prefers named\n// operations to operators. Plain IEEE arithmetic \226\128\148 the same thing `a + b` emits.\nfunc FloatAdd(left, right float64) float64 { return left + right }\n\nfunc FloatSub(left, right float64) float64 { return left - right }\n\nfunc FloatMul(left, right float64) float64 { return left * right }\n\n// FloatDiv's divisor carries a `FloatNonZero` proof, which erases; division by zero would\n// otherwise be \194\177Inf, which is what Racket's `/` on a flonum gives too. No guard is added\n// here: adding one would make the emitted program behave differently from `a / b`, and the\n// proof is the enforcement.\nfunc FloatDiv(left, right float64) float64 { return left / right }\n\n// The predicates. NaN is its own case in every one of them: `Float.isNaN x` is the only way to\n// ask, because every comparison with NaN is false \226\128\148 which is exactly why `isPositive`,\n// `isNegative` and `isZero` all answer false for it rather than one of them accidentally\n// answering true.\nfunc FloatIsNaN(value float64) bool { return math.IsNaN(value) }\n\nfunc FloatIsInfinite(value float64) bool { return math.IsInf(value, 0) }\n\nfunc FloatIsPositive(value float64) bool { return value > 0 }\n\nfunc FloatIsNegative(value float64) bool { return value < 0 }\n\nfunc FloatIsZero(value float64) bool { return value == 0 }\n\n// FloatSign answers a FLOAT (1.0, -1.0, 0.0), not an Int: it composes with float arithmetic,\n// which is what a sign is used for. NaN has no sign, and answering 0.0 for it would claim it is\n// zero \226\128\148 so it answers NaN, which propagates the way every other NaN operation does.\nfunc FloatSign(value float64) float64 {\n\tswitch {\n\tcase math.IsNaN(value):\n\t\treturn value\n\tcase value > 0:\n\t\treturn 1.0\n\tcase value < 0:\n\t\treturn -1.0\n\tdefault:\n\t\treturn 0.0\n\t}\n}\n\n// FloatInfinity and FloatNaN are the two named constants. They are functions rather than package\n// variables so nothing can assign to them.\nfunc FloatInfinity() float64 { return math.Inf(1) }\n\nfunc FloatNaN() float64 { return math.NaN() }\n\n// \226\148\128\226\148\128 The transcendentals \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// These forward to Go's `math`, and they DIVERGE from the Racket runtime: sin, cos and tan\n// differ on 22 %, 22 % and 34 % of inputs respectively (up to 9,214 ulps near a zero of the\n// function), and exp on 0.09 %. That divergence rate says the two implementations differ,\n// not which is right \226\128\148 ulp-of-result exaggerates a small absolute error near a zero \226\128\148 and\n// deciding needs a correctly-rounded reference rather than a diff against Racket. The\n// maintainer's call (2026-08-12) is to use Go's and record the divergence, not to block on\n// it, so a program that computes a sine gets an answer here instead of a refusal.\n//\n// FloatLog is the one that does NOT forward, because Go's math.Log is wrong rather than\n// merely different; see its own note.\n\nfunc FloatSin(value float64) float64 { return math.Sin(value) }\n\nfunc FloatCos(value float64) float64 { return math.Cos(value) }\n\nfunc FloatTan(value float64) float64 { return math.Tan(value) }\n\nfunc FloatExp(value float64) float64 { return math.Exp(value) }\n\n// FloatLog wraps math.Log rather than forwarding to it. Go's returns\n// -709.0895657128241 for EVERY subnormal input \226\128\148 Log(5e-324) and Log(2.5e-323) give the\n// same answer, and both are wrong (the true values are about -744.44 and -742.83). Scaling\n// a subnormal into the normal range and subtracting the scale back out reproduces both\n// exactly. Everything else forwards unchanged.\nfunc FloatLog(value float64) float64 {\n\tif value > 0 && value < smallestNormalFloat {\n\t\treturn math.Log(value*subnormalLogScale) - subnormalLogShift\n\t}\n\treturn math.Log(value)\n}\n\nconst (\n\t// The smallest positive NORMAL float64: below this the mantissa loses bits, which is\n\t// exactly where math.Log stops distinguishing inputs.\n\tsmallestNormalFloat = 2.2250738585072014e-308\n\t// 2^100, and 100\194\183ln2 \226\128\148 the scale up and the amount to take back off.\n\tsubnormalLogScale = 1267650600228229401496703205376.0\n\tsubnormalLogShift = 69.31471805599453\n)\n");
  ("hostclass.go", "package teslrt\n\nimport (\n\t\"net\"\n\t\"os\"\n\t\"strconv\"\n\t\"strings\"\n)\n\n// Address-range classification \226\128\148 the decision behind SSRF egress containment.\n//\n// The dangerous SSRF targets (cloud metadata at 169.254.169.254, RFC1918, CGNAT,\n// unique-local, link-local, 0.0.0.0/8) are refused for every outbound call, judged by the\n// address actually CONNECTED TO rather than by the hostname: `evil.example.com` can resolve\n// to 127.0.0.1 or to the metadata address, so a name check decides nothing.\n//\n// The range table is deliberately a copy of ONE Racket table\n// (`dsl/private/host-classify.rkt`), including the refusal wording, because a `.tesl`\n// application asking `Net.isForbiddenHost` and the runtime refusing to dial must agree\n// about what \"private\" means. The counterpart to `Tesl.Net`'s full host normalization \226\128\148\n// the `inet_aton` spellings (`2130706433`, `0x7f.0.0.1`) and DNS-name syntax \226\128\148 is not here:\n// this entry point judges an address a resolver already produced, and a spelling that\n// cannot be parsed is refused rather than guessed at.\n//\n// FAIL CLOSED: anything not recognizable as a public, routable address is refused.\ntype addressClass int\n\nconst (\n\tclassInvalid addressClass = iota\n\tclassPublic\n\tclassLoopback\n\tclassUnspecified\n\tclassLinkLocal\n\tclassPrivate\n\tclassCGNAT\n\tclassMulticast\n)\n\n// classifyAddress parses a bare address (an IPv6 zone id is tolerated and dropped) and\n// answers its class along with whether it denotes an IPv4 address. An IPv4-mapped or\n// IPv4-compatible IPv6 spelling denotes the IPv4 address it embeds \226\128\148 that fold is the\n// classic bypass, so it happens before classification, never after.\nfunc classifyAddress(raw string) (addressClass, bool) {\n\ttext := strings.ToLower(strings.TrimSpace(raw))\n\tif zone := strings.IndexByte(text, '%'); zone >= 0 {\n\t\ttext = text[:zone]\n\t}\n\taddress := net.ParseIP(text)\n\tif address == nil {\n\t\treturn classInvalid, false\n\t}\n\tif v4 := embeddedIPv4(address); v4 != nil {\n\t\treturn ipv4Class(v4), true\n\t}\n\tif v4 := address.To4(); v4 != nil {\n\t\treturn ipv4Class(v4), true\n\t}\n\treturn ipv6Class(address.To16()), false\n}\n\n// embeddedIPv4 answers the IPv4 address an IPv6 literal embeds, or nil.\n//\n// `net.IP.To4` already folds the IPv4-mapped form (`::ffff:a.b.c.d`). The deprecated\n// IPv4-COMPATIBLE form (`::a.b.c.d`) it does not, and that form reaches the same host, so it\n// is folded here. `::` and `::1` are their own addresses and are NOT folded \226\128\148 otherwise the\n// IPv6 loopback would be classified through the 0.0.0.0/8 branch.\nfunc embeddedIPv4(address net.IP) net.IP {\n\tfull := address.To16()\n\tif full == nil || address.To4() != nil {\n\t\treturn nil\n\t}\n\tfor _, octet := range full[:12] {\n\t\tif octet != 0 {\n\t\t\treturn nil\n\t\t}\n\t}\n\tif full[12] == 0 && full[13] == 0 {\n\t\treturn nil\n\t}\n\treturn net.IPv4(full[12], full[13], full[14], full[15]).To4()\n}\n\nfunc ipv4Class(v4 net.IP) addressClass {\n\tfirst, second := v4[0], v4[1]\n\tswitch {\n\tcase first == 0:\n\t\treturn classUnspecified // 0.0.0.0/8 \"this network\"\n\tcase first == 127:\n\t\treturn classLoopback // 127.0.0.0/8\n\tcase first == 10:\n\t\treturn classPrivate // 10.0.0.0/8\n\tcase first == 169 && second == 254:\n\t\treturn classLinkLocal // 169.254.0.0/16 (cloud metadata)\n\tcase first == 172 && second >= 16 && second <= 31:\n\t\treturn classPrivate // 172.16.0.0/12\n\tcase first == 192 && second == 168:\n\t\treturn classPrivate // 192.168.0.0/16\n\tcase first == 100 && second >= 64 && second <= 127:\n\t\treturn classCGNAT // 100.64.0.0/10\n\tcase first >= 224:\n\t\treturn classMulticast // 224.0.0.0/4 and the reserved space above it\n\tdefault:\n\t\treturn classPublic\n\t}\n}\n\nfunc ipv6Class(full net.IP) addressClass {\n\tif full == nil {\n\t\treturn classInvalid\n\t}\n\tgroup0 := uint16(full[0])<<8 | uint16(full[1])\n\tswitch {\n\tcase full.Equal(net.IPv6unspecified):\n\t\treturn classUnspecified // ::\n\tcase full.Equal(net.IPv6loopback):\n\t\treturn classLoopback // ::1\n\tcase group0&0xffc0 == 0xfe80:\n\t\treturn classLinkLocal // fe80::/10\n\tcase group0&0xfe00 == 0xfc00:\n\t\treturn classPrivate // fc00::/7 unique-local\n\tcase group0&0xff00 == 0xff00:\n\t\treturn classMulticast // ff00::/8\n\tdefault:\n\t\treturn classPublic\n\t}\n}\n\n// IPForbiddenReason names the range that makes an address unsafe to dial, or \"\" when the\n// address is public and routable. The wording matches the Racket runtime's, so a refusal\n// reads the same on both backends.\nfunc IPForbiddenReason(address string) string {\n\tclass, isIPv4 := classifyAddress(address)\n\tswitch class {\n\tcase classPublic:\n\t\treturn \"\"\n\tcase classLoopback:\n\t\tif isIPv4 {\n\t\t\treturn \"loopback 127.0.0.0/8\"\n\t\t}\n\t\treturn \"IPv6 loopback ::1\"\n\tcase classUnspecified:\n\t\tif isIPv4 {\n\t\t\treturn \"0.0.0.0/8 (this network)\"\n\t\t}\n\t\treturn \"unspecified ::\"\n\tcase classLinkLocal:\n\t\tif isIPv4 {\n\t\t\treturn \"link-local 169.254.0.0/16\"\n\t\t}\n\t\treturn \"IPv6 link-local fe80::/10\"\n\tcase classPrivate:\n\t\tif isIPv4 {\n\t\t\treturn rfc1918Label(address)\n\t\t}\n\t\treturn \"IPv6 unique-local fc00::/7\"\n\tcase classCGNAT:\n\t\treturn \"CGNAT 100.64.0.0/10\"\n\tcase classMulticast:\n\t\tif isIPv4 {\n\t\t\treturn \"multicast/reserved >= 224.0.0.0\"\n\t\t}\n\t\treturn \"IPv6 multicast ff00::/8\"\n\tcase classInvalid:\n\t\treturn \"unrecognized address form (fail-closed)\"\n\t}\n\t// Unreachable: every class is answered above. A class added later lands here, and refusing\n\t// is the only safe answer for a range whose policy nobody has decided yet.\n\treturn \"unrecognized address form (fail-closed)\"\n}\n\n// rfc1918Label names the specific private block, so the refusal is actionable.\nfunc rfc1918Label(address string) string {\n\tparsed := net.ParseIP(strings.TrimSpace(strings.ToLower(address)))\n\tif parsed == nil {\n\t\treturn \"a private range\"\n\t}\n\tv4 := embeddedIPv4(parsed)\n\tif v4 == nil {\n\t\tv4 = parsed.To4()\n\t}\n\tif v4 == nil {\n\t\treturn \"a private range\"\n\t}\n\tswitch v4[0] {\n\tcase 10:\n\t\treturn \"private 10.0.0.0/8\"\n\tcase 172:\n\t\treturn \"private 172.16.0.0/12\"\n\tdefault:\n\t\treturn \"private 192.168.0.0/16\"\n\t}\n}\n\n// HostLoopback reports whether a HOST (not a resolved address) can only be reached from\n// this machine. Used solely to bound the two development escapes below; the default secure\n// path never consults it.\nfunc HostLoopback(host string) bool {\n\tlowered := strings.ToLower(strings.TrimSpace(host))\n\tlowered = strings.TrimSuffix(strings.TrimPrefix(lowered, \"[\"), \"]\")\n\tif lowered == \"localhost\" || strings.HasSuffix(lowered, \".localhost\") {\n\t\treturn true\n\t}\n\tclass, _ := classifyAddress(lowered)\n\treturn class == classLoopback\n}\n\n// SsrfAllowLoopback reports whether loopback egress is permitted: it is the one private\n// range local development legitimately dials, so a non-deployed build allows it and a\n// deployed build refuses unless TESL_HTTP_ALLOW_LOOPBACK_EGRESS opts back in.\nfunc SsrfAllowLoopback() bool {\n\tif !envPresent(\"TESL_DEPLOYED\") {\n\t\treturn true\n\t}\n\treturn envTruthy(\"TESL_HTTP_ALLOW_LOOPBACK_EGRESS\")\n}\n\n// SsrfEgressRefusal is the egress verdict for a resolved peer address: \"\" to allow.\nfunc SsrfEgressRefusal(peer string) string {\n\treason := IPForbiddenReason(peer)\n\tswitch {\n\tcase reason == \"\":\n\t\treturn \"\"\n\tcase HostLoopback(peer) && SsrfAllowLoopback():\n\t\treturn \"\"\n\tdefault:\n\t\treturn reason\n\t}\n}\n\n// \226\148\128\226\148\128 Runtime configuration knobs \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// These read the process environment for the RUNTIME's own tuning (deadlines, egress\n// policy), which is a different question from Tesl's `env` surface in env.go: there an\n// empty variable counts as unset, because a blank deployment value must not look like a\n// configured one. Here presence is what a flag means, mirroring Racket's `getenv`.\n\nfunc envPresent(name string) bool {\n\t_, present := os.LookupEnv(name)\n\treturn present\n}\n\n// envTruthy accepts the same four spellings the Racket runtime does, so a deployment's\n// existing value keeps its meaning across backends.\nfunc envTruthy(name string) bool {\n\tvalue, present := os.LookupEnv(name)\n\tif !present {\n\t\treturn false\n\t}\n\tswitch strings.ToLower(strings.TrimSpace(value)) {\n\tcase \"1\", \"true\", \"yes\", \"on\":\n\t\treturn true\n\tdefault:\n\t\treturn false\n\t}\n}\n\n// envPositiveInt reads a positive-integer env knob, falling back to `fallback` for an\n// absent, empty, or unparseable value. Read fresh per call, not at init: a test or an\n// operator can retune a deadline without a restart.\nfunc envPositiveInt(name string, fallback int) int {\n\tvalue, present := os.LookupEnv(name)\n\tif !present {\n\t\treturn fallback\n\t}\n\tparsed, err := strconv.Atoi(strings.TrimSpace(value))\n\tif err != nil || parsed <= 0 {\n\t\treturn fallback\n\t}\n\treturn parsed\n}\n");
  ("httpclient.go", "package teslrt\n\nimport (\n\t\"context\"\n\t\"crypto/rand\"\n\t\"crypto/tls\"\n\t\"encoding/hex\"\n\t\"errors\"\n\t\"fmt\"\n\t\"io\"\n\t\"net\"\n\t\"net/http\"\n\t\"net/url\"\n\t\"os\"\n\t\"sort\"\n\t\"strconv\"\n\t\"strings\"\n\t\"sync\"\n\t\"syscall\"\n\t\"time\"\n)\n\n// millisDuration turns one of the millisecond knobs into a deadline.\nfunc millisDuration(millis int) time.Duration {\n\treturn time.Duration(millis) * time.Millisecond\n}\n\n// Outbound HTTP \226\128\148 the `Tesl.HttpClient` surface.\n//\n// The `httpClient` capability that gates every call here is a COMPILE-TIME grant, so\n// nothing about it survives to run time; what does survive is the set of protections a\n// call cannot opt out of:\n//\n//\tCR/LF header guard    a `\\r\\n` in a header name or value would split the request and\n//\t                      inject arbitrary headers, so it is refused before anything is sent;\n//\tSSRF containment      the peer address actually connected to is judged (hostclass.go), so\n//\t                      a public name resolving to 169.254.169.254 or 127.0.0.1 is refused;\n//\tverified TLS          certificate chain AND hostname, with one loopback-only development\n//\t                      escape;\n//\tdeadlines             a hung upstream fails the call rather than pinning the caller \226\128\148\n//\t                      inside a worker that is the difference between a failed job (which\n//\t                      retries and dead-letters) and a job that never finishes;\n//\ta response-body cap   an unbounded read of a hostile response is a memory DoS.\n//\n// Trace-context propagation (`traceparent`/`tracestate` on every outbound call) is NOT here\n// yet: it needs the telemetry slice, which has not been migrated. Its absence loses trace\n// continuity across services; it takes nothing else away.\ntype HttpResponse struct {\n\tStatus Int\n\tBody   string\n\t// Response headers in name order \226\128\148 one entry per value, so a repeated header\n\t// (`Set-Cookie`) keeps all of its values. Go's header map has no order of its own, so\n\t// sorting is what makes the list deterministic; Racket preserves wire order instead, and\n\t// a test that depends on either ordering is testing the transport, not the program.\n\tHeaders []Tuple2[string, string]\n}\n\n// \226\148\128\226\148\128 Deadlines \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Env vars rather than per-call arguments, matching the Racket runtime: a deadline is\n// deployment tuning of the same kind as the response-body cap, and a per-call timeout would\n// put an operational concern into all four public signatures.\n//\n//\tTESL_HTTP_CONNECT_TIMEOUT_MS   10000   reaching the host\n//\tTESL_HTTP_TIMEOUT_MS           30000   the whole response (status + body)\n//\tTESL_HTTP_MAX_RESPONSE_BYTES   10 MiB  the most a response body may occupy\n\nfunc httpConnectTimeoutMs() int { return envPositiveInt(\"TESL_HTTP_CONNECT_TIMEOUT_MS\", 10000) }\nfunc httpReadTimeoutMs() int    { return envPositiveInt(\"TESL_HTTP_TIMEOUT_MS\", 30000) }\n\nfunc httpMaxResponseBytes() int {\n\treturn envPositiveInt(\"TESL_HTTP_MAX_RESPONSE_BYTES\", 10*1024*1024)\n}\n\n// \226\148\128\226\148\128 Header hygiene \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// HttpHeaderFieldSafe reports whether a header name or value can go on the wire: a CR or LF\n// would end the field and let the rest be read as further headers or a second request.\nfunc HttpHeaderFieldSafe(field string) bool {\n\treturn !strings.ContainsAny(field, \"\\r\\n\")\n}\n\nfunc requireHeaderFieldSafe(what, field string) string {\n\tif !HttpHeaderFieldSafe(field) {\n\t\tpanic(fmt.Sprintf(\n\t\t\t\"HttpClient: outbound %s contains a CR/LF newline \226\128\148 header injection rejected\", what))\n\t}\n\treturn field\n}\n\n// \226\148\128\226\148\128 Secret-carrying headers \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// `HttpClient.bearer k` and `HttpClient.secretHeader \"X-Api-Key\" k` are the sanctioned way\n// a `secret` reaches the wire, replacing the `\"Bearer \" ++ key.value` that `secret` makes\n// impossible. Their Tesl type is `Tuple2 String String`, so the value half IS a Go string \226\128\148\n// which means the plaintext cannot be what it holds, or `Tuple2.second (bearer k)` would\n// hand a program the secret it is not allowed to see.\n//\n// So the value half is a HANDLE: unguessable text that names the plaintext in a\n// process-local table. Every String operation on it sees the handle, printing it shows only\n// \"[redacted]\", and this file is the one place that trades a handle back for the bytes that\n// go on the socket. The handle carries a per-process random nonce because it is otherwise\n// forgeable: a program that could construct the handle text from user input would make the\n// client send OUR secret to an attacker's URL.\nvar (\n\tsecretHeaderNonce = newSecretHeaderNonce()\n\tsecretHeaderStore sync.Map // handle string -> plaintext string\n\tsecretHeaderSeq   int64\n\tsecretHeaderMutex sync.Mutex\n)\n\nfunc newSecretHeaderNonce() string {\n\tbuffer := make([]byte, 16)\n\tif _, err := rand.Read(buffer); err != nil {\n\t\tpanic(\"HttpClient: no source of randomness for secret headers: \" + err.Error())\n\t}\n\treturn hex.EncodeToString(buffer)\n}\n\nfunc secretHeaderHandle(plaintext string) string {\n\tsecretHeaderMutex.Lock()\n\tsecretHeaderSeq++\n\tsequence := secretHeaderSeq\n\tsecretHeaderMutex.Unlock()\n\thandle := SecretRedaction + \"\\x00\" + secretHeaderNonce + \":\" + strconv.FormatInt(sequence, 10)\n\tsecretHeaderStore.Store(handle, plaintext)\n\treturn handle\n}\n\n// revealHeaderValue is THE unwrap point: a secret-header handle becomes plaintext here and\n// nowhere else. Anything that is not a handle is already the value it looks like.\nfunc revealHeaderValue(value string) string {\n\tif !strings.HasPrefix(value, SecretRedaction+\"\\x00\") {\n\t\treturn value\n\t}\n\tif plaintext, found := secretHeaderStore.Load(value); found {\n\t\tif text, ok := plaintext.(string); ok {\n\t\t\treturn text\n\t\t}\n\t}\n\t// A handle shape from another process (or a forgery attempt) names no plaintext here.\n\t// Sending the handle text would leak nothing, but it would also silently authenticate as\n\t// nobody, so it fails instead.\n\tpanic(\"HttpClient: this secret header value does not belong to this process\")\n}\n\n// HttpSecretHeader is `HttpClient.secretHeader name k`.\nfunc HttpSecretHeader(name string, secret SecretString) Tuple2[string, string] {\n\treturn makeSecretHeader(name, secret, \"\")\n}\n\n// HttpBearer is `HttpClient.bearer k`: the Authorization header for a bearer token.\nfunc HttpBearer(secret SecretString) Tuple2[string, string] {\n\treturn makeSecretHeader(\"Authorization\", secret, \"Bearer \")\n}\n\nfunc makeSecretHeader(name string, secret SecretString, prefix string) Tuple2[string, string] {\n\treturn Tuple2[string, string]{\n\t\tTuple2First:  requireHeaderFieldSafe(\"header name\", name),\n\t\tTuple2Second: secretHeaderHandle(prefix + secret.Reveal()),\n\t}\n}\n\n// \226\148\128\226\148\128 The four verbs \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc HttpGet(target string, headers []Tuple2[string, string]) HttpResponse {\n\treturn httpRequest(\"GET\", target, headers, nil)\n}\n\nfunc HttpPost(target string, headers []Tuple2[string, string], body string) HttpResponse {\n\treturn httpRequest(\"POST\", target, headers, &body)\n}\n\nfunc HttpPut(target string, headers []Tuple2[string, string], body string) HttpResponse {\n\treturn httpRequest(\"PUT\", target, headers, &body)\n}\n\nfunc HttpDelete(target string, headers []Tuple2[string, string]) HttpResponse {\n\treturn httpRequest(\"DELETE\", target, headers, nil)\n}\n\nfunc httpRequest(method, target string, headers []Tuple2[string, string], body *string) HttpResponse {\n\tparsed := parseOutboundURL(target)\n\t// The header guard runs BEFORE the test double is consulted, so a stubbed call is still a\n\t// well-formed one and a header-injection bug cannot hide behind a stub.\n\twire := outboundHeaders(headers)\n\tif answer, stubbed := httpStubAnswer(method, target, body); stubbed {\n\t\treturn answer\n\t}\n\treturn httpRequestNetwork(method, target, parsed, wire, body)\n}\n\nfunc parseOutboundURL(target string) *url.URL {\n\tparsed, err := url.Parse(target)\n\tif err != nil {\n\t\tpanic(fmt.Sprintf(\"HttpClient: invalid URL %q: %s\", target, err.Error()))\n\t}\n\tif parsed.Scheme != \"http\" && parsed.Scheme != \"https\" {\n\t\tpanic(fmt.Sprintf(\"HttpClient: invalid URL %q: expected an http or https scheme\", target))\n\t}\n\tif parsed.Hostname() == \"\" {\n\t\tpanic(fmt.Sprintf(\"HttpClient: invalid URL %q: could not parse host\", target))\n\t}\n\treturn parsed\n}\n\n// outboundHeaders checks every field and reveals the secret-carrying ones. A header list is\n// ordered and may repeat a name, so it becomes an http.Header by APPENDING rather than\n// setting \226\128\148 `Set` would drop all but the last of a repeated header.\nfunc outboundHeaders(headers []Tuple2[string, string]) http.Header {\n\twire := http.Header{}\n\tfor _, header := range headers {\n\t\tname := requireHeaderFieldSafe(\"header name\", header.Tuple2First)\n\t\tvalue := requireHeaderFieldSafe(\"header value\", revealHeaderValue(header.Tuple2Second))\n\t\twire[http.CanonicalHeaderKey(name)] = append(wire[http.CanonicalHeaderKey(name)], value)\n\t}\n\treturn wire\n}\n\nfunc httpRequestNetwork(method, target string, parsed *url.URL, wire http.Header,\n\tbody *string) HttpResponse {\n\tvar reader io.Reader\n\tif body != nil {\n\t\treader = strings.NewReader(*body)\n\t}\n\trequest, err := http.NewRequest(method, target, reader)\n\tif err != nil {\n\t\tpanic(fmt.Sprintf(\"HttpClient: HTTP %s to %s failed: %s\", method, target, err.Error()))\n\t}\n\trequest.Header = wire\n\tclient := &http.Client{\n\t\tTransport: outboundTransport(parsed.Hostname()),\n\t\tTimeout:   millisDuration(httpReadTimeoutMs()),\n\t\t// A redirect is a fresh request to an attacker-influenced location, so it gets the\n\t\t// same egress and TLS treatment as the first one \226\128\148 which it does, because the\n\t\t// transport is shared. Nothing else is followed automatically beyond Go's default\n\t\t// limit of 10.\n\t}\n\tresponse, err := client.Do(request)\n\tif err != nil {\n\t\tpanic(requestFailure(method, target, err))\n\t}\n\tif response == nil {\n\t\t// net/http never answers with neither a response nor an error; a transport that did\n\t\t// would make the reads below a nil dereference rather than a failed call.\n\t\tpanic(fmt.Sprintf(\"HttpClient: HTTP %s to %s failed: no response\", method, target))\n\t}\n\tdefer func() { _ = response.Body.Close() }()\n\tlimit := httpMaxResponseBytes()\n\traw, err := io.ReadAll(io.LimitReader(response.Body, int64(limit)+1))\n\tif err != nil {\n\t\tpanic(fmt.Sprintf(\"HttpClient: HTTP %s to %s failed: %s\", method, target, err.Error()))\n\t}\n\tif len(raw) > limit {\n\t\tpanic(fmt.Sprintf(\"HttpClient: response body exceeds the %d-byte cap\", limit))\n\t}\n\treturn HttpResponse{\n\t\tStatus:  FromInt64(int64(response.StatusCode)),\n\t\tBody:    string(raw),\n\t\tHeaders: responseHeaders(response.Header),\n\t}\n}\n\n// requestFailure turns a transport error into the message the Racket client raises for the\n// same condition, because the test double's traps are written to match it byte for byte \226\128\148 a\n// test that asserts on a timeout must describe what production logs.\n//\n// net/http wraps everything in a `Get \"url\": \226\128\166` url.Error, so the reason is unwrapped first;\n// otherwise every failure would read as a repeat of the URL.\nfunc requestFailure(method, target string, err error) string {\n\tmessage := err.Error()\n\tvar urlErr *url.Error\n\tif errors.As(err, &urlErr) {\n\t\tmessage = urlErr.Err.Error()\n\t}\n\t// The egress refusal is raised by the dialer's Control hook, so it arrives buried in a\n\t// dial error. It is a REFUSAL, not a transport fault, and reads as one.\n\tif index := strings.Index(message, \"SSRF: refused egress\"); index >= 0 {\n\t\treturn \"HttpClient: \" + message[index:]\n\t}\n\tif isTimeout(err) {\n\t\t// A deadline that expired during the DIAL is the connect deadline; anything later is\n\t\t// the response deadline. Racket names them separately for the same reason: they are\n\t\t// tuned by different env vars.\n\t\tif dialTimeout(err) {\n\t\t\treturn fmt.Sprintf(\"HttpClient: connect to %s timed out after %dms\",\n\t\t\t\ttarget, httpConnectTimeoutMs())\n\t\t}\n\t\treturn fmt.Sprintf(\"HttpClient: HTTP %s to %s timed out after %dms\",\n\t\t\tmethod, target, httpReadTimeoutMs())\n\t}\n\treturn fmt.Sprintf(\"HttpClient: HTTP %s to %s failed: %s\", method, target, message)\n}\n\nfunc isTimeout(err error) bool {\n\tvar timeout interface{ Timeout() bool }\n\tif errors.As(err, &timeout) && timeout.Timeout() {\n\t\treturn true\n\t}\n\treturn errors.Is(err, context.DeadlineExceeded)\n}\n\nfunc dialTimeout(err error) bool {\n\tvar opErr *net.OpError\n\tif !errors.As(err, &opErr) || opErr == nil {\n\t\treturn false\n\t}\n\treturn opErr.Op == \"dial\" && opErr.Timeout()\n}\n\nfunc responseHeaders(header http.Header) []Tuple2[string, string] {\n\theaders := make([]Tuple2[string, string], 0, len(header))\n\tnames := make([]string, 0, len(header))\n\tfor name := range header {\n\t\tnames = append(names, name)\n\t}\n\tsort.Strings(names)\n\tfor _, name := range names {\n\t\tfor _, value := range header[name] {\n\t\t\theaders = append(headers, Tuple2[string, string]{Tuple2First: name, Tuple2Second: value})\n\t\t}\n\t}\n\treturn headers\n}\n\n// outboundTransport is built per call because both of its policies depend on the target\n// host: the egress judgement is per connection, and the TLS development escape is\n// loopback-only. Connection reuse is given up for that, which costs a handshake on a\n// repeated call and buys a policy that cannot be inherited from an earlier, different one.\nfunc outboundTransport(host string) *http.Transport {\n\tdialer := &net.Dialer{\n\t\tTimeout: millisDuration(httpConnectTimeoutMs()),\n\t\t// Control runs after resolution with the address that is ABOUT to be connected, and\n\t\t// the connect then uses that same address. There is therefore no check-then-rebind\n\t\t// gap: a DNS answer cannot be swapped for an internal address after being judged.\n\t\tControl: func(_network, address string, _conn syscall.RawConn) error {\n\t\t\tpeer, _, err := net.SplitHostPort(address)\n\t\t\tif err != nil {\n\t\t\t\treturn fmt.Errorf(\"SSRF: refused egress to an unparseable peer address %q\", address)\n\t\t\t}\n\t\t\tif reason := SsrfEgressRefusal(peer); reason != \"\" {\n\t\t\t\treturn fmt.Errorf(\"SSRF: refused egress to %s \226\128\148 it resolves to %s, which is %s\",\n\t\t\t\t\thost, peer, reason)\n\t\t\t}\n\t\t\treturn nil\n\t\t},\n\t}\n\treturn &http.Transport{\n\t\tDialContext:       dialer.DialContext,\n\t\tTLSClientConfig:   outboundTLSConfig(host),\n\t\tForceAttemptHTTP2: true,\n\t\tDisableKeepAlives: true,\n\t}\n}\n\n// outboundTLSConfig verifies the peer's certificate chain AND hostname \226\128\148 Go's default, kept\n// explicit here because the one escape below is the only thing allowed to weaken it.\nfunc outboundTLSConfig(host string) *tls.Config {\n\tif tlsInsecureDevEscape(host) {\n\t\t// #nosec G402 -- The single development escape, and the reason it is safe: it engages\n\t\t// only when TESL_HTTP_TLS_INSECURE_DEV is set, only in a build that is not\n\t\t// TESL_DEPLOYED, and only for a loopback host, so the peer is a service on this\n\t\t// machine. It exists so a developer can talk to a local service presenting a\n\t\t// self-signed certificate; for any routable host the branch is not taken.\n\t\treturn &tls.Config{MinVersion: tls.VersionTLS12, InsecureSkipVerify: true}\n\t}\n\treturn &tls.Config{MinVersion: tls.VersionTLS12}\n}\n\nvar (\n\ttlsDevWarnActive  sync.Once\n\ttlsDevWarnRefused sync.Once\n)\n\nfunc tlsInsecureDevEscape(host string) bool {\n\tif envPresent(\"TESL_DEPLOYED\") || !envTruthy(\"TESL_HTTP_TLS_INSECURE_DEV\") {\n\t\treturn false\n\t}\n\tif HostLoopback(host) {\n\t\ttlsDevWarnActive.Do(func() {\n\t\t\tfmt.Fprintln(os.Stderr, \"WARNING: TESL_HTTP_TLS_INSECURE_DEV active -- HTTPS \"+\n\t\t\t\t\"certificate verification is DISABLED for loopback hosts only. \"+\n\t\t\t\t\"Never enable this in production.\")\n\t\t})\n\t\treturn true\n\t}\n\ttlsDevWarnRefused.Do(func() {\n\t\tfmt.Fprintf(os.Stderr, \"WARNING: TESL_HTTP_TLS_INSECURE_DEV is set but host %q is \"+\n\t\t\t\"not loopback -- TLS verification stays ON.\\n\", host)\n\t})\n\treturn false\n}\n");
  ("httpstub.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"strings\"\n\t\"sync\"\n)\n\n// The outbound-HTTP test double.\n//\n// Without it, every handler or worker that talks to an external service has an untestable\n// branch \226\128\148 and the branches most worth covering (an upstream 500, malformed JSON, a timeout)\n// are exactly the ones a real upstream will not produce on demand. A test declares its\n// answers up front:\n//\n//\tstubHttp        \"GET\"  \"https://rates.test/v1\"  200 \"{\\\"usd\\\":110}\"\n//\tstubHttpFailure \"POST\" \"https://rates.test/log\" \"connection refused\"\n//\tstubHttpTimeout \"GET\"  \"https://rates.test/slow\"\n//\n// and asserts afterwards with httpCalled / httpCallCount / httpLastBody.\n//\n// SCOPE. The table is cleared at the top of every emitted test function, so rules and the\n// call log cannot leak from one test block into the next \226\128\148 the isolation the Racket side gets\n// from a parameter that unwinds with the test body. Go's `go test` runs a package's tests\n// sequentially unless a test opts into t.Parallel, which emitted tests never do, so one\n// package-level table is enough and a stub is visible to the goroutines a test starts\n// (a queue worker's outbound call is the case that matters).\n//\n// PRODUCTION. `active` is false until an emitted test clears the table, and only a test file\n// does that, so a production build can never answer a call from here. That is a weaker\n// guarantee than Racket's (where the double lives in a module a production run does not\n// instantiate at all) and it is the strongest one available inside a single Go package.\ntype httpStubRule struct {\n\tmethod  string\n\turl     string\n\tkind    httpStubKind\n\tstatus  int64\n\tpayload string // the canned body, the failure message, or \"\" for a timeout\n}\n\ntype httpStubKind int\n\nconst (\n\thttpStubRespond httpStubKind = iota\n\thttpStubFail\n\thttpStubTimeout\n)\n\ntype httpStubCall struct {\n\tmethod string\n\turl    string\n\tbody   string\n}\n\nvar httpStubs = struct {\n\tmutex  sync.Mutex\n\tactive bool\n\trules  []httpStubRule\n\tcalls  []httpStubCall\n}{}\n\n// ResetHttpStubs gives the current test a fresh, empty stub table. Emitted test functions\n// call it before their first statement.\nfunc ResetHttpStubs() {\n\thttpStubs.mutex.Lock()\n\tdefer httpStubs.mutex.Unlock()\n\thttpStubs.active = true\n\thttpStubs.rules = nil\n\thttpStubs.calls = nil\n}\n\nfunc requireStubScope(who string) {\n\tif !httpStubs.active {\n\t\tpanic(who + \": outbound-HTTP stubs are only available inside a test body.\\n\" +\n\t\t\t\"  Call this from a `test`, `api-test`, or `load-test` block \226\128\148 the stub\\n\" +\n\t\t\t\"  scope is created per test so nothing leaks between them.\")\n\t}\n}\n\n// \226\148\128\226\148\128 Declaring a stub \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// StubHttp declares a canned response. Rules are consulted in DECLARATION order (first\n// match wins), so a specific stub declared before a `\"*\"` catch-all keeps winning;\n// re-declaring the exact same (method, url) pattern REPLACES the earlier rule in place, so a\n// later line in the same test overrides an earlier one rather than being shadowed by it.\nfunc StubHttp(method, target string, status Int, body string) struct{} {\n\tvalue, exact := status.Int64()\n\tif !exact {\n\t\tpanic(\"stubHttp: expected an Int status\")\n\t}\n\treturn addHTTPStubRule(\"stubHttp\", httpStubRule{\n\t\tmethod: method, url: target, kind: httpStubRespond, status: value, payload: body,\n\t})\n}\n\n// StubHttpFailure declares that the call fails the way a refused connection does.\nfunc StubHttpFailure(method, target, message string) struct{} {\n\treturn addHTTPStubRule(\"stubHttpFailure\", httpStubRule{\n\t\tmethod: method, url: target, kind: httpStubFail, payload: message,\n\t})\n}\n\n// StubHttpTimeout declares that the call blows its deadline.\nfunc StubHttpTimeout(method, target string) struct{} {\n\treturn addHTTPStubRule(\"stubHttpTimeout\", httpStubRule{\n\t\tmethod: method, url: target, kind: httpStubTimeout,\n\t})\n}\n\nfunc addHTTPStubRule(who string, rule httpStubRule) struct{} {\n\trequireStubScope(who)\n\thttpStubs.mutex.Lock()\n\tdefer httpStubs.mutex.Unlock()\n\tfor index, existing := range httpStubs.rules {\n\t\tif existing.method == rule.method && existing.url == rule.url {\n\t\t\thttpStubs.rules[index] = rule\n\t\t\treturn struct{}{}\n\t\t}\n\t}\n\thttpStubs.rules = append(httpStubs.rules, rule)\n\treturn struct{}{}\n}\n\n// \226\148\128\226\148\128 Matching \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// httpStubPatternMatches is deliberately NOT a regex: a stub pattern is a test fixture, and\n// `*` plus a trailing-`*` prefix covers every shape the lessons need without putting a second\n// pattern language into the surface. Method matching is case-insensitive; URL matching is not.\nfunc httpStubPatternMatches(pattern, value string, foldCase bool) bool {\n\tif foldCase {\n\t\tpattern = strings.ToLower(pattern)\n\t\tvalue = strings.ToLower(value)\n\t}\n\tswitch {\n\tcase pattern == \"*\":\n\t\treturn true\n\tcase strings.HasSuffix(pattern, \"*\"):\n\t\treturn strings.HasPrefix(value, strings.TrimSuffix(pattern, \"*\"))\n\tdefault:\n\t\treturn pattern == value\n\t}\n}\n\nfunc (rule httpStubRule) matches(method, target string) bool {\n\treturn httpStubPatternMatches(rule.method, method, true) &&\n\t\thttpStubPatternMatches(rule.url, target, false)\n}\n\n// httpStubAnswer is the seam tesl/http-client.rkt reaches through its hook parameter.\n// `false` means no stub is in force, which is only possible when the test declared no stubs\n// at all \226\128\148 so an existing test that really wants the network behaves as before. Once a test\n// declares its first stub, an unmatched call is a loud failure instead of a silent live\n// request.\nfunc httpStubAnswer(method, target string, body *string) (HttpResponse, bool) {\n\thttpStubs.mutex.Lock()\n\tif !httpStubs.active {\n\t\thttpStubs.mutex.Unlock()\n\t\treturn HttpResponse{}, false\n\t}\n\tsent := \"\"\n\tif body != nil {\n\t\tsent = *body\n\t}\n\t// The call is recorded BEFORE it is matched, so `httpCalled` sees a call that then failed\n\t// for want of a stub \226\128\148 which is what a test asserting on the failure needs.\n\thttpStubs.calls = append(httpStubs.calls,\n\t\thttpStubCall{method: strings.ToUpper(method), url: target, body: sent})\n\trules := make([]httpStubRule, len(httpStubs.rules))\n\tcopy(rules, httpStubs.rules)\n\thttpStubs.mutex.Unlock()\n\n\tif len(rules) == 0 {\n\t\treturn HttpResponse{}, false\n\t}\n\tfor _, rule := range rules {\n\t\tif !rule.matches(method, target) {\n\t\t\tcontinue\n\t\t}\n\t\tswitch rule.kind {\n\t\tcase httpStubTimeout:\n\t\t\t// Byte-for-byte the message the real deadline produces, so a test written against\n\t\t\t// the stub matches what production logs.\n\t\t\tpanic(fmt.Sprintf(\"HttpClient: HTTP %s to %s timed out after %dms\",\n\t\t\t\tstrings.ToUpper(method), target, httpReadTimeoutMs()))\n\t\tcase httpStubFail:\n\t\t\tpanic(fmt.Sprintf(\"HttpClient: HTTP %s to %s failed: %s\",\n\t\t\t\tstrings.ToUpper(method), target, rule.payload))\n\t\tcase httpStubRespond:\n\t\t\treturn HttpResponse{\n\t\t\t\tStatus:  FromInt64(rule.status),\n\t\t\t\tBody:    rule.payload,\n\t\t\t\tHeaders: []Tuple2[string, string]{},\n\t\t\t}, true\n\t\t}\n\t}\n\tpanic(fmt.Sprintf(\"HttpClient: no stub matches %s %s\\n\"+\n\t\t\"  This test declared outbound-HTTP stubs, so no call reaches the network.\\n\"+\n\t\t\"  declared: %s\\n\"+\n\t\t\"  hint: add `stubHttp %q %q 200 \\\"\226\128\166\\\"`, or widen a pattern with a trailing *.\",\n\t\tstrings.ToUpper(method), target, describeHTTPStubRules(rules),\n\t\tstrings.ToUpper(method), target))\n}\n\nfunc describeHTTPStubRules(rules []httpStubRule) string {\n\tif len(rules) == 0 {\n\t\treturn \"(none)\"\n\t}\n\tdescribed := make([]string, 0, len(rules))\n\tfor _, rule := range rules {\n\t\tswitch rule.kind {\n\t\tcase httpStubRespond:\n\t\t\tdescribed = append(described,\n\t\t\t\tfmt.Sprintf(\"%s %s -> status %d\", rule.method, rule.url, rule.status))\n\t\tcase httpStubFail:\n\t\t\tdescribed = append(described,\n\t\t\t\tfmt.Sprintf(\"%s %s -> failure %q\", rule.method, rule.url, rule.payload))\n\t\tcase httpStubTimeout:\n\t\t\tdescribed = append(described, fmt.Sprintf(\"%s %s -> timeout\", rule.method, rule.url))\n\t\t}\n\t}\n\treturn strings.Join(described, \"\\n            \")\n}\n\n// \226\148\128\226\148\128 Asserting on the calls made \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc matchingHTTPCalls(who, method, target string) []httpStubCall {\n\trequireStubScope(who)\n\thttpStubs.mutex.Lock()\n\tdefer httpStubs.mutex.Unlock()\n\tmatched := make([]httpStubCall, 0, len(httpStubs.calls))\n\tfor _, call := range httpStubs.calls {\n\t\tif httpStubPatternMatches(method, call.method, true) &&\n\t\t\thttpStubPatternMatches(target, call.url, false) {\n\t\t\tmatched = append(matched, call)\n\t\t}\n\t}\n\treturn matched\n}\n\nfunc HttpCallCount(method, target string) Int {\n\treturn FromInt64(int64(len(matchingHTTPCalls(\"httpCallCount\", method, target))))\n}\n\nfunc HttpCalled(method, target string) bool {\n\treturn len(matchingHTTPCalls(\"httpCalled\", method, target)) > 0\n}\n\n// HttpLastBody is the body of the most recent matching call, or a trap naming the calls that\n// were made \226\128\148 a test asserting on a body it never sent is a broken test, not a false one.\nfunc HttpLastBody(method, target string) string {\n\tmatched := matchingHTTPCalls(\"httpLastBody\", method, target)\n\tif len(matched) == 0 {\n\t\tpanic(fmt.Sprintf(\"httpLastBody: no outbound %s call matched %q\\n  calls made: %s\",\n\t\t\tmethod, target, describeHTTPCallsMade()))\n\t}\n\treturn matched[len(matched)-1].body\n}\n\nfunc describeHTTPCallsMade() string {\n\thttpStubs.mutex.Lock()\n\tdefer httpStubs.mutex.Unlock()\n\tif len(httpStubs.calls) == 0 {\n\t\treturn \"(none)\"\n\t}\n\tdescribed := make([]string, 0, len(httpStubs.calls))\n\tfor _, call := range httpStubs.calls {\n\t\tdescribed = append(described, call.method+\" \"+call.url)\n\t}\n\treturn strings.Join(described, \", \")\n}\n");
  ("int.go", "// Package teslrt contains the small, audited runtime core used by emitted Tesl\n// programs.\npackage teslrt\n\nimport (\n\t\"errors\"\n\t\"hash/fnv\"\n\t\"math\"\n\t\"math/big\"\n)\n\nvar (\n\tErrDivisionByZero = errors.New(\"teslrt.Int: division by zero\")\n\tErrNegativePower  = errors.New(\"teslrt.Int: negative exponent\")\n\tErrInvalidFloat   = errors.New(\"teslrt.Int: NaN and infinity are not integers\")\n\tErrInvalidInt     = errors.New(\"teslrt.Int: invalid decimal integer\")\n)\n\n// Int is Tesl's arbitrary-precision integer. The zero value is zero.\n//\n// big is non-nil exactly when the value is outside the int64 range. noCompare\n// makes raw == and map[Int] compile errors; callers must use Equal and Key.\ntype Int struct {\n\tnoCompare [0]func()\n\tsmall     int64\n\tbig       *big.Int\n}\n\n// IntKey is the canonical, comparable key for an Int.\ntype IntKey string\n\nfunc FromInt64(value int64) Int {\n\treturn Int{small: value}\n}\n\n// ParseDecimal parses an optional sign followed by one or more decimal digits.\nfunc ParseDecimal(value string) (Int, error) {\n\tif !validDecimal(value, true) {\n\t\treturn Int{}, ErrInvalidInt\n\t}\n\tn, ok := new(big.Int).SetString(value, 10)\n\tif !ok {\n\t\treturn Int{}, ErrInvalidInt\n\t}\n\treturn fromBig(n), nil\n}\n\nfunc MustParseDecimal(value string) Int {\n\tn, err := ParseDecimal(value)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\treturn n\n}\n\n// FromFloat64 truncates toward zero, matching Tesl's Int.fromFloat semantics.\nfunc FromFloat64(value float64) (Int, error) {\n\tif math.IsNaN(value) || math.IsInf(value, 0) {\n\t\treturn Int{}, ErrInvalidFloat\n\t}\n\tn, _ := big.NewFloat(value).Int(nil)\n\treturn fromBig(n), nil\n}\n\nfunc fromBig(value *big.Int) Int {\n\tif value == nil {\n\t\treturn Int{}\n\t}\n\tif value.IsInt64() {\n\t\treturn FromInt64(value.Int64())\n\t}\n\treturn Int{big: new(big.Int).Set(value)}\n}\n\n// bigInt always returns a new value. No operation may mutate an Int operand.\nfunc (n Int) bigInt() *big.Int {\n\t_ = n.noCompare\n\tif n.big == nil {\n\t\treturn big.NewInt(n.small)\n\t}\n\treturn new(big.Int).Set(n.big)\n}\n\nfunc validDecimal(value string, allowPlus bool) bool {\n\tif value == \"\" {\n\t\treturn false\n\t}\n\tstart := 0\n\tif value[0] == '-' || (allowPlus && value[0] == '+') {\n\t\tstart = 1\n\t}\n\tif start == len(value) {\n\t\treturn false\n\t}\n\tfor i := start; i < len(value); i++ {\n\t\tif value[i] < '0' || value[i] > '9' {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\nfunc Add(left, right Int) Int {\n\tif left.big == nil && right.big == nil {\n\t\tif (right.small > 0 && left.small <= math.MaxInt64-right.small) ||\n\t\t\t(right.small < 0 && left.small >= math.MinInt64-right.small) ||\n\t\t\tright.small == 0 {\n\t\t\treturn FromInt64(left.small + right.small)\n\t\t}\n\t}\n\treturn fromBig(new(big.Int).Add(left.bigInt(), right.bigInt()))\n}\n\nfunc Sub(left, right Int) Int {\n\tif left.big == nil && right.big == nil {\n\t\tif (right.small > 0 && left.small >= math.MinInt64+right.small) ||\n\t\t\t(right.small < 0 && left.small <= math.MaxInt64+right.small) ||\n\t\t\tright.small == 0 {\n\t\t\treturn FromInt64(left.small - right.small)\n\t\t}\n\t}\n\treturn fromBig(new(big.Int).Sub(left.bigInt(), right.bigInt()))\n}\n\nfunc Mul(left, right Int) Int {\n\tif left.big == nil && right.big == nil {\n\t\tif left.small == 0 || right.small == 0 {\n\t\t\treturn Int{}\n\t\t}\n\t\tif (left.small != math.MinInt64 || right.small != -1) &&\n\t\t\t(right.small != math.MinInt64 || left.small != -1) {\n\t\t\tproduct := left.small * right.small\n\t\t\tif product/right.small == left.small {\n\t\t\t\treturn FromInt64(product)\n\t\t\t}\n\t\t}\n\t}\n\treturn fromBig(new(big.Int).Mul(left.bigInt(), right.bigInt()))\n}\n\nfunc Neg(value Int) Int {\n\tif value.big == nil && value.small != math.MinInt64 {\n\t\treturn FromInt64(-value.small)\n\t}\n\treturn fromBig(new(big.Int).Neg(value.bigInt()))\n}\n\n// IntGcd is Racket's `gcd`: always NON-NEGATIVE, and gcd(0, 0) is 0. The sign of either\n// operand does not reach the answer, which is what makes `gcd a b == gcd (-a) b` hold.\nfunc IntGcd(left, right Int) Int {\n\tfirst := new(big.Int).Abs(left.bigInt())\n\tsecond := new(big.Int).Abs(right.bigInt())\n\treturn fromBig(new(big.Int).GCD(nil, nil, first, second))\n}\n\n// IntLcm is Racket's `lcm`: non-negative, and 0 whenever either operand is 0 (there is no\n// smaller common multiple than 0 in that case, and dividing by gcd(0,0) would trap).\nfunc IntLcm(left, right Int) Int {\n\tfirst := new(big.Int).Abs(left.bigInt())\n\tsecond := new(big.Int).Abs(right.bigInt())\n\tif first.Sign() == 0 || second.Sign() == 0 {\n\t\treturn FromInt64(0)\n\t}\n\tdivisor := new(big.Int).GCD(nil, nil, first, second)\n\treturn fromBig(new(big.Int).Div(new(big.Int).Mul(first, second), divisor))\n}\n\nfunc Abs(value Int) Int {\n\tif value.Sign() >= 0 {\n\t\treturn value\n\t}\n\treturn Neg(value)\n}\n\n// Quo truncates toward zero.\nfunc Quo(dividend, divisor Int) (Int, error) {\n\tif divisor.IsZero() {\n\t\treturn Int{}, ErrDivisionByZero\n\t}\n\tif dividend.big == nil && divisor.big == nil &&\n\t\t(dividend.small != math.MinInt64 || divisor.small != -1) {\n\t\treturn FromInt64(dividend.small / divisor.small), nil\n\t}\n\treturn fromBig(new(big.Int).Quo(dividend.bigInt(), divisor.bigInt())), nil\n}\n\n// MustQuo is used by emitted code after Tesl has proved the divisor non-zero.\n// It still fails closed if compiler/runtime invariants drift.\nfunc MustQuo(dividend, divisor Int) Int {\n\tvalue, err := Quo(dividend, divisor)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\treturn value\n}\n\n// Rem is the truncating remainder. A non-zero result has the dividend's sign.\nfunc Rem(dividend, divisor Int) (Int, error) {\n\tif divisor.IsZero() {\n\t\treturn Int{}, ErrDivisionByZero\n\t}\n\tif dividend.big == nil && divisor.big == nil {\n\t\treturn FromInt64(dividend.small % divisor.small), nil\n\t}\n\treturn fromBig(new(big.Int).Rem(dividend.bigInt(), divisor.bigInt())), nil\n}\n\nfunc MustRem(dividend, divisor Int) Int {\n\tvalue, err := Rem(dividend, divisor)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\treturn value\n}\n\n// Mod matches Racket modulo: a non-zero result has the divisor's sign.\nfunc Mod(dividend, divisor Int) (Int, error) {\n\tremainder, err := Rem(dividend, divisor)\n\tif err != nil || remainder.IsZero() || remainder.Sign() == divisor.Sign() {\n\t\treturn remainder, err\n\t}\n\treturn Add(remainder, divisor), nil\n}\n\nfunc MustMod(dividend, divisor Int) Int {\n\tvalue, err := Mod(dividend, divisor)\n\tif err != nil {\n\t\tpanic(err)\n\t}\n\treturn value\n}\n\nfunc Pow(base, exponent Int) (Int, error) {\n\tif exponent.Sign() < 0 {\n\t\treturn Int{}, ErrNegativePower\n\t}\n\treturn fromBig(new(big.Int).Exp(base.bigInt(), exponent.bigInt(), nil)), nil\n}\n\nfunc GCD(left, right Int) Int {\n\treturn fromBig(new(big.Int).GCD(nil, nil, left.bigInt(), right.bigInt()))\n}\n\nfunc LCM(left, right Int) Int {\n\tif left.IsZero() || right.IsZero() {\n\t\treturn Int{}\n\t}\n\tgcd := GCD(left, right)\n\tquotient, err := Quo(Abs(left), gcd)\n\tif err != nil {\n\t\tpanic(\"unreachable: non-zero gcd\")\n\t}\n\treturn Mul(quotient, Abs(right))\n}\n\nfunc Min(left, right Int) Int {\n\tif Compare(left, right) <= 0 {\n\t\treturn left\n\t}\n\treturn right\n}\n\nfunc Max(left, right Int) Int {\n\tif Compare(left, right) >= 0 {\n\t\treturn left\n\t}\n\treturn right\n}\n\nfunc Clamp(value, low, high Int) Int {\n\treturn Max(low, Min(high, value))\n}\n\nfunc Compare(left, right Int) int {\n\tif left.big == nil && right.big == nil {\n\t\tswitch {\n\t\tcase left.small < right.small:\n\t\t\treturn -1\n\t\tcase left.small > right.small:\n\t\t\treturn 1\n\t\tdefault:\n\t\t\treturn 0\n\t\t}\n\t}\n\treturn left.bigInt().Cmp(right.bigInt())\n}\n\nfunc Equal(left, right Int) bool {\n\treturn Compare(left, right) == 0\n}\n\nfunc (n Int) Sign() int {\n\tif n.big != nil {\n\t\treturn n.big.Sign()\n\t}\n\tswitch {\n\tcase n.small < 0:\n\t\treturn -1\n\tcase n.small > 0:\n\t\treturn 1\n\tdefault:\n\t\treturn 0\n\t}\n}\n\nfunc (n Int) IsZero() bool {\n\treturn n.big == nil && n.small == 0\n}\n\nfunc (n Int) IsPositive() bool {\n\treturn n.Sign() > 0\n}\n\nfunc (n Int) IsNegative() bool {\n\treturn n.Sign() < 0\n}\n\nfunc (n Int) IsEven() bool {\n\tif n.big == nil {\n\t\treturn n.small%2 == 0\n\t}\n\treturn n.big.Bit(0) == 0\n}\n\nfunc (n Int) IsOdd() bool {\n\treturn !n.IsEven()\n}\n\nfunc (n Int) Digits() int {\n\ttext := n.String()\n\tif text[0] == '-' {\n\t\treturn len(text) - 1\n\t}\n\treturn len(text)\n}\n\nfunc (n Int) String() string {\n\tif n.big == nil {\n\t\treturn big.NewInt(n.small).String()\n\t}\n\treturn n.big.String()\n}\n\nfunc (n Int) Int64() (int64, bool) {\n\tif n.big != nil {\n\t\treturn 0, false\n\t}\n\treturn n.small, true\n}\n\nfunc (n Int) Float64() float64 {\n\tif n.big == nil {\n\t\treturn float64(n.small)\n\t}\n\tvalue, _ := new(big.Float).SetInt(n.big).Float64()\n\treturn value\n}\n\nfunc (n Int) Key() IntKey {\n\treturn IntKey(n.String())\n}\n\nfunc (n Int) Hash64() uint64 {\n\thash := fnv.New64a()\n\t_, _ = hash.Write([]byte(n.String()))\n\treturn hash.Sum64()\n}\n\nfunc (n Int) MarshalJSON() ([]byte, error) {\n\treturn []byte(n.String()), nil\n}\n\nfunc (n *Int) UnmarshalJSON(data []byte) error {\n\ttext := string(data)\n\tif !validJSONInteger(text) {\n\t\treturn ErrInvalidInt\n\t}\n\tvalue, err := ParseDecimal(text)\n\tif err != nil {\n\t\treturn err\n\t}\n\t*n = value\n\treturn nil\n}\n\nfunc validJSONInteger(value string) bool {\n\tif !validDecimal(value, false) {\n\t\treturn false\n\t}\n\tstart := 0\n\tif value[0] == '-' {\n\t\tstart = 1\n\t}\n\tif value[start] == '0' && len(value)-start > 1 {\n\t\treturn false\n\t}\n\treturn true\n}\n\n// IntNonZero and IntNonNegative are Tesl's `Int.nonZero` / `Int.nonNegative`\n// checks: the proof they mint erases, the rejection does not. The messages and the\n// 400 status are observable behavior and match the Racket implementations.\nfunc IntNonZero(value Int) Check[Int] {\n\tif value.IsZero() {\n\t\treturn Reject[Int](400, \"expected a non-zero integer\")\n\t}\n\treturn Accept(value)\n}\n\nfunc IntNonNegative(value Int) Check[Int] {\n\tif value.Sign() < 0 {\n\t\treturn Reject[Int](400, \"expected a non-negative integer\")\n\t}\n\treturn Accept(value)\n}\n\n// The Tesl.Int leaves the emitter calls as plain functions. Clamp, Pow, IsEven and IsOdd\n// already exist above; these are the wrappers for what is otherwise only a method, plus\n// the sign and power shapes Tesl's own signatures describe.\n\n// IntSign is -1, 0 or 1, matching Int.sign in tesl/int.rkt.\nfunc IntSign(value Int) Int {\n\treturn FromInt64(int64(value.Sign()))\n}\n\nfunc IntIsEven(value Int) bool {\n\treturn value.IsEven()\n}\n\nfunc IntIsOdd(value Int) bool {\n\treturn value.IsOdd()\n}\n\nfunc IntToString(value Int) string {\n\treturn value.String()\n}\n\n// MustPow raises rather than returning an error, matching Int.pow in tesl/int.rkt, which\n// rejects a negative exponent outright: there is no integer result, and Float.pow is the\n// function for a fractional one.\nfunc MustPow(base, exponent Int) Int {\n\tresult, err := Pow(base, exponent)\n\tif err != nil {\n\t\tpanic(\"Int.pow: \" + err.Error())\n\t}\n\treturn result\n}\n");
  ("int32.go", "package teslrt\n\nimport \"math/big\"\n\n// `Tesl.Int32` \226\128\148 a JS-safe, 32-bit-bounded integer for wire and storage boundaries.\n//\n// `Int32` is NOMINAL at the type level (it does not unify with `Int`) and at run time it IS its\n// underlying integer \226\128\148 `tesl/int32.rkt` says exactly that, and the Go emitter follows by\n// rendering the type as `teslrt.Int`. Nothing here carries a wrapper, so an Int32 crossing into\n// `Int` arithmetic costs nothing and the two backends store the same value.\n//\n// The split that makes the type worth having: an operation that CANNOT leave [-2^31, 2^31)\n// answers the value, and one that CAN answers a `Maybe`. That is a property of the operation,\n// not of the inputs, so it is visible in the signature rather than discovered at run time.\nvar (\n\tint32Low  = FromInt64(-2147483648)\n\tint32High = FromInt64(2147483647)\n)\n\n// Int32InRange is the ONE range decision every operation below funnels through.\nfunc Int32InRange(value Int) bool {\n\treturn Compare(value, int32Low) >= 0 && Compare(value, int32High) <= 0\n}\n\nfunc int32Narrow(value Int) Maybe[Int] {\n\tif Int32InRange(value) {\n\t\treturn Something(value)\n\t}\n\treturn Nothing[Int]()\n}\n\n// Int32MinValue and Int32MaxValue are the bounds, as values rather than calls \226\128\148 that is how\n// `Int32.minValue` reads in Tesl.\nvar (\n\tInt32MinValue = int32Low\n\tInt32MaxValue = int32High\n)\n\n// \226\148\128\226\148\128 Conversions \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc Int32FromInt(value Int) Maybe[Int] { return int32Narrow(value) }\n\n// Int32ToInt is total: widening always fits.\nfunc Int32ToInt(value Int) Int { return value }\n\n// Int32FromIntClamped saturates. Total, and explicit in the name that it CHANGES the value \226\128\148\n// use `Int32.fromInt` where out-of-range has to be visible.\nfunc Int32FromIntClamped(value Int) Int {\n\treturn Max(int32Low, Min(int32High, value))\n}\n\nfunc Int32Parse(text string) Maybe[Int] {\n\tparsed := StringToInt(text)\n\tif !parsed.IsSomething() {\n\t\treturn Nothing[Int]()\n\t}\n\treturn int32Narrow(parsed.SomethingValue)\n}\n\n// Int32FromFloat truncates toward zero, then range-checks. A NaN or an infinity has no integer\n// truncation, so it is Nothing rather than a trap.\nfunc Int32FromFloat(value float64) Maybe[Int] {\n\ttruncated := FloatToInt(value)\n\tif !truncated.IsSomething() {\n\t\treturn Nothing[Int]()\n\t}\n\treturn int32Narrow(truncated.SomethingValue)\n}\n\nfunc Int32ToFloat(value Int) float64 { return value.Float64() }\n\nfunc Int32ToString(value Int) string { return value.String() }\n\n// \226\148\128\226\148\128 Range-closed operations \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc Int32Min(left, right Int) Int { return Min(left, right) }\n\nfunc Int32Max(left, right Int) Int { return Max(left, right) }\n\nfunc Int32Clamp(value, low, high Int) Int { return Max(low, Min(high, value)) }\n\n// Int32Modulo is the remainder, which |b| \226\137\164 2^31 keeps in range. The divisor carries an\n// `IsNonZero` proof, which erases \226\128\148 so the zero guard here is what is left of it, and it traps\n// with the message `tesl/int32.rkt` raises.\nfunc Int32Modulo(dividend, divisor Int) Int {\n\tif divisor.IsZero() {\n\t\tpanic(\"Int32.modulo: division by zero \226\128\148 use `check Int32.nonZero(b)` before calling Int32.modulo\")\n\t}\n\treturn MustRem(dividend, divisor)\n}\n\n// \226\148\128\226\148\128 Operations that can leave the range \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc Int32Add(left, right Int) Maybe[Int] { return int32Narrow(Add(left, right)) }\n\nfunc Int32Subtract(left, right Int) Maybe[Int] { return int32Narrow(Sub(left, right)) }\n\nfunc Int32Multiply(left, right Int) Maybe[Int] { return int32Narrow(Mul(left, right)) }\n\n// Negate and Abs overflow on exactly one input: -2^31 has no positive counterpart.\nfunc Int32Negate(value Int) Maybe[Int] { return int32Narrow(Neg(value)) }\n\nfunc Int32Abs(value Int) Maybe[Int] { return int32Narrow(Abs(value)) }\n\n// Int32Pow answers Nothing for a negative exponent (an integer power would be fractional) and\n// for an out-of-range result. The exponent is bounded BEFORE the power is taken: 2^2000000000\n// cannot fit either way, and computing that bignum first would burn memory to reach the same\n// Nothing. |base| > 1 needs at most 31 doublings to leave the range; 0, 1 and -1 never do.\nfunc Int32Pow(base, exponent Int) Maybe[Int] {\n\tif exponent.Sign() < 0 {\n\t\treturn Nothing[Int]()\n\t}\n\tif Compare(Abs(base), FromInt64(1)) > 0 && Compare(exponent, FromInt64(31)) > 0 {\n\t\treturn Nothing[Int]()\n\t}\n\traised, err := Pow(base, exponent)\n\tif err != nil {\n\t\treturn Nothing[Int]()\n\t}\n\treturn int32Narrow(raised)\n}\n\n// Int32Divide is integer division truncating toward zero. The divisor carries an `IsNonZero`\n// proof; Nothing covers the single overflowing quotient, -2^31 / -1 = 2^31.\nfunc Int32Divide(dividend, divisor Int) Maybe[Int] {\n\tif divisor.IsZero() {\n\t\tpanic(\"Int32.divide: division by zero \226\128\148 use `check Int32.nonZero(b)` before calling Int32.divide\")\n\t}\n\treturn int32Narrow(MustQuo(dividend, divisor))\n}\n\n// \226\148\128\226\148\128 Predicates and queries \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc Int32IsPositive(value Int) bool { return value.IsPositive() }\n\nfunc Int32IsNegative(value Int) bool { return value.IsNegative() }\n\nfunc Int32IsZero(value Int) bool { return value.IsZero() }\n\nfunc Int32IsEven(value Int) bool { return value.IsEven() }\n\nfunc Int32IsOdd(value Int) bool { return value.IsOdd() }\n\n// Int32Sign answers an `Int` rather than an Int32, so it composes with Int arithmetic and\n// compares against Int literals \226\128\148 the shape `Int.sign` has.\nfunc Int32Sign(value Int) Int { return FromInt64(int64(value.Sign())) }\n\n// Int32Digits counts the digits of the magnitude, so zero has one.\nfunc Int32Digits(value Int) Int {\n\treturn FromInt64(int64(len(new(big.Int).Abs(value.bigInt()).String())))\n}\n\n// \226\148\128\226\148\128 Proof-minting checks \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// The proofs erase, so what is left is the predicate and its refusal.\n\nfunc Int32NonZero(value Int) Check[Int] {\n\tif value.IsZero() {\n\t\treturn Reject[Int](400, \"expected a non-zero Int32\")\n\t}\n\treturn Accept(value)\n}\n\nfunc Int32NonNegative(value Int) Check[Int] {\n\tif value.IsNegative() {\n\t\treturn Reject[Int](400, \"expected a non-negative Int32\")\n\t}\n\treturn Accept(value)\n}\n");
  ("json.go", "package teslrt\n\nimport (\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"math/big\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// JSON decoding for codecs.\n//\n// The one non-obvious requirement is INTEGER PRECISION. Tesl's Int is arbitrary\n// precision and Racket's JSON reader returns exact integers, so `{\"n\": 1234567890123\n// 45678901234567890}` round-trips there. Go's encoding/json decodes a number into\n// float64 when the target is `any`, which would silently round that to\n// 1.2345678901234568e+29 \226\128\148 wrong, and only for values above 2^53, so small tests would\n// never catch it. Every parse therefore goes through UseNumber and integers are read\n// from the decimal digits.\n//\n// Object key ORDER on the way out is alphabetical, matching Racket's `jsexpr->string`\n// (verified: it sorts). Go's own map marshalling sorts too, but a struct would emit in\n// field-declaration order, so encoding goes through a map rather than a generated\n// struct \226\128\148 the response bytes are observable and the two backends must agree.\n\n// ParseJSON parses a request body. A parse failure is the caller's to report, matching\n// the \"Malformed JSON payload\" 400 the Racket server produces.\nfunc ParseJSON(data []byte) (any, error) {\n\tdecoder := json.NewDecoder(strings.NewReader(string(data)))\n\tdecoder.UseNumber()\n\tvar value any\n\tif err := decoder.Decode(&value); err != nil {\n\t\treturn nil, err\n\t}\n\treturn value, nil\n}\n\n// jsonField returns the raw value at key. A missing field is an error rather than a\n// zero value: Racket raises `codec: required field \"x\" not found in JSON` here, and the\n// server turns any decode exception into a generic 400 \226\128\148 so the TEXT is not observable\n// by default, but the failure must still happen.\nfunc jsonField(object any, key string) (any, error) {\n\tfields, ok := object.(map[string]any)\n\tif !ok {\n\t\treturn nil, fmt.Errorf(\"codec: expected a JSON object\")\n\t}\n\tvalue, present := fields[key]\n\tif !present {\n\t\treturn nil, fmt.Errorf(\"codec: required field %q not found in JSON\", key)\n\t}\n\treturn value, nil\n}\n\n// HasJSONField reports whether a key is present, for an alternative that only applies\n// when a field exists.\nfunc HasJSONField(object any, key string) bool {\n\tfields, ok := object.(map[string]any)\n\tif !ok {\n\t\treturn false\n\t}\n\t_, present := fields[key]\n\treturn present\n}\n\nfunc DecodeStringField(object any, key string) (string, error) {\n\traw, err := jsonField(object, key)\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\ttext, ok := raw.(string)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"expected JSON string, got %v\", raw)\n\t}\n\treturn text, nil\n}\n\n// DecodeIntField reads the decimal digits rather than a float64, so an integer wider\n// than 2^53 survives.\nfunc DecodeIntField(object any, key string) (Int, error) {\n\traw, err := jsonField(object, key)\n\tif err != nil {\n\t\treturn Int{}, err\n\t}\n\tnumber, ok := raw.(json.Number)\n\tif !ok {\n\t\treturn Int{}, fmt.Errorf(\"expected JSON integer, got %v\", raw)\n\t}\n\tdigits := number.String()\n\tvalue, ok := new(big.Int).SetString(digits, 10)\n\tif !ok {\n\t\treturn Int{}, fmt.Errorf(\"expected JSON integer, got %v\", digits)\n\t}\n\treturn fromBig(value), nil\n}\n\nfunc DecodeBoolField(object any, key string) (bool, error) {\n\traw, err := jsonField(object, key)\n\tif err != nil {\n\t\treturn false, err\n\t}\n\tflag, ok := raw.(bool)\n\tif !ok {\n\t\treturn false, fmt.Errorf(\"expected JSON boolean, got %v\", raw)\n\t}\n\treturn flag, nil\n}\n\nfunc DecodeFloatField(object any, key string) (float64, error) {\n\traw, err := jsonField(object, key)\n\tif err != nil {\n\t\treturn 0, err\n\t}\n\tnumber, ok := raw.(json.Number)\n\tif !ok {\n\t\treturn 0, fmt.Errorf(\"expected JSON number, got %v\", raw)\n\t}\n\treturn number.Float64()\n}\n\n// DecodeStringValue decodes an already-extracted value, for a nested codec whose field\n// was read by the caller (an `adtJson` type decodes from a bare JSON string).\nfunc DecodeStringValue(raw any) (string, error) {\n\ttext, ok := raw.(string)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"expected JSON string, got %v\", raw)\n\t}\n\treturn text, nil\n}\n\nfunc JSONFieldValue(object any, key string) (any, error) {\n\treturn jsonField(object, key)\n}\n\n// jsonTypeName names a parsed JSON shape for an error message. It lives HERE rather than beside\n// the api-test assertions because `json.go` ships with every program while the api-test half\n// does not, and the ADT tag decoder below needs it.\nfunc jsonTypeName(raw any) string {\n\tswitch raw.(type) {\n\tcase nil:\n\t\treturn \"null\"\n\tcase json.Number:\n\t\treturn \"number\"\n\tcase string:\n\t\treturn \"string\"\n\tcase bool:\n\t\treturn \"boolean\"\n\tcase []any:\n\t\treturn \"array\"\n\tcase map[string]any:\n\t\treturn \"object\"\n\tdefault:\n\t\treturn fmt.Sprintf(\"%T\", raw)\n\t}\n}\n\n// DecodeAdtTag reads the constructor name an `adtJson` codec decodes from. Racket's generated\n// decoder accepts BOTH shapes \226\128\148 `{\"tag\": \"Ctor\"}`, which is what its encoder writes, and a bare\n// \"Ctor\" string, which is what an Elm or hand-written client may send \226\128\148 so this does too.\nfunc DecodeAdtTag(raw any) (string, error) {\n\tswitch typed := raw.(type) {\n\tcase string:\n\t\treturn typed, nil\n\tcase map[string]any:\n\t\tfound, present := typed[\"tag\"]\n\t\tif !present {\n\t\t\treturn \"\", fmt.Errorf(\"expected {\\\"tag\\\": ...} or a string, got an object with no tag\")\n\t\t}\n\t\tname, ok := found.(string)\n\t\tif !ok {\n\t\t\treturn \"\", fmt.Errorf(\"expected a string tag, got %s\", jsonTypeName(found))\n\t\t}\n\t\treturn name, nil\n\tdefault:\n\t\treturn \"\", fmt.Errorf(\"expected {\\\"tag\\\": ...} or a string, got %s\", jsonTypeName(raw))\n\t}\n}\n\n// EncodeJSON renders an object. Keys are emitted in sorted order to match Racket, which\n// is why this takes a map rather than marshalling a generated struct.\nfunc EncodeJSON(fields map[string]any) string {\n\tvar builder strings.Builder\n\tbuilder.WriteByte('{')\n\tkeys := make([]string, 0, len(fields))\n\tfor key := range fields {\n\t\tkeys = append(keys, key)\n\t}\n\tsort.Strings(keys)\n\tfor index, key := range keys {\n\t\tif index > 0 {\n\t\t\tbuilder.WriteByte(',')\n\t\t}\n\t\tname, _ := json.Marshal(key)\n\t\tbuilder.Write(name)\n\t\tbuilder.WriteByte(':')\n\t\tbuilder.WriteString(EncodeJSONValue(fields[key]))\n\t}\n\tbuilder.WriteByte('}')\n\treturn builder.String()\n}\n\n// EncodeJSONValue renders one value. `Int` is rendered from its decimal digits so a\n// bignum is not routed through a float, and a nested object keeps the sorted-key rule.\nfunc EncodeJSONValue(value any) string {\n\tswitch typed := value.(type) {\n\tcase Int:\n\t\treturn typed.String()\n\tcase map[string]any:\n\t\treturn EncodeJSON(typed)\n\tcase []any:\n\t\tparts := make([]string, len(typed))\n\t\tfor index, element := range typed {\n\t\t\tparts[index] = EncodeJSONValue(element)\n\t\t}\n\t\treturn \"[\" + strings.Join(parts, \",\") + \"]\"\n\tcase float64:\n\t\treturn FormatFloat(typed)\n\tdefault:\n\t\tencoded, err := json.Marshal(typed)\n\t\tif err != nil {\n\t\t\tpanic(\"codec: value cannot be encoded as JSON\")\n\t\t}\n\t\treturn string(encoded)\n\t}\n}\n\n// DecodeObjectShape is the shape check a DERIVED record decoder runs before reading any\n// field: a record with no `codec` block still decodes from JSON, and the rule it follows is\n// the Racket runtime's generic decode (dsl/types.rkt `jsexpr->typed-value`) \226\128\148 the object's\n// keys must be EXACTLY the record's fields. A missing field is a 400 because the field has no\n// value to take, and an EXTRA field is a 400 too, which is the surprising half: silently\n// ignoring unknown keys is how a typo'd field name becomes a silent default.\nfunc DecodeObjectShape(raw any, typeName string, expected []string) (map[string]any, error) {\n\tfields, ok := raw.(map[string]any)\n\tif !ok {\n\t\treturn nil, fmt.Errorf(\"expected record JSON for type %s, got %v\", typeName, raw)\n\t}\n\tmissing := make([]string, 0, len(expected))\n\tfor _, name := range expected {\n\t\tif _, present := fields[name]; !present {\n\t\t\tmissing = append(missing, name)\n\t\t}\n\t}\n\tif len(missing) > 0 {\n\t\treturn nil, fmt.Errorf(\"record JSON for type %s is missing field%s (%s)\",\n\t\t\ttypeName, plural(len(missing)), strings.Join(missing, \" \"))\n\t}\n\textra := make([]string, 0, len(fields))\n\tfor name := range fields {\n\t\tif !containsName(expected, name) {\n\t\t\textra = append(extra, name)\n\t\t}\n\t}\n\tif len(extra) > 0 {\n\t\tsort.Strings(extra)\n\t\treturn nil, fmt.Errorf(\"record JSON for type %s has unexpected field%s (%s)\",\n\t\t\ttypeName, plural(len(extra)), strings.Join(extra, \" \"))\n\t}\n\treturn fields, nil\n}\n\nfunc plural(count int) string {\n\tif count == 1 {\n\t\treturn \"\"\n\t}\n\treturn \"s\"\n}\n\nfunc containsName(names []string, wanted string) bool {\n\tfor _, name := range names {\n\t\tif name == wanted {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n\n// The value-level counterparts of the DecodeXField helpers, for a derived decoder that has\n// already taken the object apart (and for a list element, which has no key at all).\n\nfunc DecodeIntValue(raw any) (Int, error) {\n\tnumber, ok := raw.(json.Number)\n\tif !ok {\n\t\treturn Int{}, fmt.Errorf(\"expected JSON integer, got %v\", raw)\n\t}\n\tvalue, ok := new(big.Int).SetString(number.String(), 10)\n\tif !ok {\n\t\treturn Int{}, fmt.Errorf(\"expected JSON integer, got %v\", number.String())\n\t}\n\treturn fromBig(value), nil\n}\n\nfunc DecodeBoolValue(raw any) (bool, error) {\n\tflag, ok := raw.(bool)\n\tif !ok {\n\t\treturn false, fmt.Errorf(\"expected JSON boolean, got %v\", raw)\n\t}\n\treturn flag, nil\n}\n\nfunc DecodeFloatValue(raw any) (float64, error) {\n\tnumber, ok := raw.(json.Number)\n\tif !ok {\n\t\treturn 0, fmt.Errorf(\"expected JSON number, got %v\", raw)\n\t}\n\treturn number.Float64()\n}\n\n// DecodeListValue decodes a JSON array element-wise through `element`, so a derived decoder\n// composes over `List T` without the emitter writing the loop at every use.\nfunc DecodeListValue[Element any](raw any, element func(any) (Element, error)) ([]Element, error) {\n\titems, ok := raw.([]any)\n\tif !ok {\n\t\treturn nil, fmt.Errorf(\"expected JSON array, got %v\", raw)\n\t}\n\tout := make([]Element, 0, len(items))\n\tfor _, item := range items {\n\t\tdecoded, err := element(item)\n\t\tif err != nil {\n\t\t\treturn nil, err\n\t\t}\n\t\tout = append(out, decoded)\n\t}\n\treturn out, nil\n}\n");
  ("jwt.go", "package teslrt\n\nimport (\n\t\"crypto/hmac\"\n\t\"encoding/base64\"\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"sort\"\n\t\"strings\"\n\t\"sync\"\n\t\"time\"\n)\n\n// `Tesl.JWT`: the one blessed session token \226\128\148 HS256, in one fixed cookie, with no options.\n//\n// The signature is HMAC-SHA256 over `header.payload`, which is `Tesl.Crypto`'s primitive, so a\n// token minted by either backend verifies on the other. Verification never PARSES the header:\n// it recomputes the MAC over the received bytes verbatim, which is why a foreign token, or one\n// minted before `kid` was stamped, verifies exactly the same.\n//\n// The rules below are the Racket runtime's, and several of them are fail-closed decisions worth\n// keeping visible rather than re-deriving:\n//\n//\ta structurally malformed token is a 401, never a trap \226\128\148 it comes off the wire, so\n//\t`Cookie: session=garbage` must not be a client-triggerable 500;\n//\tan unreadable `exp` means EXPIRED, not absent \226\128\148 skipping the check would accept such a\n//\ttoken forever;\n//\ta missing `exp` is accepted, because `exp` is OPTIONAL per RFC 7519 and Tesl cannot mint a\n//\ttoken without one, so this only admits a foreign token;\n//\trenewal refuses without a usable `iat`, because an unbounded-age token must not be\n//\trenewable \226\128\148 that bound is what makes \"no server-side revocation\" a defensible design.\ntype JwtToken struct {\n\tValue string\n}\n\nconst (\n\t// The renewable window and the hard stop on a renewed session's total lifetime. The cap is\n\t// named rather than derived: applying a multiplier to a shorter TTL silently yields a cap\n\t// nobody chose.\n\tjwtTTLSeconds         = 3600\n\tjwtAbsoluteMaxSeconds = 12 * jwtTTLSeconds\n\t// The only tolerance granted to an `iat` ahead of this machine's clock: enough for ordinary\n\t// NTP drift between replicas, too small to meaningfully extend a session.\n\tjwtMaxClockSkewSeconds = 60\n)\n\n// The OPTIONAL previous signing key. Verification accepts a token signed by either the current\n// key or this one, which is the overlap that lets a leaked key be rotated WITHOUT logging every\n// user out; signing and renewal always use the current key, so the slot drains on its own.\nvar (\n\tpreviousSessionKeyMutex sync.RWMutex\n\tpreviousSessionKey      *Secret\n)\n\n// SetPreviousSessionKey installs (or, with nil, clears) the rotation overlap. Clearing it while\n// rotating the current key is the global kill switch.\nfunc SetPreviousSessionKey(key *Secret) {\n\tpreviousSessionKeyMutex.Lock()\n\tdefer previousSessionKeyMutex.Unlock()\n\tpreviousSessionKey = key\n}\n\nfunc sessionKeys(current Secret) []string {\n\tpreviousSessionKeyMutex.RLock()\n\tdefer previousSessionKeyMutex.RUnlock()\n\tkeys := []string{current.Value.Reveal()}\n\tif previousSessionKey != nil {\n\t\tkeys = append(keys, previousSessionKey.Value.Reveal())\n\t}\n\treturn keys\n}\n\nfunc jwtNowSeconds() int64 { return time.Now().Unix() }\n\nfunc base64URL(raw []byte) string { return base64.RawURLEncoding.EncodeToString(raw) }\n\n// decodeBase64URL accepts the padded spelling too: a token arrives from another implementation,\n// and padding is the commonest harmless deviation.\nfunc decodeBase64URL(text string) ([]byte, bool) {\n\tif raw, err := base64.RawURLEncoding.DecodeString(text); err == nil {\n\t\treturn raw, true\n\t}\n\traw, err := base64.URLEncoding.DecodeString(text)\n\treturn raw, err == nil\n}\n\n// jwtHeader is assembled by string append rather than by marshalling a map, so the field order\n// is fixed (alg, typ, kid) and stable. `kid` is DERIVED from the key \226\128\148 a domain-separated,\n// truncated digest that is safe to log and is not proof of key possession \226\128\148 so there is no knob\n// here either, and no accessor: stamping is the part that is expensive to retrofit.\nfunc jwtHeader(key Secret) string {\n\treturn base64URL([]byte(`{\"alg\":\"HS256\",\"typ\":\"JWT\",\"kid\":\"` + KeyFingerprint(key) + `\"}`))\n}\n\nfunc jwtSignature(key, signingInput string) []byte {\n\treturn hmacSha256Bytes(key, signingInput)\n}\n\n// jwtPayload renders the claims. Keys are sorted so a token is a function of its claims and not\n// of map iteration order; `iat` and `exp` are NUMBERS, as RFC 7519 requires, while every claim a\n// program supplies is a String.\nfunc jwtPayload(claims Dict[string, string], issuedAt, expiresAt int64) string {\n\tvar builder strings.Builder\n\tbuilder.WriteString(\"{\")\n\tnames := make([]string, 0, len(claims.Entries))\n\tfor _, entry := range claims.Entries {\n\t\tnames = append(names, entry.Key)\n\t}\n\tsort.Strings(names)\n\tfor _, name := range names {\n\t\tvalue, _ := DictLookup(claims, name, stringKeyLess).Value()\n\t\tencoded, err := json.Marshal(value)\n\t\tif err != nil {\n\t\t\tpanic(\"JWT.sign: a claim value could not be encoded: \" + err.Error())\n\t\t}\n\t\tkey, err := json.Marshal(name)\n\t\tif err != nil {\n\t\t\tpanic(\"JWT.sign: a claim name could not be encoded: \" + err.Error())\n\t\t}\n\t\tbuilder.Write(key)\n\t\tbuilder.WriteString(\":\")\n\t\tbuilder.Write(encoded)\n\t\tbuilder.WriteString(\",\")\n\t}\n\tfmt.Fprintf(&builder, `\"iat\":%d,\"exp\":%d}`, issuedAt, expiresAt)\n\treturn base64URL([]byte(builder.String()))\n}\n\n// JwtSign is `JWT.sign`. It stamps BOTH `iat` and `exp` itself: `exp` because a caller who can\n// choose an expiry can choose ten years, and `iat` because renewal reads it to bound the total\n// lifetime of a session.\nfunc JwtSign(claims Dict[string, string], key Secret) JwtToken {\n\tfor _, reserved := range []string{\"exp\", \"iat\"} {\n\t\tif _, present := DictLookup(claims, reserved, stringKeyLess).Value(); present {\n\t\t\tpanic(fmt.Sprintf(\n\t\t\t\t\"JWT.sign: %s is not yours to set: JWT.sign stamps it itself, and there is no \"+\n\t\t\t\t\t\"parameter for it. Remove %s from the claims. If you need a credential that \"+\n\t\t\t\t\t\"outlives a session, a JWT is the wrong tool: use `Crypto.randomToken` and \"+\n\t\t\t\t\t\"store only its `Crypto.fingerprint`, which you can revoke.\", reserved, reserved))\n\t\t}\n\t}\n\tnow := jwtNowSeconds()\n\treturn signClaims(claims, key, now, now+jwtTTLSeconds)\n}\n\nfunc signClaims(claims Dict[string, string], key Secret, issuedAt, expiresAt int64) JwtToken {\n\tsigningInput := jwtHeader(key) + \".\" + jwtPayload(claims, issuedAt, expiresAt)\n\treturn JwtToken{\n\t\tValue: signingInput + \".\" + base64URL(jwtSignature(key.Value.Reveal(), signingInput)),\n\t}\n}\n\n// verifiedClaims is the parsed payload of a token whose signature checked out.\ntype verifiedClaims struct {\n\traw map[string]any\n}\n\n// verifyToken is the shared half of `JWT.verify` and `JWT.renew`, so the signature comparison,\n// the malformed-token rejections and the expiry rule cannot drift between verifying and\n// renewing.\nfunc verifyToken(token JwtToken, key Secret) (verifiedClaims, Check[Dict[string, string]]) {\n\tparts := strings.Split(token.Value, \".\")\n\tif len(parts) != 3 {\n\t\treturn verifiedClaims{}, Reject[Dict[string, string]](401, \"Invalid JWT format\")\n\t}\n\tsigningInput := parts[0] + \".\" + parts[1]\n\t// A signature segment that is not valid base64url falls through to the comparison with an\n\t// empty tag (which fails) rather than raising out of the handler.\n\tactual, _ := decodeBase64URL(parts[2])\n\tmatched := false\n\tfor _, candidate := range sessionKeys(key) {\n\t\t// Every candidate is compared in constant time; the NUMBER of keys (one or two) is not\n\t\t// secret.\n\t\tif hmac.Equal(jwtSignature(candidate, signingInput), actual) {\n\t\t\tmatched = true\n\t\t}\n\t}\n\tif !matched {\n\t\treturn verifiedClaims{}, Reject[Dict[string, string]](401, \"Invalid JWT signature\")\n\t}\n\tpayload, ok := decodeBase64URL(parts[1])\n\tif !ok {\n\t\treturn verifiedClaims{}, Reject[Dict[string, string]](401, \"Malformed JWT payload\")\n\t}\n\tdecoded, err := ParseJSON(payload)\n\tif err != nil {\n\t\treturn verifiedClaims{}, Reject[Dict[string, string]](401, \"Malformed JWT payload\")\n\t}\n\tclaims, isObject := decoded.(map[string]any)\n\tif !isObject {\n\t\treturn verifiedClaims{}, Reject[Dict[string, string]](401, \"Malformed JWT payload\")\n\t}\n\tif jwtExpired(claims) {\n\t\treturn verifiedClaims{}, Reject[Dict[string, string]](401, \"JWT token has expired\")\n\t}\n\treturn verifiedClaims{raw: claims}, Check[Dict[string, string]]{}\n}\n\n// jwtExpired reads `exp`. A MISSING claim means no expiry check (it is optional per the RFC and\n// Tesl always stamps one, so this only admits a foreign token); an UNREADABLE one means expired,\n// because skipping the check on a claim that cannot be read would accept the token forever.\nfunc jwtExpired(claims map[string]any) bool {\n\texpiry, present := claims[\"exp\"]\n\tif !present {\n\t\treturn false\n\t}\n\tseconds, ok := jsonSeconds(expiry)\n\tif !ok {\n\t\treturn true\n\t}\n\treturn seconds < jwtNowSeconds()\n}\n\nfunc jsonSeconds(value any) (int64, bool) {\n\tnumber, ok := value.(json.Number)\n\tif !ok {\n\t\treturn 0, false\n\t}\n\tif exact, err := number.Int64(); err == nil {\n\t\treturn exact, true\n\t}\n\t// A non-integral or oversized numeric claim: read it as a float and floor it, so a token\n\t// written by an implementation that emits `1.7853556e9` is still judged rather than refused.\n\tif approximate, err := number.Float64(); err == nil {\n\t\treturn int64(approximate), true\n\t}\n\treturn 0, false\n}\n\n// claimText renders a claim value as a String, which is what `Dict String String` promises. The\n// rule is `JWT.decode`'s, applied to verification too: a string is itself, a number or boolean\n// is its JSON text, null is \"null\", and an array or object is its compact JSON text.\n//\n// A note for authorization: an array claim decodes to JSON TEXT, which IS substring-matchable \226\128\148\n// do not branch on `String.contains` over it.\nfunc claimText(value any) string {\n\tswitch typed := value.(type) {\n\tcase nil:\n\t\treturn \"null\"\n\tcase string:\n\t\treturn typed\n\tcase bool:\n\t\tif typed {\n\t\t\treturn \"true\"\n\t\t}\n\t\treturn \"false\"\n\tcase json.Number:\n\t\treturn typed.String()\n\tdefault:\n\t\tencoded, err := json.Marshal(typed)\n\t\tif err != nil {\n\t\t\treturn fmt.Sprint(typed)\n\t\t}\n\t\treturn string(encoded)\n\t}\n}\n\nfunc claimsDict(claims map[string]any) Dict[string, string] {\n\tout := DictEmpty[string, string]()\n\tfor name, value := range claims {\n\t\tout = DictInsert(out, name, claimText(value), stringKeyLess)\n\t}\n\treturn out\n}\n\n// JwtVerify is `JWT.verify`: the claims of a token whose signature and expiry check out, or a\n// 401. On success the claims carry an `Authentic` fact, which erases \226\128\148 what it buys is that\n// \"trusted a cookie without verifying it\" stops compiling.\nfunc JwtVerify(token JwtToken, key Secret) Check[Dict[string, string]] {\n\tverified, rejection := verifyToken(token, key)\n\tif verified.raw == nil {\n\t\treturn rejection\n\t}\n\treturn Accept(claimsDict(verified.raw))\n}\n\n// JwtDecode is `JWT.decode`: the claims WITHOUT verifying anything. It exists for reading a\n// token you have not authenticated (an `iss` to decide which key to check it with, say), and\n// its result must never be trusted \226\128\148 which is why it mints no fact.\nfunc JwtDecode(token JwtToken) Dict[string, string] {\n\tparts := strings.Split(token.Value, \".\")\n\tif len(parts) != 3 {\n\t\tpanic(\"JWT.decode: malformed JWT: expected three dot-separated segments\")\n\t}\n\tpayload, ok := decodeBase64URL(parts[1])\n\tif !ok {\n\t\tpanic(\"JWT.decode: malformed JWT payload: not base64url\")\n\t}\n\tdecoded, err := ParseJSON(payload)\n\tif err != nil {\n\t\tpanic(\"JWT.decode: malformed JWT payload: \" + err.Error())\n\t}\n\tclaims, isObject := decoded.(map[string]any)\n\tif !isObject {\n\t\tpanic(\"JWT.decode: malformed JWT payload: the claims must be a JSON object\")\n\t}\n\treturn claimsDict(claims)\n}\n\n// JwtRenew is `JWT.renew`: it slides the session window forward, preserving every claim and the\n// ORIGINAL `iat`, so an active user is never logged out mid-task while an idle one still expires.\n//\n// It exists as a function because re-signing by hand is a trap: the verified claims contain\n// `exp` and `iat`, `JWT.sign` refuses both, and an author who rebuilds the dict to strip them\n// silently drops any claim they forget \226\128\148 downgrading the session on every renewal.\n//\n// It REFUSES when the token carries no usable `iat` (absent, not an exact non-negative integer,\n// or dated in the future beyond the skew allowance), or when the session has lived its absolute\n// maximum. Renewal is presented WITH the token, so a captured token can be renewed exactly as a\n// legitimate one can; the hard stop is what keeps \"a captured token is useful for a bounded\n// time\" true in a design with no server-side revocation.\nfunc JwtRenew(token JwtToken, key Secret) Check[JwtToken] {\n\tverified, rejection := verifyToken(token, key)\n\tif verified.raw == nil {\n\t\treturn Reject[JwtToken](rejection.Status(), rejection.Message())\n\t}\n\tnow := jwtNowSeconds()\n\tissuedAt, usable := jsonSeconds(verified.raw[\"iat\"])\n\tif !usable || issuedAt < 0 || issuedAt > now+jwtMaxClockSkewSeconds {\n\t\treturn Reject[JwtToken](401, \"Session cannot be renewed\")\n\t}\n\tif now-issuedAt > jwtAbsoluteMaxSeconds {\n\t\treturn Reject[JwtToken](401, \"Session has reached its maximum lifetime\")\n\t}\n\t// Every claim travels across except the two this function owns.\n\tcarried := DictEmpty[string, string]()\n\tfor name, value := range verified.raw {\n\t\tif name == \"exp\" || name == \"iat\" {\n\t\t\tcontinue\n\t\t}\n\t\tcarried = DictInsert(carried, name, claimText(value), stringKeyLess)\n\t}\n\treturn Accept(signClaims(carried, key, issuedAt, now+jwtTTLSeconds))\n}\n\n// JwtTokenText is the token as it travels \226\128\148 what a cookie writer puts on the wire.\nfunc JwtTokenText(token JwtToken) string { return token.Value }\n");
  ("list.go", "package teslrt\n\nimport (\n\t\"slices\"\n\t\"strings\"\n)\n\n// Tesl's `List a` is a Go slice, which buys idiomatic, readable emitted code. A Tesl\n// list is immutable, and nothing in a Tesl program can mutate one, so aliasing is\n// only ever observable if a function in THIS FILE writes into a backing array some\n// other Tesl value can still see. That gives the one invariant this file must never\n// break:\n//\n//\tONLY WRITERS ALLOCATE. READERS MAY ALIAS.\n//\n// So Take/Drop/Tail return plain sub-slice views in O(1), while the writers \226\128\148\n// anything append-like, plus SortBy, which sorts in place \226\128\148 build a fresh array\n// first. The combination that would be a soundness bug is a zero-copy view PLUS a\n// capacity-reusing append: `append(xs[:2], v)` writes index 2, which a longer live\n// value still sees. Since both halves live here, forbidding capacity reuse in the\n// writers is sufficient, and copying in the readers would be redundant.\n//\n// Any list writer added later must honour the same rule.\n//\n// Views are BOUNDED, because a slice keeps its whole backing array alive: a\n// `List.take 1` of a 10,000-row query result would otherwise pin all 10,000 rows for\n// as long as that one-element value lives. See boundedView.\n//\n// Remaining consequence, accepted deliberately: this is an invariant of the runtime,\n// not of the emitted module code. A Go author who sheds Tesl and appends to a Take\n// result reintroduces the classic aliasing bug \226\128\148 out of scope by design, rather than\n// paid for with O(n) on every read.\n//\n// Functions needing equality or ordering take it as a parameter. A generic Go\n// function cannot compare a `T` \226\128\148 `teslrt.Int` is deliberately non-comparable \226\128\148 but\n// the EMITTER knows the concrete element type at every call site and passes the\n// comparison in.\nfunc ListLength[T any](xs []T) Int {\n\treturn FromInt64(int64(len(xs)))\n}\n\nfunc ListIsEmpty[T any](xs []T) bool {\n\treturn len(xs) == 0\n}\n\n// ListHead is Nothing for an empty list rather than a panic: Tesl's `List.head`\n// returns `Maybe a`.\nfunc ListHead[T any](xs []T) Maybe[T] {\n\tif len(xs) == 0 {\n\t\treturn Nothing[T]()\n\t}\n\treturn Something(xs[0])\n}\n\n// ListTail drops the first element, or Nothing when there is none. The result is a\n// (bounded) view: no writer can reach the shared array \226\128\148 see the invariant above.\nfunc ListTail[T any](xs []T) Maybe[[]T] {\n\tif len(xs) == 0 {\n\t\treturn Nothing[[]T]()\n\t}\n\treturn Something(boundedView(xs[1:], xs))\n}\n\nfunc ListLast[T any](xs []T) Maybe[T] {\n\tif len(xs) == 0 {\n\t\treturn Nothing[T]()\n\t}\n\treturn Something(xs[len(xs)-1])\n}\n\n// ListAppend is a WRITER, so it allocates: appending into either input's spare\n// capacity is exactly the mutation the invariant above forbids.\nfunc ListAppend[T any](left, right []T) []T {\n\tout := make([]T, 0, len(left)+len(right))\n\tout = append(out, left...)\n\tout = append(out, right...)\n\treturn out\n}\n\n// ListTake and ListDrop are READERS: they return bounded views, clamping the count to\n// the list length. A negative count is a programming error the Racket backend rejects\n// at runtime, so it panics here rather than silently behaving like zero.\nfunc ListTake[T any](count Int, xs []T) []T {\n\treturn boundedView(xs[:clampCount(\"List.take\", count, len(xs))], xs)\n}\n\nfunc ListDrop[T any](count Int, xs []T) []T {\n\treturn boundedView(xs[clampCount(\"List.drop\", count, len(xs)):], xs)\n}\n\n// viewSlack is the array size a view may pin outright. Below it the retention is\n// irrelevant and a copy would be pure overhead.\nconst viewSlack = 32\n\n// boundedView keeps a reader O(1) while capping how much array a small result can\n// pin: past the slack, a view may retain at most twice its own length, and otherwise\n// copies. The copy costs O(len(out)) \226\128\148 by construction less than half the memory it\n// releases \226\128\148 so this cannot turn a cheap read into an expensive one.\nfunc boundedView[T any](out, source []T) []T {\n\tif cap(source) > viewSlack && cap(source) > 2*len(out) {\n\t\treturn cloneExact(out)\n\t}\n\treturn out\n}\n\nfunc clampCount(who string, count Int, length int) int {\n\tif count.Sign() < 0 {\n\t\tpanic(who + \": count must be non-negative\")\n\t}\n\tvalue, ok := count.Int64()\n\tif !ok || value > int64(length) {\n\t\treturn length\n\t}\n\treturn int(value)\n}\n\nfunc ListReverse[T any](xs []T) []T {\n\tout := make([]T, len(xs))\n\tfor index, value := range xs {\n\t\tout[len(xs)-1-index] = value\n\t}\n\treturn out\n}\n\nfunc ListSum(xs []Int) Int {\n\ttotal := FromInt64(0)\n\tfor _, value := range xs {\n\t\ttotal = Add(total, value)\n\t}\n\treturn total\n}\n\n// ListProduct is the product of every element, and of NO element it is 1 \226\128\148 the identity, which\n// is what `(apply * '())` answers in tesl/list.rkt. Answering 0 for the empty list would make\n// `product (xs ++ ys)` stop agreeing with `product xs * product ys`.\nfunc ListProduct(xs []Int) Int {\n\ttotal := FromInt64(1)\n\tfor _, value := range xs {\n\t\ttotal = Mul(total, value)\n\t}\n\treturn total\n}\n\nfunc ListMemberBy[T any](needle T, xs []T, equal func(T, T) bool) bool {\n\tfor _, value := range xs {\n\t\tif equal(needle, value) {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n\n// ListUniqueBy keeps the FIRST occurrence and preserves order, matching Racket's\n// hash-based `List.unique`. It writes only into its own fresh array.\nfunc ListUniqueBy[T any](xs []T, equal func(T, T) bool) []T {\n\tout := make([]T, 0, len(xs))\n\tfor _, value := range xs {\n\t\tif !ListMemberBy(value, out, equal) {\n\t\t\tout = append(out, value)\n\t\t}\n\t}\n\treturn out\n}\n\n// ListSortBy is STABLE, matching Racket's `sort`, and sorts a copy so the input\n// value is untouched.\nfunc ListSortBy[T any](xs []T, less func(T, T) bool) []T {\n\tout := cloneExact(xs)\n\tslices.SortStableFunc(out, func(left, right T) int {\n\t\tswitch {\n\t\tcase less(left, right):\n\t\t\treturn -1\n\t\tcase less(right, left):\n\t\t\treturn 1\n\t\tdefault:\n\t\t\treturn 0\n\t\t}\n\t})\n\treturn out\n}\n\nfunc ListEqualBy[T any](left, right []T, equal func(T, T) bool) bool {\n\tif len(left) != len(right) {\n\t\treturn false\n\t}\n\tfor index := range left {\n\t\tif !equal(left[index], right[index]) {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\n// cloneExact gives a writer a private array to work in.\nfunc cloneExact[T any](xs []T) []T {\n\tout := make([]T, len(xs))\n\tcopy(out, xs)\n\treturn out\n}\n\n// StringSplit matches Racket's `string-split` with #:trim? #f: separators are not\n// collapsed, leading and trailing empties are kept, and splitting \"\" yields [\"\"].\nfunc StringSplit(s, sep string) []string {\n\tif s == \"\" {\n\t\treturn []string{\"\"}\n\t}\n\treturn strings.Split(s, sep)\n}\n\nfunc StringJoin(parts []string, sep string) string {\n\treturn strings.Join(parts, sep)\n}\n\n// exactCount converts a proven-non-negative Int to a length. The frontend has already\n// discharged `IsNonNegative`, so a negative value here means the emitter let an unproven\n// call through; a count that does not fit cannot be allocated at all, and Racket would\n// exhaust memory attempting it, so both panic rather than silently truncating to a\n// wrong-length list.\nfunc exactCount(who string, count Int) int {\n\tif count.Sign() < 0 {\n\t\tpanic(who + \": count must be non-negative\")\n\t}\n\tvalue, ok := count.Int64()\n\tif !ok || value > int64(maxAllocElements) {\n\t\tpanic(who + \": count is too large to allocate\")\n\t}\n\treturn int(value)\n}\n\n// maxAllocElements bounds a single constructed list. Not a language limit \226\128\148 the point\n// past which the allocation cannot succeed anyway, reported as a clear panic rather than\n// an out-of-memory kill.\nconst maxAllocElements = 1 << 31\n\n// ListRange is start INCLUSIVE to end EXCLUSIVE, matching `for/list ([i (in-range \226\128\166)])`\n// in tesl/list.rkt, and empty when start >= end.\nfunc ListRange(start, end Int) []Int {\n\tif Compare(start, end) >= 0 {\n\t\treturn []Int{}\n\t}\n\tspan := exactCount(\"List.range\", Sub(end, start))\n\tout := make([]Int, span)\n\tcurrent := start\n\tfor index := range out {\n\t\tout[index] = current\n\t\tcurrent = Add(current, FromInt64(1))\n\t}\n\treturn out\n}\n\n// ListRepeat holds n copies of one value. `n` carries an IsNonNegative proof, so the\n// check here is containment rather than the enforcement.\nfunc ListRepeat[T any](value T, n Int) []T {\n\tout := make([]T, exactCount(\"List.repeat\", n))\n\tfor index := range out {\n\t\tout[index] = value\n\t}\n\treturn out\n}\n\n// ListConcat flattens ONE level, like `List.concat`/`List.flatten`. It sizes the result\n// first, so a list of n lists costs one allocation rather than n appends.\nfunc ListConcat[T any](xss [][]T) []T {\n\ttotal := 0\n\tfor _, xs := range xss {\n\t\ttotal += len(xs)\n\t}\n\tout := make([]T, 0, total)\n\tfor _, xs := range xss {\n\t\tout = append(out, xs...)\n\t}\n\treturn out\n}\n\n// ListMaximum and ListMinimum are Nothing for the empty list. Ordering comes in from the\n// emitter, which knows the concrete element type at each call site.\nfunc ListMaximum[T any](xs []T, less func(T, T) bool) Maybe[T] {\n\tif len(xs) == 0 {\n\t\treturn Nothing[T]()\n\t}\n\tbest := xs[0]\n\tfor _, value := range xs[1:] {\n\t\tif less(best, value) {\n\t\t\tbest = value\n\t\t}\n\t}\n\treturn Something(best)\n}\n\nfunc ListMinimum[T any](xs []T, less func(T, T) bool) Maybe[T] {\n\tif len(xs) == 0 {\n\t\treturn Nothing[T]()\n\t}\n\tbest := xs[0]\n\tfor _, value := range xs[1:] {\n\t\tif less(value, best) {\n\t\t\tbest = value\n\t\t}\n\t}\n\treturn Something(best)\n}\n");
  ("loadtest.go", "package teslrt\n\nimport (\n\t\"math\"\n\t\"sort\"\n\t\"testing\"\n\t\"time\"\n)\n\n// The load-test harness: an OPEN-MODEL scheduler, so latency measures what a client would see.\n//\n// Every rule here is copied from `dsl/load-test.rkt` deliberately, because the point of having it\n// on both backends is that the two sets of numbers are comparable:\n//\n//\trequests are sent at a FIXED arrival rate regardless of how long the last one took, and\n//\tlatency is measured from the SCHEDULED send time \226\128\148 a closed model (send, wait, send) hides\n//\texactly the queueing a load test exists to find, which is coordinated omission;\n//\twarm-up runs until p99 is steady (CV < 5% across three consecutive 2-second windows) or 30\n//\tseconds pass, and warm-up requests are NOT measured \226\128\148 otherwise the first JIT/allocation\n//\tcosts land in the p99 the assertion reads;\n//\tpercentiles are taken from the sorted sample by index (floor(p*n)), which is the same\n//\testimator, so a p99 here and a p99 there mean the same thing;\n//\tthe report is the same six lines, so a diff of two runs is a diff of the numbers.\n//\n// One request at a time, from one goroutine: the Racket harness is single-threaded, and matching\n// that is what makes the comparison about the RUNTIME rather than about how each harness spreads\n// work over cores.\nconst (\n\twarmupWindow      = 2 * time.Second\n\twarmupCVThreshold = 0.05\n\twarmupConsecutive = 3\n\twarmupMax         = 30 * time.Second\n)\n\n// LoadTestResult is the sample a run produced, in milliseconds.\ntype LoadTestResult struct {\n\tLatencies  []float64\n\tErrors     int\n\tTotal      int\n\tDurationS  float64\n\tThroughput float64\n\tErrorRate  float64\n}\n\n// RunLoadTest drives `request` at `rate` requests per second for `duration` seconds and reports\n// the sample. `request` answers the response status (an error is reported as 0), so the harness\n// can count a 4xx/5xx as an error without knowing anything about the program.\nfunc RunLoadTest(rate int, durationSeconds int, request func() int) LoadTestResult {\n\tif rate < 1 {\n\t\trate = 1\n\t}\n\tinterval := time.Duration(float64(time.Second) / float64(rate))\n\tresult := LoadTestResult{}\n\n\t// One request, measured from the time it was SCHEDULED to be sent.\n\tsend := func(scheduled time.Time) float64 {\n\t\tstatus := request()\n\t\tlatency := float64(time.Since(scheduled).Nanoseconds()) / 1e6\n\t\tresult.Total++\n\t\tif status == 0 || status >= 400 {\n\t\t\tresult.Errors++\n\t\t}\n\t\treturn latency\n\t}\n\n\t// \226\148\128\226\148\128 Warm-up: not measured \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\twarmupStart := time.Now()\n\twindowStart := time.Now()\n\twindow := []float64{}\n\trecent := []float64{}\n\tscheduled := time.Now()\n\tfor time.Since(warmupStart) < warmupMax {\n\t\twindow = append(window, send(scheduled))\n\t\tif time.Since(windowStart) >= warmupWindow {\n\t\t\trecent = append(recent, percentileOf(window, 0.99))\n\t\t\tif len(recent) > warmupConsecutive {\n\t\t\t\trecent = recent[len(recent)-warmupConsecutive:]\n\t\t\t}\n\t\t\tif len(recent) == warmupConsecutive &&\n\t\t\t\tcoefficientOfVariation(recent) < warmupCVThreshold {\n\t\t\t\tbreak\n\t\t\t}\n\t\t\twindow = window[:0]\n\t\t\twindowStart = time.Now()\n\t\t}\n\t\tscheduled = scheduled.Add(interval)\n\t\tif wait := time.Until(scheduled); wait > 0 {\n\t\t\ttime.Sleep(wait)\n\t\t}\n\t}\n\n\t// \226\148\128\226\148\128 Measurement \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\tresult.Total = 0\n\tresult.Errors = 0\n\tmeasureStart := time.Now()\n\tmeasureEnd := measureStart.Add(time.Duration(durationSeconds) * time.Second)\n\tscheduled = time.Now()\n\tfor time.Now().Before(measureEnd) {\n\t\tresult.Latencies = append(result.Latencies, send(scheduled))\n\t\tscheduled = scheduled.Add(interval)\n\t\tif wait := time.Until(scheduled); wait > 0 {\n\t\t\ttime.Sleep(wait)\n\t\t}\n\t}\n\tresult.DurationS = time.Since(measureStart).Seconds()\n\tif result.DurationS > 0 {\n\t\tresult.Throughput = float64(len(result.Latencies)) / result.DurationS\n\t}\n\tif result.Total > 0 {\n\t\tresult.ErrorRate = float64(result.Errors) / float64(result.Total)\n\t}\n\treturn result\n}\n\n// percentileOf takes the value at floor(p*n) of the sorted sample \226\128\148 the same estimator the Racket\n// harness uses, so the two report the same number for the same sample.\nfunc percentileOf(latencies []float64, p float64) float64 {\n\tif len(latencies) == 0 {\n\t\treturn math.Inf(1)\n\t}\n\tsorted := make([]float64, len(latencies))\n\tcopy(sorted, latencies)\n\tsort.Float64s(sorted)\n\tindex := int(math.Floor(p * float64(len(sorted))))\n\tif index >= len(sorted) {\n\t\tindex = len(sorted) - 1\n\t}\n\treturn sorted[index]\n}\n\nfunc coefficientOfVariation(values []float64) float64 {\n\tif len(values) < 2 {\n\t\treturn 1.0\n\t}\n\tsum := 0.0\n\tfor _, value := range values {\n\t\tsum += value\n\t}\n\tmean := sum / float64(len(values))\n\tif mean <= 0 {\n\t\treturn 1.0\n\t}\n\tvariance := 0.0\n\tfor _, value := range values {\n\t\tvariance += (value - mean) * (value - mean)\n\t}\n\tvariance /= float64(len(values) - 1)\n\treturn math.Sqrt(variance) / mean\n}\n\n// LoadTestMetric answers one of the metrics an `assert` names.\nfunc (result LoadTestResult) LoadTestMetric(name string) float64 {\n\tswitch name {\n\tcase \"p50\":\n\t\treturn percentileOf(result.Latencies, 0.50)\n\tcase \"p95\":\n\t\treturn percentileOf(result.Latencies, 0.95)\n\tcase \"p99\":\n\t\treturn percentileOf(result.Latencies, 0.99)\n\tcase \"p99.9\":\n\t\treturn percentileOf(result.Latencies, 0.999)\n\tcase \"errorRate\":\n\t\treturn result.ErrorRate\n\tcase \"throughput\":\n\t\treturn result.Throughput\n\tdefault:\n\t\tpanic(\"load-test: unknown metric \" + name)\n\t}\n}\n\n// ReportLoadTest prints the same six lines the Racket harness prints, so two runs diff cleanly.\n// It goes to the test log rather than stdout because a load test IS a test here.\nfunc ReportLoadTest(teslT *testing.T, result LoadTestResult) {\n\tteslT.Helper()\n\tteslT.Logf(\"  Load test results (%d requests in %.1fs):\", len(result.Latencies),\n\t\tresult.DurationS)\n\tteslT.Logf(\"    p50:  %.1fms  p95: %.1fms  p99: %.1fms  p99.9: %.1fms\",\n\t\tresult.LoadTestMetric(\"p50\"), result.LoadTestMetric(\"p95\"),\n\t\tresult.LoadTestMetric(\"p99\"), result.LoadTestMetric(\"p99.9\"))\n\tminimum, maximum := math.Inf(1), 0.0\n\tfor _, latency := range result.Latencies {\n\t\tminimum = math.Min(minimum, latency)\n\t\tmaximum = math.Max(maximum, latency)\n\t}\n\tif len(result.Latencies) == 0 {\n\t\tminimum = 0\n\t}\n\tteslT.Logf(\"    min: %.1fms  max: %.1fms  throughput: %.1frps  errors: %.2f%%\",\n\t\tminimum, maximum, result.Throughput, result.ErrorRate*100)\n\tteslT.Logf(\"    (measurements include in-process harness overhead)\")\n}\n\n// AssertLoadTest fails the test when a metric misses its threshold, naming the actual value \226\128\148\n// which is the whole content of a load-test failure.\nfunc AssertLoadTest(teslT *testing.T, result LoadTestResult, metric, operator string,\n\tthreshold float64) {\n\tteslT.Helper()\n\tactual := result.LoadTestMetric(metric)\n\tpassed := false\n\tswitch operator {\n\tcase \"<\":\n\t\tpassed = actual < threshold\n\tcase \"<=\":\n\t\tpassed = actual <= threshold\n\tcase \">\":\n\t\tpassed = actual > threshold\n\tcase \">=\":\n\t\tpassed = actual >= threshold\n\tdefault:\n\t\tpanic(\"load-test: unknown comparison \" + operator)\n\t}\n\tif !passed {\n\t\tteslT.Fatalf(\"load-test: assertion failed: %s %s %g (actual: %.2f)\",\n\t\t\tmetric, operator, threshold, actual)\n\t}\n}\n\n// LoadTestStatus is what a request thunk answers: the status an api-test response carries, or 0\n// when the request trapped. A trap is an error rather than a crash, because a load test that\n// dies on the first 500 measures nothing.\nfunc LoadTestStatus(response ApiResponse) int {\n\tstatus, exact := response.Status.Int64()\n\tif !exact {\n\t\treturn 0\n\t}\n\treturn int(status)\n}\n");
  ("maybe.go", "package teslrt\n\n// Maybe is Tesl's `Maybe a`. It lives in the runtime rather than being emitted per\n// module because a `Maybe` crosses module boundaries: two emitted packages that\n// each declared their own would produce incompatible Go types.\n//\n// The representation is the same flat tag+payload struct the emitter generates for\n// a user ADT, and Tag is exported for exactly that reason \226\128\148 an emitted package in\n// another module has to read it. The zero value is Nothing, matching Go's\n// zero-value convention as well as the tag order below.\ntype Maybe[A any] struct {\n\tTag            MaybeTag\n\tSomethingValue A\n}\n\n// MaybeTag identifies which variant a Maybe holds. The constants are an enum-like\n// set so the `exhaustive` linter can verify a switch over them.\ntype MaybeTag int\n\nconst (\n\tMaybeNothing MaybeTag = iota\n\tMaybeSomething\n)\n\n// Nothing is the empty Maybe. Emitted code writes the composite literal directly;\n// this exists for hand-written runtime code and for readable tests.\nfunc Nothing[A any]() Maybe[A] {\n\treturn Maybe[A]{Tag: MaybeNothing}\n}\n\n// Something wraps a present value.\nfunc Something[A any](value A) Maybe[A] {\n\treturn Maybe[A]{Tag: MaybeSomething, SomethingValue: value}\n}\n\n// IsSomething reports whether a value is present.\nfunc (m Maybe[A]) IsSomething() bool {\n\treturn m.Tag == MaybeSomething\n}\n\n// Value returns the wrapped value and whether it was present. The payload of a\n// Nothing is the zero value of A and must not be read as data; this signature\n// makes that impossible to do by accident.\nfunc (m Maybe[A]) Value() (A, bool) {\n\tif m.Tag != MaybeSomething {\n\t\tvar zero A\n\t\treturn zero, false\n\t}\n\treturn m.SomethingValue, true\n}\n");
  ("money.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"math/big\"\n\t\"strconv\"\n\t\"strings\"\n)\n\n// `Tesl.Money` \226\128\148 money as EXACT MINOR UNITS plus a currency, never as a float.\n//\n// The whole point of the type is that an amount cannot drift: cents are integers, arithmetic\n// on them is exact, and the only operations that round say so in their name. Where a rounding\n// does happen \226\128\148 a fractional scale, a currency conversion, a rate materialising into an amount\n// \226\128\148 it is ROUND HALF EVEN on an exact rational, which is what `tesl/money.rkt` does, so the\n// same program lands on the same cent on both backends.\n//\n// Two rules that look small and are not:\n//\n//   - a float factor is exactified DECIMAL-FAITHFULLY (0.9155 becomes 1831/2000, not the\n//     binary-float 0.91549999\226\128\166). Without it, 1000 \195\151 0.9155 = 915.4999\226\128\166 quietly rounds DOWN and\n//     the half-even contract is dodged;\n//   - `minorDigits` is per currency and load-bearing: it drives display AND the digit shift\n//     when converting between currencies with different scales (JPY has 0, USD has 2, BHD 3).\n\ntype Currency struct {\n\tCode        string\n\tMinorDigits int\n}\n\ntype Money struct {\n\tMinorUnits Int\n\tCurrency   Currency\n}\n\n// ExchangeRate carries PROVENANCE (`AsOf`) beside the rate, because a rate without a time is\n// not a fact about anything. The rate itself is stored as an exact rational: conversion math\n// never touches a float.\ntype ExchangeRate struct {\n\tFrom Currency\n\tTo   Currency\n\tRate *big.Rat\n\tAsOf PosixMillis\n}\n\n// MoneyRate is money PER quantity \226\128\148 an hourly rate, a price per kilogram. The algebra runs on\n// `PerCanonical` (minor units per SI-canonical denominator unit: per second, per kilogram);\n// the label pair is presentation, and the boundary unit a rate quantizes to.\ntype MoneyRate struct {\n\tPerCanonical *big.Rat\n\tCurrency     Currency\n\tLabelFactor  *big.Rat\n\tLabel        string\n}\n\n// MoneyOf is what a `Money.usd 1000` constructor becomes. The code and the digit count are\n// baked in by the emitter from the same ISO table the Racket constructors come from, and they\n// come LAST so the emitted call reads argument-then-constants.\nfunc MoneyOf(minorUnits Int, code string, minorDigits int) Money {\n\treturn Money{MinorUnits: minorUnits, Currency: Currency{Code: code, MinorDigits: minorDigits}}\n}\n\n// CurrencyOf is a `Usd` / `Sek` constructor: the currency itself, with no amount.\nfunc CurrencyOf(code string, minorDigits int) Currency {\n\treturn Currency{Code: code, MinorDigits: minorDigits}\n}\n\nfunc MoneyMinorUnits(money Money) Int    { return money.MinorUnits }\nfunc MoneyCurrency(money Money) Currency { return money.Currency }\n\nfunc CurrencyCode(currency Currency) string     { return currency.Code }\nfunc CurrencyMinorDigits(currency Currency) Int { return FromInt64(int64(currency.MinorDigits)) }\n\n// CurrencyFromCode resolves an ISO 4217 code at run time \226\128\148 the one place a currency is chosen\n// rather than written. An unknown code is Nothing, not a trap: the string usually came from\n// outside the program.\nfunc CurrencyFromCode(code string) Maybe[Currency] {\n\tif currency, found := currencyTable[code]; found {\n\t\treturn Something(currency)\n\t}\n\treturn Nothing[Currency]()\n}\n\n// MoneyFromMinorUnits builds an amount in a currency chosen at run time.\nfunc MoneyFromMinorUnits(currency Currency, minorUnits Int) Money {\n\treturn Money{MinorUnits: minorUnits, Currency: currency}\n}\n\n// \226\148\128\226\148\128 Arithmetic \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// MoneyScale multiplies by an exact integer \226\128\148 quantity \195\151 unit price stays exact, so it never\n// rounds and needs no rounding rule.\nfunc MoneyScale(money Money, factor Int) Money {\n\treturn Money{MinorUnits: Mul(money.MinorUnits, factor), Currency: money.Currency}\n}\n\n// MoneyScaleBy multiplies by a FRACTIONAL factor (interest, VAT, a discount): the factor is\n// exactified decimal-faithfully, the multiplication runs exact, and the result rounds half-even\n// back to whole minor units. It is deliberately not `*`: fractional money scaling rounds, so it\n// has to be a named, visible operation.\nfunc MoneyScaleBy(money Money, factor float64) Money {\n\tscaled := new(big.Rat).Mul(ratOfInt(money.MinorUnits), ratOfFloat(\"Money.scaleBy\", factor))\n\treturn Money{MinorUnits: fromBig(roundHalfEven(scaled)), Currency: money.Currency}\n}\n\nfunc MoneyNegate(money Money) Money {\n\treturn Money{MinorUnits: Sub(FromInt64(0), money.MinorUnits), Currency: money.Currency}\n}\n\nfunc MoneyAbs(money Money) Money {\n\tif Compare(money.MinorUnits, FromInt64(0)) < 0 {\n\t\treturn MoneyNegate(money)\n\t}\n\treturn money\n}\n\nfunc MoneyIsZero(money Money) bool { return Compare(money.MinorUnits, FromInt64(0)) == 0 }\n\nfunc MoneyIsNegative(money Money) bool { return Compare(money.MinorUnits, FromInt64(0)) < 0 }\n\n// MoneyAdd and MoneySubtract are SAME-CURRENCY operations. A mismatch traps rather than\n// picking a side: the checker's `SameCurrency` proof is what makes them total, so reaching\n// here with two currencies means that proof was bypassed.\nfunc MoneyAdd(left, right Money) Money {\n\trequireSameCurrency(\"Money.add\", left, right)\n\treturn Money{MinorUnits: Add(left.MinorUnits, right.MinorUnits), Currency: left.Currency}\n}\n\nfunc MoneySubtract(left, right Money) Money {\n\trequireSameCurrency(\"Money.subtract\", left, right)\n\treturn Money{MinorUnits: Sub(left.MinorUnits, right.MinorUnits), Currency: left.Currency}\n}\n\n// MoneyCompare answers -1 / 0 / 1 on minor units, which are comparable because the currency\n// is the same.\nfunc MoneyCompare(left, right Money) Int {\n\trequireSameCurrency(\"Money.compare\", left, right)\n\treturn FromInt64(int64(Compare(left.MinorUnits, right.MinorUnits)))\n}\n\nfunc requireSameCurrency(who string, left, right Money) {\n\tif left.Currency.Code != right.Currency.Code {\n\t\tpanic(fmt.Sprintf(\"%s: expected amounts in the same currency, got %s vs %s\",\n\t\t\twho, left.Currency.Code, right.Currency.Code))\n\t}\n}\n\n// \226\148\128\226\148\128 Display \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// symbolPrefix is the handful of currencies conventionally written symbol-first; everything\n// else renders amount-then-code (\"10.50 SEK\").\nvar symbolPrefix = map[string]string{\"USD\": \"$\", \"EUR\": \"\226\130\172\", \"GBP\": \"\194\163\", \"JPY\": \"\194\165\"}\n\n// MoneyDisplay renders an amount the way `tesl-money-display` does, digits driven by the\n// currency: USD 1000 \226\134\146 \"$10.00\", JPY 1000 \226\134\146 \"\194\1651000\", SEK 1050 \226\134\146 \"10.50 SEK\", and a negative\n// amount keeps its sign in front of the symbol.\nfunc MoneyDisplay(money Money) string {\n\tmagnitude := MoneyAbs(money).MinorUnits\n\tamount := magnitude.String()\n\tif money.Currency.MinorDigits > 0 {\n\t\tdivisor := new(big.Int).Exp(big.NewInt(10), big.NewInt(int64(money.Currency.MinorDigits)), nil)\n\t\tmajor, minor := new(big.Int).QuoRem(magnitude.bigInt(), divisor, new(big.Int))\n\t\tminorText := minor.String()\n\t\tpadding := money.Currency.MinorDigits - len(minorText)\n\t\tamount = major.String() + \".\" + strings.Repeat(\"0\", padding) + minorText\n\t}\n\tsign := \"\"\n\tif MoneyIsNegative(money) {\n\t\tsign = \"-\"\n\t}\n\tif prefix, found := symbolPrefix[money.Currency.Code]; found {\n\t\treturn sign + prefix + amount\n\t}\n\treturn sign + amount + \" \" + money.Currency.Code\n}\n\n// \226\148\128\226\148\128 Exchange rates and conversion \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc ExchangeRateMake(from, to Currency, rate float64, asOf PosixMillis) ExchangeRate {\n\treturn ExchangeRate{From: from, To: to, Rate: ratOfFloat(\"ExchangeRate.make\", rate), AsOf: asOf}\n}\n\nfunc ExchangeRateFromCurrency(rate ExchangeRate) Currency { return rate.From }\nfunc ExchangeRateToCurrency(rate ExchangeRate) Currency   { return rate.To }\nfunc ExchangeRateAsOf(rate ExchangeRate) PosixMillis      { return rate.AsOf }\n\n// ExchangeRateRate answers the rate as a Float \226\128\148 a lossy view of an exact value, which is why\n// the conversion math never goes through it.\nfunc ExchangeRateRate(rate ExchangeRate) float64 {\n\tvalue, _ := rate.Rate.Float64()\n\treturn value\n}\n\n// convertedMinorUnits is\n//\n//\tminor_dst = round-half-even(minor_src \195\151 rate \195\151 10^(digits_dst \226\136\146 digits_src))\n//\n// on exact rationals throughout. The digit shift rescales between minor-unit magnitudes, so\n// converting JPY (0 digits) into USD (2 digits) multiplies by 100 rather than losing the cents.\nfunc convertedMinorUnits(rate ExchangeRate, money Money) Int {\n\tshift := new(big.Rat).SetInt(new(big.Int).Exp(big.NewInt(10),\n\t\tbig.NewInt(int64(abs(rate.To.MinorDigits-rate.From.MinorDigits))), nil))\n\tif rate.To.MinorDigits < rate.From.MinorDigits {\n\t\tshift = new(big.Rat).Inv(shift)\n\t}\n\tproduct := new(big.Rat).Mul(ratOfInt(money.MinorUnits), rate.Rate)\n\tproduct = product.Mul(product, shift)\n\treturn fromBig(roundHalfEven(product))\n}\n\nfunc abs(value int) int {\n\tif value < 0 {\n\t\treturn -value\n\t}\n\treturn value\n}\n\n// MoneyConvert is the UNPROVEN path: a rate whose FROM currency is not the amount's is an Err\n// the caller has to handle, not a trap.\nfunc MoneyConvert(rate ExchangeRate, money Money) Result[Money, string] {\n\tif rate.From.Code != money.Currency.Code {\n\t\treturn MakeErr[Money, string](fmt.Sprintf(\"exchange rate is FROM %s but amount is in %s\",\n\t\t\trate.From.Code, money.Currency.Code))\n\t}\n\treturn MakeOk[Money, string](Money{MinorUnits: convertedMinorUnits(rate, money), Currency: rate.To})\n}\n\n// MoneyConvertChecked is the proof-gated path: `RateFor r m` guarantees the currencies match,\n// so this is total given the proof. A mismatch here means the proof layer was bypassed, and it\n// fails loudly rather than converting at a rate that does not apply.\nfunc MoneyConvertChecked(rate ExchangeRate, money Money) Money {\n\tif rate.From.Code != money.Currency.Code {\n\t\tpanic(fmt.Sprintf(\"Money.convertChecked: exchange rate is FROM %s but amount is in %s\"+\n\t\t\t\" \226\128\148 this needs a RateFor proof (use `check Money.requireRateFor(r, m)` first)\",\n\t\t\trate.From.Code, money.Currency.Code))\n\t}\n\treturn Money{MinorUnits: convertedMinorUnits(rate, money), Currency: rate.To}\n}\n\n// \226\148\128\226\148\128 The checks that mint the proofs \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Each answers the value the proof is about, so the caller binds it and carries the proof; the\n// proof itself erases here, and what survives is the validation and its 400.\n\nfunc MoneyRequireNonNegative(money Money) Check[Money] {\n\tif MoneyIsNegative(money) {\n\t\treturn Reject[Money](400, \"amount must be non-negative\")\n\t}\n\treturn Accept(money)\n}\n\nfunc MoneyRequireSameCurrency(left, right Money) Check[Money] {\n\tif left.Currency.Code != right.Currency.Code {\n\t\treturn Reject[Money](400, fmt.Sprintf(\n\t\t\t\"expected amounts in the same currency, got %s vs %s\",\n\t\t\tleft.Currency.Code, right.Currency.Code))\n\t}\n\treturn Accept(right)\n}\n\nfunc MoneyRequireRateFor(rate ExchangeRate, money Money) Check[Money] {\n\tif rate.From.Code != money.Currency.Code {\n\t\treturn Reject[Money](400, fmt.Sprintf(\n\t\t\t\"exchange rate is FROM %s but amount is in %s\",\n\t\t\trate.From.Code, money.Currency.Code))\n\t}\n\treturn Accept(money)\n}\n\n// \226\148\128\226\148\128 Money per quantity \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// MoneyRateOf is a fixed-denominator constructor (`MoneyRate.perHour`): the amount IS the price\n// per label unit, so per-canonical is that amount divided by the canonical units in one label\n// unit \226\128\148 3600 for an hour, 1/1000 for a litre.\nfunc MoneyRateOf(money Money, canonicalPerLabel *big.Rat, label string) MoneyRate {\n\treturn MoneyRate{\n\t\tPerCanonical: new(big.Rat).Quo(ratOfInt(money.MinorUnits), canonicalPerLabel),\n\t\tCurrency:     money.Currency,\n\t\tLabelFactor:  canonicalPerLabel,\n\t\tLabel:        label,\n\t}\n}\n\n// MoneyRateOfLabel and MoneyRateDivLabel are the shapes EMITTED code calls: the label factor\n// arrives as a fraction of plain integers, so an emitted module needs no big-number import of\n// its own for what is a compile-time constant.\nfunc MoneyRateOfLabel(money Money, labelNum, labelDen int64, label string) MoneyRate {\n\treturn MoneyRateOf(money, big.NewRat(labelNum, labelDen), label)\n}\n\nfunc MoneyRateDivLabel(money Money, quantity float64, labelNum, labelDen int64,\n\tlabel string) MoneyRate {\n\treturn MoneyRateDiv(money, quantity, big.NewRat(labelNum, labelDen), label)\n}\n\n// MoneyRateDiv is `money / quantity`. The label pair comes from the checker's type, so a rate\n// displays and quantizes per HOUR rather than per second \226\128\148 a realistic hourly rate would\n// otherwise quantize to zero at a boundary.\nfunc MoneyRateDiv(money Money, quantity float64, canonicalPerLabel *big.Rat, label string) MoneyRate {\n\tif quantity == 0 {\n\t\tpanic(\"MoneyRate: division by a zero quantity\")\n\t}\n\treturn MoneyRate{\n\t\tPerCanonical: new(big.Rat).Quo(ratOfInt(money.MinorUnits),\n\t\t\tratOfFloat(\"MoneyRate\", quantity)),\n\t\tCurrency:    money.Currency,\n\t\tLabelFactor: canonicalPerLabel,\n\t\tLabel:       label,\n\t}\n}\n\n// MoneyRateMul is `rate \195\151 quantity` \226\128\148 the ONE place a rate becomes an amount, and so the one\n// half-even rounding in the algebra.\nfunc MoneyRateMul(rate MoneyRate, quantity float64) Money {\n\tproduct := new(big.Rat).Mul(rate.PerCanonical, ratOfFloat(\"MoneyRate\", quantity))\n\treturn Money{MinorUnits: fromBig(roundHalfEven(product)), Currency: rate.Currency}\n}\n\n// MoneyRateScale rescales a rate by a scalar, exactly: nothing rounds until an amount\n// materialises.\nfunc MoneyRateScale(rate MoneyRate, factor float64) MoneyRate {\n\treturn MoneyRate{\n\t\tPerCanonical: new(big.Rat).Mul(rate.PerCanonical, ratOfFloat(\"MoneyRate\", factor)),\n\t\tCurrency:     rate.Currency,\n\t\tLabelFactor:  rate.LabelFactor,\n\t\tLabel:        rate.Label,\n\t}\n}\n\nfunc MoneyRateCurrency(rate MoneyRate) Currency { return rate.Currency }\n\n// MoneyRateDisplay renders the rate's amount per DISPLAY unit \226\128\148 \"950.00 SEK/h\", \"$0.25/kg\".\n// The rounding is for rendering only; the stored rate stays exact.\nfunc MoneyRateDisplay(rate MoneyRate) string {\n\tperLabel := new(big.Rat).Mul(rate.PerCanonical, rate.LabelFactor)\n\tamount := Money{MinorUnits: fromBig(roundHalfEven(perLabel)), Currency: rate.Currency}\n\treturn MoneyDisplay(amount) + \"/\" + rate.Label\n}\n\n// \226\148\128\226\148\128 Exact-rational helpers \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\nfunc ratOfInt(value Int) *big.Rat {\n\treturn new(big.Rat).SetInt(value.bigInt())\n}\n\n// ratOfFloat exactifies a float DECIMAL-FAITHFULLY: the shortest round-trip decimal of a\n// float64 is exactly the decimal the author wrote, so 0.9155 becomes 1831/2000 rather than the\n// binary-float value just under it. Racket reaches for the same trick\n// (`number->string` + `decimal-as-exact`), and for the same reason: without it a rate of\n// 0.9155 applied to 1000 lands on 915.4999\226\128\166 and rounds DOWN, dodging the half-even contract.\nfunc ratOfFloat(who string, value float64) *big.Rat {\n\ttext := strconv.FormatFloat(value, 'g', -1, 64)\n\trat, ok := new(big.Rat).SetString(text)\n\tif !ok {\n\t\tpanic(who + \": factor must be a finite number, got \" + text)\n\t}\n\treturn rat\n}\n\n// roundHalfEven rounds an exact rational to an integer, breaking a tie toward the EVEN\n// neighbour \226\128\148 the rule Racket's `round` follows on exact rationals, and the one that does not\n// bias a long series of roundings upward.\nfunc roundHalfEven(value *big.Rat) *big.Int {\n\tquotient, remainder := new(big.Int).QuoRem(value.Num(), value.Denom(), new(big.Int))\n\tif remainder.Sign() == 0 {\n\t\treturn quotient\n\t}\n\t// Compare |remainder| with half the denominator, without leaving the integers.\n\ttwiceRemainder := new(big.Int).Abs(remainder)\n\ttwiceRemainder.Lsh(twiceRemainder, 1)\n\tcomparison := twiceRemainder.Cmp(value.Denom())\n\troundAway := comparison > 0\n\tif comparison == 0 {\n\t\t// A tie goes to the even neighbour: away from zero only when truncation left an odd\n\t\t// quotient.\n\t\troundAway = quotient.Bit(0) == 1\n\t}\n\tif !roundAway {\n\t\treturn quotient\n\t}\n\tif value.Sign() < 0 {\n\t\treturn quotient.Sub(quotient, big.NewInt(1))\n\t}\n\treturn quotient.Add(quotient, big.NewInt(1))\n}\n");
  ("currency_data.go", "package teslrt\n\n// Generated by scripts/gen-currencies.py from the ISO 4217 active list.\n// DO NOT EDIT BY HAND \226\128\148 regenerate and commit when ISO 4217 changes.\n// 155 currencies. minorDigits is load-bearing (rounding and\n// Money.display): USD=2, JPY=0, BHD=3.\n//\n// A `Money.usd 1000` constructor needs no table \226\128\148 the emitter bakes the code and the\n// digits into the call. What needs one is `Currency.fromCode`, the single place a\n// currency is chosen at RUN time.\nvar currencyTable = map[string]Currency{\n\t\"AED\": {Code: \"AED\", MinorDigits: 2},\n\t\"AFN\": {Code: \"AFN\", MinorDigits: 2},\n\t\"ALL\": {Code: \"ALL\", MinorDigits: 2},\n\t\"AMD\": {Code: \"AMD\", MinorDigits: 2},\n\t\"ANG\": {Code: \"ANG\", MinorDigits: 2},\n\t\"AOA\": {Code: \"AOA\", MinorDigits: 2},\n\t\"ARS\": {Code: \"ARS\", MinorDigits: 2},\n\t\"AUD\": {Code: \"AUD\", MinorDigits: 2},\n\t\"AWG\": {Code: \"AWG\", MinorDigits: 2},\n\t\"AZN\": {Code: \"AZN\", MinorDigits: 2},\n\t\"BAM\": {Code: \"BAM\", MinorDigits: 2},\n\t\"BBD\": {Code: \"BBD\", MinorDigits: 2},\n\t\"BDT\": {Code: \"BDT\", MinorDigits: 2},\n\t\"BGN\": {Code: \"BGN\", MinorDigits: 2},\n\t\"BHD\": {Code: \"BHD\", MinorDigits: 3},\n\t\"BIF\": {Code: \"BIF\", MinorDigits: 0},\n\t\"BMD\": {Code: \"BMD\", MinorDigits: 2},\n\t\"BND\": {Code: \"BND\", MinorDigits: 2},\n\t\"BOB\": {Code: \"BOB\", MinorDigits: 2},\n\t\"BRL\": {Code: \"BRL\", MinorDigits: 2},\n\t\"BSD\": {Code: \"BSD\", MinorDigits: 2},\n\t\"BTN\": {Code: \"BTN\", MinorDigits: 2},\n\t\"BWP\": {Code: \"BWP\", MinorDigits: 2},\n\t\"BYN\": {Code: \"BYN\", MinorDigits: 2},\n\t\"BZD\": {Code: \"BZD\", MinorDigits: 2},\n\t\"CAD\": {Code: \"CAD\", MinorDigits: 2},\n\t\"CDF\": {Code: \"CDF\", MinorDigits: 2},\n\t\"CHF\": {Code: \"CHF\", MinorDigits: 2},\n\t\"CLP\": {Code: \"CLP\", MinorDigits: 0},\n\t\"CNY\": {Code: \"CNY\", MinorDigits: 2},\n\t\"COP\": {Code: \"COP\", MinorDigits: 2},\n\t\"CRC\": {Code: \"CRC\", MinorDigits: 2},\n\t\"CUP\": {Code: \"CUP\", MinorDigits: 2},\n\t\"CVE\": {Code: \"CVE\", MinorDigits: 2},\n\t\"CZK\": {Code: \"CZK\", MinorDigits: 2},\n\t\"DJF\": {Code: \"DJF\", MinorDigits: 0},\n\t\"DKK\": {Code: \"DKK\", MinorDigits: 2},\n\t\"DOP\": {Code: \"DOP\", MinorDigits: 2},\n\t\"DZD\": {Code: \"DZD\", MinorDigits: 2},\n\t\"EGP\": {Code: \"EGP\", MinorDigits: 2},\n\t\"ERN\": {Code: \"ERN\", MinorDigits: 2},\n\t\"ETB\": {Code: \"ETB\", MinorDigits: 2},\n\t\"EUR\": {Code: \"EUR\", MinorDigits: 2},\n\t\"FJD\": {Code: \"FJD\", MinorDigits: 2},\n\t\"FKP\": {Code: \"FKP\", MinorDigits: 2},\n\t\"GBP\": {Code: \"GBP\", MinorDigits: 2},\n\t\"GEL\": {Code: \"GEL\", MinorDigits: 2},\n\t\"GHS\": {Code: \"GHS\", MinorDigits: 2},\n\t\"GIP\": {Code: \"GIP\", MinorDigits: 2},\n\t\"GMD\": {Code: \"GMD\", MinorDigits: 2},\n\t\"GNF\": {Code: \"GNF\", MinorDigits: 0},\n\t\"GTQ\": {Code: \"GTQ\", MinorDigits: 2},\n\t\"GYD\": {Code: \"GYD\", MinorDigits: 2},\n\t\"HKD\": {Code: \"HKD\", MinorDigits: 2},\n\t\"HNL\": {Code: \"HNL\", MinorDigits: 2},\n\t\"HTG\": {Code: \"HTG\", MinorDigits: 2},\n\t\"HUF\": {Code: \"HUF\", MinorDigits: 2},\n\t\"IDR\": {Code: \"IDR\", MinorDigits: 2},\n\t\"ILS\": {Code: \"ILS\", MinorDigits: 2},\n\t\"INR\": {Code: \"INR\", MinorDigits: 2},\n\t\"IQD\": {Code: \"IQD\", MinorDigits: 3},\n\t\"IRR\": {Code: \"IRR\", MinorDigits: 2},\n\t\"ISK\": {Code: \"ISK\", MinorDigits: 0},\n\t\"JMD\": {Code: \"JMD\", MinorDigits: 2},\n\t\"JOD\": {Code: \"JOD\", MinorDigits: 3},\n\t\"JPY\": {Code: \"JPY\", MinorDigits: 0},\n\t\"KES\": {Code: \"KES\", MinorDigits: 2},\n\t\"KGS\": {Code: \"KGS\", MinorDigits: 2},\n\t\"KHR\": {Code: \"KHR\", MinorDigits: 2},\n\t\"KMF\": {Code: \"KMF\", MinorDigits: 0},\n\t\"KPW\": {Code: \"KPW\", MinorDigits: 2},\n\t\"KRW\": {Code: \"KRW\", MinorDigits: 0},\n\t\"KWD\": {Code: \"KWD\", MinorDigits: 3},\n\t\"KYD\": {Code: \"KYD\", MinorDigits: 2},\n\t\"KZT\": {Code: \"KZT\", MinorDigits: 2},\n\t\"LAK\": {Code: \"LAK\", MinorDigits: 2},\n\t\"LBP\": {Code: \"LBP\", MinorDigits: 2},\n\t\"LKR\": {Code: \"LKR\", MinorDigits: 2},\n\t\"LRD\": {Code: \"LRD\", MinorDigits: 2},\n\t\"LSL\": {Code: \"LSL\", MinorDigits: 2},\n\t\"LYD\": {Code: \"LYD\", MinorDigits: 3},\n\t\"MAD\": {Code: \"MAD\", MinorDigits: 2},\n\t\"MDL\": {Code: \"MDL\", MinorDigits: 2},\n\t\"MGA\": {Code: \"MGA\", MinorDigits: 2},\n\t\"MKD\": {Code: \"MKD\", MinorDigits: 2},\n\t\"MMK\": {Code: \"MMK\", MinorDigits: 2},\n\t\"MNT\": {Code: \"MNT\", MinorDigits: 2},\n\t\"MOP\": {Code: \"MOP\", MinorDigits: 2},\n\t\"MRU\": {Code: \"MRU\", MinorDigits: 2},\n\t\"MUR\": {Code: \"MUR\", MinorDigits: 2},\n\t\"MVR\": {Code: \"MVR\", MinorDigits: 2},\n\t\"MWK\": {Code: \"MWK\", MinorDigits: 2},\n\t\"MXN\": {Code: \"MXN\", MinorDigits: 2},\n\t\"MYR\": {Code: \"MYR\", MinorDigits: 2},\n\t\"MZN\": {Code: \"MZN\", MinorDigits: 2},\n\t\"NAD\": {Code: \"NAD\", MinorDigits: 2},\n\t\"NGN\": {Code: \"NGN\", MinorDigits: 2},\n\t\"NIO\": {Code: \"NIO\", MinorDigits: 2},\n\t\"NOK\": {Code: \"NOK\", MinorDigits: 2},\n\t\"NPR\": {Code: \"NPR\", MinorDigits: 2},\n\t\"NZD\": {Code: \"NZD\", MinorDigits: 2},\n\t\"OMR\": {Code: \"OMR\", MinorDigits: 3},\n\t\"PAB\": {Code: \"PAB\", MinorDigits: 2},\n\t\"PEN\": {Code: \"PEN\", MinorDigits: 2},\n\t\"PGK\": {Code: \"PGK\", MinorDigits: 2},\n\t\"PHP\": {Code: \"PHP\", MinorDigits: 2},\n\t\"PKR\": {Code: \"PKR\", MinorDigits: 2},\n\t\"PLN\": {Code: \"PLN\", MinorDigits: 2},\n\t\"PYG\": {Code: \"PYG\", MinorDigits: 0},\n\t\"QAR\": {Code: \"QAR\", MinorDigits: 2},\n\t\"RON\": {Code: \"RON\", MinorDigits: 2},\n\t\"RSD\": {Code: \"RSD\", MinorDigits: 2},\n\t\"RUB\": {Code: \"RUB\", MinorDigits: 2},\n\t\"RWF\": {Code: \"RWF\", MinorDigits: 0},\n\t\"SAR\": {Code: \"SAR\", MinorDigits: 2},\n\t\"SBD\": {Code: \"SBD\", MinorDigits: 2},\n\t\"SCR\": {Code: \"SCR\", MinorDigits: 2},\n\t\"SDG\": {Code: \"SDG\", MinorDigits: 2},\n\t\"SEK\": {Code: \"SEK\", MinorDigits: 2},\n\t\"SGD\": {Code: \"SGD\", MinorDigits: 2},\n\t\"SHP\": {Code: \"SHP\", MinorDigits: 2},\n\t\"SLE\": {Code: \"SLE\", MinorDigits: 2},\n\t\"SOS\": {Code: \"SOS\", MinorDigits: 2},\n\t\"SRD\": {Code: \"SRD\", MinorDigits: 2},\n\t\"SSP\": {Code: \"SSP\", MinorDigits: 2},\n\t\"STN\": {Code: \"STN\", MinorDigits: 2},\n\t\"SVC\": {Code: \"SVC\", MinorDigits: 2},\n\t\"SYP\": {Code: \"SYP\", MinorDigits: 2},\n\t\"SZL\": {Code: \"SZL\", MinorDigits: 2},\n\t\"THB\": {Code: \"THB\", MinorDigits: 2},\n\t\"TJS\": {Code: \"TJS\", MinorDigits: 2},\n\t\"TMT\": {Code: \"TMT\", MinorDigits: 2},\n\t\"TND\": {Code: \"TND\", MinorDigits: 3},\n\t\"TOP\": {Code: \"TOP\", MinorDigits: 2},\n\t\"TRY\": {Code: \"TRY\", MinorDigits: 2},\n\t\"TTD\": {Code: \"TTD\", MinorDigits: 2},\n\t\"TWD\": {Code: \"TWD\", MinorDigits: 2},\n\t\"TZS\": {Code: \"TZS\", MinorDigits: 2},\n\t\"UAH\": {Code: \"UAH\", MinorDigits: 2},\n\t\"UGX\": {Code: \"UGX\", MinorDigits: 0},\n\t\"USD\": {Code: \"USD\", MinorDigits: 2},\n\t\"UYU\": {Code: \"UYU\", MinorDigits: 2},\n\t\"UZS\": {Code: \"UZS\", MinorDigits: 2},\n\t\"VES\": {Code: \"VES\", MinorDigits: 2},\n\t\"VND\": {Code: \"VND\", MinorDigits: 0},\n\t\"VUV\": {Code: \"VUV\", MinorDigits: 0},\n\t\"WST\": {Code: \"WST\", MinorDigits: 2},\n\t\"XAF\": {Code: \"XAF\", MinorDigits: 0},\n\t\"XCD\": {Code: \"XCD\", MinorDigits: 2},\n\t\"XOF\": {Code: \"XOF\", MinorDigits: 0},\n\t\"XPF\": {Code: \"XPF\", MinorDigits: 0},\n\t\"YER\": {Code: \"YER\", MinorDigits: 2},\n\t\"ZAR\": {Code: \"ZAR\", MinorDigits: 2},\n\t\"ZMW\": {Code: \"ZMW\", MinorDigits: 2},\n\t\"ZWG\": {Code: \"ZWG\", MinorDigits: 2},\n}\n");
  ("database.go", "package teslrt\n\nimport (\n\t\"bytes\"\n\t\"context\"\n\t\"runtime\"\n\t\"strconv\"\n\t\"sync\"\n\t\"sync/atomic\"\n\t\"time\"\n\n\t\"github.com/jackc/pgx/v5\"\n)\n\n// A `database D = Database { \226\128\166 }` declaration at run time, and the dispatch that decides\n// whether a query reads the in-memory table or the PostgreSQL server.\n//\n// WHY THE DISPATCH IS DYNAMIC. Which store an entity's rows live in is NOT a property of the\n// entity: it is a property of whether something has CONNECTED. Racket's `select-many` asks\n// `database-runtime-for-entity`, which answers a connection only while a `with database D`\n// is active and only for an entity D manages; outside that extent the very same query reads\n// the in-memory source. That is not an accident of the implementation \226\128\148 it is what lets a\n// `test` block exercise a Postgres-backed entity with no server anywhere. So the emitted\n// code carries BOTH forms for an entity of a Postgres-backed database (the Go predicate and\n// the SQL statement), and picks here, exactly as Racket picks there.\n//\n// A program whose databases are all `backend: Memory` never reaches this file: its entities\n// emit the plain `Table*` calls they always did, and its go.mod requires nothing.\ntype Database struct {\n\t// Name as the declaration writes it, which is what a refusal names.\n\tName string\n\t// Config and Tables are what a connection needs: the DSN parts and the schema to create.\n\tConfig PostgresConfig\n\tTables []PostgresTable\n\n\tmutex sync.RWMutex\n\t// The live connection, non-nil only inside `with database D`.\n\topen *PostgresDB\n}\n\n// The database `with database D` most recently bound, program-wide.\n//\n// Racket holds this in ONE parameter (`current-database-runtime`), so at most one database is\n// connected at a time and `transaction { \226\128\166 }` needs no argument to know which server it opens a\n// transaction on \226\128\148 it opens one on whatever is bound. That is the model reproduced here, rather\n// than a per-declaration binding that would have to invent an answer for a `transaction` written\n// where two databases are in scope.\nvar boundDatabase atomic.Pointer[Database]\n\n// NewDatabase, PostgresTableOf and PostgresColumnOf are what a `database` declaration emits.\n// They are constructors rather than struct literals because gofmt ALIGNS the values in a\n// multi-line composite literal and breaks the alignment run at a nested multi-line value \226\128\148 a\n// rule the emitter would have to reproduce exactly, at every shape, forever. A call whose\n// arguments sit one per line is stable at every size instead.\nfunc NewDatabase(name string, config PostgresConfig, tables []PostgresTable) *Database {\n\treturn &Database{Name: name, Config: config, Tables: tables}\n}\n\nfunc PostgresTableOf(name string, columns ...PostgresColumn) PostgresTable {\n\treturn PostgresTable{Name: name, Columns: columns}\n}\n\n// PostgresTableWithIndexes is the same table plus its declared UNIQUE indexes, which the\n// bootstrap creates. Kept separate from PostgresTableOf so a table with none stays a one-line\n// call in emitted code.\nfunc PostgresTableWithIndexes(name string, unique []PostgresIndex,\n\tcolumns ...PostgresColumn) PostgresTable {\n\treturn PostgresTable{Name: name, Columns: columns, Unique: unique}\n}\n\nfunc PostgresIndexOf(name string, columns ...string) PostgresIndex {\n\treturn PostgresIndex{Name: name, Columns: columns}\n}\n\nfunc PostgresColumnOf(name, columnType string, primaryKey, nullable bool) PostgresColumn {\n\treturn PostgresColumn{Name: name, Type: columnType, PrimaryKey: primaryKey, Nullable: nullable}\n}\n\n// WithDatabase is `with database D { \226\128\166 }`: it connects, runs the body with D bound, and\n// unbinds afterwards \226\128\148 including when the body panics, since a check failure unwinds through\n// here and the next block must not inherit a binding from it.\n//\n// The POOL is not closed on the way out, unlike Racket's `disconnect-database`. `OpenPostgres`\n// is idempotent per configuration, so the second `with database D` in a program reuses the\n// first one's pool rather than paying a fresh handshake; a pool that outlives the block holds\n// idle connections, which is what a pool is for. Nothing observable differs \226\128\148 a query outside\n// the block does not reach the server either way, because the binding is what routes it.\nfunc WithDatabase(database *Database, body func()) {\n\tconnection := OpenPostgres(database.Config, database.Tables)\n\tdatabase.mutex.Lock()\n\tprevious := database.open\n\tdatabase.open = connection\n\tdatabase.mutex.Unlock()\n\tpreviousBound := boundDatabase.Swap(database)\n\tdefer func() {\n\t\tboundDatabase.Store(previousBound)\n\t\tdatabase.mutex.Lock()\n\t\tdatabase.open = previous\n\t\tdatabase.mutex.Unlock()\n\t}()\n\tbody()\n}\n\n// bound answers the connection a statement should run on, or nil for the in-memory store.\nfunc (database *Database) bound() *PostgresDB {\n\tif database == nil {\n\t\treturn nil\n\t}\n\tdatabase.mutex.RLock()\n\tdefer database.mutex.RUnlock()\n\treturn database.open\n}\n\n// \226\148\128\226\148\128 Transactions \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// A transaction has to run every statement on ONE connection, so the executor a statement\n// picks up has to follow the code that opened it \226\128\148 and that code is an ordinary Tesl function\n// which may call other ordinary Tesl functions, none of which carry a handle.\n//\n// Racket solves this with a parameter, which is THREAD-local, and its web server serves each\n// request on its own thread. The faithful Go reading of \"thread-local\" for a server that\n// serves each request on its own goroutine is goroutine-local, so that is what this is: the\n// open transaction is keyed by goroutine id, and a statement finds it only from inside the\n// goroutine that began it. Two requests in two transactions therefore cannot see each other's\n// uncommitted rows, which a package-level handle would get wrong.\n//\n// The one place this differs from Racket: a goroutine STARTED inside a transaction does not\n// inherit it, where a Racket thread created inside a `parameterize` does. A transaction body\n// that spawns work and expects that work to join the transaction is refused by the emitter\n// rather than silently running outside it.\nvar openTransactions sync.Map // goroutine id -> pgx.Tx\n\n// WithTransaction is `transaction { \226\128\166 }`. Against the in-memory store it is the body and\n// nothing else \226\128\148 Racket's `call-with-queue-transaction` is `(thunk)` with no connection \226\128\148 and\n// against PostgreSQL it is a real BEGIN/COMMIT, rolled back if the body panics so a check\n// failure halfway through leaves nothing behind.\nfunc WithTransaction(body func()) {\n\tdatabase := boundDatabase.Load()\n\tconnection := database.bound()\n\tif connection == nil {\n\t\tbody()\n\t\treturn\n\t}\n\tctx, cancel := context.WithTimeout(context.Background(), 5*time.Minute)\n\tdefer cancel()\n\ttransaction, err := connection.pool.Begin(ctx)\n\tif err != nil {\n\t\tpanic(\"transaction: cannot begin: \" + err.Error())\n\t}\n\tkey := goroutineID()\n\tif _, nested := openTransactions.Load(key); nested {\n\t\t// Racket nests by starting a SAVEPOINT; until that is built, a nested block would\n\t\t// silently commit the outer one at its own end, so it is refused here rather than\n\t\t// answered with weaker atomicity than it claims.\n\t\t_ = transaction.Rollback(ctx)\n\t\tpanic(\"transaction: a transaction is already open on database \" + database.Name)\n\t}\n\topenTransactions.Store(key, transaction)\n\tcommitted := false\n\tdefer func() {\n\t\topenTransactions.Delete(key)\n\t\tif !committed {\n\t\t\t_ = transaction.Rollback(ctx)\n\t\t}\n\t}()\n\tbody()\n\tif err := transaction.Commit(ctx); err != nil {\n\t\tpanic(\"transaction: cannot commit: \" + err.Error())\n\t}\n\tcommitted = true\n}\n\n// goroutineID reads the id out of the goroutine's own stack header, which is the only place\n// the runtime exposes it. The header's shape (\"goroutine 17 [running]:\") is fixed by\n// `runtime.Stack` itself; an unreadable one answers 0, which is a key like any other \226\128\148 every\n// goroutine that fails to parse would share one transaction, and none can, because the parse\n// cannot fail for a header this function is looking at.\nfunc goroutineID() uint64 {\n\tvar header [64]byte\n\twritten := runtime.Stack(header[:], false)\n\tfields := bytes.Fields(header[:written])\n\tif len(fields) < 2 {\n\t\treturn 0\n\t}\n\tid, err := strconv.ParseUint(string(fields[1]), 10, 64)\n\tif err != nil {\n\t\treturn 0\n\t}\n\treturn id\n}\n\n// currentTransaction is the open transaction for THIS goroutine, if any.\nfunc currentTransaction() pgx.Tx {\n\tif found, open := openTransactions.Load(goroutineID()); open {\n\t\tif transaction, ok := found.(pgx.Tx); ok {\n\t\t\treturn transaction\n\t\t}\n\t}\n\treturn nil\n}\n");
  ("dbquery.go", "package teslrt\n\nimport (\n\t\"github.com/jackc/pgx/v5\"\n)\n\n// The query dispatchers for an entity of a Postgres-backed database.\n//\n// Each takes BOTH forms of the same query \226\128\148 the Go predicate the in-memory store needs and the\n// statement the server needs \226\128\148 and runs whichever the binding selects (see database.go). They\n// exist only for entities a `backend: Postgres` database manages; a Memory-backed entity emits\n// the plain `Table*` call directly, so nothing about a memory-only program changes.\n//\n// WHY THE ARGUMENTS ARE A THUNK. Both forms sit at the same call site, which is what keeps them\n// from drifting apart \226\128\148 but only one of them RUNS. Were the arguments a plain slice they would\n// be evaluated on the way in even when the memory path is taken, and a `where` operand that\n// reads the clock, draws a random value or fails a check would then run where the memory-only\n// emission never ran it. The thunk is what makes \"emitted twice\" mean \"evaluated once, on the\n// path that is taken\".\n//\n// The statement's TEXT never contains a value: every operand is a `$n` placeholder, so nothing\n// a request sends can change what a statement SAYS.\ntype PgPlan struct {\n\tSQL string\n\t// Args is nil for a statement with no placeholders.\n\tArgs func() []any\n}\n\n// PgSql is the constructor the emitter calls. A function rather than a struct literal because\n// gofmt aligns the keys of a multi-line composite literal and breaks the alignment run at a\n// nested multi-line value; a call with the argument thunk last is stable at every size.\nfunc PgSql(statement string, args func() []any) PgPlan {\n\treturn PgPlan{SQL: statement, Args: args}\n}\n\nfunc (plan PgPlan) arguments() []any {\n\tif plan.Args == nil {\n\t\treturn nil\n\t}\n\treturn plan.Args()\n}\n\n// DbSelect is `select \226\128\166 [order \226\128\166] [limit \226\128\166] [offset \226\128\166]`. `less` is nil when the query has no\n// order clause; on the server side the ordering and the range are already in the statement.\nfunc DbSelect[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tless func(Row, Row) bool, offset, limit int, plan PgPlan,\n\tscan func(pgx.CollectableRow) (Row, error)) []Row {\n\tif connection := database.bound(); connection != nil {\n\t\treturn PgQuery(connection, plan.SQL, plan.arguments(), scan)\n\t}\n\tif less == nil {\n\t\treturn TableSelectRange(table, match, offset, limit)\n\t}\n\treturn TableSelectSorted(table, match, less, offset, limit)\n}\n\n// DbSelectOne is `selectOne`: the statement carries `limit 1`, so \"no row\" is the only thing\n// left to decide and both paths decide it the same way.\nfunc DbSelectOne[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tless func(Row, Row) bool, plan PgPlan,\n\tscan func(pgx.CollectableRow) (Row, error)) Maybe[Row] {\n\tif connection := database.bound(); connection != nil {\n\t\treturn PgQueryOne(connection, plan.SQL, plan.arguments(), scan)\n\t}\n\tif less == nil {\n\t\treturn TableSelectOne(table, match)\n\t}\n\treturn TableSelectOneSorted(table, match, less)\n}\n\n// DbCount is `selectCount`. The server counts in the database rather than shipping rows to be\n// counted here, which is the whole reason an aggregate is a separate query shape.\nfunc DbCount[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tplan PgPlan) Int {\n\tif connection := database.bound(); connection != nil {\n\t\treturn PgCount(connection, plan.SQL, plan.arguments())\n\t}\n\treturn TableCount(table, match)\n}\n\n// DbSum is `selectSum` over a scalar column. The statement is `coalesce(sum(col), 0)`, so a\n// sum over no rows is zero on both paths rather than NULL on one of them.\nfunc DbSum[Row any, Value any](database *Database, table *Table[Row], match func(Row) bool,\n\tproject func(Row) Value, combine func(Value, Value) Value, zero Value, plan PgPlan,\n\tscan func(pgx.Row) (Value, error)) Value {\n\tif connection := database.bound(); connection != nil {\n\t\treturn PgScalar(connection, plan.SQL, plan.arguments(), scan)\n\t}\n\treturn TableFold(table, match, project, combine, zero)\n}\n\n// DbSumMoney is `selectSum` over a MONEY column, whose two refusals (an empty row set has no\n// currency for its zero; two currencies have no common total) need two more facts than the sum\n// itself. Both come back in ONE statement \226\128\148 `sum`, `count(distinct currency)`, `min(currency)`\n// \226\128\148 which is the shape `dsl/sql.rkt` uses, so neither path makes a second pass over the rows.\nfunc DbSumMoney[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tproject func(Row) Money, entity, field string, plan PgPlan) Money {\n\tif connection := database.bound(); connection != nil {\n\t\treturn PgSumMoney(connection, plan.SQL, plan.arguments(), entity, field)\n\t}\n\treturn TableSumMoney(table, match, project, entity, field)\n}\n\n// DbExtreme is `selectMax` / `selectMin`: `max(col)` / `min(col)` on the server, and the same\n// `Maybe` on both paths, since SQL's aggregate over no rows is NULL where Tesl says Nothing.\nfunc DbExtreme[Row any, Value any](database *Database, table *Table[Row], match func(Row) bool,\n\tproject func(Row) Value, better func(Value, Value) bool, plan PgPlan,\n\tscan func(pgx.Row) (Maybe[Value], error)) Maybe[Value] {\n\tif connection := database.bound(); connection != nil {\n\t\treturn PgScalar(connection, plan.SQL, plan.arguments(), scan)\n\t}\n\treturn TableExtreme(table, match, project, better)\n}\n\n// DbInsert is `insert`. The duplicate-key refusal is the SERVER's on the Postgres path \226\128\148 the\n// primary key column carries the constraint \226\128\148 and the emitted `conflicts` comparison on the\n// memory path, which is what makes the two agree about which programs run.\n//\n// The arguments are read off the ROW rather than emitted a second time from the same field\n// expressions: a row whose `createdAt` is `Time.nowMillis()` would otherwise be STORED with one\n// instant and ANSWERED with another.\nfunc DbInsert[Row any](database *Database, table *Table[Row], entity string, row Row,\n\tconflicts func(Row, Row) bool, statement string, bind func(Row) []any,\n\tunique ...UniqueIndex[Row]) Row {\n\tif connection := database.bound(); connection != nil {\n\t\t// A declared `unique index` is a real index on the server, created by the bootstrap,\n\t\t// so the constraint is enforced where the rows are rather than twice.\n\t\tPgExec(connection, statement, bind(row))\n\t\treturn row\n\t}\n\treturn TableInsert(table, entity, row, conflicts, unique...)\n}\n\n// DbInsertMany inserts in order. One statement per row rather than a multi-row VALUES list, so\n// a row conflicting with an EARLIER row of the same batch is refused exactly where it would be\n// if the two had been inserted separately \226\128\148 Racket's `insert-many!` is a loop over\n// `insert-one!`, and a single statement would instead fail the whole batch at a different point.\n// The rows are only known at run time, so the statement is fixed and its ARGUMENTS are read off\n// each row by the emitter's binder \226\128\148 the one place in this file where a plan cannot be a value.\nfunc DbInsertMany[Row any](database *Database, table *Table[Row], entity string, rows []Row,\n\tconflicts func(Row, Row) bool, statement string, bind func(Row) []any,\n\tunique ...UniqueIndex[Row]) struct{} {\n\tif connection := database.bound(); connection != nil {\n\t\tfor _, row := range rows {\n\t\t\tPgExec(connection, statement, bind(row))\n\t\t}\n\t\treturn struct{}{}\n\t}\n\treturn TableInsertMany(table, entity, rows, conflicts, unique...)\n}\n\n// DbUpdate is `update \226\128\166 set \226\128\166`. Nothing is reported back: a plain update is a statement.\nfunc DbUpdate[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tapply func(Row) Row, plan PgPlan, unique ...UniqueIndex[Row]) struct{} {\n\tif connection := database.bound(); connection != nil {\n\t\tPgExec(connection, plan.SQL, plan.arguments())\n\t\treturn struct{}{}\n\t}\n\treturn TableUpdate(table, match, apply, unique...)\n}\n\n// DbUpdateReturnOne is `updateAndReturnOne`: the statement carries `returning`, and a predicate\n// that matched nothing is a failure on both paths rather than a zero value.\nfunc DbUpdateReturnOne[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tapply func(Row) Row, plan PgPlan, scan func(pgx.CollectableRow) (Row, error),\n\tunique ...UniqueIndex[Row]) Row {\n\tif connection := database.bound(); connection != nil {\n\t\tupdated := PgQuery(connection, plan.SQL, plan.arguments(), scan)\n\t\tif len(updated) == 0 {\n\t\t\tpanic(\"updateAndReturnOne: no row matched\")\n\t\t}\n\t\treturn updated[0]\n\t}\n\treturn TableUpdateReturnOne(table, match, apply, unique...)\n}\n\n// DbDelete is `delete`.\nfunc DbDelete[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tplan PgPlan) struct{} {\n\tif connection := database.bound(); connection != nil {\n\t\tPgExec(connection, plan.SQL, plan.arguments())\n\t\treturn struct{}{}\n\t}\n\treturn TableDelete(table, match)\n}\n\n// DbTruncate empties an entity's store, for a test block that starts from an empty one.\nfunc DbTruncate[Row any](database *Database, table *Table[Row], tableName string) {\n\tif connection := database.bound(); connection != nil {\n\t\tPgTruncate(connection, tableName)\n\t\treturn\n\t}\n\tTableTruncate(table)\n}\n\n// DbDeleteResult is `deleteAndReturnResult`. On the server the count comes back from the\n// statement's own tag rather than from a second query, so the rows are walked once on either\n// path.\nfunc DbDeleteResult[Row any](database *Database, table *Table[Row], match func(Row) bool,\n\tplan PgPlan) DeleteResult {\n\tif connection := database.bound(); connection != nil {\n\t\tremoved := PgExec(connection, plan.SQL, plan.arguments())\n\t\tif removed == 0 {\n\t\t\treturn NoRowDeleted()\n\t\t}\n\t\treturn RowsDeleted(FromInt64(removed))\n\t}\n\treturn TableDeleteResult(table, match)\n}\n");
  ("postgres.go", "package teslrt\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"math/big\"\n\t\"os\"\n\t\"strconv\"\n\t\"strings\"\n\t\"sync\"\n\t\"time\"\n\n\t\"github.com/jackc/pgx/v5\"\n\t\"github.com/jackc/pgx/v5/pgconn\"\n\t\"github.com/jackc/pgx/v5/pgtype\"\n\t\"github.com/jackc/pgx/v5/pgxpool\"\n)\n\n// The PostgreSQL backend: `database D = Database { \226\128\166 backend: Postgres (\226\128\166) }`.\n//\n// This is the second approved non-stdlib dependency (`github.com/jackc/pgx/v5`), and it ships\n// only with a program that declares a Postgres database \226\128\148 a Memory-backed program still emits a\n// go.mod requiring nothing. Go has no Postgres driver in its standard library, so there is no\n// substitute available the way `crypto/hmac` was for MACs.\n//\n// WHAT THE COMPILER DOES AND WHAT THIS DOES. Every statement is built at COMPILE time: the\n// emitter turns a `select \226\128\166 where \226\128\166` into SQL text with `$1`-style placeholders plus the\n// arguments, because the query's shape is known statically and building it here would mean\n// re-deriving what the emitter already knows. So this file holds the connection, the schema\n// bootstrap, the value mapping, and the four executors \226\128\148 nothing that decides what a query says.\n//\n// THE COLUMN TYPES ARE THE RACKET RUNTIME'S, not a fresh choice. They have to be, or a table\n// created by one backend is unreadable by the other:\n//\n//\tInt          NUMERIC             arbitrary precision, lossless at any magnitude\n//\tInt32        INTEGER             the opt-in compact width\n//\tFloat        DOUBLE PRECISION\n//\tString       TEXT\n//\tBool         BOOLEAN\n//\tPosixMillis  BIGINT              the one deliberate exception to Int \226\134\146 NUMERIC\n//\n// A newtype takes its base's column type; a `Maybe X` column is X's type, nullable.\n//\n// WHICH PROGRAMS GET THIS FILE. Only one that declares a Postgres-backed database: the driver\n// and its dependency chain have no place in a binary that never opens a connection, the same\n// argument that gates the HTTP half. A declared database is still INERT until something\n// connects \226\128\148 `with database D` is the only thing that does \226\128\148 so a `test` block over a\n// Postgres-backed entity runs against the in-memory table with no server anywhere, which is\n// the dispatch `database.go` performs.\ntype PostgresConfig struct {\n\tDBName   string\n\tUser     string\n\tPassword string\n\tHost     string\n\tPort     int\n\t// SocketDir selects a Unix-socket connection (`SocketConnection`), which is how a local\n\t// cluster is usually reached; when it is set, Host and Port are ignored.\n\tSocketDir string\n\tSchema    string\n}\n\n// PostgresColumn describes one column, as the entity declares it.\ntype PostgresColumn struct {\n\tName       string\n\tType       string\n\tPrimaryKey bool\n\tNullable   bool\n}\n\n// PostgresTable is an entity's table, named as the entity names it.\ntype PostgresTable struct {\n\tName    string\n\tColumns []PostgresColumn\n\t// The declared UNIQUE indexes. A plain index is a performance hint with no observable\n\t// effect and is not created here; a unique one is an INVARIANT, and the two backends have\n\t// to agree about which programs run.\n\tUnique []PostgresIndex\n}\n\n// PostgresIndex is one declared index: the name `dsl/sql.rkt` derives (`<table>_<cols>_idx`,\n// or an explicit `as \"\226\128\166\"`) and the columns it covers. The NAME matters \226\128\148 a table shared with\n// the Racket backend must not end up with two indexes doing the same job.\ntype PostgresIndex struct {\n\tName    string\n\tColumns []string\n}\n\n// PostgresDB is a live pool plus the schema every statement is qualified with.\ntype PostgresDB struct {\n\tpool   *pgxpool.Pool\n\tschema string\n}\n\nvar postgresConnectOnce sync.Map // schema+dsn -> *PostgresDB\n\n// OpenPostgres connects, creates the schema and the declared tables if they are absent, and\n// answers a pool. It is idempotent per configuration: a program with several databases on one\n// cluster opens one pool per configuration rather than one per call site.\n//\n// The environment wins over the declaration for the password and the host, because those are\n// deployment facts rather than program facts: `PGPASSWORD`, `PGHOST` and `PGPORT` are the names\n// every other Postgres client already reads, so a deployment does not need a Tesl-specific one.\nfunc OpenPostgres(config PostgresConfig, tables []PostgresTable) *PostgresDB {\n\tdsn := postgresDSN(config)\n\tkey := config.Schema + \"\\x00\" + dsn\n\tif existing, found := postgresConnectOnce.Load(key); found {\n\t\tif db, ok := existing.(*PostgresDB); ok {\n\t\t\treturn db\n\t\t}\n\t}\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\tpool, err := pgxpool.New(ctx, dsn)\n\tif err != nil {\n\t\tpanic(\"database: cannot connect to PostgreSQL: \" + err.Error())\n\t}\n\tdb := &PostgresDB{pool: pool, schema: config.Schema}\n\tdb.bootstrap(ctx, tables)\n\tpostgresConnectOnce.Store(key, db)\n\treturn db\n}\n\n// PgPort reads a port out of the environment, falling back to the number the declaration\n// wrote. It is not `EnvInt`: a Tesl `Int` is unbounded and a port is an ordinary `int`, and a\n// value that is not a port at all is the declaration's fallback rather than a zero \226\128\148 a zero\n// port would silently become \"let the OS choose\", which is not what an unset variable means.\nfunc PgPort(name string, fallback int) int {\n\ttext := os.Getenv(name)\n\tif text == \"\" {\n\t\treturn fallback\n\t}\n\tparsed, err := strconv.Atoi(text)\n\tif err != nil || parsed <= 0 || parsed > 65535 {\n\t\treturn fallback\n\t}\n\treturn parsed\n}\n\nfunc postgresDSN(config PostgresConfig) string {\n\tsettings := []string{}\n\tadd := func(key, value string) {\n\t\tif value != \"\" {\n\t\t\tsettings = append(settings, key+\"=\"+value)\n\t\t}\n\t}\n\tadd(\"dbname\", config.DBName)\n\tadd(\"user\", config.User)\n\tpassword := config.Password\n\tif fromEnv := os.Getenv(\"PGPASSWORD\"); fromEnv != \"\" {\n\t\tpassword = fromEnv\n\t}\n\tadd(\"password\", password)\n\tif config.SocketDir != \"\" {\n\t\tadd(\"host\", config.SocketDir)\n\t} else {\n\t\thost := config.Host\n\t\tif fromEnv := os.Getenv(\"PGHOST\"); fromEnv != \"\" {\n\t\t\thost = fromEnv\n\t\t}\n\t\tadd(\"host\", host)\n\t\tport := config.Port\n\t\tif fromEnv := os.Getenv(\"PGPORT\"); fromEnv != \"\" {\n\t\t\tif parsed, err := strconv.Atoi(fromEnv); err == nil {\n\t\t\t\tport = parsed\n\t\t\t}\n\t\t}\n\t\tif port > 0 {\n\t\t\tadd(\"port\", strconv.Itoa(port))\n\t\t}\n\t}\n\treturn strings.Join(settings, \" \")\n}\n\n// bootstrap creates the schema and any missing table. `if not exists` throughout: a second\n// process starting at the same time must not fail, and an EXISTING table is left exactly as it\n// is \226\128\148 this is not a migration tool, and silently altering a live table would be worse than\n// refusing to.\nfunc (db *PostgresDB) bootstrap(ctx context.Context, tables []PostgresTable) {\n\tif db.schema != \"\" {\n\t\tif _, err := db.pool.Exec(ctx,\n\t\t\t\"create schema if not exists \"+quoteIdentifier(db.schema)); err != nil {\n\t\t\tpanic(\"database: cannot create schema \" + db.schema + \": \" + err.Error())\n\t\t}\n\t}\n\tfor _, table := range tables {\n\t\tdefinitions := make([]string, 0, len(table.Columns))\n\t\tfor _, column := range table.Columns {\n\t\t\tdefinition := quoteIdentifier(column.Name) + \" \" + column.Type\n\t\t\tswitch {\n\t\t\tcase column.PrimaryKey:\n\t\t\t\tdefinition += \" PRIMARY KEY\"\n\t\t\tcase !column.Nullable:\n\t\t\t\tdefinition += \" NOT NULL\"\n\t\t\t}\n\t\t\tdefinitions = append(definitions, definition)\n\t\t}\n\t\tstatement := fmt.Sprintf(\"create table if not exists %s (%s)\",\n\t\t\tdb.QualifiedTable(table.Name), strings.Join(definitions, \", \"))\n\t\tif _, err := db.pool.Exec(ctx, statement); err != nil {\n\t\t\tpanic(\"database: cannot create table \" + table.Name + \": \" + err.Error())\n\t\t}\n\t\tfor _, index := range table.Unique {\n\t\t\tcolumns := make([]string, 0, len(index.Columns))\n\t\t\tfor _, column := range index.Columns {\n\t\t\t\tcolumns = append(columns, quoteIdentifier(column))\n\t\t\t}\n\t\t\tindexStatement := fmt.Sprintf(\"create unique index if not exists %s on %s (%s)\",\n\t\t\t\tquoteIdentifier(index.Name), db.QualifiedTable(table.Name),\n\t\t\t\tstrings.Join(columns, \", \"))\n\t\t\tif _, err := db.pool.Exec(ctx, indexStatement); err != nil {\n\t\t\t\tpanic(\"database: cannot create unique index \" + index.Name + \": \" + err.Error())\n\t\t\t}\n\t\t}\n\t}\n}\n\n// QualifiedTable is `\"schema\".\"table\"`, quoted so a name that needs quoting works and a name\n// that does not is unchanged.\nfunc (db *PostgresDB) QualifiedTable(table string) string {\n\tif db.schema == \"\" {\n\t\treturn quoteIdentifier(table)\n\t}\n\treturn quoteIdentifier(db.schema) + \".\" + quoteIdentifier(table)\n}\n\n// quoteIdentifier doubles any embedded quote, which is the only escape a quoted SQL identifier\n// has. Identifiers here come from the PROGRAM (an entity's declared table name), never from a\n// request, but quoting is what keeps that true if one ever does.\nfunc quoteIdentifier(name string) string {\n\treturn `\"` + strings.ReplaceAll(name, `\"`, `\"\"`) + `\"`\n}\n\n// \226\148\128\226\148\128 Executing \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Four executors, because a Tesl query is one of four shapes. Each takes SQL the EMITTER built,\n// so nothing here decides what a query says.\n\n// Every statement goes through `executor` rather than through the pool directly: inside a\n// `transaction { \226\128\166 }` the statements have to share ONE connection, and the transaction is the\n// only thing that knows which. Both *pgxpool.Pool and pgx.Tx answer this shape, so the choice\n// is made once, here, rather than at each of the executors below.\ntype pgExecutor interface {\n\tQuery(ctx context.Context, sql string, arguments ...any) (pgx.Rows, error)\n\tQueryRow(ctx context.Context, sql string, arguments ...any) pgx.Row\n\tExec(ctx context.Context, sql string, arguments ...any) (pgconn.CommandTag, error)\n}\n\nfunc (db *PostgresDB) executor() pgExecutor {\n\tif transaction := currentTransaction(); transaction != nil {\n\t\treturn transaction\n\t}\n\treturn db.pool\n}\n\n// PgQuery runs a select and scans every row through `scan`.\nfunc PgQuery[Row any](db *PostgresDB, statement string, arguments []any,\n\tscan func(pgx.CollectableRow) (Row, error)) []Row {\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\trows, err := db.executor().Query(ctx, statement, arguments...)\n\tif err != nil {\n\t\tpanic(\"database: \" + err.Error())\n\t}\n\tcollected, err := pgx.CollectRows(rows, scan)\n\tif err != nil {\n\t\tpanic(\"database: \" + err.Error())\n\t}\n\treturn collected\n}\n\n// PgQueryOne is `selectOne`: the first row, or Nothing. `limit 1` is the emitter's job, so this\n// only decides what \"no row\" means.\nfunc PgQueryOne[Row any](db *PostgresDB, statement string, arguments []any,\n\tscan func(pgx.CollectableRow) (Row, error)) Maybe[Row] {\n\trows := PgQuery(db, statement, arguments, scan)\n\tif len(rows) == 0 {\n\t\treturn Nothing[Row]()\n\t}\n\treturn Something(rows[0])\n}\n\n// PgExec runs a statement that answers no rows, and reports how many it touched.\nfunc PgExec(db *PostgresDB, statement string, arguments []any) int64 {\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\ttag, err := db.executor().Exec(ctx, statement, arguments...)\n\tif err != nil {\n\t\tpanic(\"database: \" + err.Error())\n\t}\n\treturn tag.RowsAffected()\n}\n\n// PgCount is `count \226\128\166 from \226\128\166`: one row, one number.\nfunc PgCount(db *PostgresDB, statement string, arguments []any) Int {\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\trow := db.executor().QueryRow(ctx, statement, arguments...)\n\tvar counted int64\n\tif err := row.Scan(&counted); err != nil {\n\t\tpanic(\"database: \" + err.Error())\n\t}\n\treturn FromInt64(counted)\n}\n\n// PgScalar is the aggregate executor: one row, one value, scanned by the emitter's own reader\n// because the column's type is the entity's rather than something this file can know.\nfunc PgScalar[Value any](db *PostgresDB, statement string, arguments []any,\n\tscan func(pgx.Row) (Value, error)) Value {\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\tvalue, err := scan(db.executor().QueryRow(ctx, statement, arguments...))\n\tif err != nil {\n\t\tpanic(\"database: \" + err.Error())\n\t}\n\treturn value\n}\n\n// PgSumMoney is `selectSum` over a Money column. The statement answers the total, the number of\n// DISTINCT currencies and one witness code in a single row, because the two refusals need those\n// facts and a second query could see a different set of rows than the sum did.\n//\n// A stored code that is not an ISO 4217 currency is data corruption or a schema written by an\n// incompatible build, and it traps rather than decoding into a half-formed Money \226\128\148 Racket's\n// `money-stored-currency` takes the same line.\nfunc PgSumMoney(db *PostgresDB, statement string, arguments []any, entity, field string) Money {\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\tvar total pgtype.Numeric\n\tvar distinct int64\n\tvar witness *string\n\tif err := db.executor().QueryRow(ctx, statement, arguments...).\n\t\tScan(&total, &distinct, &witness); err != nil {\n\t\tpanic(\"database: \" + err.Error())\n\t}\n\tcurrency := Currency{}\n\tif witness != nil {\n\t\tknown := CurrencyFromCode(*witness)\n\t\tif !known.IsSomething() {\n\t\t\tpanic(\"field \" + field + \" on entity \" + entity + \": stored currency code \" + *witness +\n\t\t\t\t\" is not a known ISO 4217 currency \226\128\148 the column holds corrupt data or was\" +\n\t\t\t\t\" written by an incompatible schema\")\n\t\t}\n\t\tcurrency = known.SomethingValue\n\t}\n\treturn MoneySumResult(entity, field, PgIntOf(total), currency, int(distinct))\n}\n\n// PgTruncate empties a table, for a test block that starts from an empty store. It is the\n// Postgres counterpart of `TableTruncate`, and it exists for the same reason: one test block's\n// rows must not be another's.\nfunc PgTruncate(db *PostgresDB, table string) {\n\tctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n\tdefer cancel()\n\tif _, err := db.executor().Exec(ctx, \"truncate table \"+db.QualifiedTable(table)); err != nil {\n\t\t// A table that does not exist yet is not an error here: a test may run before anything\n\t\t// created it, and the bootstrap is what creates it.\n\t\tif !strings.Contains(err.Error(), \"does not exist\") {\n\t\t\tpanic(\"database: cannot truncate \" + table + \": \" + err.Error())\n\t\t}\n\t}\n}\n\n// \226\148\128\226\148\128 Values \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// An `Int` is arbitrary precision on both sides \226\128\148 Tesl's integers are unbounded and the column is\n// NUMERIC \226\128\148 so it travels as a pgtype.Numeric rather than through int64, which would silently\n// fail exactly where the arbitrary precision matters.\n\n// PgInt binds an Int as a NUMERIC parameter. It goes through the decimal TEXT of the value\n// rather than through int64, because a Tesl Int is unbounded and int64 is exactly where that\n// stops being true.\nfunc PgInt(value Int) pgtype.Numeric {\n\tparsed, ok := new(big.Int).SetString(value.String(), 10)\n\tif !ok {\n\t\tpanic(\"database: \" + value.String() + \" is not an integer\")\n\t}\n\treturn pgtype.Numeric{Int: parsed, Exp: 0, Valid: true}\n}\n\n// PgIntOf reads a NUMERIC (or any integer column) back into an Int.\nfunc PgIntOf(value pgtype.Numeric) Int {\n\tif !value.Valid || value.Int == nil {\n\t\treturn FromInt64(0)\n\t}\n\tif value.Exp == 0 {\n\t\treturn fromBig(new(big.Int).Set(value.Int))\n\t}\n\t// A scale the column carries but a Tesl Int cannot: the value is not an integer, and\n\t// answering a truncated one would be a silent wrong answer.\n\tif value.Exp > 0 {\n\t\tscaled := new(big.Int).Set(value.Int)\n\t\tten := big.NewInt(10)\n\t\tfor range int(value.Exp) {\n\t\t\tscaled.Mul(scaled, ten)\n\t\t}\n\t\treturn fromBig(scaled)\n\t}\n\tpanic(\"database: a NUMERIC column holds a fractional value where Tesl expects an Int\")\n}\n\n// PgNull binds a `Maybe X` column: Nothing is SQL NULL, Something is the bound value. The\n// binding of the inner value is the caller's, because only the emitter knows the column's type.\nfunc PgNull[T any](value Maybe[T], bind func(T) any) any {\n\tif !value.IsSomething() {\n\t\treturn nil\n\t}\n\treturn bind(value.SomethingValue)\n}\n\n// MaybeOfPointer reads a nullable column back: a NULL is Nothing, and anything else is decoded\n// by the caller's reader. The reader is a thunk rather than a value so the decode is not run on\n// the NULL \226\128\148 dereferencing the carrier there would panic.\nfunc MaybeOfPointer[Carrier any, Value any](carrier *Carrier, decode func() Value) Maybe[Value] {\n\tif carrier == nil {\n\t\treturn Nothing[Value]()\n\t}\n\treturn Something(decode())\n}\n\n// PgBigint binds a PosixMillis-shaped value, whose column is BIGINT.\nfunc PgBigint(value Int) int64 {\n\texact, ok := value.Int64()\n\tif !ok {\n\t\tpanic(\"database: \" + value.String() + \" does not fit a BIGINT column\")\n\t}\n\treturn exact\n}\n");
  ("units.go", "package teslrt\n\nimport (\n\t\"math\"\n\t\"math/big\"\n)\n\n// `Tesl.Units` \226\128\148 dimensioned quantities.\n//\n// A quantity ERASES to a plain float64: the DIMENSION lives in the compiler's type layer, which\n// is where `meters / seconds = meters per second` is checked and where adding a length to a mass\n// is rejected. Nothing about that survives to run time, so everything here is ordinary\n// arithmetic \226\128\148 the value is already in SI-canonical units by the time it gets here.\n//\n// The per-unit conversions live in units_data.go, generated from `tesl/units.rkt` so the factors\n// have ONE source. What is written by hand is what is not a factor: the affine temperature\n// scales, the polymorphic operations, and the non-zero check.\n\n// Temperature is AFFINE, not a scale factor: 0 \194\176C is 273.15 K, so a Celsius value cannot be\n// converted by multiplying. The resulting quantity is absolute kelvin.\n//\n// (Adding two absolute temperatures type-checks \226\128\148 same dimension \226\128\148 and is rarely meaningful;\n// the manual says so, and the type system deliberately does not try to encode the difference\n// between an absolute temperature and a temperature INTERVAL.)\nfunc TemperatureKelvin(value float64) float64 { return value }\n\nfunc TemperatureCelsius(value float64) float64 { return value + 273.15 }\n\nfunc TemperatureFahrenheit(value float64) float64 {\n\treturn (value-32.0)*(5.0/9.0) + 273.15\n}\n\nfunc TemperatureInKelvin(quantity float64) float64 { return quantity }\n\nfunc TemperatureInCelsius(quantity float64) float64 { return quantity - 273.15 }\n\nfunc TemperatureInFahrenheit(quantity float64) float64 {\n\treturn (quantity-273.15)*1.8 + 32.0\n}\n\n// The polymorphic dimension operations. Each application site is dimension-checked by the\n// compiler \226\128\148 `Units.mul` on a length and a length is an area there \226\128\148 so at run time they are\n// the obvious float operations.\n\nfunc UnitsMul(left, right float64) float64 { return left * right }\n\nfunc UnitsDiv(left, right float64) float64 { return left / right }\n\nfunc UnitsSquare(value float64) float64 { return value * value }\n\nfunc UnitsSqrt(value float64) float64 { return math.Sqrt(value) }\n\nfunc UnitsAbs(value float64) float64 { return math.Abs(value) }\n\nfunc UnitsNegate(value float64) float64 { return -value }\n\nfunc UnitsMin(left, right float64) float64 { return math.Min(left, right) }\n\nfunc UnitsMax(left, right float64) float64 { return math.Max(left, right) }\n\nfunc UnitsSum(values []float64) float64 {\n\ttotal := 0.0\n\tfor _, value := range values {\n\t\ttotal += value\n\t}\n\treturn total\n}\n\n// DurationToMillis and DurationFromMillis bridge a typed Duration and the exact-Int\n// milliseconds `PosixMillis` arithmetic speaks. `toMillis` rounds HALF-EVEN on the exact\n// rational \226\128\148 the Money stance, and the reason a duration that lands exactly between two\n// milliseconds does not drift upward over a long series.\n//\n// The conversion goes through the float's BINARY value, not its shortest decimal \226\128\148 the\n// opposite of what the Money paths do, and deliberately so: `tesl/units.rkt` writes\n// `inexact->exact` here while `tesl/money.rkt` exactifies decimal-faithfully. The difference\n// is observable, so it is matched rather than tidied: 0.0025 s is a hair ABOVE 2.5 ms in\n// binary, so it rounds to 3, where the decimal reading would tie and round to the even 2.\nfunc DurationToMillis(quantity float64) Int {\n\texact := new(big.Rat).SetFloat64(quantity)\n\tif exact == nil {\n\t\t// NaN and the infinities have no exact value to round; a duration that is one of\n\t\t// them is a bug upstream, and answering some millisecond count would hide it.\n\t\tpanic(\"Duration.toMillis: expected a finite duration\")\n\t}\n\tscaled := new(big.Rat).Mul(exact, big.NewRat(1000, 1))\n\treturn fromBig(roundHalfEven(scaled))\n}\n\nfunc DurationFromMillis(millis Int) float64 {\n\tvalue, _ := new(big.Rat).SetInt(millis.bigInt()).Float64()\n\treturn value / 1000.0\n}\n\n// UnitsRequireNonZero is the divisor check: quantities erase to floats, so the SAME\n// `FloatNonZero` predicate that guards float division guards quantity division \226\128\148 `d / t`\n// demands a non-zero divisor proof exactly like every other `/`.\n//\n// The 422 is Racket's: a zero divisor is a value the caller sent, not a broken server.\nfunc UnitsRequireNonZero(quantity float64) Check[float64] {\n\tif quantity == 0 {\n\t\treturn Reject[float64](422, \"expected a non-zero quantity\")\n\t}\n\treturn Accept(quantity)\n}\n");
  ("units_data.go", "package teslrt\n\n// Generated by scripts/gen-go-units.py from tesl/units.rkt.\n// DO NOT EDIT BY HAND \226\128\148 regenerate and commit when a conversion factor changes.\n//\n// A dimensioned quantity ERASES to a plain float64: the dimension lives in the\n// compiler's type layer, so every function here is ordinary arithmetic. A CONSTRUCTOR\n// converts a value in the named unit to the SI-canonical magnitude (meters, kilograms,\n// seconds, \226\128\166); an ACCESSOR converts back. The factors are the Racket runtime's, read\n// from the file that owns them.\n\nfunc LengthMeters(value float64) float64                            { return value * 1.0 }\nfunc LengthKilometers(value float64) float64                        { return value * 1000.0 }\nfunc LengthCentimeters(value float64) float64                       { return value * 0.01 }\nfunc LengthMillimeters(value float64) float64                       { return value * 0.001 }\nfunc LengthMiles(value float64) float64                             { return value * 1609.344 }\nfunc LengthFeet(value float64) float64                              { return value * 0.3048 }\nfunc LengthInches(value float64) float64                            { return value * 0.0254 }\nfunc LengthYards(value float64) float64                             { return value * 0.9144 }\nfunc LengthNauticalMiles(value float64) float64                     { return value * 1852.0 }\nfunc LengthInMeters(quantity float64) float64                       { return quantity / 1.0 }\nfunc LengthInKilometers(quantity float64) float64                   { return quantity / 1000.0 }\nfunc LengthInCentimeters(quantity float64) float64                  { return quantity / 0.01 }\nfunc LengthInMillimeters(quantity float64) float64                  { return quantity / 0.001 }\nfunc LengthInMiles(quantity float64) float64                        { return quantity / 1609.344 }\nfunc LengthInFeet(quantity float64) float64                         { return quantity / 0.3048 }\nfunc LengthInInches(quantity float64) float64                       { return quantity / 0.0254 }\nfunc LengthInYards(quantity float64) float64                        { return quantity / 0.9144 }\nfunc LengthInNauticalMiles(quantity float64) float64                { return quantity / 1852.0 }\nfunc MassKilograms(value float64) float64                           { return value * 1.0 }\nfunc MassGrams(value float64) float64                               { return value * 0.001 }\nfunc MassMilligrams(value float64) float64                          { return value * 0.000001 }\nfunc MassTonnes(value float64) float64                              { return value * 1000.0 }\nfunc MassPounds(value float64) float64                              { return value * 0.45359237 }\nfunc MassOunces(value float64) float64                              { return value * 0.028349523125 }\nfunc MassInKilograms(quantity float64) float64                      { return quantity / 1.0 }\nfunc MassInGrams(quantity float64) float64                          { return quantity / 0.001 }\nfunc MassInMilligrams(quantity float64) float64                     { return quantity / 0.000001 }\nfunc MassInTonnes(quantity float64) float64                         { return quantity / 1000.0 }\nfunc MassInPounds(quantity float64) float64                         { return quantity / 0.45359237 }\nfunc MassInOunces(quantity float64) float64                         { return quantity / 0.028349523125 }\nfunc DurationSeconds(value float64) float64                         { return value * 1.0 }\nfunc DurationMilliseconds(value float64) float64                    { return value * 0.001 }\nfunc DurationMinutes(value float64) float64                         { return value * 60.0 }\nfunc DurationHours(value float64) float64                           { return value * 3600.0 }\nfunc DurationDays(value float64) float64                            { return value * 86400.0 }\nfunc DurationInSeconds(quantity float64) float64                    { return quantity / 1.0 }\nfunc DurationInMilliseconds(quantity float64) float64               { return quantity / 0.001 }\nfunc DurationInMinutes(quantity float64) float64                    { return quantity / 60.0 }\nfunc DurationInHours(quantity float64) float64                      { return quantity / 3600.0 }\nfunc DurationInDays(quantity float64) float64                       { return quantity / 86400.0 }\nfunc SpeedMetersPerSecond(value float64) float64                    { return value * 1.0 }\nfunc SpeedKilometersPerHour(value float64) float64                  { return value * 0.2777777777777778 }\nfunc SpeedMilesPerHour(value float64) float64                       { return value * 0.44704 }\nfunc SpeedKnots(value float64) float64                              { return value * 0.5144444444444445 }\nfunc SpeedInMetersPerSecond(quantity float64) float64               { return quantity / 1.0 }\nfunc SpeedInKilometersPerHour(quantity float64) float64             { return quantity / 0.2777777777777778 }\nfunc SpeedInMilesPerHour(quantity float64) float64                  { return quantity / 0.44704 }\nfunc SpeedInKnots(quantity float64) float64                         { return quantity / 0.5144444444444445 }\nfunc AccelerationMetersPerSecondSquared(value float64) float64      { return value * 1.0 }\nfunc AccelerationInMetersPerSecondSquared(quantity float64) float64 { return quantity / 1.0 }\nfunc AreaSquareMeters(value float64) float64                        { return value * 1.0 }\nfunc AreaSquareKilometers(value float64) float64                    { return value * 1000000.0 }\nfunc AreaHectares(value float64) float64                            { return value * 10000.0 }\nfunc AreaSquareFeet(value float64) float64                          { return value * 0.09290304 }\nfunc AreaAcres(value float64) float64                               { return value * 4046.8564224 }\nfunc AreaInSquareMeters(quantity float64) float64                   { return quantity / 1.0 }\nfunc AreaInSquareKilometers(quantity float64) float64               { return quantity / 1000000.0 }\nfunc AreaInHectares(quantity float64) float64                       { return quantity / 10000.0 }\nfunc AreaInSquareFeet(quantity float64) float64                     { return quantity / 0.09290304 }\nfunc AreaInAcres(quantity float64) float64                          { return quantity / 4046.8564224 }\nfunc VolumeCubicMeters(value float64) float64                       { return value * 1.0 }\nfunc VolumeLiters(value float64) float64                            { return value * 0.001 }\nfunc VolumeMilliliters(value float64) float64                       { return value * 0.000001 }\nfunc VolumeGallons(value float64) float64                           { return value * 0.003785411784 }\nfunc VolumeInCubicMeters(quantity float64) float64                  { return quantity / 1.0 }\nfunc VolumeInLiters(quantity float64) float64                       { return quantity / 0.001 }\nfunc VolumeInMilliliters(quantity float64) float64                  { return quantity / 0.000001 }\nfunc VolumeInGallons(quantity float64) float64                      { return quantity / 0.003785411784 }\nfunc ForceNewtons(value float64) float64                            { return value * 1.0 }\nfunc ForceInNewtons(quantity float64) float64                       { return quantity / 1.0 }\nfunc EnergyJoules(value float64) float64                            { return value * 1.0 }\nfunc EnergyKilojoules(value float64) float64                        { return value * 1000.0 }\nfunc EnergyKilowattHours(value float64) float64                     { return value * 3600000.0 }\nfunc EnergyCalories(value float64) float64                          { return value * 4.184 }\nfunc EnergyInJoules(quantity float64) float64                       { return quantity / 1.0 }\nfunc EnergyInKilojoules(quantity float64) float64                   { return quantity / 1000.0 }\nfunc EnergyInKilowattHours(quantity float64) float64                { return quantity / 3600000.0 }\nfunc EnergyInCalories(quantity float64) float64                     { return quantity / 4.184 }\nfunc PowerWatts(value float64) float64                              { return value * 1.0 }\nfunc PowerKilowatts(value float64) float64                          { return value * 1000.0 }\nfunc PowerHorsepower(value float64) float64                         { return value * 745.6998715822702 }\nfunc PowerInWatts(quantity float64) float64                         { return quantity / 1.0 }\nfunc PowerInKilowatts(quantity float64) float64                     { return quantity / 1000.0 }\nfunc PowerInHorsepower(quantity float64) float64                    { return quantity / 745.6998715822702 }\nfunc FrequencyHertz(value float64) float64                          { return value * 1.0 }\nfunc FrequencyKilohertz(value float64) float64                      { return value * 1000.0 }\nfunc FrequencyInHertz(quantity float64) float64                     { return quantity / 1.0 }\nfunc FrequencyInKilohertz(quantity float64) float64                 { return quantity / 1000.0 }\nfunc PressurePascals(value float64) float64                         { return value * 1.0 }\nfunc PressureKilopascals(value float64) float64                     { return value * 1000.0 }\nfunc PressureBar(value float64) float64                             { return value * 100000.0 }\nfunc PressureInPascals(quantity float64) float64                    { return quantity / 1.0 }\nfunc PressureInKilopascals(quantity float64) float64                { return quantity / 1000.0 }\nfunc PressureInBar(quantity float64) float64                        { return quantity / 100000.0 }\n");
  ("password.go", "package teslrt\n\nimport (\n\t\"crypto/rand\"\n\t\"crypto/subtle\"\n\t\"encoding/base64\"\n\t\"fmt\"\n\t\"strconv\"\n\t\"strings\"\n\t\"sync\"\n\n\t\"golang.org/x/crypto/argon2\"\n)\n\n// Password storage: Argon2id, in the SAME encoding the Racket runtime writes.\n//\n// This is the one place the emitted runtime takes a dependency outside Go's standard library\n// (`golang.org/x/crypto/argon2`), and it is a deliberate, approved decision rather than a\n// convenience: Racket hashes with libsodium's Argon2id, and the two alternatives are both\n// worse. Substituting stdlib PBKDF2 would mint hashes the Racket side cannot verify, so a\n// shared database becomes a silent lockout on migration; hand-writing Argon2id would put\n// hand-rolled cryptography in the runtime. The dependency ships ONLY with this file, which is\n// ONLY emitted for a program that stores passwords, so nothing else carries it.\n//\n// The encoding is the PHC string libsodium's `crypto_pwhash_str` produces at its INTERACTIVE\n// parameters:\n//\n//\t$argon2id$v=19$m=65536,t=2,p=1$<salt>$<hash>\n//\n// with standard-alphabet base64 and no padding, a 16-byte salt and a 32-byte tag. A hash\n// written by either backend therefore verifies on both \226\128\148 which is the whole point, and what a\n// test asserts against a real libsodium string.\nconst (\n\targonMemoryKiB = 64 * 1024 // libsodium crypto_pwhash_MEMLIMIT_INTERACTIVE, in KiB\n\targonTime      = 2         // libsodium crypto_pwhash_OPSLIMIT_INTERACTIVE\n\targonThreads   = 1\n\targonSaltBytes = 16\n\targonKeyBytes  = 32\n\n\t// A password longer than this is refused rather than hashed: hashing is deliberately\n\t// expensive, so an unbounded input is a denial-of-service amplifier. Same limit as the\n\t// Racket runtime's.\n\tmaxPasswordBytes = 1024\n)\n\n// PasswordHash is opaque to Tesl: it holds the PHC string, and a program can store it, verify\n// against it, or ask whether it needs re-minting \226\128\148 never read it apart.\ntype PasswordHash struct {\n\tValue string\n}\n\nfunc requirePasswordLength(who string, plaintext SecretString) {\n\tif len(plaintext.Reveal()) > maxPasswordBytes {\n\t\t// A rejection at the request boundary, not a crash: the input came from a client, and\n\t\t// inside a handler this answers 400 the way any other check rejection does.\n\t\tpanic(RequestRejection{\n\t\t\tStatus: 400,\n\t\t\tMessage: fmt.Sprintf(\n\t\t\t\t\"%s: password is too long: %d bytes, maximum is %d. Reject over-long input at \"+\n\t\t\t\t\t\"the request boundary; hashing it is a denial-of-service amplifier.\",\n\t\t\t\twho, len(plaintext.Reveal()), maxPasswordBytes),\n\t\t})\n\t}\n}\n\n// HashPassword is `Crypto.hashPassword`: a fresh random salt, then Argon2id at the interactive\n// parameters. The `::: HashFor plaintext` half of its Tesl type is compile-time only \226\128\148 the fact\n// is about the argument, and proofs erase \226\128\148 so what it buys is that storing the WRONG\n// password's hash does not compile.\n//\n// The plaintext arrives as a SecretString so that the emitted CALL SITE never handles it as an\n// ordinary string: a `secret Password = String` hands over its payload, and a plain String is\n// wrapped on the way in. The plaintext therefore exists only inside this file.\nfunc HashPassword(plaintext SecretString) PasswordHash {\n\trequirePasswordLength(\"Crypto.hashPassword\", plaintext)\n\tsalt := make([]byte, argonSaltBytes)\n\tif _, err := rand.Read(salt); err != nil {\n\t\tpanic(\"Crypto.hashPassword: no source of randomness: \" + err.Error())\n\t}\n\treturn PasswordHash{Value: encodeArgon2id(salt,\n\t\targon2.IDKey([]byte(plaintext.Reveal()), salt, argonTime, argonMemoryKiB, argonThreads,\n\t\t\targonKeyBytes))}\n}\n\nfunc encodeArgon2id(salt, key []byte) string {\n\traw := base64.RawStdEncoding\n\treturn fmt.Sprintf(\"$argon2id$v=%d$m=%d,t=%d,p=%d$%s$%s\",\n\t\targon2.Version, argonMemoryKiB, argonTime, argonThreads,\n\t\traw.EncodeToString(salt), raw.EncodeToString(key))\n}\n\n// argon2Params is what a stored PHC string says it was minted with.\ntype argon2Params struct {\n\tmemoryKiB uint32\n\ttime      uint32\n\tthreads   uint8\n\tsalt      []byte\n\tkey       []byte\n}\n\n// parseArgon2id reads a stored hash. Anything that is not an Argon2id v19 PHC string \226\128\148 a\n// foreign hash, a scheme that has been dropped, truncated text \226\128\148 fails to parse, and every\n// caller treats that as \"does not verify\" and \"needs re-minting\".\nfunc parseArgon2id(stored string) (argon2Params, bool) {\n\tfields := strings.Split(stored, \"$\")\n\t// \"\", \"argon2id\", \"v=19\", \"m=\226\128\166,t=\226\128\166,p=\226\128\166\", salt, key\n\tif len(fields) != 6 || fields[0] != \"\" || fields[1] != \"argon2id\" {\n\t\treturn argon2Params{}, false\n\t}\n\tif fields[2] != \"v=\"+strconv.Itoa(argon2.Version) {\n\t\treturn argon2Params{}, false\n\t}\n\tvar memory, time uint64\n\tvar threads uint64\n\tfor _, setting := range strings.Split(fields[3], \",\") {\n\t\tname, value, found := strings.Cut(setting, \"=\")\n\t\tif !found {\n\t\t\treturn argon2Params{}, false\n\t\t}\n\t\tparsed, err := strconv.ParseUint(value, 10, 32)\n\t\tif err != nil {\n\t\t\treturn argon2Params{}, false\n\t\t}\n\t\tswitch name {\n\t\tcase \"m\":\n\t\t\tmemory = parsed\n\t\tcase \"t\":\n\t\t\ttime = parsed\n\t\tcase \"p\":\n\t\t\tthreads = parsed\n\t\tdefault:\n\t\t\treturn argon2Params{}, false\n\t\t}\n\t}\n\tif memory == 0 || time == 0 || threads == 0 || threads > 255 {\n\t\treturn argon2Params{}, false\n\t}\n\traw := base64.RawStdEncoding\n\tsalt, saltErr := raw.DecodeString(fields[4])\n\tkey, keyErr := raw.DecodeString(fields[5])\n\tif saltErr != nil || keyErr != nil || len(salt) == 0 {\n\t\treturn argon2Params{}, false\n\t}\n\t// The tag length is FIXED at 32 bytes: that is what libsodium's `crypto_pwhash_str` writes,\n\t// and pinning it keeps the recomputation below free of a length taken from the stored string\n\t// (which would be an attacker-influenced integer conversion). A hash with any other tag\n\t// length is treated as unparseable \226\128\148 fail closed, and `needsRehash` says to re-mint it.\n\tif len(key) != argonKeyBytes {\n\t\treturn argon2Params{}, false\n\t}\n\treturn argon2Params{\n\t\tmemoryKiB: uint32(memory), time: uint32(time), threads: uint8(threads),\n\t\tsalt: salt, key: key,\n\t}, true\n}\n\nfunc verifyArgon2id(stored, candidate string) bool {\n\tparams, ok := parseArgon2id(stored)\n\tif !ok {\n\t\treturn false\n\t}\n\tcomputed := argon2.IDKey([]byte(candidate), params.salt, params.time, params.memoryKiB,\n\t\tparams.threads, argonKeyBytes)\n\treturn subtle.ConstantTimeCompare(computed, params.key) == 1\n}\n\n// timingEqualiser is a real hash of a random password at the CURRENT parameters, computed once\n// and reused. It exists so that verifying against a MISSING account costs the same as verifying\n// a wrong password \226\128\148 otherwise a login endpoint answers in microseconds for an unknown address\n// and in ~80ms for a known one, which enumerates the user table. Generated rather than\n// hardcoded so it cannot drift from the live parameters.\nvar (\n\ttimingEqualiserOnce sync.Once\n\ttimingEqualiserPHC  string\n)\n\nfunc timingEqualiser() string {\n\ttimingEqualiserOnce.Do(func() {\n\t\tfiller := make([]byte, 32)\n\t\tif _, err := rand.Read(filler); err != nil {\n\t\t\tpanic(\"Crypto.checkPassword: no source of randomness: \" + err.Error())\n\t\t}\n\t\ttimingEqualiserPHC = HashPassword(MakeSecret(string(filler))).Value\n\t})\n\treturn timingEqualiserPHC\n}\n\n// CheckPassword is `Crypto.checkPassword`. It takes a `Maybe PasswordHash` deliberately: a\n// missing account and a wrong password have to cost the same and answer the same, so `Nothing`\n// still performs a full verification \226\128\148 against the timing equaliser \226\128\148 and then fails with the\n// identical message. The caller does not have to know that this problem exists, which is the\n// point.\nfunc CheckPassword(stored Maybe[PasswordHash], candidate SecretString) Check[Maybe[PasswordHash]] {\n\trequirePasswordLength(\"Crypto.checkPassword\", candidate)\n\thash, present := stored.Value()\n\tphc := timingEqualiser()\n\tif present {\n\t\tphc = hash.Value\n\t}\n\tverified := verifyArgon2id(phc, candidate.Reveal())\n\tif !verified || !present {\n\t\treturn Reject[Maybe[PasswordHash]](401, \"invalid credentials\")\n\t}\n\treturn Accept(stored)\n}\n\n// NeedsRehash is `Crypto.needsRehash`: true when the stored hash was minted with weaker\n// parameters than the current ones, or in a format this runtime cannot parse at all (a foreign\n// hash, or one from a scheme that has been dropped) \226\128\148 in both cases the right move on the next\n// successful login is to re-mint, so both answer true.\n//\n// Tesl deliberately does NOT perform the rehash: a crypto function writing to the database\n// would be a hidden effect, and it is one explicit line in the application.\nfunc NeedsRehash(stored PasswordHash) bool {\n\tparams, ok := parseArgon2id(stored.Value)\n\tif !ok {\n\t\treturn true\n\t}\n\treturn params.memoryKiB < argonMemoryKiB || params.time < argonTime ||\n\t\tparams.threads != argonThreads\n}\n");
  ("prop.go", "package teslrt\n\nimport (\n\t\"fmt\"\n)\n\n// Property tests: `property \"name\" (n: Int) { \226\128\166 }` inside a `test` block, run many times over\n// generated values.\n//\n// The generators are `dsl/test-support.rkt`'s, value for value, because a property that holds\n// on one backend and fails on the other would otherwise be a difference in what was GENERATED\n// rather than in what the program does:\n//\n//\tInt      uniform in [-1_000_000, 1_000_000]\n//\tBool     a coin flip\n//\tString   \"s\" followed by a number below 1_000_000 \226\128\148 short, printable, and never empty\n//\tList T   0 to 7 elements, each generated by T's own generator\n//\tMaybe T  Nothing or Something, evenly\n//\n// A type with no generator is REFUSED at emission rather than generated as a zero: Racket's\n// fallback produces `0` for anything it does not know, and a property that \"passes\" over 200\n// copies of the same wrong-typed value tests nothing at all.\n//\n// Randomness is unseeded, as Racket's is: a property test is a search for a counterexample,\n// not a fixture, and the same seed every run would search the same 200 points forever. The\n// source is `crypto/rand` through `RandomInt`, for the reason the rest of this runtime uses it\n// \226\128\148 no Tesl surface can seed a generator, so there is nothing to gain from a weak one, and\n// gosec flags math/rand wherever it appears.\n\n// PropRandom is Racket's `tesl-prop-random`: a value in [0, bound). It is also what a custom\n// generator receives its variety from.\nfunc PropRandom(bound Int) Int {\n\tif Compare(bound, FromInt64(1)) < 0 {\n\t\tpanic(\"property generator: bound must be a positive Int, got \" + bound.String())\n\t}\n\treturn RandomInt(FromInt64(0), bound)\n}\n\n// PropInt generates a signed value around zero, so a property meets negatives, zero and\n// positives rather than only the happy side.\nfunc PropInt() Int {\n\treturn RandomInt(FromInt64(-1_000_000), FromInt64(1_000_001))\n}\n\nfunc PropBool() bool {\n\treturn Compare(RandomInt(FromInt64(0), FromInt64(2)), FromInt64(0)) == 0\n}\n\nfunc PropString() string {\n\treturn fmt.Sprintf(\"s%s\", RandomInt(FromInt64(0), FromInt64(1_000_000)).String())\n}\n\n// PropList generates 0 to 7 elements \226\128\148 the empty list included, since that is the case a\n// property over a list most often gets wrong.\nfunc PropList[Element any](element func() Element) []Element {\n\tcount, _ := RandomInt(FromInt64(0), FromInt64(8)).Int64()\n\tout := make([]Element, count)\n\tfor index := range out {\n\t\tout[index] = element()\n\t}\n\treturn out\n}\n\nfunc PropMaybe[Element any](element func() Element) Maybe[Element] {\n\tif Compare(RandomInt(FromInt64(0), FromInt64(2)), FromInt64(0)) == 0 {\n\t\treturn Nothing[Element]()\n\t}\n\treturn Something(element())\n}\n");
  ("queue.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"sort\"\n\t\"sync\"\n)\n\n// An in-memory job queue: the `backend: Memory` counterpart of tesl/queue.rkt's store.\n//\n// Jobs are dequeued in ENQUEUE ORDER. That is parity, not a detail: Racket's store is a\n// hash keyed by job id, and dequeuing it in hash order was a real bug (the PostgreSQL path\n// is `order by created_at asc`), fixed there with a monotonic sequence number. This store\n// keeps the sequence for the same reason.\n//\n// A payload is held as `any` because one queue may carry several job types; the emitter\n// knows them statically and passes a dispatcher that type-switches.\ntype Queue struct {\n\tmutex       sync.Mutex\n\tname        string\n\tmaxAttempts int\n\tnextSeq     int64\n\tjobs        map[string]*queuedJob\n}\n\ntype queuedJob struct {\n\tpayload  any\n\tstatus   string // \"pending\" | \"processing\" | \"dead\"\n\tattempts int\n\tseq      int64\n}\n\n// JobStatus values a test can ask about.\nconst (\n\tjobPending    = \"pending\"\n\tjobProcessing = \"processing\"\n\tjobDead       = \"dead\"\n)\n\nfunc NewQueue(name string, maxAttempts int) *Queue {\n\tif maxAttempts < 1 {\n\t\tmaxAttempts = 1\n\t}\n\treturn &Queue{name: name, maxAttempts: maxAttempts, jobs: map[string]*queuedJob{}}\n}\n\n// Enqueue stores a payload and answers the new job's id.\n//\n// The id is a UUID v7, NOT a counter: a process-local counter replayed ids after a restart,\n// which let a committed job id collide with a fresh one (the same reasoning as the Racket\n// side's move off gensym).\nfunc Enqueue(queue *Queue, payload any) string {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\tid := UUIDv7()\n\tqueue.nextSeq++\n\tqueue.jobs[id] = &queuedJob{payload: payload, status: jobPending, seq: queue.nextSeq}\n\treturn id\n}\n\nfunc PendingJobCount(queue *Queue) Int {\n\treturn FromInt64(int64(queue.countWithStatus(jobPending)))\n}\n\nfunc DeadJobCount(queue *Queue) Int {\n\treturn FromInt64(int64(queue.countWithStatus(jobDead)))\n}\n\nfunc (queue *Queue) countWithStatus(status string) int {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\tmatched := 0\n\tfor _, job := range queue.jobs {\n\t\tif job.status == status {\n\t\t\tmatched++\n\t\t}\n\t}\n\treturn matched\n}\n\n// dequeue claims the OLDEST job with `status`, marking it processing.\n//\n// The candidates carry their entry rather than being re-read from the map by id: a second\n// lookup would be a map read whose success only the surrounding lock explains, which is\n// exactly what nilaway (in the emitted-code gate) refuses to take on trust.\nfunc (queue *Queue) dequeue(status string) (string, any, int, bool) {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\ttype candidate struct {\n\t\tid    string\n\t\tentry *queuedJob\n\t}\n\tcandidates := make([]candidate, 0, len(queue.jobs))\n\tfor id, job := range queue.jobs {\n\t\tif job.status == status {\n\t\t\tcandidates = append(candidates, candidate{id: id, entry: job})\n\t\t}\n\t}\n\tif len(candidates) == 0 {\n\t\treturn \"\", nil, 0, false\n\t}\n\toldest := candidates[0]\n\tfor _, next := range candidates[1:] {\n\t\tif next.entry.seq < oldest.entry.seq {\n\t\t\toldest = next\n\t\t}\n\t}\n\toldest.entry.status = jobProcessing\n\treturn oldest.id, oldest.entry.payload, oldest.entry.attempts, true\n}\n\n// DeadJob is one entry in a queue's dead letter. It is OPAQUE to Tesl \226\128\148 `deadJobs` is typed\n// `List DeadJob` and the type has no accessors \226\128\148 so it carries the job's identity for the\n// runtime's use and nothing a program can read: what a test does with the list is count it.\ntype DeadJob struct {\n\tID string\n\t// The queue the job came out of. `requeue job` names no queue \226\128\148 the value carries it, as\n\t// Racket's dead-job carries its queue-spec \226\128\148 so this is what makes that call resolvable.\n\tqueue *Queue\n}\n\n// DeadJobs is the dead letter's contents, oldest first. The order is by enqueue sequence\n// rather than by map iteration, matching the PostgreSQL path's `order by created_at asc`.\nfunc DeadJobs(queue *Queue) []DeadJob {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\ttype entry struct {\n\t\tid  string\n\t\tseq int64\n\t}\n\tfound := make([]entry, 0, len(queue.jobs))\n\tfor id, job := range queue.jobs {\n\t\tif job.status == jobDead {\n\t\t\tfound = append(found, entry{id: id, seq: job.seq})\n\t\t}\n\t}\n\tsort.Slice(found, func(left, right int) bool { return found[left].seq < found[right].seq })\n\tdead := make([]DeadJob, 0, len(found))\n\tfor _, each := range found {\n\t\tdead = append(dead, DeadJob{ID: each.id, queue: queue})\n\t}\n\treturn dead\n}\n\n// ResetQueue empties the store, for a test block that starts from a fresh queue. The\n// sequence counter is deliberately NOT reset: it only has to be monotonic, and a counter that\n// restarts is how job ids came to repeat once already.\nfunc ResetQueue(queue *Queue) {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\tqueue.jobs = map[string]*queuedJob{}\n}\n\n// JobOutcome is what a worker run produced, for the `expectJobOk` style assertions.\ntype JobOutcome struct {\n\tRan     bool   // a job was claimed at all\n\tOK      bool   // the worker returned without rejecting or trapping\n\tMessage string // the rejection or trap message, when OK is false\n}\n\n// ProcessNextJob runs the next pending job through `handler` and applies the retry rule:\n// success completes the job, failure increments attempts and either re-queues it or \226\128\148 once\n// attempts reach maxAttempts \226\128\148 moves it to the dead letter. Identical to the Racket\n// in-memory path, including that a TRAP counts as a failed attempt rather than propagating.\n//\n// The retry BACKOFF delay is deliberately not simulated here: the in-memory Racket path\n// does not sleep either, and a test that had to wait for an exponential backoff would be a\n// slow test rather than a truthful one.\nfunc ProcessNextJob(queue *Queue, handler func(any) JobOutcome) JobOutcome {\n\tid, payload, attempts, found := queue.dequeue(jobPending)\n\tif !found {\n\t\treturn JobOutcome{}\n\t}\n\toutcome := runJob(handler, payload)\n\tif outcome.OK {\n\t\tqueue.complete(id)\n\t} else {\n\t\tqueue.fail(id, attempts)\n\t}\n\toutcome.Ran = true\n\treturn outcome\n}\n\n// ProcessNextDeadJob is the dead-letter counterpart: it claims a job that exhausted its\n// attempts and hands it to the dead-letter worker. A dead job is REMOVED whatever the\n// outcome \226\128\148 there is no second dead letter to fall into.\nfunc ProcessNextDeadJob(queue *Queue, handler func(any) JobOutcome) JobOutcome {\n\tid, payload, _, found := queue.dequeue(jobDead)\n\tif !found {\n\t\treturn JobOutcome{}\n\t}\n\toutcome := runJob(handler, payload)\n\tqueue.complete(id)\n\toutcome.Ran = true\n\treturn outcome\n}\n\n// DrainQueue processes pending jobs until none remain, and reports how many ran. It stops\n// at `limit` attempts so a job that keeps failing back into the queue cannot spin forever.\nfunc DrainQueue(queue *Queue, handler func(any) JobOutcome, limit int) Int {\n\tran := 0\n\tfor ran < limit {\n\t\tif outcome := ProcessNextJob(queue, handler); !outcome.Ran {\n\t\t\tbreak\n\t\t}\n\t\tran++\n\t}\n\treturn FromInt64(int64(ran))\n}\n\n// A worker that traps is a FAILED ATTEMPT, not a crashed process: the Racket path wraps the\n// handler in `with-handlers` and calls fail-job!, so the queue keeps its retry semantics.\nfunc runJob(handler func(any) JobOutcome, payload any) (outcome JobOutcome) {\n\tdefer func() {\n\t\tif recovered := recover(); recovered != nil {\n\t\t\toutcome = JobOutcome{OK: false, Message: fmt.Sprint(recovered)}\n\t\t}\n\t}()\n\treturn handler(payload)\n}\n\nfunc (queue *Queue) complete(id string) {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\tdelete(queue.jobs, id)\n}\n\nfunc (queue *Queue) fail(id string, attempts int) {\n\tqueue.mutex.Lock()\n\tdefer queue.mutex.Unlock()\n\tjob, found := queue.jobs[id]\n\tif !found {\n\t\treturn\n\t}\n\tjob.attempts = attempts + 1\n\tif job.attempts >= queue.maxAttempts {\n\t\tjob.status = jobDead\n\t} else {\n\t\tjob.status = jobPending\n\t}\n}\n\n// JobResult is what an api-test's `processNextJob` answers: the job that ran, and \226\128\148 when\n// the worker rejected or trapped \226\128\148 the error alongside it. `JobFailed` carries BOTH, as it\n// does on the Racket side (`JobFailed job error`), so a test can assert on the payload of a\n// job that failed.\n//\n// Runtime-provided like Maybe and Either, since a JobResult crosses module boundaries.\ntype JobResult[Payload any] struct {\n\tTag         JobResultTag\n\tOkJob       Payload\n\tFailedJob   Payload\n\tFailedError string\n}\n\ntype JobResultTag int\n\nconst (\n\tJobResultOk JobResultTag = iota\n\tJobResultFailed\n)\n\nfunc JobOk[Payload any](job Payload) JobResult[Payload] {\n\treturn JobResult[Payload]{Tag: JobResultOk, OkJob: job}\n}\n\nfunc JobFailed[Payload any](job Payload, failure string) JobResult[Payload] {\n\treturn JobResult[Payload]{Tag: JobResultFailed, FailedJob: job, FailedError: failure}\n}\n\n// ExpectJobOk is the api-test assertion: the job, or a trap naming the worker's failure.\nfunc ExpectJobOk[Payload any](result JobResult[Payload]) Payload {\n\tif result.Tag != JobResultOk {\n\t\tpanic(\"expectJobOk: expected JobOk but the worker failed with \" + result.FailedError)\n\t}\n\treturn result.OkJob\n}\n\n// ExpectJobFailed is its counterpart: the job that failed, or a trap.\n// ExpectJobFailed answers the ERROR, not the job: `tesl/api-test.rkt` returns\n// `JobFailed-error` here, and a test written against it (`expect isNotNull err`) reads a\n// message rather than a payload. The job is still reachable through the `JobResult` itself.\nfunc ExpectJobFailed[Payload any](result JobResult[Payload]) string {\n\tif result.Tag != JobResultFailed {\n\t\tpanic(\"expectJobFailed: expected JobFailed but the worker succeeded\")\n\t}\n\treturn result.FailedError\n}\n\n// EmptyQueue is the trap an api-test gets when it asks for a job and there is none \226\128\148 the\n// same failure Racket raises, with the same hint, because the usual cause is that the HTTP\n// action meant to enqueue the job did not run.\nfunc EmptyQueue(queueName, who string) string {\n\treturn fmt.Sprintf(\"%s: queue %s is empty \226\128\148 expected at least one pending job\\n\"+\n\t\t\"hint: did the HTTP action that enqueues the job run and return a success status?\",\n\t\twho, queueName)\n}\n\n// EnqueueJob is `enqueue`, whose Tesl type is Unit: the job id is not surfaced to the\n// program (Racket's enqueue! answers void too), so the id stays internal to the store.\nfunc EnqueueJob(queue *Queue, payload any) struct{} {\n\tEnqueue(queue, payload)\n\treturn struct{}{}\n}\n\n// Requeue resets a dead job to pending with a fresh attempt count, so the workers pick it up\n// again. It answers whether the job was there to reset \226\128\148 a job the dead letter no longer holds\n// is `False` rather than a trap, which is Racket's answer for the same case.\nfunc Requeue(job DeadJob) bool {\n\tif job.queue == nil {\n\t\treturn false\n\t}\n\tjob.queue.mutex.Lock()\n\tdefer job.queue.mutex.Unlock()\n\tfound, present := job.queue.jobs[job.ID]\n\tif !present {\n\t\treturn false\n\t}\n\tfound.status = jobPending\n\tfound.attempts = 0\n\treturn true\n}\n");
  ("random.go", "package teslrt\n\nimport (\n\t\"crypto/rand\"\n\t\"encoding/hex\"\n\t\"fmt\"\n\t\"math/big\"\n\t\"time\"\n)\n\n// Randomness is an effect Tesl gates behind the `random` capability, which the checker\n// enforces; nothing about that survives to run time.\n//\n// The source is crypto/rand rather than math/rand: a Tesl program cannot seed a generator\n// (there is no such surface), so a predictable stream would be a security property nobody\n// asked for, and gosec flags math/rand for exactly that reason.\n\n// RandomInt is `randomInt lo hi`: a uniform draw from [lo, hi). The caller proves lo < hi \226\128\148\n// the range is a compile-time obligation on the inputs \226\128\148 so an empty range is a trap rather\n// than a silent lo.\nfunc RandomInt(low, high Int) Int {\n\tspan := Sub(high, low)\n\tif Compare(span, FromInt64(1)) < 0 {\n\t\tpanic(\"randomInt: the range is empty (high must be greater than low)\")\n\t}\n\toffset, err := rand.Int(rand.Reader, span.bigInt())\n\tif err != nil {\n\t\tpanic(\"randomInt: no randomness available: \" + err.Error())\n\t}\n\treturn Add(low, fromBig(offset))\n}\n\n// RandomFloat is `randomFloat()`: a uniform draw from [0, 1), a fresh value per call.\n//\n// It is built from 53 random bits divided by 2^53 \226\128\148 the number of bits a float64 can hold\n// exactly \226\128\148 so every representable value in the range is reachable and none is favoured.\nfunc RandomFloat() float64 {\n\tconst bits = 1 << 53\n\tdrawn, err := rand.Int(rand.Reader, big.NewInt(bits))\n\tif err != nil {\n\t\tpanic(\"randomFloat: no randomness available: \" + err.Error())\n\t}\n\treturn float64(drawn.Int64()) / float64(bits)\n}\n\n// GeneratePrefixedId is `generatePrefixedId prefix`: `<prefix>-<32 hex chars>`, 16 CSPRNG\n// bytes rendered as hex, matching `tesl-generate-prefixed-id`. An id is often used as a\n// token, so it must be unguessable \226\128\148 the shape (prefix, dash, 32 hex digits) is part of the\n// contract, since ids land in URLs and databases.\nfunc GeneratePrefixedId(prefix string) string {\n\traw := make([]byte, 16)\n\tif _, err := rand.Read(raw); err != nil {\n\t\tpanic(\"generateId: no randomness available: \" + err.Error())\n\t}\n\treturn prefix + \"-\" + hex.EncodeToString(raw)\n}\n\n// GenerateId is `generateId()`: the same value with an empty prefix, so it still begins\n// with the dash \226\128\148 parity with Racket, which appends the separator unconditionally.\nfunc GenerateId() string {\n\treturn GeneratePrefixedId(\"\")\n}\n\n// UUIDv7 is a time-ordered UUID: 48 bits of Unix milliseconds, then random bits, with the\n// version and variant nibbles set (RFC 9562). The layout matches `uuid-v7-string` in\n// tesl/private/uuid-gen.rkt byte for byte, so an id generated on either backend sorts and\n// parses the same way.\n//\n// Time-ordered matters for the queue: a job id that is monotonic makes \"oldest first\"\n// recoverable from the id alone, and a v4 id would not.\nfunc UUIDv7() string {\n\tmilliseconds := time.Now().UnixMilli()\n\traw := make([]byte, 16)\n\tif _, err := rand.Read(raw[6:]); err != nil {\n\t\tpanic(\"UUID.v7: no randomness available: \" + err.Error())\n\t}\n\t// Masked, not just shifted: the truncation to a byte is the POINT (a 48-bit timestamp\n\t// spread over six bytes), and gosec's integer-overflow check is right to ask that it be\n\t// written explicitly rather than left to conversion.\n\tstamp := uint64(milliseconds)\n\traw[0] = byte((stamp >> 40) & 0xff)\n\traw[1] = byte((stamp >> 32) & 0xff)\n\traw[2] = byte((stamp >> 24) & 0xff)\n\traw[3] = byte((stamp >> 16) & 0xff)\n\traw[4] = byte((stamp >> 8) & 0xff)\n\traw[5] = byte(stamp & 0xff)\n\traw[6] = 0x70 | (raw[6] & 0x0f)\n\traw[8] = 0x80 | (raw[8] & 0x3f)\n\treturn fmt.Sprintf(\"%x-%x-%x-%x-%x\", raw[0:4], raw[4:6], raw[6:8], raw[8:10], raw[10:16])\n}\n\n// UUIDv4 is a RANDOM UUID: 122 random bits with the version and variant nibbles set\n// (RFC 9562), the same layout `uuid-v4-string` writes in tesl/private/uuid-gen.rkt.\n//\n// Randomness comes from crypto/rand, not math/rand, for the reason every id in this file\n// does: a UUID is routinely used as an unguessable handle, and a predictable one is a\n// vulnerability rather than a collision.\nfunc UUIDv4() string {\n\traw := make([]byte, 16)\n\tif _, err := rand.Read(raw); err != nil {\n\t\tpanic(\"UUID.v4: no randomness available: \" + err.Error())\n\t}\n\traw[6] = 0x40 | (raw[6] & 0x0f)\n\traw[8] = 0x80 | (raw[8] & 0x3f)\n\treturn fmt.Sprintf(\"%x-%x-%x-%x-%x\", raw[0:4], raw[4:6], raw[6:8], raw[8:10], raw[10:16])\n}\n\n// ValidUUID reports the 8-4-4-4-12 hex shape, in either case \226\128\148 the same set\n// `uuid-regexp` accepts in tesl/uuid.rkt. It is written as a scan rather than as a regular\n// expression so the runtime carries no regexp engine for one shape.\nfunc ValidUUID(value string) bool {\n\tif len(value) != 36 {\n\t\treturn false\n\t}\n\tfor index := 0; index < len(value); index++ {\n\t\tcharacter := value[index]\n\t\tswitch index {\n\t\tcase 8, 13, 18, 23:\n\t\t\tif character != '-' {\n\t\t\t\treturn false\n\t\t\t}\n\t\tdefault:\n\t\t\tisDigit := character >= '0' && character <= '9'\n\t\t\tisLower := character >= 'a' && character <= 'f'\n\t\t\tisUpper := character >= 'A' && character <= 'F'\n\t\t\tif !isDigit && !isLower && !isUpper {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t}\n\treturn true\n}\n\n// UUIDValidate is `UUID.validate`: a CHECK, so an invalid string is a 400 the request\n// answers with rather than a trap. The proof it mints (`IsUuid`) erases, so what survives\n// is the string it verified.\nfunc UUIDValidate(value string) Check[string] {\n\tif !ValidUUID(value) {\n\t\treturn Reject[string](400, \"not a valid UUID\")\n\t}\n\treturn Accept(value)\n}\n");
  ("request.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"strings\"\n)\n\n// RequestScope is the per-request state the Racket runtime keeps in a `parameterize`\n// scope: the cookies a handler has recorded on the response being built.\n//\n// Capabilities are deliberately NOT here. Every capability site in the AST carries a\n// STATIC list of names \226\128\148 `requires [...]`, `withCapabilities [...]`, `serve`, `workers` \226\128\148\n// so the granted set at any point is known at compile time and the checker already\n// verifies each call against it. A runtime check would compare two compile-time constants.\n// Keeping one would also force this scope through every capability-gated function, which\n// is exactly the blast radius the call-graph threading exists to avoid. Racket checks at\n// runtime because its capability set is a dynamic parameter; that mechanism is also what\n// broke capability delegation at deferred tool execution (\"passes test, fails live\"), a\n// failure mode a compile-time-only design does not have.\n//\n// This type exists because Go has no goroutine-local storage. A Racket parameter is\n// thread-local and every request is served on its own thread, so a handler deep in a call\n// chain can call `Http.clearSessionCookie` and the runtime knows which response it belongs\n// to. In Go the scope has to be passed explicitly \226\128\148 and the emitter passes it ONLY to the\n// functions whose call graph actually reaches a scope-using leaf, so ordinary pure\n// functions keep their plain signatures and the emitted code still reads like Go someone\n// would write.\n//\n// A nil scope means \"no live HTTP response\", which is exactly the Racket condition that\n// raises: startup code, `main`, and queue workers have no response to attach a cookie to.\ntype RequestScope struct {\n\t// Full `Set-Cookie` header values, in send order.\n\tcookies []string\n}\n\nfunc NewRequestScope() *RequestScope {\n\treturn &RequestScope{}\n}\n\n// SetCookieHeader records one complete `Set-Cookie` value. LAST CALL WINS PER COOKIE\n// NAME, and send order is otherwise preserved \226\128\148 the same rule dsl/response-cookies.rkt\n// states, so `setSessionCookie` followed by `clearSessionCookie` emits exactly one\n// `Set-Cookie` for that name rather than two contradictory ones.\nfunc (scope *RequestScope) SetCookieHeader(who, value string) {\n\tif scope == nil {\n\t\tpanic(who + \": no HTTP response to attach a cookie to.\\n\" +\n\t\t\t\"  it is only callable while serving an HTTP request \226\128\148 from a handler body, an\\n\" +\n\t\t\t\"  `auth` block, or an SSE subscribe.  Startup, `main` and queue-worker code have\\n\" +\n\t\t\t\"  no HTTP response.\")\n\t}\n\tname := value\n\tif index := strings.IndexByte(value, '='); index >= 0 {\n\t\tname = value[:index]\n\t}\n\tfor index, existing := range scope.cookies {\n\t\texistingName := existing\n\t\tif at := strings.IndexByte(existing, '='); at >= 0 {\n\t\t\texistingName = existing[:at]\n\t\t}\n\t\tif existingName == name {\n\t\t\tscope.cookies[index] = value\n\t\t\treturn\n\t\t}\n\t}\n\tscope.cookies = append(scope.cookies, value)\n}\n\n// CookieHeaders hands back a copy, so a caller cannot reach the scope's own storage.\n//\n// An EMPTY slice rather than nil for \"no scope\": callers range over or index the result, and\n// nilaway is right that a nil return travelling into a slice operation is a nil-panic waiting\n// for the one caller who forgets to check.\nfunc (scope *RequestScope) CookieHeaders() []string {\n\tif scope == nil {\n\t\treturn []string{}\n\t}\n\tout := make([]string, len(scope.cookies))\n\tcopy(out, scope.cookies)\n\treturn out\n}\n\n// The session cookie's shape is fixed by tesl/http.rkt and must match byte for byte:\n// the name is `__Host-`-prefixed (so the browser enforces Secure + host-only + Path=/),\n// and SameSite=Lax rather than Strict so following an ordinary link back into the app\n// arrives authenticated.\nconst (\n\tsessionCookieName       = \"__Host-session\"\n\tsessionCookieAttributes = \"Path=/; HttpOnly; Secure; SameSite=Lax\"\n)\n\n// ClearSessionCookie is the logout half: same cookie, same attributes, empty value and\n// `Max-Age=0`, which is how a browser is told to drop it. It removes the BROWSER's copy\n// and does not invalidate the token \226\128\148 the same documented limitation Racket carries.\n// Returns Tesl's Unit (an empty struct), so the emitted code can use it in value\n// position the way a Tesl expression of type Unit is used.\nfunc ClearSessionCookie(scope *RequestScope) struct{} {\n\tscope.SetCookieHeader(\"Http.clearSessionCookie\",\n\t\tfmt.Sprintf(\"%s=; %s; Max-Age=0\", sessionCookieName, sessionCookieAttributes))\n\treturn struct{}{}\n}\n\n// HttpRequest is what an `auth` function and a handler see of the incoming request.\n// It is a plain value built once per request by the dispatcher \226\128\148 the fields Tesl exposes,\n// nothing more, so an ejecting author is not handed the whole net/http surface.\n//\n// The maps are teslrt.Dict values because that is what Tesl's `Dict.lookup` expects.\n// A Dict is stored sorted by key, and these are built with plain string ordering, which is\n// exactly the comparator the emitter passes at a `Dict.lookup` call site on String keys.\ntype HttpRequest struct {\n\tMethod          string\n\tPath            string\n\tCookies         Dict[string, string]\n\tHeaders         Dict[string, string]\n\tQueryParameters Dict[string, string]\n\tBody            string\n}\n\n// NewHttpRequest snapshots the request. Header names are lower-cased so a lookup does not\n// depend on the casing a client happened to send; cookie names are left exactly as sent.\n//\n// A REPEATED name \226\128\148 `?q=first&q=second`, or two `X-Forwarded-For` headers \226\128\148 collapses to the\n// LAST value, because a Dict holds one value per key and that is the answer the Racket runtime\n// gives: both its header and query hashes are built with `for/hash`, which keeps the last\n// binding. Taking the first instead is the natural Go spelling (`Query().Get`) and the wrong\n// one; `tests/query-parameters-tests.tesl` pins the rule.\nfunc NewHttpRequest(request *http.Request, body string) HttpRequest {\n\theaders := DictEmpty[string, string]()\n\tfor name, values := range request.Header {\n\t\tif len(values) > 0 {\n\t\t\theaders = DictInsert(headers, strings.ToLower(name), values[len(values)-1], stringKeyLess)\n\t\t}\n\t}\n\tcookies := DictEmpty[string, string]()\n\tfor _, cookie := range request.Cookies() {\n\t\tcookies = DictInsert(cookies, cookie.Name, cookie.Value, stringKeyLess)\n\t}\n\tquery := DictEmpty[string, string]()\n\tfor name, values := range request.URL.Query() {\n\t\tif len(values) > 0 {\n\t\t\tquery = DictInsert(query, name, values[len(values)-1], stringKeyLess)\n\t\t}\n\t}\n\treturn HttpRequest{\n\t\tMethod:          request.Method,\n\t\tPath:            request.URL.Path,\n\t\tCookies:         cookies,\n\t\tHeaders:         headers,\n\t\tQueryParameters: query,\n\t\tBody:            body,\n\t}\n}\n\n// SetSessionCookie is `Http.setSessionCookie`: the ONE blessed session transport. The name is\n// `__Host-`-prefixed so the browser enforces Secure, host-only and Path=/, and the lifetime\n// matches the token's own TTL \226\128\148 a cookie that outlived its token would only produce 401s.\n//\n// There is no name parameter and no attribute parameter: every option here is a way to get it\n// wrong, and the fixed shape is what makes `Http.sessionToken` able to read it back.\nfunc SetSessionCookie(scope *RequestScope, token JwtToken) struct{} {\n\t// The value must LOOK like a signed token \226\128\148 three non-empty base64url segments. A session\n\t// cookie carrying anything else is a misuse (writing an unverified cookie value straight\n\t// back, say), and writing it would mint a session out of attacker-supplied text. Racket\n\t// applies the same shape check for the same reason, and the trap is contained by the\n\t// router's sanitized 500 rather than reaching the client.\n\tif !looksLikeJWT(token.Value) {\n\t\tpanic(fmt.Sprintf(\"Http.setSessionCookie: expected a JwtToken produced by `JWT.sign`, \"+\n\t\t\t\"got %q.\\n  A session cookie must carry a signed token; this value is not a\\n\"+\n\t\t\t\"  well-formed JWT and will not be written to the response.\", token.Value))\n\t}\n\tscope.SetCookieHeader(\"Http.setSessionCookie\",\n\t\tfmt.Sprintf(\"%s=%s; %s; Max-Age=%d\",\n\t\t\tsessionCookieName, token.Value, sessionCookieAttributes, jwtTTLSeconds))\n\treturn struct{}{}\n}\n\n// looksLikeJWT is the wire SHAPE only \226\128\148 `header.payload.signature`, base64url \226\128\148 never a\n// verification. Deciding whether a token is authentic is `JWT.verify`'s job, and conflating the\n// two here would be the more dangerous kind of check: one that looks like a guarantee.\nfunc looksLikeJWT(value string) bool {\n\tsegments := strings.Split(value, \".\")\n\tif len(segments) != 3 {\n\t\treturn false\n\t}\n\tfor _, segment := range segments {\n\t\tif segment == \"\" {\n\t\t\treturn false\n\t\t}\n\t\tfor _, character := range segment {\n\t\t\tisBase64URL := (character >= 'A' && character <= 'Z') ||\n\t\t\t\t(character >= 'a' && character <= 'z') ||\n\t\t\t\t(character >= '0' && character <= '9') ||\n\t\t\t\tcharacter == '_' || character == '-'\n\t\t\tif !isBase64URL {\n\t\t\t\treturn false\n\t\t\t}\n\t\t}\n\t}\n\treturn true\n}\n\n// SessionToken is `Http.sessionToken`: the reader, so the fixed cookie name is written down\n// once rather than spelled out at every call site \226\128\148 where a typo is a permanent 401. It is pure\n// and ungated; `JWT.verify` is what sits between this and a fact.\nfunc SessionToken(request HttpRequest) Maybe[JwtToken] {\n\tvalue, found := DictLookup(request.Cookies, sessionCookieName, stringKeyLess).Value()\n\tif !found {\n\t\treturn Nothing[JwtToken]()\n\t}\n\treturn Something(JwtToken{Value: value})\n}\n\n// ResponseCookie is the api-test reader: the session cookie a response set, as a\n// Cookie-header-ready `name=value` pair, so a round-trip test can feed it straight back. The\n// ATTRIBUTES are stripped deliberately \226\128\148 assert those against the raw `set-cookie` header,\n// which is the full line.\nfunc ResponseCookie(response ApiResponse) Maybe[string] {\n\tline, found := DictLookup(response.Headers, \"set-cookie\", stringKeyLess).Value()\n\tif !found {\n\t\treturn Nothing[string]()\n\t}\n\t// The pair is everything up to the first `;`. Indexed only after a length check: a split\n\t// result is a slice whose emptiness nothing here guarantees to a reader (or to nilaway).\n\tsegments := strings.Split(line, \";\")\n\tif len(segments) == 0 {\n\t\treturn Nothing[string]()\n\t}\n\tpair := strings.TrimSpace(segments[0])\n\tif !strings.Contains(pair, \"=\") {\n\t\treturn Nothing[string]()\n\t}\n\treturn Something(pair)\n}\n");
  ("result.go", "package teslrt\n\n// Result is Tesl's `Result ok err`: Ok carries the value, Err the failure. Like Maybe and\n// Either it lives in the runtime because a Result crosses module boundaries, and it uses\n// the same flat tag+payload shape the emitter generates for a user ADT.\n//\n// The zero value is an Ok holding the zero value of Ok \226\128\148 the tag order below \226\128\148 which is the\n// one asymmetry worth stating: a zero Result reads as success. Emitted code never produces\n// one (every construction names a constructor), and Tesl has no way to observe an\n// uninitialised value, so this is a Go-level note rather than a semantic choice.\ntype Result[Ok any, Err any] struct {\n\tTag      ResultTag\n\tOkValue  Ok\n\tErrValue Err\n}\n\n// ResultTag identifies which side a Result holds. The constants are an enum-like set so the\n// `exhaustive` linter can verify a switch over them.\ntype ResultTag int\n\nconst (\n\tResultOk ResultTag = iota\n\tResultErr\n)\n\n// MakeOk and MakeErr exist for hand-written runtime code and readable tests; emitted code\n// writes the composite literal directly. They are not named `Ok`/`Err` because those are\n// the FIELD-bearing type parameters above, and a function may not share a name with them.\nfunc MakeOk[Ok any, Err any](value Ok) Result[Ok, Err] {\n\treturn Result[Ok, Err]{Tag: ResultOk, OkValue: value}\n}\n\nfunc MakeErr[Ok any, Err any](failure Err) Result[Ok, Err] {\n\treturn Result[Ok, Err]{Tag: ResultErr, ErrValue: failure}\n}\n\n// IsOk reports which side is held.\nfunc (r Result[Ok, Err]) IsOk() bool {\n\treturn r.Tag == ResultOk\n}\n");
  ("secret.go", "package teslrt\n\nimport \"crypto/subtle\"\n\n// SecretRedaction is the one string every sink substitutes for a secret's payload.\n//\n// Fixed, and deliberately carrying no length, prefix or hash of the value: partial\n// disclosure in a log or a debugger is the convenience that erodes the guarantee. Same text\n// as `secret-redaction-text` in dsl/types.rkt, so the two backends redact identically.\nconst SecretRedaction = \"[redacted]\"\n\n// SecretString is the payload of a `secret` newtype over String.\n//\n// It is a distinct type rather than a bare string so that a `%v`/`%s` of it \226\128\148 a log line, a\n// panic message, a test failure \226\128\148 prints the redaction instead of the plaintext. The\n// plaintext is reached through `Reveal`, which is spelled to be searchable: a review can ask\n// where it is called.\ntype SecretString struct {\n\tplaintext string\n}\n\nfunc MakeSecret(plaintext string) SecretString {\n\treturn SecretString{plaintext: plaintext}\n}\n\n// String makes redaction the DEFAULT: fmt reaches for this method, so every accidental\n// print is safe without the call sites having to know they are handling a secret.\nfunc (secret SecretString) String() string { return SecretRedaction }\n\n// GoString covers `%#v`, which would otherwise print the struct with its field.\nfunc (secret SecretString) GoString() string { return SecretRedaction }\n\n// MarshalJSON keeps a secret out of an encoded payload the same way, rather than relying on\n// every codec to remember.\nfunc (secret SecretString) MarshalJSON() ([]byte, error) {\n\treturn []byte(`\"` + SecretRedaction + `\"`), nil\n}\n\n// Reveal hands back the plaintext. Every use is a deliberate disclosure \226\128\148 sending it on the\n// wire, hashing it, comparing it \226\128\148 and is meant to be greppable.\nfunc (secret SecretString) Reveal() string { return secret.plaintext }\n\n// SecretEqual compares two secrets without an early-exit data dependency, so a caller\n// cannot learn a prefix from how long the comparison took. `subtle.ConstantTimeCompare`\n// itself returns 0 for differing LENGTHS without comparing, which is the same shape Racket's\n// `constant-time-bytes=?/secret` has.\nfunc SecretEqual(left, right SecretString) bool {\n\treturn subtle.ConstantTimeCompare([]byte(left.plaintext), []byte(right.plaintext)) == 1\n}\n");
  ("serve.go", "package teslrt\n\nimport (\n\t\"context\"\n\t\"errors\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"os\"\n\t\"os/signal\"\n\t\"path\"\n\t\"path/filepath\"\n\t\"strings\"\n\t\"sync\"\n\t\"syscall\"\n\t\"time\"\n)\n\n// Activation: what `main` returning an `App { \226\128\166 }` becomes.\n//\n// The App record is not a runtime value \226\128\148 the compiler lowers it into the startup chain it\n// describes (start each queue's workers, then serve), so what is left here are the two things\n// that chain calls. Everything the App configures is therefore decided at compile time and read\n// here as ordinary arguments.\n\n// ServeOptions is what an `App` says about the HTTP surface. Grouped in a struct rather than\n// passed positionally because most programs set only `Port`, and a five-argument call whose\n// middle three are usually zero reads worse in the emitted code than a literal with named\n// fields.\ntype ServeOptions struct {\n\tPort int\n\t// StaticDir serves files, with an index.html SPA fallback, when set.\n\tStaticDir string\n\t// MountPath scopes exactly the DECLARED api surface (`/api`), while static files, the SPA\n\t// fallback and the health probe keep answering on the raw path \226\128\148 the same split the Racket\n\t// runtime makes, and for its reason: the SPA is precisely the other thing sharing this\n\t// origin that the prefix exists to separate the API from.\n\tMountPath string\n}\n\n// Serve runs the HTTP server until the process is asked to stop, then drains in-flight requests.\n//\n// Timeouts are set rather than left at Go's defaults (which are \"none\"): a server with no read\n// timeout can be held open by a slow client indefinitely, which is the same class of problem the\n// outbound deadlines close on the client side. `ReadHeaderTimeout` in particular is what stops a\n// Slowloris-style hold.\n// Answers Tesl's Unit (an empty struct) rather than nothing, so the emitted `main` can return it\n// the way it returns any other statement's value \226\128\148 the same shape `EnqueueJob` and the cookie\n// writers use.\nfunc Serve(server Server, options ServeOptions) struct{} {\n\tport := options.Port\n\tif port == 0 {\n\t\tport = 8080\n\t}\n\thandler := server.handlerWith(options)\n\taddress := fmt.Sprintf(\":%d\", port)\n\thttpServer := &http.Server{\n\t\tAddr:              address,\n\t\tHandler:           handler,\n\t\tReadHeaderTimeout: 10 * time.Second,\n\t\tReadTimeout:       30 * time.Second,\n\t\tWriteTimeout:      60 * time.Second,\n\t\tIdleTimeout:       120 * time.Second,\n\t}\n\t// Graceful: an interrupt stops accepting and lets in-flight requests finish, so a deploy does\n\t// not answer a request with a truncated response.\n\tshutdown, stop := signal.NotifyContext(context.Background(),\n\t\tos.Interrupt, syscall.SIGTERM)\n\tdefer stop()\n\tgo func() {\n\t\t<-shutdown.Done()\n\t\tdrain, cancel := context.WithTimeout(context.Background(), 15*time.Second)\n\t\tdefer cancel()\n\t\t_ = httpServer.Shutdown(drain)\n\t}()\n\tfmt.Fprintf(os.Stderr, \"tesl: serving on http://localhost:%d\\n\", port)\n\tif err := httpServer.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {\n\t\tpanic(\"serve: \" + err.Error())\n\t}\n\treturn struct{}{}\n}\n\n// handlerWith wraps the router with the static-file surface an App may declare. The API routes\n// are reached under `MountPath`; everything else the runtime owns answers on the raw path.\nfunc (server Server) handlerWith(options ServeOptions) http.Handler {\n\tmount := strings.Trim(options.MountPath, \"/\")\n\tstatic := options.StaticDir\n\treturn http.HandlerFunc(func(writer http.ResponseWriter, request *http.Request) {\n\t\trouted := request\n\t\tif mount != \"\" {\n\t\t\ttrimmed := strings.TrimPrefix(strings.TrimPrefix(request.URL.Path, \"/\"), mount)\n\t\t\tif !strings.HasPrefix(strings.TrimPrefix(request.URL.Path, \"/\"), mount) {\n\t\t\t\t// Not under the mount prefix: only the static surface can answer it.\n\t\t\t\tif static != \"\" && serveStatic(writer, request, static) {\n\t\t\t\t\treturn\n\t\t\t\t}\n\t\t\t\twriteResponse(writer, nil, Fail(404, \"not found\"))\n\t\t\t\treturn\n\t\t\t}\n\t\t\trouted = request.Clone(request.Context())\n\t\t\tif trimmed == \"\" {\n\t\t\t\ttrimmed = \"/\"\n\t\t\t}\n\t\t\tif !strings.HasPrefix(trimmed, \"/\") {\n\t\t\t\ttrimmed = \"/\" + trimmed\n\t\t\t}\n\t\t\trouted.URL.Path = trimmed\n\t\t}\n\t\tif server.routeExists(routed) {\n\t\t\tserver.ServeHTTP(writer, routed)\n\t\t\treturn\n\t\t}\n\t\tif static != \"\" && serveStatic(writer, request, static) {\n\t\t\treturn\n\t\t}\n\t\tserver.ServeHTTP(writer, routed)\n\t})\n}\n\n// routeExists reports whether the router has a route for this path at all \226\128\148 used to decide\n// between the API surface and the static one WITHOUT letting the router answer 404 first.\nfunc (server Server) routeExists(request *http.Request) bool {\n\tfor _, route := range server.Routes {\n\t\tif pathMatches(route.Path, request.URL.Path) {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n\n// serveStatic answers a GET from the static directory, falling back to index.html so a\n// single-page app's client-side routes load. Reports whether it answered.\n//\n// The path is resolved and then CHECKED to be inside the directory: `..` segments and absolute\n// paths are how a static handler becomes an arbitrary-file read, and the check is on the\n// resolved path rather than on the request text so an encoded traversal cannot slip past.\nfunc serveStatic(writer http.ResponseWriter, request *http.Request, directory string) bool {\n\tif request.Method != http.MethodGet && request.Method != http.MethodHead {\n\t\treturn false\n\t}\n\troot, err := filepath.Abs(directory)\n\tif err != nil {\n\t\treturn false\n\t}\n\trelative := strings.TrimPrefix(path.Clean(\"/\"+request.URL.Path), \"/\")\n\tif relative == \"\" {\n\t\trelative = \"index.html\"\n\t}\n\tcandidate := filepath.Join(root, filepath.FromSlash(relative))\n\tif !strings.HasPrefix(candidate, root+string(os.PathSeparator)) && candidate != root {\n\t\treturn false\n\t}\n\tif info, err := os.Stat(candidate); err == nil && !info.IsDir() {\n\t\thttp.ServeFile(writer, request, candidate)\n\t\treturn true\n\t}\n\t// The SPA fallback: an unknown path serves index.html, so client-side routing works on a\n\t// cold load. Only when there IS an index.html \226\128\148 otherwise the router's own 404 is the honest\n\t// answer.\n\tindex := filepath.Join(root, \"index.html\")\n\tif info, err := os.Stat(index); err == nil && !info.IsDir() {\n\t\thttp.ServeFile(writer, request, index)\n\t\treturn true\n\t}\n\treturn false\n}\n\n// \226\148\128\226\148\128 Queue workers \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n\n// StartWorkers activates a queue's workers: `concurrency` goroutines, each claiming and running\n// one job at a time. `dead` selects the dead-letter worker instead of the ordinary one.\n//\n// A worker that finds nothing sleeps briefly rather than spinning: the in-memory store has no\n// condition variable to wait on, and a busy loop would burn a core per queue. The Racket runtime\n// waits on a semaphore instead \226\128\148 same behaviour, one poll interval of latency on an idle queue.\nfunc StartWorkers(queue *Queue, handler func(any) JobOutcome, concurrency int, dead bool) struct{} {\n\tif concurrency < 1 {\n\t\tconcurrency = 1\n\t}\n\tfor range concurrency {\n\t\tgo func() {\n\t\t\tfor {\n\t\t\t\toutcome := JobOutcome{}\n\t\t\t\tif dead {\n\t\t\t\t\toutcome = ProcessNextDeadJob(queue, handler)\n\t\t\t\t} else {\n\t\t\t\t\toutcome = ProcessNextJob(queue, handler)\n\t\t\t\t}\n\t\t\t\tif !outcome.Ran {\n\t\t\t\t\ttime.Sleep(50 * time.Millisecond)\n\t\t\t\t}\n\t\t\t}\n\t\t}()\n\t}\n\treturn struct{}{}\n}\n\n// \226\148\128\226\148\128 Request-level guards \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Two refusals the Racket runtime applies to every request, mirrored here so a program does not\n// lose them by changing backend. Both are cheap and neither is configurable per route.\n\nvar publicOriginOnce sync.Once\nvar publicOrigin string\n\n// PublicOrigin is `TESL_PUBLIC_ORIGIN` when set: the origin this deployment answers as.\nfunc PublicOrigin() string {\n\tpublicOriginOnce.Do(func() {\n\t\tpublicOrigin = strings.TrimSpace(os.Getenv(\"TESL_PUBLIC_ORIGIN\"))\n\t})\n\treturn publicOrigin\n}\n\n// requestRefusal is the guard pair: a cross-site state-changing request is refused (403), and a\n// Host that does not match the configured public origin is refused (421). Answering the second\n// with 421 rather than 404 is deliberate \226\128\148 it says \"not this origin\", which is what a\n// misconfigured proxy needs to hear.\nfunc requestRefusal(request *http.Request) (Response, bool) {\n\tswitch request.Method {\n\tcase http.MethodPost, http.MethodPut, http.MethodDelete, http.MethodPatch:\n\t\tif strings.EqualFold(strings.TrimSpace(request.Header.Get(\"Sec-Fetch-Site\")), \"cross-site\") {\n\t\t\treturn Fail(403, \"cross-site request refused\"), true\n\t\t}\n\t}\n\torigin := PublicOrigin()\n\tif origin == \"\" {\n\t\treturn Response{}, false\n\t}\n\tif hostOf(request.Host) == hostOf(origin) {\n\t\treturn Response{}, false\n\t}\n\treturn Fail(421, \"Host does not match the configured public origin\"), true\n}\n\n// hostOf takes the host out of either a bare Host header or a full origin, without its port: a\n// deployment behind a proxy sees the port the proxy forwards to, not the public one.\nfunc hostOf(value string) string {\n\ttext := strings.TrimSpace(strings.ToLower(value))\n\tif index := strings.Index(text, \"://\"); index >= 0 {\n\t\ttext = text[index+3:]\n\t}\n\tif index := strings.IndexAny(text, \"/?#\"); index >= 0 {\n\t\ttext = text[:index]\n\t}\n\tif strings.HasPrefix(text, \"[\") {\n\t\tif end := strings.Index(text, \"]\"); end >= 0 {\n\t\t\treturn text[:end+1]\n\t\t}\n\t\treturn text\n\t}\n\tif index := strings.LastIndex(text, \":\"); index >= 0 {\n\t\treturn text[:index]\n\t}\n\treturn text\n}\n\n// PortOf narrows a Tesl `Int` to a listening port. A port is a 16-bit number, so anything\n// outside that range is a configuration error worth failing at startup rather than silently\n// binding somewhere else.\nfunc PortOf(value Int) int {\n\tport, exact := value.Int64()\n\tif !exact || port < 1 || port > 65535 {\n\t\tpanic(\"serve: port \" + value.String() + \" is not a valid TCP port (1-65535)\")\n\t}\n\treturn int(port)\n}\n");
  ("server.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"os\"\n\t\"runtime/debug\"\n\t\"strings\"\n)\n\n// The HTTP surface an emitted module exposes: the ROUTES an `api` declares, and the\n// SERVER that binds each route's endpoint name to a handler.\n//\n// Emitted modules build these as ordinary values, so a Go author who sheds Tesl can read\n// the routing table, call a handler directly, or mount the server on any net/http mux.\n// Nothing here reaches into request-scoped globals: the per-request state a handler may\n// touch (cookies) is created by the dispatcher and passed in explicitly.\n\ntype Route struct {\n\tMethod   string\n\tPath     string\n\tEndpoint string\n}\n\n// HandlerFunc is one endpoint's implementation. The scope carries the response state the\n// handler is allowed to write (see request.go); `Response` says what to send.\ntype HandlerFunc func(scope *RequestScope, request *http.Request) Response\n\ntype Response struct {\n\tStatus int\n\t// Body is an already-encoded JSON value (map / slice / scalar), rendered by\n\t// EncodeJSONValue so object keys keep the sorted order Racket produces.\n\tBody any\n}\n\n// Fail builds the error body shape the Racket server sends:\n// {\"ok\":false,\"error\":<message>,\"details\":[]}.\nfunc Fail(status int, message string) Response {\n\treturn Response{\n\t\tStatus: status,\n\t\tBody: map[string]any{\n\t\t\t\"ok\":      false,\n\t\t\t\"error\":   message,\n\t\t\t\"details\": []any{},\n\t\t},\n\t}\n}\n\n// StreamFunc is an endpoint that STREAMS rather than answering once: an `sse` route. It takes\n// the writer directly, because its response is written over the life of the connection instead\n// of being built and then sent.\ntype StreamFunc func(writer http.ResponseWriter, request *http.Request)\n\ntype Server struct {\n\tRoutes   []Route\n\tHandlers map[string]HandlerFunc\n\t// Streams holds the SSE endpoints, keyed like Handlers. A route resolves to one or the\n\t// other, never both: `sse` and the request/response methods are different declarations.\n\tStreams map[string]StreamFunc\n}\n\n// ServeHTTP dispatches by method and path. A path segment written `:name` in the route\n// matches any single segment.\n//\n// The status choices mirror the Racket router: an unknown path is 404, and a known path\n// with the wrong method is 405 \226\128\148 reporting 404 for a method mismatch would tell a client\n// the resource does not exist when it does.\n// callHandler runs one handler and converts a check rejection that escaped its body into\n// the response that rejection describes. Any OTHER panic is left alone: a genuine bug must\n// not be reported to the client as a validation failure.\nfunc callHandler(handler HandlerFunc, scope *RequestScope, request *http.Request) (response Response) {\n\tdefer func() {\n\t\trecovered := recover()\n\t\tif recovered == nil {\n\t\t\treturn\n\t\t}\n\t\tif rejection, isRejection := recovered.(RequestRejection); isRejection {\n\t\t\tresponse = Fail(rejection.Status, rejection.Message)\n\t\t\treturn\n\t\t}\n\t\t// ANY other trap becomes a SANITIZED 500. The message is deliberately generic and the\n\t\t// trap text never reaches the client: a trap message carries whatever the program was\n\t\t// holding (a path, a key, a row), and an `auth` block is exactly where a malformed\n\t\t// cookie can provoke one \226\128\148 so a client-triggerable trap must not become a\n\t\t// client-readable disclosure. The detail is still available to the operator, on stderr.\n\t\tfmt.Fprintf(os.Stderr, \"tesl: handler trapped: %v\\n%s\", recovered, debug.Stack())\n\t\tresponse = Fail(500, \"Internal server error\")\n\t}()\n\treturn handler(scope, request)\n}\n\nfunc (server Server) ServeHTTP(writer http.ResponseWriter, request *http.Request) {\n\t// The two request-level refusals the Racket runtime applies to every request (serve.go):\n\t// a cross-site state-changing request, and a Host that is not this deployment's. Checked\n\t// here rather than in `Serve` so an api-test \226\128\148 which drives this method directly \226\128\148 exercises\n\t// them too.\n\tif refusal, refused := requestRefusal(request); refused {\n\t\twriteResponse(writer, nil, refusal)\n\t\treturn\n\t}\n\tpathMatched := false\n\tfor _, route := range server.Routes {\n\t\tif !pathMatches(route.Path, request.URL.Path) {\n\t\t\tcontinue\n\t\t}\n\t\tpathMatched = true\n\t\tif route.Method != request.Method {\n\t\t\tcontinue\n\t\t}\n\t\tif stream, streams := server.Streams[route.Endpoint]; streams {\n\t\t\t// A stream owns its own response: status, headers and body are written by the\n\t\t\t// handler over the life of the connection, so nothing is wrapped around it here.\n\t\t\tstream(writer, request)\n\t\t\treturn\n\t\t}\n\t\thandler, found := server.Handlers[route.Endpoint]\n\t\tif !found {\n\t\t\twriteResponse(writer, nil, Fail(500, \"endpoint \"+route.Endpoint+\" has no handler\"))\n\t\t\treturn\n\t\t}\n\t\tscope := NewRequestScope()\n\t\twriteResponse(writer, scope, callHandler(handler, scope, request))\n\t\treturn\n\t}\n\tif pathMatched {\n\t\twriteResponse(writer, nil, Fail(405, \"method not allowed\"))\n\t\treturn\n\t}\n\twriteResponse(writer, nil, Fail(404, \"not found\"))\n}\n\nfunc pathMatches(pattern, path string) bool {\n\tpatternParts := strings.Split(strings.Trim(pattern, \"/\"), \"/\")\n\tpathParts := strings.Split(strings.Trim(path, \"/\"), \"/\")\n\tif len(patternParts) != len(pathParts) {\n\t\treturn false\n\t}\n\tfor index, part := range patternParts {\n\t\tif strings.HasPrefix(part, \":\") {\n\t\t\tif pathParts[index] == \"\" {\n\t\t\t\treturn false\n\t\t\t}\n\t\t\tcontinue\n\t\t}\n\t\tif part != pathParts[index] {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n\n// PathParam reads the segment a `:name` placeholder matched.\nfunc PathParam(pattern, path, name string) (string, bool) {\n\tpatternParts := strings.Split(strings.Trim(pattern, \"/\"), \"/\")\n\tpathParts := strings.Split(strings.Trim(path, \"/\"), \"/\")\n\tif len(patternParts) != len(pathParts) {\n\t\treturn \"\", false\n\t}\n\tfor index, part := range patternParts {\n\t\tif part == \":\"+name {\n\t\t\treturn pathParts[index], true\n\t\t}\n\t}\n\treturn \"\", false\n}\n\nfunc writeResponse(writer http.ResponseWriter, scope *RequestScope, response Response) {\n\t// Cookies attach to 2xx responses only, matching dsl/web.rkt: a handler that sets a\n\t// cookie and then fails mints no session.\n\tif scope != nil && response.Status >= 200 && response.Status < 300 {\n\t\tfor _, cookie := range scope.CookieHeaders() {\n\t\t\twriter.Header().Add(\"Set-Cookie\", cookie)\n\t\t}\n\t}\n\twriter.Header().Set(\"Content-Type\", \"application/json\")\n\twriter.WriteHeader(response.Status)\n\t_, _ = writer.Write([]byte(EncodeJSONValue(response.Body)))\n}\n");
  ("set.go", "package teslrt\n\n// Set is Tesl's `Set a`, held as elements kept SORTED with no duplicates.\n//\n// Same reasoning as Dict (see dict.go): a Go map cannot be keyed by the non-comparable\n// teslrt.Int, and Go randomises map iteration order per run \226\128\148 which would make the same\n// binary print different output across runs, worse than Racket's unspecified-but-stable\n// order. Sorted order is deterministic and free here, since the insertion point is the\n// binary search membership already performs.\n//\n// Racket's `Set.toList` iterates a hash, so element order is UNSPECIFIED in Tesl: a\n// program may observe membership and size, never order. That divergence is real and\n// demonstrated \226\128\148 see the Dict entry in the migration tracker.\n//\n// Ordering comes in from the emitter, which knows the concrete element type at every\n// call site.\ntype Set[A any] struct {\n\tElements []A\n}\n\nfunc SetEmpty[A any]() Set[A] {\n\treturn Set[A]{}\n}\n\nfunc SetSingleton[A any](value A) Set[A] {\n\treturn Set[A]{Elements: []A{value}}\n}\n\nfunc SetSize[A any](s Set[A]) Int {\n\treturn FromInt64(int64(len(s.Elements)))\n}\n\nfunc SetIsEmpty[A any](s Set[A]) bool {\n\treturn len(s.Elements) == 0\n}\n\n// setSearch returns the insertion index and whether the element was present.\nfunc setSearch[A any](s Set[A], value A, less func(A, A) bool) (int, bool) {\n\tlow, high := 0, len(s.Elements)\n\tfor low < high {\n\t\tmiddle := int(uint(low+high) >> 1)\n\t\tswitch {\n\t\tcase less(s.Elements[middle], value):\n\t\t\tlow = middle + 1\n\t\tcase less(value, s.Elements[middle]):\n\t\t\thigh = middle\n\t\tdefault:\n\t\t\treturn middle, true\n\t\t}\n\t}\n\treturn low, false\n}\n\nfunc SetMember[A any](value A, s Set[A], less func(A, A) bool) bool {\n\t_, found := setSearch(s, value, less)\n\treturn found\n}\n\n// SetInsert is a WRITER, so it allocates rather than reusing the caller's spare\n// capacity \226\128\148 the same rule list.go states.\nfunc SetInsert[A any](value A, s Set[A], less func(A, A) bool) Set[A] {\n\tindex, found := setSearch(s, value, less)\n\tif found {\n\t\treturn s\n\t}\n\telements := make([]A, 0, len(s.Elements)+1)\n\telements = append(elements, s.Elements[:index]...)\n\telements = append(elements, value)\n\telements = append(elements, s.Elements[index:]...)\n\treturn Set[A]{Elements: elements}\n}\n\nfunc SetRemove[A any](value A, s Set[A], less func(A, A) bool) Set[A] {\n\tindex, found := setSearch(s, value, less)\n\tif !found {\n\t\treturn s\n\t}\n\telements := make([]A, 0, len(s.Elements)-1)\n\telements = append(elements, s.Elements[:index]...)\n\telements = append(elements, s.Elements[index+1:]...)\n\treturn Set[A]{Elements: elements}\n}\n\n// SetToList hands back a copy in element order, so a caller cannot reach the set's own\n// storage through the list.\nfunc SetToList[A any](s Set[A]) []A {\n\tout := make([]A, len(s.Elements))\n\tcopy(out, s.Elements)\n\treturn out\n}\n\n// SetFromList sorts once and drops duplicates rather than inserting n times, which\n// would be O(n\194\178).\nfunc SetFromList[A any](values []A, less func(A, A) bool) Set[A] {\n\tout := SetEmpty[A]()\n\tif len(values) == 0 {\n\t\treturn out\n\t}\n\tsorted := ListSortBy(values, less)\n\telements := make([]A, 0, len(sorted))\n\tfor index, value := range sorted {\n\t\tif index == 0 || less(elements[len(elements)-1], value) {\n\t\t\telements = append(elements, value)\n\t\t}\n\t}\n\treturn Set[A]{Elements: elements}\n}\n\n// SetUnion, SetIntersection, and SetDifference each merge two ordered runs in one pass,\n// so they are O(n+m) rather than n lookups.\nfunc SetUnion[A any](left, right Set[A], less func(A, A) bool) Set[A] {\n\telements := make([]A, 0, len(left.Elements)+len(right.Elements))\n\tleftIndex, rightIndex := 0, 0\n\tfor leftIndex < len(left.Elements) && rightIndex < len(right.Elements) {\n\t\tswitch {\n\t\tcase less(left.Elements[leftIndex], right.Elements[rightIndex]):\n\t\t\telements = append(elements, left.Elements[leftIndex])\n\t\t\tleftIndex++\n\t\tcase less(right.Elements[rightIndex], left.Elements[leftIndex]):\n\t\t\telements = append(elements, right.Elements[rightIndex])\n\t\t\trightIndex++\n\t\tdefault:\n\t\t\telements = append(elements, left.Elements[leftIndex])\n\t\t\tleftIndex++\n\t\t\trightIndex++\n\t\t}\n\t}\n\telements = append(elements, left.Elements[leftIndex:]...)\n\telements = append(elements, right.Elements[rightIndex:]...)\n\treturn Set[A]{Elements: elements}\n}\n\nfunc SetIntersection[A any](left, right Set[A], less func(A, A) bool) Set[A] {\n\telements := make([]A, 0, min(len(left.Elements), len(right.Elements)))\n\tleftIndex, rightIndex := 0, 0\n\tfor leftIndex < len(left.Elements) && rightIndex < len(right.Elements) {\n\t\tswitch {\n\t\tcase less(left.Elements[leftIndex], right.Elements[rightIndex]):\n\t\t\tleftIndex++\n\t\tcase less(right.Elements[rightIndex], left.Elements[leftIndex]):\n\t\t\trightIndex++\n\t\tdefault:\n\t\t\telements = append(elements, left.Elements[leftIndex])\n\t\t\tleftIndex++\n\t\t\trightIndex++\n\t\t}\n\t}\n\treturn Set[A]{Elements: elements}\n}\n\nfunc SetDifference[A any](left, right Set[A], less func(A, A) bool) Set[A] {\n\telements := make([]A, 0, len(left.Elements))\n\tleftIndex, rightIndex := 0, 0\n\tfor leftIndex < len(left.Elements) && rightIndex < len(right.Elements) {\n\t\tswitch {\n\t\tcase less(left.Elements[leftIndex], right.Elements[rightIndex]):\n\t\t\telements = append(elements, left.Elements[leftIndex])\n\t\t\tleftIndex++\n\t\tcase less(right.Elements[rightIndex], left.Elements[leftIndex]):\n\t\t\trightIndex++\n\t\tdefault:\n\t\t\tleftIndex++\n\t\t\trightIndex++\n\t\t}\n\t}\n\telements = append(elements, left.Elements[leftIndex:]...)\n\treturn Set[A]{Elements: elements}\n}\n\n// SetIsSubset reports whether every element of left is also in right.\nfunc SetIsSubset[A any](left, right Set[A], less func(A, A) bool) bool {\n\trightIndex := 0\n\tfor _, value := range left.Elements {\n\t\tfor rightIndex < len(right.Elements) && less(right.Elements[rightIndex], value) {\n\t\t\trightIndex++\n\t\t}\n\t\tif rightIndex >= len(right.Elements) || less(value, right.Elements[rightIndex]) {\n\t\t\treturn false\n\t\t}\n\t\trightIndex++\n\t}\n\treturn true\n}\n\n// SetEqualBy compares in one pass, since both sides are ordered.\nfunc SetEqualBy[A any](left, right Set[A], equal func(A, A) bool) bool {\n\tif len(left.Elements) != len(right.Elements) {\n\t\treturn false\n\t}\n\tfor index := range left.Elements {\n\t\tif !equal(left.Elements[index], right.Elements[index]) {\n\t\t\treturn false\n\t\t}\n\t}\n\treturn true\n}\n");
  ("sse.go", "package teslrt\n\nimport (\n\t\"sync\"\n)\n\n// Server-Sent Events: `sseChannel C(key) = SseChannel { \226\128\166 }`, `publish C(key) payload`, and the\n// `sse \"/path\" subscribe C(key)` route that streams them.\n//\n// Events flow server\226\134\146client over ordinary HTTP on the SAME port as the API, which is why Tesl\n// has no WebSocket server: a browser subscribes with EventSource and no proxy needs special\n// configuration. The wire format is the one `tesl/sse.rkt` writes, byte for byte:\n//\n//\tan event     data: {\"channel\":\"<name>\",\"payload\":<json>}\\n\\n\n//\ta heartbeat  : heartbeat\\n\\n        every 10s, so a proxy does not reap an idle connection\n//\ton connect   : ok\\n\\n               so the browser fires onopen without waiting 10s\n//\n// PUBLISHING MUST NEVER BLOCK. A publish usually runs inside a request handler, so a listener\n// that is slow \226\128\148 or dead but still registered \226\128\148 would otherwise cost that request. Each\n// connection therefore has a BOUNDED buffer and a full buffer drops the event for that one\n// consumer, counting it. Racket learned this the hard way (issue #32: a day of development\n// left 109 stale listeners on one key, and a chat turn took over a minute).\n\ntype sseListener struct {\n\tevents chan any\n}\n\n// SseChannel is one `sseChannel` declaration: a name, and the listeners registered under each\n// channel KEY. The key is what makes a channel per-entity \226\128\148 `RunEvents(runId)` delivers only to\n// the subscribers of that run \226\128\148 and a channel declared with a fixed string is the broadcast\n// case of the same rule.\ntype SseChannel struct {\n\tname      string\n\tmutex     sync.Mutex\n\tlisteners map[string][]*sseListener\n\t// active counts live connections per channel, mirroring registry membership rather than\n\t// being derived from it: the gauge must not drift when metrics are off for a while.\n\tactive int\n}\n\nfunc NewSseChannel(name string) *SseChannel {\n\treturn &SseChannel{name: name, listeners: map[string][]*sseListener{}}\n}\n\nfunc sseChannelAttribute(name string) []Tuple2[string, string] {\n\treturn []Tuple2[string, string]{{Tuple2First: \"tesl.channel\", Tuple2Second: name}}\n}\n\n// Publish delivers one event to every listener on `key`. The payload is the ALREADY-ENCODED\n// JSON value (what the type's codec produces), for the reason the response body is: the wire\n// shape is decided by the codec, and deciding it twice is how the two ends drift.\n//\n// Delivery is non-blocking by construction \226\128\148 see the buffer note above.\nfunc Publish(channel *SseChannel, key string, payload any) struct{} {\n\tchannel.mutex.Lock()\n\tlisteners := make([]*sseListener, len(channel.listeners[key]))\n\tcopy(listeners, channel.listeners[key])\n\tchannel.mutex.Unlock()\n\tfor _, listener := range listeners {\n\t\tselect {\n\t\tcase listener.events <- payload:\n\t\tdefault:\n\t\t\t// This consumer is a whole buffer behind. The event is dropped for it alone, and\n\t\t\t// counted: a rising counter is the observable symptom of a stuck subscriber.\n\t\t\tCounter(\"tesl.sse.events.dropped\", FromInt64(1), sseChannelAttribute(channel.name))\n\t\t}\n\t}\n\treturn struct{}{}\n}\n\n// ResetChannel drops every listener, for a test block that starts from a channel nobody is on.\n// It is the channel's counterpart of `TableTruncate`.\nfunc ResetChannel(channel *SseChannel) {\n\tchannel.mutex.Lock()\n\tdefer channel.mutex.Unlock()\n\tchannel.listeners = map[string][]*sseListener{}\n\tchannel.active = 0\n}\n");
  ("sse_http.go", "package teslrt\n\nimport (\n\t\"bufio\"\n\t\"fmt\"\n\t\"io\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"strings\"\n\t\"sync\"\n\t\"time\"\n)\n\n// The HTTP half of Server-Sent Events: the route that STREAMS a channel to a browser, and the\n// api-test surface that subscribes to it. Both need the HTTP runtime, so they live apart from\n// the channel itself \226\128\148 a module that only publishes ships no server.\n\n// sseEventBufferLimit is how many undelivered events ONE connection may hold; see the\n// never-block note on the channel itself.\nconst sseEventBufferLimit = 64\n\n// sseHeartbeatInterval is Racket's 10 seconds.\nconst sseHeartbeatInterval = 10 * time.Second\n\nfunc (channel *SseChannel) register(key string) *sseListener {\n\tlistener := &sseListener{events: make(chan any, sseEventBufferLimit)}\n\tchannel.mutex.Lock()\n\tchannel.listeners[key] = append(channel.listeners[key], listener)\n\tchannel.active++\n\tcount := channel.active\n\tchannel.mutex.Unlock()\n\tGauge(\"tesl.sse.connections.active\", float64(count), sseChannelAttribute(channel.name))\n\treturn listener\n}\n\n// unregister is idempotent: every exit path calls it, and a connection that ended twice (an\n// orderly close racing a cancelled request) must not corrupt the registry.\nfunc (channel *SseChannel) unregister(key string, listener *sseListener) {\n\tchannel.mutex.Lock()\n\tremaining := channel.listeners[key][:0]\n\tremoved := false\n\tfor _, registered := range channel.listeners[key] {\n\t\tif registered == listener && !removed {\n\t\t\tremoved = true\n\t\t\tcontinue\n\t\t}\n\t\tremaining = append(remaining, registered)\n\t}\n\tif len(remaining) == 0 {\n\t\t// Drop the KEY as well: a churn of one-off keys would otherwise leak an entry each.\n\t\tdelete(channel.listeners, key)\n\t} else {\n\t\tchannel.listeners[key] = remaining\n\t}\n\tif removed && channel.active > 0 {\n\t\tchannel.active--\n\t}\n\tcount := channel.active\n\tchannel.mutex.Unlock()\n\tif removed {\n\t\tGauge(\"tesl.sse.connections.active\", float64(count), sseChannelAttribute(channel.name))\n\t}\n}\n\n// SseStream is the `sse \"/path\" subscribe C(key)` route's handler. It streams until the client\n// goes away, and it removes its listener on EVERY exit path \226\128\148 a listener that outlives its\n// connection is charged to every future publish.\nfunc SseStream(channel *SseChannel, key string) func(http.ResponseWriter, *http.Request) {\n\treturn func(writer http.ResponseWriter, request *http.Request) {\n\t\tflusher, canFlush := writer.(http.Flusher)\n\t\tif !canFlush {\n\t\t\t// Nothing can be streamed to this client. Say so rather than opening a connection\n\t\t\t// that would silently never deliver.\n\t\t\thttp.Error(writer, \"streaming unsupported\", http.StatusInternalServerError)\n\t\t\treturn\n\t\t}\n\t\twriter.Header().Set(\"Content-Type\", \"text/event-stream\")\n\t\twriter.Header().Set(\"Cache-Control\", \"no-cache\")\n\t\twriter.Header().Set(\"Connection\", \"keep-alive\")\n\t\twriter.WriteHeader(http.StatusOK)\n\n\t\tlistener := channel.register(key)\n\t\tdefer channel.unregister(key, listener)\n\n\t\t// An immediate comment so the browser fires onopen now rather than at the first\n\t\t// heartbeat: with chunked encoding it opens on the first body chunk.\n\t\tif _, err := writer.Write([]byte(\": ok\\n\\n\")); err != nil {\n\t\t\treturn\n\t\t}\n\t\tflusher.Flush()\n\n\t\theartbeat := time.NewTicker(sseHeartbeatInterval)\n\t\tdefer heartbeat.Stop()\n\t\tfor {\n\t\t\tselect {\n\t\t\tcase <-request.Context().Done():\n\t\t\t\treturn\n\t\t\tcase event := <-listener.events:\n\t\t\t\tbody := fmt.Sprintf(\"data: %s\\n\\n\", EncodeJSONValue(map[string]any{\n\t\t\t\t\t\"channel\": channel.name,\n\t\t\t\t\t\"payload\": event,\n\t\t\t\t}))\n\t\t\t\tif _, err := writer.Write([]byte(body)); err != nil {\n\t\t\t\t\treturn\n\t\t\t\t}\n\t\t\t\tflusher.Flush()\n\t\t\t\tCounter(\"tesl.sse.events.sent\", FromInt64(1), sseChannelAttribute(channel.name))\n\t\t\tcase <-heartbeat.C:\n\t\t\t\tif _, err := writer.Write([]byte(\": heartbeat\\n\\n\")); err != nil {\n\t\t\t\t\treturn\n\t\t\t\t}\n\t\t\t\tflusher.Flush()\n\t\t\t}\n\t\t}\n\t}\n}\n\n// SseStreamParam is the same route when the channel key comes from a `:param` SEGMENT of the\n// path (`sse \"/events/:userId\" subscribe Notices(userId)`): each connection keys on its own\n// segment, which is what makes the channel per-entity.\nfunc SseStreamParam(channel *SseChannel, pattern, param string) func(http.ResponseWriter, *http.Request) {\n\treturn func(writer http.ResponseWriter, request *http.Request) {\n\t\tkey, found := PathParam(pattern, request.URL.Path, param)\n\t\tif !found {\n\t\t\t// The router matched the pattern, so a missing segment is an emitter bug rather\n\t\t\t// than a client error \226\128\148 say so instead of streaming on an empty key.\n\t\t\thttp.Error(writer, \"stream key segment \"+param+\" is missing\", http.StatusInternalServerError)\n\t\t\treturn\n\t\t}\n\t\tSseStream(channel, key)(writer, request)\n\t}\n}\n\n// \226\148\128\226\148\128 api-test side \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// `subscribe \"/path\"` in an api-test opens a REAL connection to the emitted server and reads\n// the stream, rather than registering on the channel behind the route's back. That way the\n// test meets everything a browser would: the route lookup, the auth check, every declared\n// capture check, and the wire format itself. A subscription the server refuses fails the test\n// where it is written, with the status and message the client would have received.\n\ntype SseTestStream struct {\n\tname   string\n\tserver *httptest.Server\n\tbody   io.ReadCloser\n\tevents chan any\n\t// closed guards the teardown, which every exit path calls.\n\tclosed  bool\n\tmutex   sync.Mutex\n\tbacklog []any\n}\n\n// sseTestBufferLimit bounds what one test stream holds. A test that publishes more than this\n// without collecting is not measuring delivery any more.\nconst sseTestBufferLimit = 256\n\n// SubscribeStream opens `path` on the emitted server as an event stream. `cookies` are the\n// api-test cookie headers, so a stream behind `auth \226\128\166 via cookieAuth` can be subscribed the\n// way the endpoint expects.\nfunc SubscribeStream(server Server, path string, cookies []string) *SseTestStream {\n\ttestServer := httptest.NewServer(server)\n\trequest, err := http.NewRequest(http.MethodGet, testServer.URL+path, nil) //nolint:noctx // torn down by UnsubscribeStream\n\tif err != nil {\n\t\ttestServer.Close()\n\t\tpanic(\"subscribe: \" + path + \": \" + err.Error())\n\t}\n\trequest.Header.Set(\"Accept\", \"text/event-stream\")\n\tfor _, cookie := range cookies {\n\t\trequest.Header.Add(\"Cookie\", cookie)\n\t}\n\tresponse, err := http.DefaultClient.Do(request)\n\tif err != nil || response == nil {\n\t\ttestServer.Close()\n\t\tmessage := \"no response\"\n\t\tif err != nil {\n\t\t\tmessage = err.Error()\n\t\t}\n\t\tpanic(\"subscribe: \" + path + \": \" + message)\n\t}\n\tif response.StatusCode != http.StatusOK {\n\t\tbody, _ := io.ReadAll(io.LimitReader(response.Body, 4096))\n\t\t_ = response.Body.Close()\n\t\ttestServer.Close()\n\t\t// The status and message the CLIENT would have seen: a refused subscription is an\n\t\t// answer about the route, not a missing-events problem for the next assertion.\n\t\tpanic(fmt.Sprintf(\"subscribe failed for %s: %d %s\", path, response.StatusCode,\n\t\t\tstrings.TrimSpace(string(body))))\n\t}\n\tstream := &SseTestStream{\n\t\tname: path, server: testServer, body: response.Body,\n\t\tevents: make(chan any, sseTestBufferLimit),\n\t}\n\tgo stream.read()\n\treturn stream\n}\n\n// read turns the event lines into payloads. A heartbeat or the opening comment is not an\n// event; neither is a malformed line, which ends the stream rather than being reported as\n// data.\nfunc (stream *SseTestStream) read() {\n\treader := bufio.NewReader(stream.body)\n\tfor {\n\t\tline, err := reader.ReadString('\\n')\n\t\tif err != nil {\n\t\t\treturn\n\t\t}\n\t\tif !strings.HasPrefix(line, \"data: \") {\n\t\t\tcontinue\n\t\t}\n\t\tparsed, parseErr := ParseJSON([]byte(strings.TrimRight(strings.TrimPrefix(line, \"data: \"), \"\\r\\n\")))\n\t\tif parseErr != nil {\n\t\t\tcontinue\n\t\t}\n\t\t// What a test collects is the PAYLOAD, which is what Racket's api-test stream yields;\n\t\t// the channel name is already known from the path it subscribed to.\n\t\tevent := parsed\n\t\tif envelope, isObject := parsed.(map[string]any); isObject {\n\t\t\tif payload, present := envelope[\"payload\"]; present {\n\t\t\t\tevent = payload\n\t\t\t}\n\t\t}\n\t\tselect {\n\t\tcase stream.events <- event:\n\t\tdefault:\n\t\t\treturn\n\t\t}\n\t}\n}\n\n// UnsubscribeStream closes the connection and the test server behind it. Emitted at the end of\n// the block, so one block's stream cannot collect another's events.\nfunc UnsubscribeStream(stream *SseTestStream) {\n\tstream.mutex.Lock()\n\talready := stream.closed\n\tstream.closed = true\n\tstream.mutex.Unlock()\n\tif already {\n\t\treturn\n\t}\n\t_ = stream.body.Close()\n\tstream.server.Close()\n}\n\n// drain moves everything currently buffered into the backlog and answers it.\nfunc (stream *SseTestStream) drain() []any {\n\tstream.mutex.Lock()\n\tdefer stream.mutex.Unlock()\n\tfor {\n\t\tselect {\n\t\tcase event := <-stream.events:\n\t\t\tstream.backlog = append(stream.backlog, event)\n\t\tdefault:\n\t\t\tevents := make([]any, len(stream.backlog))\n\t\t\tcopy(events, stream.backlog)\n\t\t\treturn events\n\t\t}\n\t}\n}\n\nfunc (stream *SseTestStream) keep(from int) {\n\tstream.mutex.Lock()\n\tdefer stream.mutex.Unlock()\n\tif from >= len(stream.backlog) {\n\t\tstream.backlog = nil\n\t\treturn\n\t}\n\tstream.backlog = append([]any{}, stream.backlog[from:]...)\n}\n\n// ssePollInterval is Racket's 50ms poll; the collect loop is a poll on both backends because\n// the deadline belongs to the whole collect, not to one event.\nconst ssePollInterval = 50 * time.Millisecond\n\n// CollectCount waits for at least `count` events and answers everything drained \226\128\148 Racket's\n// `finish-all!`, which hands back the extra events rather than hiding them.\n//\n// A timeout is a test FAILURE, not an empty answer: the assertion that follows would otherwise\n// report \"expected non-empty\" for what is really \"the action never published\".\nfunc CollectCount(stream *SseTestStream, count Int, timeoutMillis Int) JsonValue {\n\twanted, exact := count.Int64()\n\tif !exact || wanted < 1 {\n\t\tpanic(\"collect: count must be a positive Int, got \" + count.String())\n\t}\n\tdeadline := time.Now().Add(sseTimeout(timeoutMillis))\n\tfor {\n\t\tevents := stream.drain()\n\t\tif int64(len(events)) >= wanted {\n\t\t\tstream.keep(len(events))\n\t\t\treturn JsonOf(events)\n\t\t}\n\t\tif !time.Now().Before(deadline) {\n\t\t\tpanic(sseTimeoutMessage(stream, timeoutMillis,\n\t\t\t\tfmt.Sprintf(\"count %d\", wanted), events))\n\t\t}\n\t\ttime.Sleep(ssePollInterval)\n\t}\n}\n\n// CollectUntil waits for an event MATCHING the pattern and answers the events up to and\n// including it; the rest stay on the stream for the next collect.\nfunc CollectUntil(stream *SseTestStream, until any, timeoutMillis Int) JsonValue {\n\tdeadline := time.Now().Add(sseTimeout(timeoutMillis))\n\tfor {\n\t\tevents := stream.drain()\n\t\tfor index, event := range events {\n\t\t\tif jsonMatch(until, event) {\n\t\t\t\tstream.keep(index + 1)\n\t\t\t\treturn JsonOf(events[:index+1])\n\t\t\t}\n\t\t}\n\t\tif !time.Now().Before(deadline) {\n\t\t\tpanic(sseTimeoutMessage(stream, timeoutMillis,\n\t\t\t\t\"until \"+EncodeJSONValue(until), events))\n\t\t}\n\t\ttime.Sleep(ssePollInterval)\n\t}\n}\n\n// CollectWithin waits out the whole timeout and answers whatever arrived \226\128\148 including nothing,\n// which is how a test asserts that an action published NO event.\nfunc CollectWithin(stream *SseTestStream, timeoutMillis Int) JsonValue {\n\tdeadline := time.Now().Add(sseTimeout(timeoutMillis))\n\tfor time.Now().Before(deadline) {\n\t\ttime.Sleep(ssePollInterval)\n\t}\n\tevents := stream.drain()\n\tstream.keep(len(events))\n\treturn JsonOf(events)\n}\n\nfunc sseTimeout(timeoutMillis Int) time.Duration {\n\tmillis, exact := timeoutMillis.Int64()\n\tif !exact || millis < 0 {\n\t\tpanic(\"collect: timeout must be a non-negative number of milliseconds, got \" +\n\t\t\ttimeoutMillis.String())\n\t}\n\treturn time.Duration(millis) * time.Millisecond\n}\n\nfunc sseTimeoutMessage(stream *SseTestStream, timeoutMillis Int, description string,\n\tevents []any) string {\n\tmillis, _ := timeoutMillis.Int64()\n\tduration := fmt.Sprintf(\"%dms\", millis)\n\tif millis%1000 == 0 {\n\t\tduration = fmt.Sprintf(\"%ds\", millis/1000)\n\t}\n\treturn fmt.Sprintf(\n\t\t\"collect: timed out after %s waiting for %s\\nreceived %d events on stream %q\\n\"+\n\t\t\t\"hint: did the action that produces events run successfully?\",\n\t\tduration, description, len(events), stream.name)\n}\n");
  ("string.go", "package teslrt\n\nimport (\n\t\"strings\"\n\t\"unicode/utf8\"\n)\n\n// Tesl's String is Go's string, but Tesl's string INDICES AND LENGTHS ARE CODE\n// POINTS, because the Racket backend's `string-length`, `substring`, and match\n// positions all count Racket chars. Every function here that exposes a length or an\n// index therefore converts, and none of them may use len() or a byte offset as a\n// Tesl-visible number.\n//\n// Two deliberate, documented differences from the Racket backend, both cases where\n// Go's behavior is the defensible one (see roadmap/next/migrate_to_golang.md):\n//\n//   - StringToInt accepts an optional sign followed by decimal digits, and nothing\n//     else. Racket's `string->number` also accepts reader syntax \226\128\148 \"#x1A\" is 26,\n//     \"1/1\" is the exact integer 1 \226\128\148 which is an implementation leak rather than a\n//     language surface.\n//   - StringTrim trims Unicode whitespace (Go's unicode.IsSpace), where Racket's\n//     default `\\s` is ASCII-only. Case mapping is Go's per-rune simple mapping, so\n//     \"\195\159\" uppercases to \"\195\159\" rather than Racket's full-mapping \"SS\".\nfunc StringLength(s string) Int {\n\treturn FromInt64(int64(utf8.RuneCountInString(s)))\n}\n\nfunc StringIsEmpty(s string) bool {\n\treturn s == \"\"\n}\n\nfunc StringStartsWith(s, prefix string) bool {\n\treturn strings.HasPrefix(s, prefix)\n}\n\nfunc StringEndsWith(s, suffix string) bool {\n\treturn strings.HasSuffix(s, suffix)\n}\n\nfunc StringContains(s, sub string) bool {\n\treturn strings.Contains(s, sub)\n}\n\nfunc StringConcat(left, right string) string {\n\treturn left + right\n}\n\n// StringReplace replaces every occurrence, matching Racket's string-replace.\nfunc StringReplace(s, from, to string) string {\n\treturn strings.ReplaceAll(s, from, to)\n}\n\n// StringSlice is a zero-based, code-point-indexed slice with both bounds clamped,\n// so it is total: no index can panic and no bound can be inverted.\nfunc StringSlice(s string, start, end Int) string {\n\trunes := []rune(s)\n\tlength := int64(len(runes))\n\tlo := clampIndex(start, length)\n\thi := clampIndex(end, length)\n\tif hi < lo {\n\t\thi = lo\n\t}\n\treturn string(runes[lo:hi])\n}\n\n// clampIndex pins an arbitrary-precision index into [0, length]. An Int outside\n// int64 range clamps by sign rather than truncating, which a naive conversion\n// would get exactly backwards.\nfunc clampIndex(index Int, length int64) int64 {\n\tvalue, ok := index.Int64()\n\tif !ok {\n\t\tif index.Sign() < 0 {\n\t\t\treturn 0\n\t\t}\n\t\treturn length\n\t}\n\tif value < 0 {\n\t\treturn 0\n\t}\n\tif value > length {\n\t\treturn length\n\t}\n\treturn value\n}\n\nfunc StringToUpper(s string) string {\n\treturn strings.ToUpper(s)\n}\n\nfunc StringToLower(s string) string {\n\treturn strings.ToLower(s)\n}\n\nfunc StringTrim(s string) string {\n\treturn strings.TrimSpace(s)\n}\n\nfunc StringFromInt(value Int) string {\n\treturn value.String()\n}\n\n// StringToInt parses an optional sign followed by decimal digits; anything else is\n// Nothing. Arbitrary precision is preserved.\nfunc StringToInt(s string) Maybe[Int] {\n\tvalue, err := ParseDecimal(s)\n\tif err != nil {\n\t\treturn Nothing[Int]()\n\t}\n\treturn Something(value)\n}\n\n// StringIndexOf reports the first occurrence as a CODE POINT index, not a byte\n// offset \226\128\148 the two differ for any string with a multi-byte rune before the match.\nfunc StringIndexOf(s, sub string) Maybe[Int] {\n\toffset := strings.Index(s, sub)\n\tif offset < 0 {\n\t\treturn Nothing[Int]()\n\t}\n\treturn Something(FromInt64(int64(utf8.RuneCountInString(s[:offset]))))\n}\n\nfunc StringDropPrefix(s, prefix string) string {\n\treturn strings.TrimPrefix(s, prefix)\n}\n\nfunc StringDropSuffix(s, suffix string) string {\n\treturn strings.TrimSuffix(s, suffix)\n}\n\n// StringPadLeft pads to a code-point width using the first rune of pad. A string\n// already at or over the width is returned unchanged, never truncated.\nfunc StringPadLeft(s string, width Int, pad string) string {\n\tfill, count := padding(s, width, pad)\n\tif count <= 0 {\n\t\treturn s\n\t}\n\treturn strings.Repeat(fill, count) + s\n}\n\nfunc StringPadRight(s string, width Int, pad string) string {\n\tfill, count := padding(s, width, pad)\n\tif count <= 0 {\n\t\treturn s\n\t}\n\treturn s + strings.Repeat(fill, count)\n}\n\nfunc padding(s string, width Int, pad string) (string, int) {\n\tfill, size := utf8.DecodeRuneInString(pad)\n\tif size == 0 {\n\t\treturn \"\", 0\n\t}\n\ttarget, ok := width.Int64()\n\tif !ok {\n\t\t// A width beyond int64 cannot be materialised; treat it as no padding\n\t\t// rather than attempting an allocation that would kill the process.\n\t\treturn string(fill), 0\n\t}\n\tcurrent := int64(utf8.RuneCountInString(s))\n\tif target <= current {\n\t\treturn string(fill), 0\n\t}\n\treturn string(fill), int(target - current)\n}\n\nfunc StringRepeat(s string, times Int) string {\n\tcount, ok := times.Int64()\n\tif !ok || count <= 0 {\n\t\treturn \"\"\n\t}\n\treturn strings.Repeat(s, int(count))\n}\n\n// StringReverse reverses by code point, matching Racket's string->list round trip.\nfunc StringReverse(s string) string {\n\trunes := []rune(s)\n\tfor left, right := 0, len(runes)-1; left < right; left, right = left+1, right-1 {\n\t\trunes[left], runes[right] = runes[right], runes[left]\n\t}\n\treturn string(runes)\n}\n\n// StringRequireNonEmpty is Tesl's `String.requireNonEmpty` check: the proof erases,\n// the rejection does not.\nfunc StringRequireNonEmpty(s string) Check[string] {\n\tif s == \"\" {\n\t\treturn Reject[string](400, \"expected a non-empty string\")\n\t}\n\treturn Accept(s)\n}\n");
  ("table.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"sort\"\n\t\"strings\"\n\t\"sync\"\n)\n\n// An in-memory table, the `backend: Memory` store.\n//\n// Rows are held as VALUES, and every read returns a copy of the slice header's contents\n// rather than the table's own storage \226\128\148 the same \"only writers allocate, readers may not\n// alias the store\" rule list.go states, applied here because a table is mutable and long\n// lived where a Tesl list is neither.\n//\n// The mutex is not decoration: an HTTP server serves each request on its own goroutine, so\n// two handlers touching the same table race by construction. The gates run `go test -race`\n// on emitted code, which is what would catch a missing lock.\ntype Table[Row any] struct {\n\tmutex sync.RWMutex\n\trows  []Row\n}\n\nfunc NewTable[Row any]() *Table[Row] {\n\treturn &Table[Row]{}\n}\n\n// TableInsert stores a row, rejecting one whose primary key is already present \226\128\148\n// `conflicts(existing, inserted)` compares the two keys, since their type is known only\n// to the emitter. The Racket memory backend keys its store BY the primary key and raises\n// on a duplicate, so this is parity rather than an extra: a table that silently accepted\n// two rows with one key would answer `selectOne` differently on the two backends.\nfunc TableInsert[Row any](table *Table[Row], entity string, row Row, conflicts func(Row, Row) bool,\n\tunique ...UniqueIndex[Row]) Row {\n\ttable.mutex.Lock()\n\tdefer table.mutex.Unlock()\n\ttable.checkUniqueLocked(entity, row, unique, -1)\n\ttable.insertLocked(entity, row, conflicts)\n\treturn row\n}\n\n// A declared `unique index [a, b]`. It is an INVARIANT rather than a performance hint, so the\n// in-memory store enforces it: the whole point of that store is that a test fails the way\n// production fails, and skipping the check would let `tesl test` pass on data PostgreSQL\n// rejects. On a Postgres-backed entity the SERVER enforces it \226\128\148 the bootstrap creates the\n// index \226\128\148 so the check here is the memory path's alone.\ntype UniqueIndex[Row any] struct {\n\t// Entity as the declaration names it, which is what the refusal names.\n\tEntity string\n\t// Columns as the declaration lists them, which is what the refusal names.\n\tColumns []string\n\t// Constrained is false when the row holds a NULL in an indexed column: two NULLs are not\n\t// equal, so such a row is unconstrained. PostgreSQL's rule, and Racket's.\n\tConstrained func(Row) bool\n\tSame        func(Row, Row) bool\n\t// Describe renders the indexed values for the refusal.\n\tDescribe func(Row) string\n}\n\n// UniqueIndexOf is what a declaration emits. A constructor rather than a struct literal for\n// the reason `NewDatabase` is one: gofmt ALIGNS the keys of a multi-line composite literal and\n// breaks the alignment run at a nested multi-line value, a rule the emitter would have to\n// reproduce exactly at every shape. A call with one argument per line is stable at every size.\nfunc UniqueIndexOf[Row any](entity string, columns []string, constrained func(Row) bool,\n\tsame func(Row, Row) bool, describe func(Row) string) UniqueIndex[Row] {\n\treturn UniqueIndex[Row]{\n\t\tEntity:      entity,\n\t\tColumns:     columns,\n\t\tConstrained: constrained,\n\t\tSame:        same,\n\t\tDescribe:    describe,\n\t}\n}\n\n// The caller holds the write lock. `skip` is the position of the row being REPLACED, so an\n// update never conflicts with itself; -1 skips nothing.\nfunc (table *Table[Row]) checkUniqueLocked(entity string, row Row, indexes []UniqueIndex[Row],\n\tskip int) {\n\tfor _, index := range indexes {\n\t\tif index.Constrained != nil && !index.Constrained(row) {\n\t\t\tcontinue\n\t\t}\n\t\tfor position, existing := range table.rows {\n\t\t\tif position == skip || !index.Same(existing, row) {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tpanic(\"entity \" + entity + \" already contains a row with (\" +\n\t\t\t\tstrings.Join(index.Columns, \" \") + \") = \" + index.Describe(row) +\n\t\t\t\t\"; the declared unique index on (\" + strings.Join(index.Columns, \", \") +\n\t\t\t\t\") forbids duplicates\")\n\t\t}\n\t}\n}\n\n// TableInsertMany inserts in order, and a row conflicting with an EARLIER row of the same\n// batch is rejected just as it would be if the two were inserted separately \226\128\148 Racket's\n// insert-many! is a loop over insert-one!.\nfunc TableInsertMany[Row any](table *Table[Row], entity string, rows []Row,\n\tconflicts func(Row, Row) bool, unique ...UniqueIndex[Row]) struct{} {\n\ttable.mutex.Lock()\n\tdefer table.mutex.Unlock()\n\tfor _, row := range rows {\n\t\ttable.checkUniqueLocked(entity, row, unique, -1)\n\t\ttable.insertLocked(entity, row, conflicts)\n\t}\n\treturn struct{}{}\n}\n\n// The caller holds the write lock.\nfunc (table *Table[Row]) insertLocked(entity string, row Row, conflicts func(Row, Row) bool) {\n\tfor _, existing := range table.rows {\n\t\tif conflicts(existing, row) {\n\t\t\tpanic(fmt.Sprintf(\"insert: entity %s already contains a row with that primary key\", entity))\n\t\t}\n\t}\n\ttable.rows = append(table.rows, row)\n}\n\n// TableSelectOne returns the FIRST row matching, in insertion order. Tesl's `selectOne`\n// yields `Maybe`, so \"no row\" is a value rather than an error.\nfunc TableSelectOne[Row any](table *Table[Row], match func(Row) bool) Maybe[Row] {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\tfor _, row := range table.rows {\n\t\tif match(row) {\n\t\t\treturn Something(row)\n\t\t}\n\t}\n\treturn Nothing[Row]()\n}\n\nfunc TableSelect[Row any](table *Table[Row], match func(Row) bool) []Row {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\tout := make([]Row, 0, len(table.rows))\n\tfor _, row := range table.rows {\n\t\tif match(row) {\n\t\t\tout = append(out, row)\n\t\t}\n\t}\n\treturn out\n}\n\n// An aggregate walks the rows in place: it needs no copy of them, and on the Memory store\n// it is the whole implementation. On a SQL backend an aggregate must instead become a\n// `SELECT COUNT/SUM/MAX(...) ... WHERE ...` \226\128\148 the work belongs in the database, and the\n// query structure to build it from is the same recovered seed. Nothing here should be\n// carried over to that path.\nfunc TableCount[Row any](table *Table[Row], match func(Row) bool) Int {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\tmatched := 0\n\tfor _, row := range table.rows {\n\t\tif match(row) {\n\t\t\tmatched++\n\t\t}\n\t}\n\treturn FromInt64(int64(matched))\n}\n\n// TableSelectRange is `limit` / `offset` without an `order` clause: rows come back in\n// insertion order, which is the order the un-ordered Racket path yields as well. A\n// negative `limit` means \"no limit\".\nfunc TableSelectRange[Row any](table *Table[Row], match func(Row) bool, offset, limit int) []Row {\n\treturn sliceRange(TableSelect(table, match), offset, limit)\n}\n\n// TableSelectSorted is `order by`, plus any `limit`/`offset` applied after the sort.\n// `less` reports strictly-before, and the sort is STABLE, so rows comparing equal keep\n// their insertion order.\nfunc TableSelectSorted[Row any](table *Table[Row], match func(Row) bool, less func(Row, Row) bool, offset, limit int) []Row {\n\trows := TableSelect(table, match)\n\tsort.SliceStable(rows, func(left, right int) bool { return less(rows[left], rows[right]) })\n\treturn sliceRange(rows, offset, limit)\n}\n\n// TableSelectOneSorted is `selectOne \226\128\166 order \226\128\166`: the first row after sorting.\nfunc TableSelectOneSorted[Row any](table *Table[Row], match func(Row) bool, less func(Row, Row) bool) Maybe[Row] {\n\trows := TableSelectSorted(table, match, less, 0, 1)\n\tif len(rows) == 0 {\n\t\treturn Nothing[Row]()\n\t}\n\treturn Something(rows[0])\n}\n\n// TableFold is the scalar aggregate (`selectSum`): the matching rows' projections\n// combined left to right from `zero`, so an empty table yields `zero` \226\128\148 the same answer\n// SQL's SUM-over-no-rows gives once Tesl has typed it as the column type.\nfunc TableFold[Row any, Value any](table *Table[Row], match func(Row) bool, project func(Row) Value, combine func(Value, Value) Value, zero Value) Value {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\ttotal := zero\n\tfor _, row := range table.rows {\n\t\tif match(row) {\n\t\t\ttotal = combine(total, project(row))\n\t\t}\n\t}\n\treturn total\n}\n\n// TableSumMoney is `selectSum` over a MONEY column. It is not a `TableFold` because the fold's\n// zero would need a currency before any row has been seen; instead the currency is adopted from\n// the first matching row and every later row is checked against it, in ONE pass over the same\n// rows under the same read lock \226\128\148 nothing is materialised.\n//\n// The two refusals are Racket's (`money-sum-result`), and both are about answering honestly\n// rather than plausibly: a total over no rows has no currency to carry (`$0.00` would be a\n// guess), and a total across currencies needs a rate, which is dated data a SUM may not invent.\nfunc TableSumMoney[Row any](table *Table[Row], match func(Row) bool, project func(Row) Money,\n\tentity, field string) Money {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\ttotal := FromInt64(0)\n\tvar currency Currency\n\tdistinct := map[string]struct{}{}\n\tfor _, row := range table.rows {\n\t\tif !match(row) {\n\t\t\tcontinue\n\t\t}\n\t\tamount := project(row)\n\t\tif len(distinct) == 0 {\n\t\t\tcurrency = amount.Currency\n\t\t}\n\t\tdistinct[amount.Currency.Code] = struct{}{}\n\t\ttotal = Add(total, amount.MinorUnits)\n\t}\n\treturn MoneySumResult(entity, field, total, currency, len(distinct))\n}\n\n// MoneySumResult is the rule both stores answer a Money sum by, so the two cannot phrase a\n// refusal differently: the counted currencies decide, and the wording is Racket's\n// (`money-sum-result`) down to the count it names.\nfunc MoneySumResult(entity, field string, total Int, currency Currency, distinct int) Money {\n\tif distinct == 0 {\n\t\tpanic(\"field \" + field + \" on entity \" + entity +\n\t\t\t\": cannot sum Money over an empty row set (no currency for the zero total);\" +\n\t\t\t\" guard with a count first\")\n\t}\n\tif distinct > 1 {\n\t\tpanic(\"field \" + field + \" on entity \" + entity +\n\t\t\t\": cannot sum Money across mixed currencies (found \" + FromInt64(int64(distinct)).String() +\n\t\t\t\"); filter by currency first\")\n\t}\n\treturn Money{MinorUnits: total, Currency: currency}\n}\n\n// TableExtreme is `selectMax` / `selectMin`: the projection of the matching rows that no\n// other beats, where `better(candidate, best)` reports that the candidate wins.\n//\n// The answer is a `Maybe`: with no matching row there is no value of the column's type to\n// return, and unlike `selectSum` there is no identity to fall back on. Racket's aggregates\n// answer `Nothing` in the same case.\nfunc TableExtreme[Row any, Value any](table *Table[Row], match func(Row) bool, project func(Row) Value, better func(Value, Value) bool) Maybe[Value] {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\tvar best Value\n\tfound := false\n\tfor _, row := range table.rows {\n\t\tif !match(row) {\n\t\t\tcontinue\n\t\t}\n\t\tcandidate := project(row)\n\t\tif !found || better(candidate, best) {\n\t\t\tbest = candidate\n\t\t\tfound = true\n\t\t}\n\t}\n\tif !found {\n\t\treturn Nothing[Value]()\n\t}\n\treturn Something(best)\n}\n\n// SqlLike is the `like` / `ilike` predicate: `%` matches any run of characters and `_`\n// exactly one, and the whole value must match. It is a direct matcher rather than a\n// translation to a regular expression, so a pattern can never inject one.\nfunc SqlLike(value, pattern string, foldCase bool) bool {\n\tif foldCase {\n\t\tvalue = strings.ToLower(value)\n\t\tpattern = strings.ToLower(pattern)\n\t}\n\ttext := []rune(value)\n\tglob := []rune(pattern)\n\t// Standard backtracking match: `star` remembers where the last `%` was, so a failure\n\t// after it resumes by letting that `%` consume one more character.\n\ttextAt, globAt, star, retry := 0, 0, -1, 0\n\tfor textAt < len(text) {\n\t\tswitch {\n\t\tcase globAt < len(glob) && (glob[globAt] == '_' || glob[globAt] == text[textAt]):\n\t\t\ttextAt++\n\t\t\tglobAt++\n\t\tcase globAt < len(glob) && glob[globAt] == '%':\n\t\t\tstar = globAt\n\t\t\tglobAt++\n\t\t\tretry = textAt\n\t\tcase star >= 0:\n\t\t\tglobAt = star + 1\n\t\t\tretry++\n\t\t\ttextAt = retry\n\t\tdefault:\n\t\t\treturn false\n\t\t}\n\t}\n\tfor globAt < len(glob) && glob[globAt] == '%' {\n\t\tglobAt++\n\t}\n\treturn globAt == len(glob)\n}\n\nfunc sliceRange[Row any](rows []Row, offset, limit int) []Row {\n\tif offset >= len(rows) {\n\t\treturn []Row{}\n\t}\n\trows = rows[offset:]\n\tif limit >= 0 && limit < len(rows) {\n\t\trows = rows[:limit]\n\t}\n\treturn rows\n}\n\n// TableDelete removes every matching row. `delete` is a statement in Tesl, so the count\n// goes nowhere; TableDeleteCount is the form that reports it.\nfunc TableDelete[Row any](table *Table[Row], match func(Row) bool) struct{} {\n\tTableDeleteCount(table, match)\n\treturn struct{}{}\n}\n\nfunc TableDeleteCount[Row any](table *Table[Row], match func(Row) bool) Int {\n\ttable.mutex.Lock()\n\tdefer table.mutex.Unlock()\n\tkept := make([]Row, 0, len(table.rows))\n\tremoved := 0\n\tfor _, row := range table.rows {\n\t\tif match(row) {\n\t\t\tremoved++\n\t\t\tcontinue\n\t\t}\n\t\tkept = append(kept, row)\n\t}\n\ttable.rows = kept\n\treturn FromInt64(int64(removed))\n}\n\n// TableUpdate replaces every matching row with the result of `apply`. Like `delete`, a\n// plain `update` is a statement, so nothing is reported back.\nfunc TableUpdate[Row any](table *Table[Row], match func(Row) bool, apply func(Row) Row,\n\tunique ...UniqueIndex[Row]) struct{} {\n\ttable.mutex.Lock()\n\tdefer table.mutex.Unlock()\n\tfor index, row := range table.rows {\n\t\tif !match(row) {\n\t\t\tcontinue\n\t\t}\n\t\tupdated := apply(row)\n\t\ttable.checkUniqueLocked(uniqueEntity(unique), updated, unique, index)\n\t\ttable.rows[index] = updated\n\t}\n\treturn struct{}{}\n}\n\n// The entity NAME a unique-index refusal carries. `update` does not take one \226\128\148 nothing else in\n// its emission needs it \226\128\148 so it travels on the index, where the refusal is built.\nfunc uniqueEntity[Row any](indexes []UniqueIndex[Row]) string {\n\tif len(indexes) == 0 {\n\t\treturn \"\"\n\t}\n\treturn indexes[0].Entity\n}\n\n// TableUpdateReturnOne is `updateAndReturnOne`: it yields the FIRST updated row, and\n// traps when the predicate matched nothing \226\128\148 Racket takes the `car` of the updated rows,\n// which errors on an empty list, so \"no row updated\" is a failure on both backends rather\n// than a zero value.\nfunc TableUpdateReturnOne[Row any](table *Table[Row], match func(Row) bool, apply func(Row) Row,\n\tunique ...UniqueIndex[Row]) Row {\n\ttable.mutex.Lock()\n\tdefer table.mutex.Unlock()\n\tfor index, row := range table.rows {\n\t\tif !match(row) {\n\t\t\tcontinue\n\t\t}\n\t\tupdated := apply(row)\n\t\ttable.checkUniqueLocked(uniqueEntity(unique), updated, unique, index)\n\t\ttable.rows[index] = updated\n\t\treturn updated\n\t}\n\tpanic(\"updateAndReturnOne: no row matched\")\n}\n\n// TableTruncate empties the table, for a test that wants a fresh store.\nfunc TableTruncate[Row any](table *Table[Row]) {\n\ttable.mutex.Lock()\n\tdefer table.mutex.Unlock()\n\ttable.rows = nil\n}\n\n// `deleteAndReturnResult` answers a `DeleteResult`, which is `NoRowDeleted` or `RowsDeleted n`.\n// It is a runtime-provided ADT for the reason `Maybe` is: it crosses module boundaries, so it\n// cannot be emitted once per module that mentions it.\n//\n// The distinction it draws is the one a plain count does not: deleting nothing is a different\n// OUTCOME from deleting rows, and a caller that has to act on \"nothing matched\" reads it as a\n// case rather than as a comparison with zero.\ntype DeleteResultTag int\n\nconst (\n\tDeleteResultNoRowDeleted DeleteResultTag = iota\n\tDeleteResultRowsDeleted\n)\n\ntype DeleteResult struct {\n\tTag              DeleteResultTag\n\tRowsDeletedCount Int\n}\n\nfunc NoRowDeleted() DeleteResult {\n\treturn DeleteResult{Tag: DeleteResultNoRowDeleted}\n}\n\nfunc RowsDeleted(count Int) DeleteResult {\n\treturn DeleteResult{Tag: DeleteResultRowsDeleted, RowsDeletedCount: count}\n}\n\n// TableDeleteResult is `deleteAndReturnResult` over the in-memory store.\nfunc TableDeleteResult[Row any](table *Table[Row], match func(Row) bool) DeleteResult {\n\tremoved := TableDeleteCount(table, match)\n\tif Compare(removed, FromInt64(0)) == 0 {\n\t\treturn NoRowDeleted()\n\t}\n\treturn RowsDeleted(removed)\n}\n\n// TableAny reports whether any row matches. It is what an `innerJoin` becomes on the in-memory\n// store: the clause filters the MAIN entity's rows to those with a counterpart, and the result\n// is still a list of that entity \226\128\148 so it is an existence test, not a product.\n//\n// That reading is also why the Postgres path emits `exists (\226\128\166)` rather than a real INNER JOIN:\n// a join would DUPLICATE a main row for every counterpart, which a `List Order` cannot hold.\n// The Racket backends disagree here \226\128\148 its memory store tests existence while its Postgres path\n// joins \226\128\148 and the two Go paths agree with each other and with the declared result type.\nfunc TableAny[Row any](table *Table[Row], match func(Row) bool) bool {\n\ttable.mutex.RLock()\n\tdefer table.mutex.RUnlock()\n\tfor _, row := range table.rows {\n\t\tif match(row) {\n\t\t\treturn true\n\t\t}\n\t}\n\treturn false\n}\n");
  ("telemetry.go", "package teslrt\n\nimport (\n\t\"fmt\"\n\t\"os\"\n\t\"sort\"\n\t\"strings\"\n\t\"sync\"\n)\n\n// `Tesl.Telemetry`: the ambient signals \226\128\148 a telemetry EVENT (`telemetry \"name\" { \226\128\166 }`) and the\n// three metric instruments (`counter`, `histogram`, `gauge`).\n//\n// Ambient by design, and that is a language decision rather than an implementation shortcut: an\n// observability call that needed a capability would either be threaded through every signature\n// or be left out of the code that most needs it. So there is nothing to grant and nothing to\n// pass; what a program configures is the SINK, once, in `main`.\n//\n// WHAT THIS RECORDS AND WHAT IT DOES NOT EXPORT. Events and metrics accumulate in process, and\n// with `console True` they are written to stderr. OTLP export \226\128\148 the wire protocol, the batching\n// exporter, the `/v1/metrics` endpoint, and the span tree the Racket runtime builds around every\n// request and outbound call \226\128\148 is NOT here yet. That is a deliberate boundary rather than an\n// oversight: the corpus configures `endpoint \"in-memory\"`, every assertion in it is that\n// telemetry does not disturb the program's own answers, and an exporter that silently dropped\n// spans would be worse than one that is honestly absent. A program that needs the wire today\n// runs on the Racket backend.\n//\n// The recorder is guarded by one mutex. Metrics are per (name, attribute-set), which is what\n// makes a counter cumulative rather than per-call, and the attribute set is normalised to a\n// sorted key so `{a=1,b=2}` and `{b=2,a=1}` are one series.\ntype TelemetryEvent struct {\n\tService     string\n\tEndpoint    string\n\tMessage     string\n\tAttributes  []Tuple2[string, string]\n\tTimestampMs int64\n}\n\n// MetricKind distinguishes the three instruments. A counter SUMS, a gauge keeps the LAST value,\n// and a histogram keeps the shape of a distribution (count/sum/min/max) \226\128\148 not the bucket\n// boundaries an OTLP histogram carries, which belong with the exporter.\ntype MetricKind int\n\nconst (\n\tMetricCounter MetricKind = iota\n\tMetricHistogram\n\tMetricGauge\n)\n\n// MetricSeries is one instrument at one attribute set.\ntype MetricSeries struct {\n\tName       string\n\tKind       MetricKind\n\tAttributes []Tuple2[string, string]\n\tCount      int64\n\tSum        float64\n\tLast       float64\n\tMin        float64\n\tMax        float64\n}\n\nvar telemetry = struct {\n\tmutex             sync.Mutex\n\tservice           string\n\tendpoint          string\n\tconsole           bool\n\tmetricsEnabled    bool\n\ttracesEnabled     bool\n\ttraceRatio        float64\n\tmetricsIntervalMs int\n\tevents            []TelemetryEvent\n\tseries            map[string]*MetricSeries\n}{service: \"tesl\", metricsEnabled: true, traceRatio: 1.0, series: map[string]*MetricSeries{}}\n\n// InitTelemetry is `initTelemetry service \226\128\166 endpoint \226\128\166 console \226\128\166`: it configures the sink, once,\n// from `main`. Calling it twice replaces the configuration and clears what was recorded, which\n// is what a fresh process would have.\nfunc InitTelemetry(service, endpoint string, console, metrics, traces bool,\n\tmetricsIntervalMs int, traceRatio float64) struct{} {\n\ttelemetry.mutex.Lock()\n\tdefer telemetry.mutex.Unlock()\n\tif service != \"\" {\n\t\ttelemetry.service = service\n\t}\n\ttelemetry.endpoint = endpoint\n\ttelemetry.console = console\n\ttelemetry.metricsEnabled = metrics\n\ttelemetry.tracesEnabled = traces\n\ttelemetry.metricsIntervalMs = metricsIntervalMs\n\ttelemetry.traceRatio = traceRatio\n\ttelemetry.events = nil\n\ttelemetry.series = map[string]*MetricSeries{}\n\tif endpoint != \"\" && endpoint != \"in-memory\" {\n\t\t// Said once, at configuration time, rather than silently dropping every span and metric\n\t\t// for the lifetime of the process.\n\t\tfmt.Fprintf(os.Stderr,\n\t\t\t\"tesl: telemetry endpoint %q is configured, but this runtime records in process only \"+\n\t\t\t\t\"\226\128\148 OTLP export is not implemented on the Go backend yet.\\n\", endpoint)\n\t}\n\treturn struct{}{}\n}\n\n// Telemetry is the `telemetry \"name\" { key = value }` block: one event, with its attributes\n// already rendered to text by the emitter (which knows each field's type, and renders a `secret`\n// as its redaction rather than its payload).\nfunc Telemetry(message string, attributes []Tuple2[string, string]) struct{} {\n\tevent := TelemetryEvent{\n\t\tService:     telemetry.service,\n\t\tEndpoint:    telemetry.endpoint,\n\t\tMessage:     message,\n\t\tAttributes:  attributes,\n\t\tTimestampMs: telemetryNowMillis(),\n\t}\n\ttelemetry.mutex.Lock()\n\ttelemetry.events = append(telemetry.events, event)\n\tconsole := telemetry.console\n\ttelemetry.mutex.Unlock()\n\tif console {\n\t\tfmt.Fprintf(os.Stderr, \"[telemetry] %s %s%s\\n\", event.Service, message,\n\t\t\trenderAttributes(attributes))\n\t}\n\treturn struct{}{}\n}\n\n// telemetryNowMillis is the event timestamp. An Int wider than int64 cannot be a wall clock, so\n// the conversion is total here in a way it is not in general.\nfunc telemetryNowMillis() int64 {\n\tmillis, _ := NowMillis().Value.Int64()\n\treturn millis\n}\n\nfunc renderAttributes(attributes []Tuple2[string, string]) string {\n\tif len(attributes) == 0 {\n\t\treturn \"\"\n\t}\n\tparts := make([]string, 0, len(attributes))\n\tfor _, attribute := range attributes {\n\t\tparts = append(parts, attribute.Tuple2First+\"=\"+attribute.Tuple2Second)\n\t}\n\treturn \" {\" + strings.Join(parts, \", \") + \"}\"\n}\n\n// seriesKey is the identity of a metric series: its name plus its attribute set, normalised so\n// the same attributes in a different order are the same series.\nfunc seriesKey(name string, kind MetricKind, attributes []Tuple2[string, string]) string {\n\tpairs := make([]string, 0, len(attributes))\n\tfor _, attribute := range attributes {\n\t\tpairs = append(pairs, attribute.Tuple2First+\"\\x00\"+attribute.Tuple2Second)\n\t}\n\tsort.Strings(pairs)\n\treturn fmt.Sprintf(\"%d\\x00%s\\x00%s\", kind, name, strings.Join(pairs, \"\\x01\"))\n}\n\nfunc recordMetric(name string, kind MetricKind, value float64,\n\tattributes []Tuple2[string, string]) {\n\ttelemetry.mutex.Lock()\n\tdefer telemetry.mutex.Unlock()\n\tif !telemetry.metricsEnabled {\n\t\treturn\n\t}\n\tkey := seriesKey(name, kind, attributes)\n\tseries, found := telemetry.series[key]\n\tif !found {\n\t\tseries = &MetricSeries{Name: name, Kind: kind, Attributes: attributes,\n\t\t\tMin: value, Max: value}\n\t\ttelemetry.series[key] = series\n\t}\n\tseries.Count++\n\tseries.Sum += value\n\tseries.Last = value\n\tif value < series.Min {\n\t\tseries.Min = value\n\t}\n\tif value > series.Max {\n\t\tseries.Max = value\n\t}\n}\n\n// Counter is `counter \"name\" delta attrs`: CUMULATIVE, which is why the series is kept rather\n// than the call. A negative delta is allowed for the same reason Racket allows it \226\128\148 the\n// instrument records what the program says.\nfunc Counter(name string, delta Int, attributes []Tuple2[string, string]) struct{} {\n\trecordMetric(name, MetricCounter, delta.Float64(), attributes)\n\treturn struct{}{}\n}\n\nfunc Histogram(name string, value float64, attributes []Tuple2[string, string]) struct{} {\n\trecordMetric(name, MetricHistogram, value, attributes)\n\treturn struct{}{}\n}\n\nfunc Gauge(name string, value float64, attributes []Tuple2[string, string]) struct{} {\n\trecordMetric(name, MetricGauge, value, attributes)\n\treturn struct{}{}\n}\n\n// \226\148\128\226\148\128 Inspection seams \226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\226\148\128\n//\n// Not Tesl-surface names: they exist so the runtime's own tests can assert what was recorded,\n// and so a program that has shed Tesl can read its own telemetry without an exporter.\n\nfunc TelemetryEvents() []TelemetryEvent {\n\ttelemetry.mutex.Lock()\n\tdefer telemetry.mutex.Unlock()\n\tout := make([]TelemetryEvent, len(telemetry.events))\n\tcopy(out, telemetry.events)\n\treturn out\n}\n\n// MetricSeriesSnapshot is every series, in a deterministic order (by name, then by key), so a\n// test or a dump reads the same way twice.\nfunc MetricSeriesSnapshot() []MetricSeries {\n\ttelemetry.mutex.Lock()\n\tdefer telemetry.mutex.Unlock()\n\tkeys := make([]string, 0, len(telemetry.series))\n\tfor key := range telemetry.series {\n\t\tkeys = append(keys, key)\n\t}\n\tsort.Strings(keys)\n\tout := make([]MetricSeries, 0, len(keys))\n\tfor _, key := range keys {\n\t\tif series := telemetry.series[key]; series != nil {\n\t\t\tout = append(out, *series)\n\t\t}\n\t}\n\tsort.SliceStable(out, func(left, right int) bool { return out[left].Name < out[right].Name })\n\treturn out\n}\n\n// ResetTelemetry clears what was recorded, for a test that asserts on it. The emitted per-test\n// reset calls it for the same reason it truncates tables: one test block's signals must not be\n// another's.\nfunc ResetTelemetry() {\n\ttelemetry.mutex.Lock()\n\tdefer telemetry.mutex.Unlock()\n\ttelemetry.events = nil\n\ttelemetry.series = map[string]*MetricSeries{}\n}\n\n// MillisOf narrows a Tesl `Int` interval to the plain int the recorder keeps. An interval outside\n// int64 is a configuration mistake rather than a duration, so it fails at configuration time.\nfunc MillisOf(value Int) int {\n\tmillis, exact := value.Int64()\n\tif !exact || millis < 0 {\n\t\tpanic(\"initTelemetry: \" + value.String() + \" is not a millisecond interval\")\n\t}\n\treturn int(millis)\n}\n");
  ("time.go", "package teslrt\n\nimport (\n\t\"math/big\"\n\t\"time\"\n)\n\n// PosixMillis is Tesl's canonical instant: milliseconds since the Unix epoch, UTC.\n//\n// It is a NEWTYPE over the exact integer, not an int64 \226\128\148 `tesl/time.rkt` defines it as\n// `(define-newtype PosixMillis Integer)` and Racket's integers are exact and unbounded, so\n// arithmetic on a timestamp cannot silently wrap here either. The type lives in the runtime\n// rather than being emitted per module for the reason `Maybe` does: an instant crosses\n// module boundaries, and two packages declaring their own would be different Go types.\ntype PosixMillis struct {\n\tValue Int\n}\n\n// NowMillis reads the clock. Tesl gates this behind the `time` capability, which the\n// checker enforces; nothing about that survives to run time.\nfunc NowMillis() PosixMillis {\n\treturn PosixMillis{Value: FromInt64(time.Now().UnixMilli())}\n}\n\n// PosixToSeconds truncates toward zero, which is what Racket's `quotient` does \226\128\148 NOT floor,\n// so an instant before the epoch rounds up toward it on both backends.\nfunc PosixToSeconds(instant PosixMillis) Int {\n\treturn MustQuo(instant.Value, FromInt64(1000))\n}\n\nfunc SecondsToPosix(seconds Int) PosixMillis {\n\treturn PosixMillis{Value: Mul(seconds, FromInt64(1000))}\n}\n\nfunc AddMs(instant PosixMillis, delta Int) PosixMillis {\n\treturn PosixMillis{Value: Add(instant.Value, delta)}\n}\n\nfunc SubtractMs(instant PosixMillis, delta Int) PosixMillis {\n\treturn PosixMillis{Value: Sub(instant.Value, delta)}\n}\n\n// DiffMs is later minus earlier, in the argument order Tesl uses: `diffMs a b` = b - a.\nfunc DiffMs(earlier, later PosixMillis) Int {\n\treturn Sub(later.Value, earlier.Value)\n}\n\n// DurationMs is how long ago an instant was, clamped at zero \226\128\148 a clock that went backwards\n// must not produce a negative age (Racket clamps with `max` for the same reason).\nfunc DurationMs(past PosixMillis) Int {\n\telapsed := DiffMs(past, NowMillis())\n\tif Compare(elapsed, FromInt64(0)) < 0 {\n\t\treturn FromInt64(0)\n\t}\n\treturn elapsed\n}\n\n// The units-typed instant surface: `Time.add ts (Duration.hours 2.0)`. The millisecond forms\n// (`addMs`, `diffMs`) stay canonical \226\128\148 they are exact integer arithmetic \226\128\148 and these convert a\n// Duration, which is SI SECONDS as a float, into that exact millisecond count.\n//\n// Seconds \226\134\146 milliseconds rounds HALF-EVEN on the exact rational, the same stance Money takes,\n// so an instant cannot drift by a millisecond over a long series of additions.\nfunc TimeAdd(instant PosixMillis, duration float64) PosixMillis {\n\treturn PosixMillis{Value: Add(instant.Value, DurationToMillis(duration))}\n}\n\nfunc TimeSubtract(instant PosixMillis, duration float64) PosixMillis {\n\treturn PosixMillis{Value: Sub(instant.Value, DurationToMillis(duration))}\n}\n\n// TimeDiff answers (later - earlier) as a Duration in seconds \226\128\148 the typed counterpart of\n// `diffMs`.\nfunc TimeDiff(earlier, later PosixMillis) float64 {\n\tdifference := Sub(later.Value, earlier.Value)\n\tvalue, _ := new(big.Rat).SetInt(difference.bigInt()).Float64()\n\treturn value / 1000.0\n}\n");
  ("timezone.go", "package teslrt\n\nimport (\n\t\"strconv\"\n\t\"strings\"\n\t\"time\"\n\t// The IANA database, compiled in. Without it `time.LoadLocation` reads the host's\n\t// /usr/share/zoneinfo, so the same program formats \"Europe/Stockholm\" correctly on a\n\t// developer's laptop and falls back to UTC inside a scratch container \226\128\148 a wrong TIME\n\t// rather than an error. A self-contained binary is the point of this backend, and a\n\t// timestamp that silently shifts by an hour is exactly the class of bug that is found\n\t// in production rather than in a test.\n\t_ \"time/tzdata\"\n)\n\n// formatTime and the calendar surface.\n//\n// The format string is Tesl's own strftime-like vocabulary, not Go's reference-layout one:\n// the two backends have to render a timestamp identically, and `%Y-%m-%d` is what the\n// source says. The directives are exactly those `tesl/time.rkt` implements \226\128\148 anything else\n// passes through as itself, which is what its regexp does.\n\n// FormatTime renders an instant in a named zone.\n//\n// The zone is a STRING here rather than the TimeZone value the truncation family takes,\n// because that is the surface: `formatTime ts \"UTC\" \"%Y-%m-%d\"`. An empty name or \"local\"\n// means the host's zone, matching Racket's `#f` case; an unknown name falls back to UTC\n// rather than trapping, which is also what Racket does when `TZ` names a zone the host has\n// never heard of.\nfunc FormatTime(instant PosixMillis, zone, format string) string {\n\tmillis, exact := instant.Value.Int64()\n\tif !exact {\n\t\tpanic(\"formatTime: the instant does not fit in a 64-bit millisecond count\")\n\t}\n\tmoment := time.UnixMilli(millis).In(formatLocation(zone))\n\treturn renderTimeFormat(moment, millis, format)\n}\n\nfunc formatLocation(zone string) *time.Location {\n\tswitch zone {\n\tcase \"\", \"local\":\n\t\treturn time.Local\n\tcase \"UTC\", \"utc\":\n\t\treturn time.UTC\n\t}\n\tif location, err := time.LoadLocation(zone); err == nil {\n\t\treturn location\n\t}\n\treturn time.UTC\n}\n\n// renderTimeFormat walks the format once, expanding the directives Tesl defines and copying\n// everything else through. `%3N` is milliseconds, and it is read from the ORIGINAL count\n// rather than from the parsed moment so a negative instant keeps Racket's `remainder`\n// semantics (the sign of the dividend).\nfunc renderTimeFormat(moment time.Time, millis int64, format string) string {\n\tvar rendered strings.Builder\n\tfor index := 0; index < len(format); index++ {\n\t\tif format[index] != '%' || index+1 >= len(format) {\n\t\t\trendered.WriteByte(format[index])\n\t\t\tcontinue\n\t\t}\n\t\t// `%3N` is the one two-character directive.\n\t\tif strings.HasPrefix(format[index+1:], \"3N\") {\n\t\t\trendered.WriteString(zeroPad(int(millis%1000), 3))\n\t\t\tindex += 2\n\t\t\tcontinue\n\t\t}\n\t\tswitch format[index+1] {\n\t\tcase 'Y':\n\t\t\trendered.WriteString(zeroPad(moment.Year(), 4))\n\t\tcase 'm':\n\t\t\trendered.WriteString(zeroPad(int(moment.Month()), 2))\n\t\tcase 'd':\n\t\t\trendered.WriteString(zeroPad(moment.Day(), 2))\n\t\tcase 'H':\n\t\t\trendered.WriteString(zeroPad(moment.Hour(), 2))\n\t\tcase 'M':\n\t\t\trendered.WriteString(zeroPad(moment.Minute(), 2))\n\t\tcase 'S':\n\t\t\trendered.WriteString(zeroPad(moment.Second(), 2))\n\t\tcase 'z':\n\t\t\trendered.WriteString(utcOffsetText(moment))\n\t\tcase 'Z':\n\t\t\tname, _ := moment.Zone()\n\t\t\trendered.WriteString(name)\n\t\tcase '%':\n\t\t\trendered.WriteByte('%')\n\t\tdefault:\n\t\t\t// An unknown directive is copied through as it was written, which is what\n\t\t\t// Racket's regexp does with anything outside its alternation.\n\t\t\trendered.WriteByte('%')\n\t\t\trendered.WriteByte(format[index+1])\n\t\t}\n\t\tindex++\n\t}\n\treturn rendered.String()\n}\n\nfunc zeroPad(value, width int) string {\n\ttext := strconv.Itoa(value)\n\tif len(text) >= width {\n\t\treturn text\n\t}\n\treturn strings.Repeat(\"0\", width-len(text)) + text\n}\n\nfunc utcOffsetText(moment time.Time) string {\n\t_, offset := moment.Zone()\n\tsign := \"+\"\n\tif offset < 0 {\n\t\tsign = \"-\"\n\t\toffset = -offset\n\t}\n\treturn sign + zeroPad(offset/3600, 2) + zeroPad((offset%3600)/60, 2)\n}\n");
  ("tuple.go", "package teslrt\n\n// Tuple2 and Tuple3 are Tesl's anonymous products. They live in the runtime for the\n// same reason Maybe does: a tuple crosses module boundaries, so two emitted packages\n// declaring their own would be incompatible Go types.\n//\n// Unlike Maybe they carry NO tag \226\128\148 a single-variant type has nothing to discriminate,\n// and a tag would be a wasted word in a value used as a lightweight pair. The emitter\n// knows this shape and matches `Tuple2 x y -> \226\128\166` by binding the fields directly\n// instead of switching.\n//\n// Field names are the ones the emitter derives for a constructor's payload\n// (constructor name + field name), so `Tuple2.first p` emits as `p.Tuple2First`.\ntype Tuple2[A any, B any] struct {\n\tTuple2First  A\n\tTuple2Second B\n}\n\ntype Tuple3[A any, B any, C any] struct {\n\tTuple3First  A\n\tTuple3Second B\n\tTuple3Third  C\n}\n");
]
